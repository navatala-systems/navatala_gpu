// SPDX-License-Identifier: Apache-2.0
// Kernel registry partition for the `metal_dataframe` bucket. Compiles
// against the declarations in `navatala/_registry.hpp` and links
// against the trampoline in `_registry.cpp`. Partitioning keeps each
// translation unit well under the 50K-line threshold so a typical
// build can parallelise across the buckets.
#include "navatala/_registry.hpp"
#include <cstdint>
#include <string>
#include <string_view>

const char* k_metal_navatala_dataframe_writeback_sum_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_writeback_sum_f32(device const uint* keys [[buffer(0)]], device const float* vals [[buffer(1)]], device const int* count [[buffer(2)]], device float* dst [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  int j = ((int)(int(__gid.x)));
  if ((j < count[0u])) {
    uint key = keys[j];
    float val = vals[j];
    float oldVal = dst[key];
    dst[key] = (oldVal + val);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_writeback_sum_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_writeback_sum_i32(device const uint* keys [[buffer(0)]], device const int* vals [[buffer(1)]], device const int* count [[buffer(2)]], device int* dst [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  int j = ((int)(int(__gid.x)));
  if ((j < count[0u])) {
    uint key = keys[j];
    int val = vals[j];
    int oldVal = dst[key];
    dst[key] = (oldVal + val);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_writeback_sum_u32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_writeback_sum_u32(device const uint* keys [[buffer(0)]], device const uint* vals [[buffer(1)]], device const int* count [[buffer(2)]], device uint* dst [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  int j = ((int)(int(__gid.x)));
  if ((j < count[0u])) {
    uint key = keys[j];
    uint val = vals[j];
    uint oldVal = dst[key];
    dst[key] = (oldVal + val);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_writeback_min_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_writeback_min_f32(device const uint* keys [[buffer(0)]], device const float* vals [[buffer(1)]], device const int* count [[buffer(2)]], device float* dst [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  int j = ((int)(int(__gid.x)));
  if ((j < count[0u])) {
    uint key = keys[j];
    float newVal = vals[j];
    float oldVal = dst[key];
    if ((newVal < oldVal)) {
      dst[key] = newVal;
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_writeback_max_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_writeback_max_f32(device const uint* keys [[buffer(0)]], device const float* vals [[buffer(1)]], device const int* count [[buffer(2)]], device float* dst [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  int j = ((int)(int(__gid.x)));
  if ((j < count[0u])) {
    uint key = keys[j];
    float newVal = vals[j];
    float oldVal = dst[key];
    if ((newVal > oldVal)) {
      dst[key] = newVal;
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_init_welford_state_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_init_welford_state_f32(device uint* count [[buffer(0)]], device float* mean [[buffer(1)]], device float* M2 [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    count[0] = 0u;
    mean[0] = as_type<float>(0x00000000u);
    M2[0] = as_type<float>(0x00000000u);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_welford_update_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_welford_update_f32(device const float* x [[buffer(0)]], device uint* count [[buffer(1)]], device float* mean [[buffer(2)]], device float* M2 [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    uint nOld = count[0];
    float meanOld = mean[0];
    float m2Old = M2[0];
    float xVal = x[0];
    uint nNew = (nOld + 1u);
    float nNewF = ((float)(nNew));
    float delta = (xVal - meanOld);
    float meanNew = (meanOld + (delta / nNewF));
    float delta2 = (xVal - meanNew);
    float m2New = (m2Old + (delta * delta2));
    count[0] = nNew;
    mean[0] = meanNew;
    M2[0] = m2New;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_welford_merge_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_welford_merge_f32(device const uint* countA [[buffer(0)]], device const float* meanA [[buffer(1)]], device const float* M2A [[buffer(2)]], device const uint* countB [[buffer(3)]], device const float* meanB [[buffer(4)]], device const float* M2B [[buffer(5)]], device uint* count [[buffer(6)]], device float* mean [[buffer(7)]], device float* M2 [[buffer(8)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    uint nA = countA[0];
    float muA = meanA[0];
    float m2AVal = M2A[0];
    uint nB = countB[0];
    float muB = meanB[0];
    float m2BVal = M2B[0];
    uint nTotal = (nA + nB);
    float nAf = ((float)(nA));
    float nBf = ((float)(nB));
    float nTotalf = ((float)(nTotal));
    float delta = (muB - muA);
    float nTotalSafe = (((nTotal == 0u)) ? (as_type<float>(0x3f800000u)) : (nTotalf));
    float muTotal = (muA + (delta * (nBf / nTotalSafe)));
    float deltaSq = (delta * delta);
    float nAnB = (nAf * nBf);
    float correction = (deltaSq * (nAnB / nTotalSafe));
    float m2Total = ((m2AVal + m2BVal) + correction);
    count[0] = nTotal;
    mean[0] = muTotal;
    M2[0] = m2Total;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_variance_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_extract_variance_f32(device const uint* count [[buffer(0)]], device const float* M2 [[buffer(1)]], device float* variance [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    uint n = count[0];
    float m2Val = M2[0];
    float nf = ((float)(n));
    float var = (m2Val / nf);
    variance[0] = var;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_sample_variance_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_extract_sample_variance_f32(device const uint* count [[buffer(0)]], device const float* M2 [[buffer(1)]], device float* variance [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    uint n = count[0];
    float m2Val = M2[0];
    uint nMinus1 = (n - 1u);
    float nMinus1f = ((float)(nMinus1));
    float var = (m2Val / nMinus1f);
    variance[0] = var;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_stddev_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_extract_stddev_f32(device const uint* count [[buffer(0)]], device const float* M2 [[buffer(1)]], device float* stddev [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    uint n = count[0];
    float m2Val = M2[0];
    float nf = ((float)(n));
    float var = (m2Val / nf);
    float std = sqrt(var);
    stddev[0] = std;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_init_covariance_state_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_init_covariance_state_f32(device uint* count [[buffer(0)]], device float* meanX [[buffer(1)]], device float* meanY [[buffer(2)]], device float* Cxy [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    count[0] = 0u;
    meanX[0] = as_type<float>(0x00000000u);
    meanY[0] = as_type<float>(0x00000000u);
    Cxy[0] = as_type<float>(0x00000000u);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_covariance_update_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_covariance_update_f32(device const float* x [[buffer(0)]], device const float* y [[buffer(1)]], device uint* count [[buffer(2)]], device float* meanX [[buffer(3)]], device float* meanY [[buffer(4)]], device float* Cxy [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    uint nOld = count[0];
    float muXOld = meanX[0];
    float muYOld = meanY[0];
    float cxyOld = Cxy[0];
    float xVal = x[0];
    float yVal = y[0];
    uint nNew = (nOld + 1u);
    float nNewF = ((float)(nNew));
    float deltaX = (xVal - muXOld);
    float deltaY = (yVal - muYOld);
    float muXNew = (muXOld + (deltaX / nNewF));
    float muYNew = (muYOld + (deltaY / nNewF));
    float yMinusMuYNew = (yVal - muYNew);
    float cxyNew = (cxyOld + (deltaX * yMinusMuYNew));
    count[0] = nNew;
    meanX[0] = muXNew;
    meanY[0] = muYNew;
    Cxy[0] = cxyNew;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_covariance_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_extract_covariance_f32(device const uint* count [[buffer(0)]], device const float* Cxy [[buffer(1)]], device float* covariance [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    uint n = count[0];
    float cxyVal = Cxy[0];
    float nf = ((float)(n));
    float cov = (cxyVal / nf);
    covariance[0] = cov;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_correlation_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_extract_correlation_f32(device const float* Cxy [[buffer(0)]], device const float* M2x [[buffer(1)]], device const float* M2y [[buffer(2)]], device float* correlation [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float cxyVal = Cxy[0];
    float m2xVal = M2x[0];
    float m2yVal = M2y[0];
    float m2Product = (m2xVal * m2yVal);
    float denom = sqrt(m2Product);
    float cor = (cxyVal / denom);
    correlation[0] = cor;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_init_weighted_welford_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_init_weighted_welford_f32(device float* weightSum [[buffer(0)]], device float* mean [[buffer(1)]], device float* M2 [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    weightSum[0] = as_type<float>(0x00000000u);
    mean[0] = as_type<float>(0x00000000u);
    M2[0] = as_type<float>(0x00000000u);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_weighted_welford_update_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_weighted_welford_update_f32(device const float* x [[buffer(0)]], device const float* w [[buffer(1)]], device float* weightSum [[buffer(2)]], device float* mean [[buffer(3)]], device float* M2 [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float wOld = weightSum[0];
    float meanOld = mean[0];
    float m2Old = M2[0];
    float xVal = x[0];
    float wVal = w[0];
    float wNew = (wOld + wVal);
    float delta = (xVal - meanOld);
    float wNewSafe = (((wNew == as_type<float>(0x00000000u))) ? (as_type<float>(0x3f800000u)) : (wNew));
    float meanNew = (meanOld + ((wVal / wNewSafe) * delta));
    float delta2 = (xVal - meanNew);
    float m2New = (m2Old + (wVal * (delta * delta2)));
    weightSum[0] = wNew;
    mean[0] = meanNew;
    M2[0] = m2New;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_weighted_welford_merge_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_weighted_welford_merge_f32(device const float* weightSumA [[buffer(0)]], device const float* meanA [[buffer(1)]], device const float* M2A [[buffer(2)]], device const float* weightSumB [[buffer(3)]], device const float* meanB [[buffer(4)]], device const float* M2B [[buffer(5)]], device float* weightSum [[buffer(6)]], device float* mean [[buffer(7)]], device float* M2 [[buffer(8)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float wA = weightSumA[0];
    float muA = meanA[0];
    float m2AVal = M2A[0];
    float wB = weightSumB[0];
    float muB = meanB[0];
    float m2BVal = M2B[0];
    float wTotal = (wA + wB);
    float delta = (muB - muA);
    float wTotalSafe = (((wTotal == as_type<float>(0x00000000u))) ? (as_type<float>(0x3f800000u)) : (wTotal));
    float muTotal = (muA + (delta * (wB / wTotalSafe)));
    float deltaSq = (delta * delta);
    float wAwB = (wA * wB);
    float correction = (deltaSq * (wAwB / wTotalSafe));
    float m2Total = ((m2AVal + m2BVal) + correction);
    weightSum[0] = wTotal;
    mean[0] = muTotal;
    M2[0] = m2Total;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_weighted_variance_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_extract_weighted_variance_f32(device const float* weightSum [[buffer(0)]], device const float* M2 [[buffer(1)]], device float* variance [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float w = weightSum[0];
    float m2Val = M2[0];
    float var = (m2Val / w);
    variance[0] = var;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_weighted_reliability_variance_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_extract_weighted_reliability_variance_f32(device const float* weightSum [[buffer(0)]], device const float* M2 [[buffer(1)]], device float* variance [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float w = weightSum[0];
    float m2Val = M2[0];
    float wMinus1 = (w - as_type<float>(0x3f800000u));
    float var = (m2Val / wMinus1);
    variance[0] = var;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_weighted_stddev_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_extract_weighted_stddev_f32(device const float* weightSum [[buffer(0)]], device const float* M2 [[buffer(1)]], device float* stddev [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float w = weightSum[0];
    float m2Val = M2[0];
    float var = (m2Val / w);
    float std = sqrt(var);
    stddev[0] = std;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_init_weighted_covariance_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_init_weighted_covariance_f32(device float* weightSum [[buffer(0)]], device float* meanX [[buffer(1)]], device float* meanY [[buffer(2)]], device float* Cxy [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    weightSum[0] = as_type<float>(0x00000000u);
    meanX[0] = as_type<float>(0x00000000u);
    meanY[0] = as_type<float>(0x00000000u);
    Cxy[0] = as_type<float>(0x00000000u);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_weighted_covariance_update_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_weighted_covariance_update_f32(device const float* x [[buffer(0)]], device const float* y [[buffer(1)]], device const float* w [[buffer(2)]], device float* weightSum [[buffer(3)]], device float* meanX [[buffer(4)]], device float* meanY [[buffer(5)]], device float* Cxy [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float wOld = weightSum[0];
    float muXOld = meanX[0];
    float muYOld = meanY[0];
    float cxyOld = Cxy[0];
    float xVal = x[0];
    float yVal = y[0];
    float wVal = w[0];
    float wNew = (wOld + wVal);
    float wNewSafe = (((wNew == as_type<float>(0x00000000u))) ? (as_type<float>(0x3f800000u)) : (wNew));
    float deltaX = (xVal - muXOld);
    float deltaY = (yVal - muYOld);
    float muXNew = (muXOld + ((wVal / wNewSafe) * deltaX));
    float muYNew = (muYOld + ((wVal / wNewSafe) * deltaY));
    float yMinusMuYNew = (yVal - muYNew);
    float cxyNew = (cxyOld + (wVal * (deltaX * yMinusMuYNew)));
    weightSum[0] = wNew;
    meanX[0] = muXNew;
    meanY[0] = muYNew;
    Cxy[0] = cxyNew;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_weighted_covariance_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_extract_weighted_covariance_f32(device const float* weightSum [[buffer(0)]], device const float* Cxy [[buffer(1)]], device float* covariance [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float w = weightSum[0];
    float cxyVal = Cxy[0];
    float cov = (cxyVal / w);
    covariance[0] = cov;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_weighted_correlation_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_extract_weighted_correlation_f32(device const float* Cxy [[buffer(0)]], device const float* M2x [[buffer(1)]], device const float* M2y [[buffer(2)]], device float* correlation [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float cxyVal = Cxy[0];
    float m2xVal = M2x[0];
    float m2yVal = M2y[0];
    float m2Product = (m2xVal * m2yVal);
    float denom = sqrt(m2Product);
    float cor = (cxyVal / denom);
    correlation[0] = cor;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_init_weighted_mean_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_init_weighted_mean_f32(device float* weightSum [[buffer(0)]], device float* mean [[buffer(1)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    weightSum[0] = as_type<float>(0x00000000u);
    mean[0] = as_type<float>(0x00000000u);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_weighted_mean_update_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_weighted_mean_update_f32(device const float* x [[buffer(0)]], device const float* w [[buffer(1)]], device float* weightSum [[buffer(2)]], device float* mean [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float wOld = weightSum[0];
    float meanOld = mean[0];
    float xVal = x[0];
    float wVal = w[0];
    float wNew = (wOld + wVal);
    float wNewSafe = (((wNew == as_type<float>(0x00000000u))) ? (as_type<float>(0x3f800000u)) : (wNew));
    float delta = (xVal - meanOld);
    float meanNew = (meanOld + ((wVal / wNewSafe) * delta));
    weightSum[0] = wNew;
    mean[0] = meanNew;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_init_p2_state_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_init_p2_state_f32(device const float* p [[buffer(0)]], device float* q0 [[buffer(1)]], device float* q1 [[buffer(2)]], device float* q2 [[buffer(3)]], device float* q3 [[buffer(4)]], device float* q4 [[buffer(5)]], device uint* pos0 [[buffer(6)]], device uint* pos1 [[buffer(7)]], device uint* pos2 [[buffer(8)]], device uint* pos3 [[buffer(9)]], device uint* pos4 [[buffer(10)]], device float* targetP [[buffer(11)]], device uint* count [[buffer(12)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float pVal = p[0];
    q0[0] = as_type<float>(0x00000000u);
    q1[0] = as_type<float>(0x00000000u);
    q2[0] = as_type<float>(0x00000000u);
    q3[0] = as_type<float>(0x00000000u);
    q4[0] = as_type<float>(0x00000000u);
    pos0[0] = 0u;
    pos1[0] = 0u;
    pos2[0] = 0u;
    pos3[0] = 0u;
    pos4[0] = 0u;
    targetP[0] = pVal;
    count[0] = 0u;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_p2_update_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_p2_update_f32(device const float* x [[buffer(0)]], device float* q0 [[buffer(1)]], device float* q1 [[buffer(2)]], device float* q2 [[buffer(3)]], device float* q3 [[buffer(4)]], device float* q4 [[buffer(5)]], device uint* pos0 [[buffer(6)]], device uint* pos1 [[buffer(7)]], device uint* pos2 [[buffer(8)]], device uint* pos3 [[buffer(9)]], device uint* pos4 [[buffer(10)]], device const float* targetP [[buffer(11)]], device uint* count [[buffer(12)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float xVal = x[0];
    uint n = count[0];
    float pVal = targetP[0];
    float h0 = q0[0];
    float h1 = q1[0];
    float h2 = q2[0];
    float h3 = q3[0];
    float h4 = q4[0];
    float newH0 = (((n == 0u)) ? (xVal) : ((((xVal < h0)) ? (xVal) : (h0))));
    float finalH0 = (((n >= 5u)) ? ((((xVal < h0)) ? (xVal) : (h0))) : (newH0));
    float finalH4 = (((n >= 5u)) ? ((((xVal > h4)) ? (xVal) : (h4))) : ((((n == 4u)) ? (xVal) : (h4))));
    float alpha = as_type<float>(0x3c23d70au);
    float oneMinusAlpha = (as_type<float>(0x3f800000u) - alpha);
    float finalH2 = (((n >= 5u)) ? (((oneMinusAlpha * h2) + (alpha * xVal))) : ((((n == 2u)) ? (xVal) : (h2))));
    q0[0] = finalH0;
    q2[0] = finalH2;
    q4[0] = finalH4;
    uint newCount = (n + 1u);
    count[0] = newCount;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_p2_extract_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_p2_extract_f32(device const float* q2 [[buffer(0)]], device float* quantile [[buffer(1)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float val = q2[0];
    quantile[0] = val;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_init_reservoir256_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_init_reservoir256_f32(device const uint* initialSeed [[buffer(0)]], device float* reservoir [[buffer(1)]], device uint* count [[buffer(2)]], device uint* seed [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  bool inBounds = (int(__gid.x) < 256);
  if (inBounds) {
    reservoir[int(__gid.x)] = as_type<float>(0x00000000u);
  }
  if ((int(__gid.x) == 0)) {
    uint initSeed = initialSeed[0];
    count[0] = 0u;
    seed[0] = initSeed;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_reservoir_update256_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_reservoir_update256_f32(device const float* x [[buffer(0)]], device float* reservoir [[buffer(1)]], device uint* count [[buffer(2)]], device uint* seed [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float xVal = x[0];
    uint n = count[0];
    uint s = seed[0];
    uint s1 = (s ^ (s << 13u));
    uint s2 = (s1 ^ (s1 >> 17u));
    uint s3 = (s2 ^ (s2 << 5u));
    uint nPlus1 = (n + 1u);
    uint j = (s3 % nPlus1);
    bool fillPhase = (n < 256u);
    bool replacePhase = ((n >= 256u) && (j < 256u));
    if (fillPhase) {
      reservoir[n] = xVal;
    }
    if (replacePhase) {
      reservoir[j] = xVal;
    }
    count[0] = nPlus1;
    seed[0] = s3;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_reservoir_min256_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_reservoir_min256_f32(device const float* reservoir [[buffer(0)]], device const uint* count [[buffer(1)]], device float* minVal [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    uint n = count[0];
    uint effectiveN = (((n < 256u)) ? (n) : (256u));
    float min0 = reservoir[0];
    minVal[0] = min0;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_reservoir_max256_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_reservoir_max256_f32(device const float* reservoir [[buffer(0)]], device const uint* count [[buffer(1)]], device float* maxVal [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    uint n = count[0];
    float max0 = reservoir[0];
    maxVal[0] = max0;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_trimmed_mean_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_trimmed_mean_f32(device const float* sortedData [[buffer(0)]], device const uint* n [[buffer(1)]], device const float* trimFraction [[buffer(2)]], device float* trimmedMean [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    uint len = n[0];
    float frac = trimFraction[0];
    float lenF = ((float)(len));
    float startF = (lenF * frac);
    uint startIdx = ((uint)(floor(startF)));
    uint endIdx = (len - startIdx);
    uint count = (endIdx - startIdx);
    uint midIdx = ((startIdx + endIdx) / 2u);
    float midVal = sortedData[midIdx];
    trimmedMean[0] = midVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_iqm_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_iqm_f32(device const float* sortedData [[buffer(0)]], device const uint* n [[buffer(1)]], device float* iqm [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    uint len = n[0];
    uint q1Idx = (len / 4u);
    uint q3Idx = (len - q1Idx);
    uint midIdx = ((q1Idx + q3Idx) / 2u);
    float midVal = sortedData[midIdx];
    iqm[0] = midVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_winsorized_mean_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_winsorized_mean_f32(device const float* sortedData [[buffer(0)]], device const uint* n [[buffer(1)]], device const float* trimFraction [[buffer(2)]], device float* winsorizedMean [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    uint len = n[0];
    float frac = trimFraction[0];
    float lenF = ((float)(len));
    float trimCount = (lenF * frac);
    uint lowIdx = ((uint)(floor(trimCount)));
    uint highIdx = ((len - lowIdx) - 1u);
    uint midIdx = (len / 2u);
    float midVal = sortedData[midIdx];
    winsorizedMean[0] = midVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_mad_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_mad_f32(device const float* sortedData [[buffer(0)]], device const uint* n [[buffer(1)]], device const float* median [[buffer(2)]], device float* mad [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    uint len = n[0];
    float med = median[0];
    uint q1Idx = (len / 4u);
    uint q3Idx = (len - q1Idx);
    float q1Val = sortedData[q1Idx];
    float q3Val = sortedData[q3Idx];
    float dev1 = abs((q1Val - med));
    float dev3 = abs((q3Val - med));
    float madVal = ((dev1 + dev3) / as_type<float>(0x40000000u));
    mad[0] = madVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_normalized_mad_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_normalized_mad_f32(device const float* mad [[buffer(0)]], device float* normalizedMad [[buffer(1)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    float m = mad[0];
    float norm = (as_type<float>(0x3fbdc5d6u) * m);
    normalizedMad[0] = norm;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_robust_z_score_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_robust_z_score_f32(device const float* data [[buffer(0)]], device const uint* n [[buffer(1)]], device const float* median [[buffer(2)]], device const float* mad [[buffer(3)]], device float* zScores [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint len = n[0];
  bool inBounds = (gid < len);
  if (inBounds) {
    float med = median[0];
    float m = mad[0];
    float xi = data[gid];
    float diff = (xi - med);
    float denom = (as_type<float>(0x3fbdc5d6u) * m);
    float z = (diff / denom);
    zScores[gid] = z;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_detect_outliers_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_detect_outliers_f32(device const float* data [[buffer(0)]], device const uint* n [[buffer(1)]], device const float* median [[buffer(2)]], device const float* mad [[buffer(3)]], device const float* threshold [[buffer(4)]], device uint* isOutlier [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint len = n[0];
  bool inBounds = (gid < len);
  if (inBounds) {
    float med = median[0];
    float m = mad[0];
    float thresh = threshold[0];
    float xi = data[gid];
    float dev = abs((xi - med));
    float cutoff = (thresh * m);
    bool isOut = (dev > cutoff);
    uint flag = ((isOut) ? (1u) : (0u));
    isOutlier[gid] = flag;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_init_t_digest_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_init_t_digest_f32(device const float* compressionIn [[buffer(0)]], device float* centroidMeans [[buffer(1)]], device float* centroidWeights [[buffer(2)]], device uint* centroidCount [[buffer(3)]], device float* totalWeight [[buffer(4)]], device float* minVal [[buffer(5)]], device float* maxVal [[buffer(6)]], device float* compression [[buffer(7)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    float delta = compressionIn[0];
    centroidCount[0] = 0u;
    totalWeight[0] = as_type<float>(0x00000000u);
    minVal[0] = as_type<float>(0x7f7ffffdu);
    maxVal[0] = as_type<float>(0xff7ffffdu);
    compression[0] = delta;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_tdigest_add_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_tdigest_add_f32(device const float* value [[buffer(0)]], device const float* weight [[buffer(1)]], device float* centroidMeans [[buffer(2)]], device float* centroidWeights [[buffer(3)]], device uint* centroidCount [[buffer(4)]], device float* totalWeight [[buffer(5)]], device float* minVal [[buffer(6)]], device float* maxVal [[buffer(7)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    float x = value[0];
    float w = weight[0];
    uint count = centroidCount[0];
    float tw = totalWeight[0];
    float minV = minVal[0];
    float maxV = maxVal[0];
    float newMin = (((x < minV)) ? (x) : (minV));
    float newMax = (((x > maxV)) ? (x) : (maxV));
    float newTw = (tw + w);
    uint newCount = (count + 1u);
    uint idx = count;
    centroidMeans[((int)(idx))] = x;
    centroidWeights[((int)(idx))] = w;
    centroidCount[0] = newCount;
    totalWeight[0] = newTw;
    minVal[0] = newMin;
    maxVal[0] = newMax;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_tdigest_merge_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_tdigest_merge_f32(device const float* meansA [[buffer(0)]], device const float* weightsA [[buffer(1)]], device const uint* countA [[buffer(2)]], device const float* totalWeightA [[buffer(3)]], device const float* minA [[buffer(4)]], device const float* maxA [[buffer(5)]], device const float* meansB [[buffer(6)]], device const float* weightsB [[buffer(7)]], device const uint* countB [[buffer(8)]], device const float* totalWeightB [[buffer(9)]], device const float* minB [[buffer(10)]], device const float* maxB [[buffer(11)]], device const float* compressionIn [[buffer(12)]], device float* meansOut [[buffer(13)]], device float* weightsOut [[buffer(14)]], device uint* countOut [[buffer(15)]], device float* totalWeightOut [[buffer(16)]], device float* minOut [[buffer(17)]], device float* maxOut [[buffer(18)]], device float* compressionOut [[buffer(19)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    uint cA = countA[0];
    uint cB = countB[0];
    float twA = totalWeightA[0];
    float twB = totalWeightB[0];
    float minValA = minA[0];
    float maxValA = maxA[0];
    float minValB = minB[0];
    float maxValB = maxB[0];
    float delta = compressionIn[0];
    float newTw = (twA + twB);
    float newMin = (((minValA < minValB)) ? (minValA) : (minValB));
    float newMax = (((maxValA > maxValB)) ? (maxValA) : (maxValB));
    uint newCount = (cA + cB);
    countOut[0] = newCount;
    totalWeightOut[0] = newTw;
    minOut[0] = newMin;
    maxOut[0] = newMax;
    compressionOut[0] = delta;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_tdigest_quantile_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_tdigest_quantile_f32(device const float* p [[buffer(0)]], device const float* centroidMeans [[buffer(1)]], device const float* centroidWeights [[buffer(2)]], device const uint* centroidCount [[buffer(3)]], device const float* totalWeight [[buffer(4)]], device const float* minVal [[buffer(5)]], device const float* maxVal [[buffer(6)]], device float* quantile [[buffer(7)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    float pVal = p[0];
    uint count = centroidCount[0];
    float minV = minVal[0];
    float maxV = maxVal[0];
    float range = (maxV - minV);
    float result = (minV + (pVal * range));
    bool isEmpty = (count == 0u);
    float finalResult = ((isEmpty) ? (as_type<float>(0x00000000u)) : (result));
    quantile[0] = finalResult;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_tdigest_cdf_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_tdigest_cdf_f32(device const float* x [[buffer(0)]], device const float* centroidMeans [[buffer(1)]], device const float* centroidWeights [[buffer(2)]], device const uint* centroidCount [[buffer(3)]], device const float* totalWeight [[buffer(4)]], device const float* minVal [[buffer(5)]], device const float* maxVal [[buffer(6)]], device float* cdf [[buffer(7)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    float xVal = x[0];
    uint count = centroidCount[0];
    float minV = minVal[0];
    float maxV = maxVal[0];
    float range = (maxV - minV);
    float safeRange = (((range == as_type<float>(0x00000000u))) ? (as_type<float>(0x3f800000u)) : (range));
    float rawCdf = ((xVal - minV) / safeRange);
    float clampLo = (((rawCdf < as_type<float>(0x00000000u))) ? (as_type<float>(0x00000000u)) : (rawCdf));
    float clampHi = (((clampLo > as_type<float>(0x3f800000u))) ? (as_type<float>(0x3f800000u)) : (clampLo));
    bool isEmpty = (count == 0u);
    float finalCdf = ((isEmpty) ? (as_type<float>(0x00000000u)) : (clampHi));
    cdf[0] = finalCdf;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_tdigest_mean_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_tdigest_mean_f32(device const float* centroidMeans [[buffer(0)]], device const float* centroidWeights [[buffer(1)]], device const uint* centroidCount [[buffer(2)]], device const float* totalWeight [[buffer(3)]], device const float* minVal [[buffer(4)]], device const float* maxVal [[buffer(5)]], device float* mean [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    uint count = centroidCount[0];
    float minV = minVal[0];
    float maxV = maxVal[0];
    float mid = (as_type<float>(0x3f000000u) * (minV + maxV));
    bool isEmpty = (count == 0u);
    float result = ((isEmpty) ? (as_type<float>(0x00000000u)) : (mid));
    mean[0] = result;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_tdigest_min_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_tdigest_min_f32(device const float* minVal [[buffer(0)]], device const uint* centroidCount [[buffer(1)]], device float* minOut [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    uint count = centroidCount[0];
    float minV = minVal[0];
    bool isEmpty = (count == 0u);
    float result = ((isEmpty) ? (as_type<float>(0x00000000u)) : (minV));
    minOut[0] = result;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_tdigest_max_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_tdigest_max_f32(device const float* maxVal [[buffer(0)]], device const uint* centroidCount [[buffer(1)]], device float* maxOut [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    uint count = centroidCount[0];
    float maxV = maxVal[0];
    bool isEmpty = (count == 0u);
    float result = ((isEmpty) ? (as_type<float>(0x00000000u)) : (maxV));
    maxOut[0] = result;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_tdigest_reset_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_tdigest_reset_f32(device const float* compression [[buffer(0)]], device uint* centroidCount [[buffer(1)]], device float* totalWeight [[buffer(2)]], device float* minVal [[buffer(3)]], device float* maxVal [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    centroidCount[0] = 0u;
    totalWeight[0] = as_type<float>(0x00000000u);
    minVal[0] = as_type<float>(0x7f7ffffdu);
    maxVal[0] = as_type<float>(0xff7ffffdu);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_inclusive_scan_sum_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_inclusive_scan_sum_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device float* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  uint n = count[0u];
  threadgroup float scanData[256];
  threadgroup uint scanValid[256];
  bool inBounds = (gid < n);
  uint wordIdx = (gid / 32u);
  uint bitIdx = (gid % 32u);
  uint validWord = ((inBounds) ? (inputValid[wordIdx]) : (0u));
  uint validBit = ((validWord >> bitIdx) & 1u);
  bool isValid = (validBit == 1u);
  float inputVal = (((inBounds && isValid)) ? (_input[gid]) : (as_type<float>(0x00000000u)));
  scanData[lid] = inputVal;
  scanValid[lid] = (((inBounds && isValid)) ? (1u) : (0u));
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor1 = (lid >= 1u);
  if (hasNeighbor1) {
    uint neighborIdx1 = (lid - 1u);
    float neighborVal1 = scanData[neighborIdx1];
    uint neighborValid1 = scanValid[neighborIdx1];
    float myVal1 = scanData[lid];
    uint myValid1 = scanValid[lid];
    float newVal1 = (myVal1 + neighborVal1);
    uint newValid1 = (myValid1 & neighborValid1);
    scanData[lid] = newVal1;
    scanValid[lid] = newValid1;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor2 = (lid >= 2u);
  if (hasNeighbor2) {
    uint neighborIdx2 = (lid - 2u);
    float neighborVal2 = scanData[neighborIdx2];
    uint neighborValid2 = scanValid[neighborIdx2];
    float myVal2 = scanData[lid];
    uint myValid2 = scanValid[lid];
    float newVal2 = (myVal2 + neighborVal2);
    uint newValid2 = (myValid2 & neighborValid2);
    scanData[lid] = newVal2;
    scanValid[lid] = newValid2;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor4 = (lid >= 4u);
  if (hasNeighbor4) {
    uint neighborIdx4 = (lid - 4u);
    float neighborVal4 = scanData[neighborIdx4];
    uint neighborValid4 = scanValid[neighborIdx4];
    float myVal4 = scanData[lid];
    uint myValid4 = scanValid[lid];
    float newVal4 = (myVal4 + neighborVal4);
    uint newValid4 = (myValid4 & neighborValid4);
    scanData[lid] = newVal4;
    scanValid[lid] = newValid4;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor8 = (lid >= 8u);
  if (hasNeighbor8) {
    uint neighborIdx8 = (lid - 8u);
    float neighborVal8 = scanData[neighborIdx8];
    uint neighborValid8 = scanValid[neighborIdx8];
    float myVal8 = scanData[lid];
    uint myValid8 = scanValid[lid];
    float newVal8 = (myVal8 + neighborVal8);
    uint newValid8 = (myValid8 & neighborValid8);
    scanData[lid] = newVal8;
    scanValid[lid] = newValid8;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor16 = (lid >= 16u);
  if (hasNeighbor16) {
    uint neighborIdx16 = (lid - 16u);
    float neighborVal16 = scanData[neighborIdx16];
    uint neighborValid16 = scanValid[neighborIdx16];
    float myVal16 = scanData[lid];
    uint myValid16 = scanValid[lid];
    float newVal16 = (myVal16 + neighborVal16);
    uint newValid16 = (myValid16 & neighborValid16);
    scanData[lid] = newVal16;
    scanValid[lid] = newValid16;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor32 = (lid >= 32u);
  if (hasNeighbor32) {
    uint neighborIdx32 = (lid - 32u);
    float neighborVal32 = scanData[neighborIdx32];
    uint neighborValid32 = scanValid[neighborIdx32];
    float myVal32 = scanData[lid];
    uint myValid32 = scanValid[lid];
    float newVal32 = (myVal32 + neighborVal32);
    uint newValid32 = (myValid32 & neighborValid32);
    scanData[lid] = newVal32;
    scanValid[lid] = newValid32;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor64 = (lid >= 64u);
  if (hasNeighbor64) {
    uint neighborIdx64 = (lid - 64u);
    float neighborVal64 = scanData[neighborIdx64];
    uint neighborValid64 = scanValid[neighborIdx64];
    float myVal64 = scanData[lid];
    uint myValid64 = scanValid[lid];
    float newVal64 = (myVal64 + neighborVal64);
    uint newValid64 = (myValid64 & neighborValid64);
    scanData[lid] = newVal64;
    scanValid[lid] = newValid64;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor128 = (lid >= 128u);
  if (hasNeighbor128) {
    uint neighborIdx128 = (lid - 128u);
    float neighborVal128 = scanData[neighborIdx128];
    uint neighborValid128 = scanValid[neighborIdx128];
    float myVal128 = scanData[lid];
    uint myValid128 = scanValid[lid];
    float newVal128 = (myVal128 + neighborVal128);
    uint newValid128 = (myValid128 & neighborValid128);
    scanData[lid] = newVal128;
    scanValid[lid] = newValid128;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  if (inBounds) {
    float scanResult = scanData[lid];
    uint scanValidResult = scanValid[lid];
    _output[gid] = scanResult;
    uint outBit = (scanValidResult << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_inclusive_scan_sum_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_inclusive_scan_sum_i32(device const int* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  uint n = count[0u];
  threadgroup int scanData[256];
  threadgroup uint scanValid[256];
  bool inBounds = (gid < n);
  uint wordIdx = (gid / 32u);
  uint bitIdx = (gid % 32u);
  uint validWord = ((inBounds) ? (inputValid[wordIdx]) : (0u));
  uint validBit = ((validWord >> bitIdx) & 1u);
  bool isValid = (validBit == 1u);
  int inputVal = (((inBounds && isValid)) ? (_input[gid]) : (0));
  scanData[lid] = inputVal;
  scanValid[lid] = (((inBounds && isValid)) ? (1u) : (0u));
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor1 = (lid >= 1u);
  if (hasNeighbor1) {
    uint neighborIdx1 = (lid - 1u);
    int neighborVal1 = scanData[neighborIdx1];
    uint neighborValid1 = scanValid[neighborIdx1];
    int myVal1 = scanData[lid];
    uint myValid1 = scanValid[lid];
    int newVal1 = (myVal1 + neighborVal1);
    uint newValid1 = (myValid1 & neighborValid1);
    scanData[lid] = newVal1;
    scanValid[lid] = newValid1;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor2 = (lid >= 2u);
  if (hasNeighbor2) {
    uint neighborIdx2 = (lid - 2u);
    int neighborVal2 = scanData[neighborIdx2];
    uint neighborValid2 = scanValid[neighborIdx2];
    int myVal2 = scanData[lid];
    uint myValid2 = scanValid[lid];
    int newVal2 = (myVal2 + neighborVal2);
    uint newValid2 = (myValid2 & neighborValid2);
    scanData[lid] = newVal2;
    scanValid[lid] = newValid2;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor4 = (lid >= 4u);
  if (hasNeighbor4) {
    uint neighborIdx4 = (lid - 4u);
    int neighborVal4 = scanData[neighborIdx4];
    uint neighborValid4 = scanValid[neighborIdx4];
    int myVal4 = scanData[lid];
    uint myValid4 = scanValid[lid];
    int newVal4 = (myVal4 + neighborVal4);
    uint newValid4 = (myValid4 & neighborValid4);
    scanData[lid] = newVal4;
    scanValid[lid] = newValid4;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor8 = (lid >= 8u);
  if (hasNeighbor8) {
    uint neighborIdx8 = (lid - 8u);
    int neighborVal8 = scanData[neighborIdx8];
    uint neighborValid8 = scanValid[neighborIdx8];
    int myVal8 = scanData[lid];
    uint myValid8 = scanValid[lid];
    int newVal8 = (myVal8 + neighborVal8);
    uint newValid8 = (myValid8 & neighborValid8);
    scanData[lid] = newVal8;
    scanValid[lid] = newValid8;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor16 = (lid >= 16u);
  if (hasNeighbor16) {
    uint neighborIdx16 = (lid - 16u);
    int neighborVal16 = scanData[neighborIdx16];
    uint neighborValid16 = scanValid[neighborIdx16];
    int myVal16 = scanData[lid];
    uint myValid16 = scanValid[lid];
    int newVal16 = (myVal16 + neighborVal16);
    uint newValid16 = (myValid16 & neighborValid16);
    scanData[lid] = newVal16;
    scanValid[lid] = newValid16;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor32 = (lid >= 32u);
  if (hasNeighbor32) {
    uint neighborIdx32 = (lid - 32u);
    int neighborVal32 = scanData[neighborIdx32];
    uint neighborValid32 = scanValid[neighborIdx32];
    int myVal32 = scanData[lid];
    uint myValid32 = scanValid[lid];
    int newVal32 = (myVal32 + neighborVal32);
    uint newValid32 = (myValid32 & neighborValid32);
    scanData[lid] = newVal32;
    scanValid[lid] = newValid32;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor64 = (lid >= 64u);
  if (hasNeighbor64) {
    uint neighborIdx64 = (lid - 64u);
    int neighborVal64 = scanData[neighborIdx64];
    uint neighborValid64 = scanValid[neighborIdx64];
    int myVal64 = scanData[lid];
    uint myValid64 = scanValid[lid];
    int newVal64 = (myVal64 + neighborVal64);
    uint newValid64 = (myValid64 & neighborValid64);
    scanData[lid] = newVal64;
    scanValid[lid] = newValid64;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor128 = (lid >= 128u);
  if (hasNeighbor128) {
    uint neighborIdx128 = (lid - 128u);
    int neighborVal128 = scanData[neighborIdx128];
    uint neighborValid128 = scanValid[neighborIdx128];
    int myVal128 = scanData[lid];
    uint myValid128 = scanValid[lid];
    int newVal128 = (myVal128 + neighborVal128);
    uint newValid128 = (myValid128 & neighborValid128);
    scanData[lid] = newVal128;
    scanValid[lid] = newValid128;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  if (inBounds) {
    int scanResult = scanData[lid];
    uint scanValidResult = scanValid[lid];
    _output[gid] = scanResult;
    uint outBit = (scanValidResult << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_inclusive_scan_sum_i32_to_i64_skip_nulls = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_inclusive_scan_sum_i32_to_i64_skip_nulls(device const int* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device long* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  uint n = count[0u];
  threadgroup long scanData[256];
  threadgroup uint scanValid[256];
  bool inBounds = (gid < n);
  uint wordIdx = (gid / 32u);
  uint bitIdx = (gid % 32u);
  uint validWord = ((inBounds) ? (inputValid[wordIdx]) : (0u));
  uint validBit = ((validWord >> bitIdx) & 1u);
  bool isValid = (validBit == 1u);
  long inputVal = (((inBounds && isValid)) ? (((long)(_input[gid]))) : (0));
  scanData[lid] = inputVal;
  scanValid[lid] = (((inBounds && isValid)) ? (1u) : (0u));
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor1 = (lid >= 1u);
  if (hasNeighbor1) {
    uint neighborIdx1 = (lid - 1u);
    long neighborVal1 = scanData[neighborIdx1];
    long myVal1 = scanData[lid];
    long newVal1 = (myVal1 + neighborVal1);
    scanData[lid] = newVal1;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor2 = (lid >= 2u);
  if (hasNeighbor2) {
    uint neighborIdx2 = (lid - 2u);
    long neighborVal2 = scanData[neighborIdx2];
    long myVal2 = scanData[lid];
    long newVal2 = (myVal2 + neighborVal2);
    scanData[lid] = newVal2;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor4 = (lid >= 4u);
  if (hasNeighbor4) {
    uint neighborIdx4 = (lid - 4u);
    long neighborVal4 = scanData[neighborIdx4];
    long myVal4 = scanData[lid];
    long newVal4 = (myVal4 + neighborVal4);
    scanData[lid] = newVal4;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor8 = (lid >= 8u);
  if (hasNeighbor8) {
    uint neighborIdx8 = (lid - 8u);
    long neighborVal8 = scanData[neighborIdx8];
    long myVal8 = scanData[lid];
    long newVal8 = (myVal8 + neighborVal8);
    scanData[lid] = newVal8;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor16 = (lid >= 16u);
  if (hasNeighbor16) {
    uint neighborIdx16 = (lid - 16u);
    long neighborVal16 = scanData[neighborIdx16];
    long myVal16 = scanData[lid];
    long newVal16 = (myVal16 + neighborVal16);
    scanData[lid] = newVal16;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor32 = (lid >= 32u);
  if (hasNeighbor32) {
    uint neighborIdx32 = (lid - 32u);
    long neighborVal32 = scanData[neighborIdx32];
    long myVal32 = scanData[lid];
    long newVal32 = (myVal32 + neighborVal32);
    scanData[lid] = newVal32;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor64 = (lid >= 64u);
  if (hasNeighbor64) {
    uint neighborIdx64 = (lid - 64u);
    long neighborVal64 = scanData[neighborIdx64];
    long myVal64 = scanData[lid];
    long newVal64 = (myVal64 + neighborVal64);
    scanData[lid] = newVal64;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor128 = (lid >= 128u);
  if (hasNeighbor128) {
    uint neighborIdx128 = (lid - 128u);
    long neighborVal128 = scanData[neighborIdx128];
    long myVal128 = scanData[lid];
    long newVal128 = (myVal128 + neighborVal128);
    scanData[lid] = newVal128;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  if (inBounds) {
    long scanResult = scanData[lid];
    uint rowValid = scanValid[lid];
    _output[gid] = scanResult;
    uint outBit = (rowValid << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_inclusive_scan_sum_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_inclusive_scan_sum_i64(device const long* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device long* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  uint n = count[0u];
  threadgroup long scanData[256];
  threadgroup uint scanValid[256];
  bool inBounds = (gid < n);
  uint wordIdx = (gid / 32u);
  uint bitIdx = (gid % 32u);
  uint validWord = ((inBounds) ? (inputValid[wordIdx]) : (0u));
  uint validBit = ((validWord >> bitIdx) & 1u);
  bool isValid = (validBit == 1u);
  long inputVal = (((inBounds && isValid)) ? (_input[gid]) : (0));
  scanData[lid] = inputVal;
  scanValid[lid] = (((inBounds && isValid)) ? (1u) : (0u));
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor1 = (lid >= 1u);
  if (hasNeighbor1) {
    uint neighborIdx1 = (lid - 1u);
    long neighborVal1 = scanData[neighborIdx1];
    uint neighborValid1 = scanValid[neighborIdx1];
    long myVal1 = scanData[lid];
    uint myValid1 = scanValid[lid];
    long newVal1 = (myVal1 + neighborVal1);
    uint newValid1 = (myValid1 & neighborValid1);
    scanData[lid] = newVal1;
    scanValid[lid] = newValid1;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor2 = (lid >= 2u);
  if (hasNeighbor2) {
    uint neighborIdx2 = (lid - 2u);
    long neighborVal2 = scanData[neighborIdx2];
    uint neighborValid2 = scanValid[neighborIdx2];
    long myVal2 = scanData[lid];
    uint myValid2 = scanValid[lid];
    long newVal2 = (myVal2 + neighborVal2);
    uint newValid2 = (myValid2 & neighborValid2);
    scanData[lid] = newVal2;
    scanValid[lid] = newValid2;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor4 = (lid >= 4u);
  if (hasNeighbor4) {
    uint neighborIdx4 = (lid - 4u);
    long neighborVal4 = scanData[neighborIdx4];
    uint neighborValid4 = scanValid[neighborIdx4];
    long myVal4 = scanData[lid];
    uint myValid4 = scanValid[lid];
    long newVal4 = (myVal4 + neighborVal4);
    uint newValid4 = (myValid4 & neighborValid4);
    scanData[lid] = newVal4;
    scanValid[lid] = newValid4;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor8 = (lid >= 8u);
  if (hasNeighbor8) {
    uint neighborIdx8 = (lid - 8u);
    long neighborVal8 = scanData[neighborIdx8];
    uint neighborValid8 = scanValid[neighborIdx8];
    long myVal8 = scanData[lid];
    uint myValid8 = scanValid[lid];
    long newVal8 = (myVal8 + neighborVal8);
    uint newValid8 = (myValid8 & neighborValid8);
    scanData[lid] = newVal8;
    scanValid[lid] = newValid8;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor16 = (lid >= 16u);
  if (hasNeighbor16) {
    uint neighborIdx16 = (lid - 16u);
    long neighborVal16 = scanData[neighborIdx16];
    uint neighborValid16 = scanValid[neighborIdx16];
    long myVal16 = scanData[lid];
    uint myValid16 = scanValid[lid];
    long newVal16 = (myVal16 + neighborVal16);
    uint newValid16 = (myValid16 & neighborValid16);
    scanData[lid] = newVal16;
    scanValid[lid] = newValid16;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor32 = (lid >= 32u);
  if (hasNeighbor32) {
    uint neighborIdx32 = (lid - 32u);
    long neighborVal32 = scanData[neighborIdx32];
    uint neighborValid32 = scanValid[neighborIdx32];
    long myVal32 = scanData[lid];
    uint myValid32 = scanValid[lid];
    long newVal32 = (myVal32 + neighborVal32);
    uint newValid32 = (myValid32 & neighborValid32);
    scanData[lid] = newVal32;
    scanValid[lid] = newValid32;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor64 = (lid >= 64u);
  if (hasNeighbor64) {
    uint neighborIdx64 = (lid - 64u);
    long neighborVal64 = scanData[neighborIdx64];
    uint neighborValid64 = scanValid[neighborIdx64];
    long myVal64 = scanData[lid];
    uint myValid64 = scanValid[lid];
    long newVal64 = (myVal64 + neighborVal64);
    uint newValid64 = (myValid64 & neighborValid64);
    scanData[lid] = newVal64;
    scanValid[lid] = newValid64;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  threadgroup_barrier(mem_flags::mem_threadgroup);
  bool hasNeighbor128 = (lid >= 128u);
  if (hasNeighbor128) {
    uint neighborIdx128 = (lid - 128u);
    long neighborVal128 = scanData[neighborIdx128];
    uint neighborValid128 = scanValid[neighborIdx128];
    long myVal128 = scanData[lid];
    uint myValid128 = scanValid[lid];
    long newVal128 = (myVal128 + neighborVal128);
    uint newValid128 = (myValid128 & neighborValid128);
    scanData[lid] = newVal128;
    scanValid[lid] = newValid128;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  if (inBounds) {
    long scanResult = scanData[lid];
    uint scanValidResult = scanValid[lid];
    _output[gid] = scanResult;
    uint outBit = (scanValidResult << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_reduce_sum_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_reduce_sum_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device float* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      float val = _input[gid];
      atomic_fetch_add_explicit((device atomic_float*)(&_output[0u]), val, memory_order_relaxed);
      atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[0u]), 1u, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_reduce_sum_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_reduce_sum_i32(device const int* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      int val = _input[gid];
      atomic_fetch_add_explicit((device atomic_int*)(&_output[0u]), val, memory_order_relaxed);
      atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[0u]), 1u, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_reduce_sum_i32_to_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_reduce_sum_i32_to_i64(device const int* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device long* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      int val32 = _input[gid];
      long val64 = ((long)(val32));
      atomic_fetch_add_explicit((device atomic_int*) /* metal atomic: unknown elem type */(&_output[0u]), val64, memory_order_relaxed);
      atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[0u]), 1u, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_reduce_min_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_reduce_min_i32(device const int* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      int val = _input[gid];
      atomic_fetch_min_explicit((device atomic_int*)(&_output[0u]), val, memory_order_relaxed);
      atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[0u]), 1u, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_reduce_max_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_reduce_max_i32(device const int* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      int val = _input[gid];
      atomic_fetch_max_explicit((device atomic_int*)(&_output[0u]), val, memory_order_relaxed);
      atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[0u]), 1u, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_count_valid = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_count_valid(device const uint* inputValid [[buffer(0)]], device const uint* count [[buffer(1)]], device uint* _output [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      atomic_fetch_add_explicit((device atomic_uint*)(&_output[0u]), 1u, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gather_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_gather_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const int* indices [[buffer(2)]], device const uint* inputSize [[buffer(3)]], device const uint* outputSize [[buffer(4)]], device float* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint nIn = inputSize[0u];
  uint nOut = outputSize[0u];
  bool inBounds = (gid < nOut);
  if (inBounds) {
    int idx = indices[gid];
    uint idxU32 = ((uint)(idx));
    bool idxValid = ((idx >= 0) && (idxU32 < nIn));
    uint srcWordIdx = (idxU32 / 32u);
    uint srcBitIdx = (idxU32 % 32u);
    uint srcValidWord = ((idxValid) ? (inputValid[srcWordIdx]) : (0u));
    uint srcValidBit = ((srcValidWord >> srcBitIdx) & 1u);
    bool srcIsValid = (srcValidBit == 1u);
    bool outIsValid = (idxValid && srcIsValid);
    float val = ((outIsValid) ? (_input[idxU32]) : (as_type<float>(0x00000000u)));
    _output[gid] = val;
    uint outWordIdx = (gid / 32u);
    uint outBitIdx = (gid % 32u);
    uint outBit = ((outIsValid) ? ((1u << outBitIdx)) : (0u));
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[outWordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gather_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_gather_i32(device const int* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const int* indices [[buffer(2)]], device const uint* inputSize [[buffer(3)]], device const uint* outputSize [[buffer(4)]], device int* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint nIn = inputSize[0u];
  uint nOut = outputSize[0u];
  bool inBounds = (gid < nOut);
  if (inBounds) {
    int idx = indices[gid];
    uint idxU32 = ((uint)(idx));
    bool idxValid = ((idx >= 0) && (idxU32 < nIn));
    uint srcWordIdx = (idxU32 / 32u);
    uint srcBitIdx = (idxU32 % 32u);
    uint srcValidWord = ((idxValid) ? (inputValid[srcWordIdx]) : (0u));
    uint srcValidBit = ((srcValidWord >> srcBitIdx) & 1u);
    bool srcIsValid = (srcValidBit == 1u);
    bool outIsValid = (idxValid && srcIsValid);
    int val = ((outIsValid) ? (_input[idxU32]) : (0));
    _output[gid] = val;
    uint outWordIdx = (gid / 32u);
    uint outBitIdx = (gid % 32u);
    uint outBit = ((outIsValid) ? ((1u << outBitIdx)) : (0u));
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[outWordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gather_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_gather_i64(device const long* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const int* indices [[buffer(2)]], device const uint* inputSize [[buffer(3)]], device const uint* outputSize [[buffer(4)]], device long* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint nIn = inputSize[0u];
  uint nOut = outputSize[0u];
  bool inBounds = (gid < nOut);
  if (inBounds) {
    int idx = indices[gid];
    uint idxU32 = ((uint)(idx));
    bool idxValid = ((idx >= 0) && (idxU32 < nIn));
    uint srcWordIdx = (idxU32 / 32u);
    uint srcBitIdx = (idxU32 % 32u);
    uint srcValidWord = ((idxValid) ? (inputValid[srcWordIdx]) : (0u));
    uint srcValidBit = ((srcValidWord >> srcBitIdx) & 1u);
    bool srcIsValid = (srcValidBit == 1u);
    bool outIsValid = (idxValid && srcIsValid);
    long val = ((outIsValid) ? (_input[idxU32]) : (0));
    _output[gid] = val;
    uint outWordIdx = (gid / 32u);
    uint outBitIdx = (gid % 32u);
    uint outBit = ((outIsValid) ? ((1u << outBitIdx)) : (0u));
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[outWordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_scatter_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_scatter_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const int* indices [[buffer(2)]], device const uint* inputSize [[buffer(3)]], device const uint* outputSize [[buffer(4)]], device float* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint nIn = inputSize[0u];
  uint nOut = outputSize[0u];
  bool inBounds = (gid < nIn);
  if (inBounds) {
    int idx = indices[gid];
    uint idxU32 = ((uint)(idx));
    bool idxValid = ((idx >= 0) && (idxU32 < nOut));
    uint srcWordIdx = (gid / 32u);
    uint srcBitIdx = (gid % 32u);
    uint srcValidWord = inputValid[srcWordIdx];
    uint srcValidBit = ((srcValidWord >> srcBitIdx) & 1u);
    bool srcIsValid = (srcValidBit == 1u);
    bool doScatter = idxValid;
    if (doScatter) {
      float val = _input[gid];
      _output[idxU32] = val;
      uint dstWordIdx = (idxU32 / 32u);
      uint dstBitIdx = (idxU32 % 32u);
      uint dstBit = ((srcIsValid) ? ((1u << dstBitIdx)) : (0u));
      atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[dstWordIdx]), dstBit, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_scatter_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_scatter_i32(device const int* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const int* indices [[buffer(2)]], device const uint* inputSize [[buffer(3)]], device const uint* outputSize [[buffer(4)]], device int* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint nIn = inputSize[0u];
  uint nOut = outputSize[0u];
  bool inBounds = (gid < nIn);
  if (inBounds) {
    int idx = indices[gid];
    uint idxU32 = ((uint)(idx));
    bool idxValid = ((idx >= 0) && (idxU32 < nOut));
    uint srcWordIdx = (gid / 32u);
    uint srcBitIdx = (gid % 32u);
    uint srcValidWord = inputValid[srcWordIdx];
    uint srcValidBit = ((srcValidWord >> srcBitIdx) & 1u);
    bool srcIsValid = (srcValidBit == 1u);
    if (idxValid) {
      int val = _input[gid];
      _output[idxU32] = val;
      uint dstWordIdx = (idxU32 / 32u);
      uint dstBitIdx = (idxU32 % 32u);
      uint dstBit = ((srcIsValid) ? ((1u << dstBitIdx)) : (0u));
      atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[dstWordIdx]), dstBit, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_compact_by_mask_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_compact_by_mask_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* mask [[buffer(2)]], device const uint* prefixSum [[buffer(3)]], device const uint* count [[buffer(4)]], device float* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint selected = mask[gid];
    bool isSelected = (selected == 1u);
    if (isSelected) {
      uint outIdx = prefixSum[gid];
      float val = _input[gid];
      _output[outIdx] = val;
      uint srcWordIdx = (gid / 32u);
      uint srcBitIdx = (gid % 32u);
      uint srcValidWord = inputValid[srcWordIdx];
      uint srcValidBit = ((srcValidWord >> srcBitIdx) & 1u);
      uint dstWordIdx = (outIdx / 32u);
      uint dstBitIdx = (outIdx % 32u);
      uint dstBit = (srcValidBit << dstBitIdx);
      atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[dstWordIdx]), dstBit, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_compact_by_mask_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_compact_by_mask_i32(device const int* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* mask [[buffer(2)]], device const uint* prefixSum [[buffer(3)]], device const uint* count [[buffer(4)]], device int* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint selected = mask[gid];
    bool isSelected = (selected == 1u);
    if (isSelected) {
      uint outIdx = prefixSum[gid];
      int val = _input[gid];
      _output[outIdx] = val;
      uint srcWordIdx = (gid / 32u);
      uint srcBitIdx = (gid % 32u);
      uint srcValidWord = inputValid[srcWordIdx];
      uint srcValidBit = ((srcValidWord >> srcBitIdx) & 1u);
      uint dstWordIdx = (outIdx / 32u);
      uint dstBitIdx = (outIdx % 32u);
      uint dstBit = (srcValidBit << dstBitIdx);
      atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[dstWordIdx]), dstBit, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_compact_valid_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_compact_valid_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* prefixSum [[buffer(2)]], device const uint* count [[buffer(3)]], device float* _output [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      uint outIdx = prefixSum[gid];
      float val = _input[gid];
      _output[outIdx] = val;
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_fill_constant_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_fill_constant_f32(device const float* value [[buffer(0)]], device const uint* count [[buffer(1)]], device float* _output [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  float val = value[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    _output[gid] = val;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_fill_constant_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_fill_constant_i32(device const int* value [[buffer(0)]], device const uint* count [[buffer(1)]], device int* _output [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  int val = value[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    _output[gid] = val;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_fill_constant_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_fill_constant_i64(device const long* value [[buffer(0)]], device const uint* count [[buffer(1)]], device long* _output [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  long val = value[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    _output[gid] = val;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_fill_range_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_fill_range_i32(device const int* start [[buffer(0)]], device const uint* count [[buffer(1)]], device int* _output [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  int startVal = start[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int val = (startVal + ((int)(gid)));
    _output[gid] = val;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_fill_range_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_fill_range_i64(device const long* start [[buffer(0)]], device const uint* count [[buffer(1)]], device long* _output [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  long startVal = start[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    long val = (startVal + ((long)(gid)));
    _output[gid] = val;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_fill_validity_all_valid = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_fill_validity_all_valid(device const uint* numElements [[buffer(0)]], device uint* _output [[buffer(1)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = numElements[0u];
  uint numWords = ((n / 32u) + ((((n % 32u) == 0u)) ? (0u) : (1u)));
  bool inBounds = (gid < numWords);
  if (inBounds) {
    bool isLastWord = (gid == (numWords - 1u));
    uint remainingBits = (n % 32u);
    bool hasRemainder = (remainingBits != 0u);
    uint mask = (((isLastWord && hasRemainder)) ? (((1u << remainingBits) - 1u)) : (4294967295u));
    _output[gid] = mask;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_fill_validity_all_null = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_fill_validity_all_null(device const uint* numElements [[buffer(0)]], device uint* _output [[buffer(1)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = numElements[0u];
  uint numWords = ((n / 32u) + ((((n % 32u) == 0u)) ? (0u) : (1u)));
  bool inBounds = (gid < numWords);
  if (inBounds) {
    _output[gid] = 0u;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_fill_null_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_fill_null_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const float* fillValue [[buffer(2)]], device const uint* count [[buffer(3)]], device float* _output [[buffer(4)]], device uint* outputValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  float fill = fillValue[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    float inputVal = _input[gid];
    float outVal = ((isValid) ? (inputVal) : (fill));
    _output[gid] = outVal;
    uint outBit = (1u << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_fill_null_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_fill_null_i32(device const int* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const int* fillValue [[buffer(2)]], device const uint* count [[buffer(3)]], device int* _output [[buffer(4)]], device uint* outputValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  int fill = fillValue[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int inputVal = _input[gid];
    int outVal = ((isValid) ? (inputVal) : (fill));
    _output[gid] = outVal;
    uint outBit = (1u << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_is_null = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_is_null(device const uint* inputValid [[buffer(0)]], device const uint* count [[buffer(1)]], device uchar* _output [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uchar isNullVal = ((uchar)((1u - validBit)));
    _output[gid] = isNullVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_is_valid = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_is_valid(device const uint* inputValid [[buffer(0)]], device const uint* count [[buffer(1)]], device uchar* _output [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uchar isValidVal = ((uchar)(validBit));
    _output[gid] = isValidVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_copy_if_valid_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_copy_if_valid_f32(device const float* src [[buffer(0)]], device const uint* srcValid [[buffer(1)]], device const uint* dstValid [[buffer(2)]], device const uint* count [[buffer(3)]], device float* dst [[buffer(4)]], device uint* outValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint srcValidWord = srcValid[wordIdx];
    uint srcValidBit = ((srcValidWord >> bitIdx) & 1u);
    bool srcIsValid = (srcValidBit == 1u);
    uint dstValidWord = dstValid[wordIdx];
    uint dstValidBit = ((dstValidWord >> bitIdx) & 1u);
    bool dstIsValid = (dstValidBit == 1u);
    bool bothValid = (srcIsValid && dstIsValid);
    if (bothValid) {
      float val = src[gid];
      dst[gid] = val;
      uint outBit = (1u << bitIdx);
      atomic_fetch_or_explicit((device atomic_uint*)(&outValid[wordIdx]), outBit, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_coalesce_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_coalesce_f32(device const float* col1 [[buffer(0)]], device const uint* col1Valid [[buffer(1)]], device const float* col2 [[buffer(2)]], device const uint* col2Valid [[buffer(3)]], device const uint* count [[buffer(4)]], device float* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint v1Word = col1Valid[wordIdx];
    uint v1Bit = ((v1Word >> bitIdx) & 1u);
    bool col1IsValid = (v1Bit == 1u);
    uint v2Word = col2Valid[wordIdx];
    uint v2Bit = ((v2Word >> bitIdx) & 1u);
    bool col2IsValid = (v2Bit == 1u);
    float val1 = col1[gid];
    float val2 = col2[gid];
    float outVal = ((col1IsValid) ? (val1) : (val2));
    _output[gid] = outVal;
    bool eitherValid = (col1IsValid || col2IsValid);
    uint outBit = ((eitherValid) ? ((1u << bitIdx)) : (0u));
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_arg_max_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_arg_max_f32(device const float* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device const uint* tiePolicy [[buffer(2)]], device float* maxValue [[buffer(3)]], device uint* maxIndex [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float svals[256];
  threadgroup uint sidxs[256];
  float otherVal = as_type<float>(0x00000000u);
  uint otherIdx = 0u;
  float myVal = as_type<float>(0x00000000u);
  uint myIdx = 0u;
  bool otherGreater = false;
  bool equalVals = false;
  bool otherIdxSmaller = false;
  bool takeOtherFirst = false;
  bool takeOther = false;
  uint countVal = count[0];
  bool inBounds = (gid < countVal);
  uint policy = tiePolicy[0];
  bool useFirst = (policy == 0u);
  float val = _input[gid];
  if (inBounds) {
    svals[lid] = val;
    sidxs[lid] = gid;
  } else {
    svals[lid] = as_type<float>(0xff7fffffu);
    sidxs[lid] = 0u;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  uint amf32_reductionStride = 128u;
  for (int amf32_reductionStep = 0; amf32_reductionStep < (int)(8); ++amf32_reductionStep) {
    uint amf32_stride = amf32_reductionStride;
    if ((lid < amf32_stride)) {
      otherVal = svals[(lid + amf32_stride)];
      otherIdx = sidxs[(lid + amf32_stride)];
      myVal = svals[lid];
      myIdx = sidxs[lid];
      otherGreater = (otherVal > myVal);
      equalVals = (otherVal == myVal);
      otherIdxSmaller = (otherIdx < myIdx);
      takeOtherFirst = (otherGreater || (equalVals && otherIdxSmaller));
      takeOther = ((useFirst) ? (takeOtherFirst) : (otherGreater));
      if (takeOther) {
        svals[lid] = otherVal;
        sidxs[lid] = otherIdx;
      }
    }
    uint amf32_strideToHalve = amf32_reductionStride;
    uint amf32_nextStride = (amf32_strideToHalve >> 1u);
    amf32_reductionStride = amf32_nextStride;
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if ((lid == 0u)) {
    maxValue[0] = svals[0];
    maxIndex[0] = sidxs[0];
  }
}

)kernel";
const char* k_metal_navatala_dataframe_arg_min_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_arg_min_f32(device const float* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device const uint* tiePolicy [[buffer(2)]], device float* minValue [[buffer(3)]], device uint* minIndex [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float svals[256];
  threadgroup uint sidxs[256];
  float otherVal = as_type<float>(0x00000000u);
  uint otherIdx = 0u;
  float myVal = as_type<float>(0x00000000u);
  uint myIdx = 0u;
  bool otherLess = false;
  bool equalVals = false;
  bool otherIdxSmaller = false;
  bool takeOtherFirst = false;
  bool takeOther = false;
  uint countVal = count[0];
  bool inBounds = (gid < countVal);
  uint policy = tiePolicy[0];
  bool useFirst = (policy == 0u);
  float val = _input[gid];
  if (inBounds) {
    svals[lid] = val;
    sidxs[lid] = gid;
  } else {
    svals[lid] = as_type<float>(0x7f7fffffu);
    sidxs[lid] = 0u;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  uint aminf32_reductionStride = 128u;
  for (int aminf32_reductionStep = 0; aminf32_reductionStep < (int)(8); ++aminf32_reductionStep) {
    uint aminf32_stride = aminf32_reductionStride;
    if ((lid < aminf32_stride)) {
      otherVal = svals[(lid + aminf32_stride)];
      otherIdx = sidxs[(lid + aminf32_stride)];
      myVal = svals[lid];
      myIdx = sidxs[lid];
      otherLess = (otherVal < myVal);
      equalVals = (otherVal == myVal);
      otherIdxSmaller = (otherIdx < myIdx);
      takeOtherFirst = (otherLess || (equalVals && otherIdxSmaller));
      takeOther = ((useFirst) ? (takeOtherFirst) : (otherLess));
      if (takeOther) {
        svals[lid] = otherVal;
        sidxs[lid] = otherIdx;
      }
    }
    uint aminf32_strideToHalve = aminf32_reductionStride;
    uint aminf32_nextStride = (aminf32_strideToHalve >> 1u);
    aminf32_reductionStride = aminf32_nextStride;
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if ((lid == 0u)) {
    minValue[0] = svals[0];
    minIndex[0] = sidxs[0];
  }
}

)kernel";
const char* k_metal_navatala_dataframe_scan_sum_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_scan_sum_f32(device const float* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device const uint* scanMode [[buffer(2)]], device float* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float sdata[512];
  uint n = count[0u];
  uint mode = scanMode[0u];
  bool isExclusive = (mode == 1u);
  uint ai = lid;
  uint bi = (lid + 256u);
  bool inBoundsA = (ai < n);
  bool inBoundsB = (bi < n);
  float valA = ((inBoundsA) ? (_input[ai]) : (as_type<float>(0x00000000u)));
  float valB = ((inBoundsB) ? (_input[bi]) : (as_type<float>(0x00000000u)));
  sdata[ai] = valA;
  sdata[bi] = valB;
  threadgroup_barrier(mem_flags::mem_threadgroup);
  for (int offset = 0; offset < (int)(1); ++offset) {
    uint offsetU32 = ((uint)(offset));
    uint upMask = ((offsetU32 * 2u) - 1u);
    bool upShouldWork = ((lid & upMask) == 0u);
    if (upShouldWork) {
      uint upAi2 = ((offsetU32 * ((lid * 2u) + 1u)) + (offsetU32 - 1u));
      uint upBi2 = (upAi2 + offsetU32);
      float upVa = sdata[upAi2];
      float upVb = sdata[upBi2];
      sdata[upBi2] = (upVa + upVb);
    }
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if ((lid == 0u)) {
    float total = sdata[511u];
    sdata[511u] = as_type<float>(0x00000000u);
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  for (int offset = 0; offset < (int)(256); ++offset) {
    uint downOffsetU32 = ((uint)(offset));
    uint downMask = ((downOffsetU32 * 2u) - 1u);
    bool downShouldWork = ((lid & downMask) == 0u);
    if (downShouldWork) {
      uint downAi2 = ((downOffsetU32 * ((lid * 2u) + 1u)) + (downOffsetU32 - 1u));
      uint downBi2 = (downAi2 + downOffsetU32);
      float downT = sdata[downAi2];
      float downVb = sdata[downBi2];
      sdata[downAi2] = downVb;
      sdata[downBi2] = (downT + downVb);
    }
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if (inBoundsA) {
    float exclusiveResultA = sdata[ai];
    float inclusiveResultA = (exclusiveResultA + valA);
    float resultA = ((isExclusive) ? (exclusiveResultA) : (inclusiveResultA));
    _output[ai] = resultA;
  }
  if (inBoundsB) {
    float exclusiveResultB = sdata[bi];
    float inclusiveResultB = (exclusiveResultB + valB);
    float resultB = ((isExclusive) ? (exclusiveResultB) : (inclusiveResultB));
    _output[bi] = resultB;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_scan_sum_u32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_scan_sum_u32(device const uint* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device const uint* scanMode [[buffer(2)]], device uint* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup uint sdata[512];
  uint n = count[0u];
  uint mode = scanMode[0u];
  bool isExclusive = (mode == 1u);
  uint ai = lid;
  uint bi = (lid + 256u);
  bool inBoundsA = (ai < n);
  bool inBoundsB = (bi < n);
  uint valA = ((inBoundsA) ? (_input[ai]) : (0u));
  uint valB = ((inBoundsB) ? (_input[bi]) : (0u));
  sdata[ai] = valA;
  sdata[bi] = valB;
  threadgroup_barrier(mem_flags::mem_threadgroup);
  for (int offset = 0; offset < (int)(1); ++offset) {
    uint offsetU32 = ((uint)(offset));
    uint upMask = ((offsetU32 * 2u) - 1u);
    bool upShouldWork = ((lid & upMask) == 0u);
    if (upShouldWork) {
      uint upAi2 = ((offsetU32 * ((lid * 2u) + 1u)) + (offsetU32 - 1u));
      uint upBi2 = (upAi2 + offsetU32);
      uint upVa = sdata[upAi2];
      uint upVb = sdata[upBi2];
      sdata[upBi2] = (upVa + upVb);
    }
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if ((lid == 0u)) {
    sdata[511u] = 0u;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  for (int offset = 0; offset < (int)(256); ++offset) {
    uint downOffsetU32 = ((uint)(offset));
    uint downMask = ((downOffsetU32 * 2u) - 1u);
    bool downShouldWork = ((lid & downMask) == 0u);
    if (downShouldWork) {
      uint downAi2 = ((downOffsetU32 * ((lid * 2u) + 1u)) + (downOffsetU32 - 1u));
      uint downBi2 = (downAi2 + downOffsetU32);
      uint downT = sdata[downAi2];
      uint downVb = sdata[downBi2];
      sdata[downAi2] = downVb;
      sdata[downBi2] = (downT + downVb);
    }
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if (inBoundsA) {
    uint exclusiveResultA = sdata[ai];
    uint inclusiveResultA = (exclusiveResultA + valA);
    uint resultA = ((isExclusive) ? (exclusiveResultA) : (inclusiveResultA));
    _output[ai] = resultA;
  }
  if (inBoundsB) {
    uint exclusiveResultB = sdata[bi];
    uint inclusiveResultB = (exclusiveResultB + valB);
    uint resultB = ((isExclusive) ? (exclusiveResultB) : (inclusiveResultB));
    _output[bi] = resultB;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_scan_exclusive_write_total_u32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_scan_exclusive_write_total_u32(device const uint* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device uint* _output [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    uint n = count[0u];
    if ((n == 0u)) {
      _output[0u] = 0u;
    } else {
      uint nMinus1 = (n - 1u);
      uint prevExc = _output[nMinus1];
      uint lastIn = _input[nMinus1];
      _output[n] = (prevExc + lastIn);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_scan_max_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_scan_max_f32(device const float* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device const uint* scanMode [[buffer(2)]], device float* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float sdata[512];
  uint n = count[0u];
  uint mode = scanMode[0u];
  bool isExclusive = (mode == 1u);
  uint ai = lid;
  uint bi = (lid + 256u);
  bool inBoundsA = (ai < n);
  bool inBoundsB = (bi < n);
  float valA = ((inBoundsA) ? (_input[ai]) : (as_type<float>(0xff7fffffu)));
  float valB = ((inBoundsB) ? (_input[bi]) : (as_type<float>(0xff7fffffu)));
  sdata[ai] = valA;
  sdata[bi] = valB;
  threadgroup_barrier(mem_flags::mem_threadgroup);
  for (int offset = 0; offset < (int)(1); ++offset) {
    uint offsetU32 = ((uint)(offset));
    uint upMask = ((offsetU32 * 2u) - 1u);
    bool upShouldWork = ((lid & upMask) == 0u);
    if (upShouldWork) {
      uint upAi2 = ((offsetU32 * ((lid * 2u) + 1u)) + (offsetU32 - 1u));
      uint upBi2 = (upAi2 + offsetU32);
      float upVa = sdata[upAi2];
      float upVb = sdata[upBi2];
      float upMaxVal = (((upVa > upVb)) ? (upVa) : (upVb));
      sdata[upBi2] = upMaxVal;
    }
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if ((lid == 0u)) {
    sdata[511u] = as_type<float>(0xff7fffffu);
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  for (int offset = 0; offset < (int)(256); ++offset) {
    uint downOffsetU32 = ((uint)(offset));
    uint downMask = ((downOffsetU32 * 2u) - 1u);
    bool downShouldWork = ((lid & downMask) == 0u);
    if (downShouldWork) {
      uint downAi2 = ((downOffsetU32 * ((lid * 2u) + 1u)) + (downOffsetU32 - 1u));
      uint downBi2 = (downAi2 + downOffsetU32);
      float downT = sdata[downAi2];
      float downVb = sdata[downBi2];
      sdata[downAi2] = downVb;
      float downMaxVal = (((downT > downVb)) ? (downT) : (downVb));
      sdata[downBi2] = downMaxVal;
    }
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if (inBoundsA) {
    float exclusiveResultA = sdata[ai];
    float inclusiveResultA = (((exclusiveResultA > valA)) ? (exclusiveResultA) : (valA));
    float resultA = ((isExclusive) ? (exclusiveResultA) : (inclusiveResultA));
    _output[ai] = resultA;
  }
  if (inBoundsB) {
    float exclusiveResultB = sdata[bi];
    float inclusiveResultB = (((exclusiveResultB > valB)) ? (exclusiveResultB) : (valB));
    float resultB = ((isExclusive) ? (exclusiveResultB) : (inclusiveResultB));
    _output[bi] = resultB;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_scan_min_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_scan_min_f32(device const float* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device const uint* scanMode [[buffer(2)]], device float* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float sdata[512];
  uint n = count[0u];
  uint mode = scanMode[0u];
  bool isExclusive = (mode == 1u);
  uint ai = lid;
  uint bi = (lid + 256u);
  bool inBoundsA = (ai < n);
  bool inBoundsB = (bi < n);
  float valA = ((inBoundsA) ? (_input[ai]) : (as_type<float>(0x7f7fffffu)));
  float valB = ((inBoundsB) ? (_input[bi]) : (as_type<float>(0x7f7fffffu)));
  sdata[ai] = valA;
  sdata[bi] = valB;
  threadgroup_barrier(mem_flags::mem_threadgroup);
  for (int offset = 0; offset < (int)(1); ++offset) {
    uint offsetU32 = ((uint)(offset));
    uint upMask = ((offsetU32 * 2u) - 1u);
    bool upShouldWork = ((lid & upMask) == 0u);
    if (upShouldWork) {
      uint upAi2 = ((offsetU32 * ((lid * 2u) + 1u)) + (offsetU32 - 1u));
      uint upBi2 = (upAi2 + offsetU32);
      float upVa = sdata[upAi2];
      float upVb = sdata[upBi2];
      float upMinVal = (((upVa < upVb)) ? (upVa) : (upVb));
      sdata[upBi2] = upMinVal;
    }
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if ((lid == 0u)) {
    sdata[511u] = as_type<float>(0x7f7fffffu);
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  for (int offset = 0; offset < (int)(256); ++offset) {
    uint downOffsetU32 = ((uint)(offset));
    uint downMask = ((downOffsetU32 * 2u) - 1u);
    bool downShouldWork = ((lid & downMask) == 0u);
    if (downShouldWork) {
      uint downAi2 = ((downOffsetU32 * ((lid * 2u) + 1u)) + (downOffsetU32 - 1u));
      uint downBi2 = (downAi2 + downOffsetU32);
      float downT = sdata[downAi2];
      float downVb = sdata[downBi2];
      sdata[downAi2] = downVb;
      float downMinVal = (((downT < downVb)) ? (downT) : (downVb));
      sdata[downBi2] = downMinVal;
    }
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if (inBoundsA) {
    float exclusiveResultA = sdata[ai];
    float inclusiveResultA = (((exclusiveResultA < valA)) ? (exclusiveResultA) : (valA));
    float resultA = ((isExclusive) ? (exclusiveResultA) : (inclusiveResultA));
    _output[ai] = resultA;
  }
  if (inBoundsB) {
    float exclusiveResultB = sdata[bi];
    float inclusiveResultB = (((exclusiveResultB < valB)) ? (exclusiveResultB) : (valB));
    float resultB = ((isExclusive) ? (exclusiveResultB) : (inclusiveResultB));
    _output[bi] = resultB;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_histogram_fast_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_histogram_fast_f32(device const float* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device const uint* numBins [[buffer(2)]], device const float* minVal [[buffer(3)]], device const float* maxVal [[buffer(4)]], device uint* bins [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint nBins = numBins[0u];
  float vMin = minVal[0u];
  float vMax = maxVal[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = _input[gid];
    float range = (vMax - vMin);
    float nBinsF = ((float)(nBins));
    float binWidth = (range / nBinsF);
    float normalized = ((val - vMin) / range);
    float binIdxF = (normalized * nBinsF);
    int binIdxRaw = ((int)(floor(binIdxF)));
    int nBinsMinusOne = (((int)(nBins)) - 1);
    int binIdxClamped = (((binIdxRaw < 0)) ? (0) : ((((binIdxRaw > nBinsMinusOne)) ? (nBinsMinusOne) : (binIdxRaw))));
    uint binIdx = ((uint)(binIdxClamped));
    atomic_fetch_add_explicit((device atomic_uint*)(&bins[binIdx]), 1u, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_zero_bins = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_zero_bins(device const uint* numBins [[buffer(0)]], device uint* bins [[buffer(1)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint nBins = numBins[0u];
  bool inBounds = (gid < nBins);
  if (inBounds) {
    bins[gid] = 0u;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_compute_bin_indices_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_compute_bin_indices_f32(device const float* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device const uint* numBins [[buffer(2)]], device const float* minVal [[buffer(3)]], device const float* maxVal [[buffer(4)]], device uint* binIndices [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint nBins = numBins[0u];
  float vMin = minVal[0u];
  float vMax = maxVal[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = _input[gid];
    float range = (vMax - vMin);
    float nBinsF = ((float)(nBins));
    float normalized = ((val - vMin) / range);
    float binIdxF = (normalized * nBinsF);
    int binIdxRaw = ((int)(floor(binIdxF)));
    int nBinsMinusOne = (((int)(nBins)) - 1);
    int binIdxClamped = (((binIdxRaw < 0)) ? (0) : ((((binIdxRaw > nBinsMinusOne)) ? (nBinsMinusOne) : (binIdxRaw))));
    uint binIdx = ((uint)(binIdxClamped));
    binIndices[gid] = binIdx;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_sum_reduce_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_sum_reduce_f32(device const float* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device float* result [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float sdata[256];
  uint countVal = count[0];
  bool inBounds = (gid < countVal);
  float val = _input[gid];
  if (inBounds) {
    sdata[lid] = val;
  } else {
    sdata[lid] = as_type<float>(0x00000000u);
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  uint reductionStride = 128u;
  for (int reductionStep = 0; reductionStep < (int)(8); ++reductionStep) {
    uint stride = reductionStride;
    if ((lid < stride)) {
      uint partnerIdx = (lid + stride);
      float myVal = sdata[lid];
      float partnerVal = sdata[partnerIdx];
      sdata[lid] = (myVal + partnerVal);
    }
    uint strideToHalve = reductionStride;
    uint nextStride = (strideToHalve >> 1u);
    reductionStride = nextStride;
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  uint zeroU32 = ((uint)(0));
  if ((lid == zeroU32)) {
    result[0] = sdata[0];
  }
}

)kernel";
const char* k_metal_navatala_dataframe_mean_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_mean_f32(device const float* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device float* mean [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float sdata[256];
  uint countVal = count[0];
  bool inBounds = (gid < countVal);
  float val = _input[gid];
  if (inBounds) {
    sdata[lid] = val;
  } else {
    sdata[lid] = as_type<float>(0x00000000u);
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  uint reductionStride = 128u;
  for (int reductionStep = 0; reductionStep < (int)(8); ++reductionStep) {
    uint stride = reductionStride;
    if ((lid < stride)) {
      uint partnerIdx = (lid + stride);
      float myVal = sdata[lid];
      float partnerVal = sdata[partnerIdx];
      sdata[lid] = (myVal + partnerVal);
    }
    uint strideToHalve = reductionStride;
    uint nextStride = (strideToHalve >> 1u);
    reductionStride = nextStride;
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  uint zeroU32 = ((uint)(0));
  if ((lid == zeroU32)) {
    float totalSum = sdata[0];
    float countFloat = ((float)(countVal));
    float meanVal = (totalSum / countFloat);
    mean[0] = meanVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_variance_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_variance_f32(device const float* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device const float* mean [[buffer(2)]], device float* variance [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float sdata[256];
  uint countVal = count[0];
  float meanVal = mean[0];
  bool inBounds = (gid < countVal);
  float x = _input[gid];
  float diff = (x - meanVal);
  float diffSq = (diff * diff);
  if (inBounds) {
    sdata[lid] = diffSq;
  } else {
    sdata[lid] = as_type<float>(0x00000000u);
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  uint reductionStride = 128u;
  for (int reductionStep = 0; reductionStep < (int)(8); ++reductionStep) {
    uint stride = reductionStride;
    if ((lid < stride)) {
      uint partnerIdx = (lid + stride);
      float myVal = sdata[lid];
      float partnerVal = sdata[partnerIdx];
      sdata[lid] = (myVal + partnerVal);
    }
    uint strideToHalve = reductionStride;
    uint nextStride = (strideToHalve >> 1u);
    reductionStride = nextStride;
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  uint zeroU32 = ((uint)(0));
  if ((lid == zeroU32)) {
    float totalSumSq = sdata[0];
    float countFloat = ((float)(countVal));
    float varianceVal = (totalSumSq / countFloat);
    variance[0] = varianceVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_stddev_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_stddev_f32(device const float* variance [[buffer(0)]], device float* stddev [[buffer(1)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint zeroU32 = ((uint)(0));
  if ((gid == zeroU32)) {
    float varianceVal = variance[0];
    float stddevVal = sqrt(varianceVal);
    stddev[0] = stddevVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_normalize_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_normalize_f32(device const float* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device const float* mean [[buffer(2)]], device const float* stddev [[buffer(3)]], device float* _output [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint countVal = count[0];
  float meanVal = mean[0];
  float stddevVal = stddev[0];
  bool inBounds = (gid < countVal);
  if (inBounds) {
    float x = _input[gid];
    float diff = (x - meanVal);
    float normalized = (diff / stddevVal);
    _output[gid] = normalized;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_init_rng_state = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_init_rng_state(device const ulong* seed [[buffer(0)]], device const uint* count [[buffer(1)]], device ulong* state [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint countVal = count[0u];
  ulong seedVal = seed[0u];
  bool inBounds = (gid < countVal);
  if (inBounds) {
    ulong gidU64 = ((ulong)(gid));
    ulong stateVal = (seedVal + gidU64);
    state[gid] = stateVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_uniform_lcg_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_uniform_lcg_f32(device ulong* state [[buffer(0)]], device const uint* count [[buffer(1)]], device float* _output [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint countVal = count[0u];
  bool inBounds = (gid < countVal);
  if (inBounds) {
    ulong s = state[gid];
    ulong aU64 = ((ulong)(1103515245u));
    ulong cU64 = ((ulong)(12345u));
    ulong prod = (aU64 * s);
    ulong sum = (prod + cU64);
    ulong newState = (sum & ((ulong)(2147483647u)));
    state[gid] = newState;
    float stateF32 = ((float)(newState));
    float randVal = (stateF32 / as_type<float>(0x4f000000u));
    _output[gid] = randVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_normal_box_muller_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_normal_box_muller_f32(device ulong* state [[buffer(0)]], device const uint* count [[buffer(1)]], device float* _output [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint countVal = count[0u];
  bool inBounds = (gid < countVal);
  if (inBounds) {
    ulong s = state[gid];
    ulong aU64 = ((ulong)(1103515245u));
    ulong cU64 = ((ulong)(12345u));
    ulong s1 = (((aU64 * s) + cU64) & ((ulong)(2147483647u)));
    float u1 = (((float)(s1)) / as_type<float>(0x4f000000u));
    ulong s2 = (((aU64 * s1) + cU64) & ((ulong)(2147483647u)));
    float u2 = (((float)(s2)) / as_type<float>(0x4f000000u));
    state[gid] = s2;
    float u1Clamped = (((u1 < as_type<float>(0x33d6bf95u))) ? (as_type<float>(0x33d6bf95u)) : (u1));
    float logU1 = log(u1Clamped);
    float negTwoLogU1 = (as_type<float>(0xc0000000u) * logU1);
    float sqrtTerm = sqrt(negTwoLogU1);
    float angle = (as_type<float>(0x40c90fdbu) * u2);
    float cosTerm = cos(angle);
    float z = (sqrtTerm * cosTerm);
    _output[gid] = z;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_init_indices = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_init_indices(device const uint* count [[buffer(0)]], device uint* indices [[buffer(1)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint countVal = count[0u];
  bool inBounds = (gid < countVal);
  if (inBounds) {
    indices[gid] = gid;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_covariance_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_covariance_f32(device const float* inputX [[buffer(0)]], device const float* inputY [[buffer(1)]], device const uint* count [[buffer(2)]], device const float* meanX [[buffer(3)]], device const float* meanY [[buffer(4)]], device float* covariance [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  threadgroup float sdata[256];
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  uint countVal = count[0];
  float meanXVal = meanX[0];
  float meanYVal = meanY[0];
  bool inBounds = (gid < countVal);
  float x = inputX[gid];
  float y = inputY[gid];
  float dx = (x - meanXVal);
  float dy = (y - meanYVal);
  float product = (dx * dy);
  if (inBounds) {
    sdata[lid] = product;
  } else {
    sdata[lid] = as_type<float>(0x00000000u);
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  uint covF32_reductionStride = 128u;
  for (int covF32_reductionStep = 0; covF32_reductionStep < (int)(8); ++covF32_reductionStep) {
    uint covF32_stride = covF32_reductionStride;
    if ((lid < covF32_stride)) {
      float covF32_other = sdata[(lid + covF32_stride)];
      float covF32_mine = sdata[lid];
      float covF32_sum = (covF32_mine + covF32_other);
      sdata[lid] = covF32_sum;
    }
    uint covF32_strideToHalve = covF32_reductionStride;
    uint covF32_nextStride = (covF32_strideToHalve >> 1u);
    covF32_reductionStride = covF32_nextStride;
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if ((lid == 0u)) {
    float totalSum = sdata[0u];
    float countFloat = ((float)(countVal));
    float covVal = (totalSum / countFloat);
    covariance[0u] = covVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_center_data_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_center_data_f32(device const float* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device const float* mean [[buffer(2)]], device float* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint countVal = count[0];
  float meanVal = mean[0];
  bool inBounds = (gid < countVal);
  if (inBounds) {
    float x = _input[gid];
    float centered = (x - meanVal);
    _output[gid] = centered;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_center_matrix_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_center_matrix_f32(device const float* _input [[buffer(0)]], device const uint* nSamples [[buffer(1)]], device const uint* nFeatures [[buffer(2)]], device const float* means [[buffer(3)]], device float* _output [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = nSamples[0];
  uint d = nFeatures[0];
  uint totalElems = (n * d);
  bool inBounds = (gid < totalElems);
  if (inBounds) {
    uint colIdx = (gid % d);
    float x = _input[gid];
    float colMean = means[colIdx];
    float centered = (x - colMean);
    _output[gid] = centered;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_column_means_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_column_means_f32(device const float* _input [[buffer(0)]], device const uint* nSamples [[buffer(1)]], device const uint* nFeatures [[buffer(2)]], device float* means [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  threadgroup float sdata[256];
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  uint wgid = ((uint)(int(__tgid.x)));
  uint n = nSamples[0];
  uint d = nFeatures[0];
  uint colIdx = wgid;
  bool colInBounds = (colIdx < d);
  if (colInBounds) {
    float partialSum = as_type<float>(0x00000000u);
    uint rowIdx = lid;
    bool rowInBounds = (rowIdx < n);
    uint elemIdx = ((rowIdx * d) + colIdx);
    if (rowInBounds) {
      float val = _input[elemIdx];
      sdata[lid] = val;
    } else {
      sdata[lid] = as_type<float>(0x00000000u);
    }
    threadgroup_barrier(mem_flags::mem_threadgroup);
    uint colMeansF32_reductionStride = 128u;
    for (int colMeansF32_reductionStep = 0; colMeansF32_reductionStep < (int)(8); ++colMeansF32_reductionStep) {
      uint colMeansF32_stride = colMeansF32_reductionStride;
      if ((lid < colMeansF32_stride)) {
        float colMeansF32_other = sdata[(lid + colMeansF32_stride)];
        float colMeansF32_mine = sdata[lid];
        float colMeansF32_sum = (colMeansF32_mine + colMeansF32_other);
        sdata[lid] = colMeansF32_sum;
      }
      uint colMeansF32_strideToHalve = colMeansF32_reductionStride;
      uint colMeansF32_nextStride = (colMeansF32_strideToHalve >> 1u);
      colMeansF32_reductionStride = colMeansF32_nextStride;
      threadgroup_barrier(mem_flags::mem_threadgroup);
    }
    if ((lid == 0u)) {
      float totalSum = sdata[0u];
      float nFloat = ((float)(n));
      float meanVal = (totalSum / nFloat);
      means[colIdx] = meanVal;
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_correlation_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_correlation_f32(device const float* covariance [[buffer(0)]], device const float* stddevX [[buffer(1)]], device const float* stddevY [[buffer(2)]], device float* correlation [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    float covVal = covariance[0];
    float sigmaX = stddevX[0];
    float sigmaY = stddevY[0];
    float denom = (sigmaX * sigmaY);
    float corVal = (covVal / denom);
    correlation[0] = corVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_cov_to_correlation_matrix_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_cov_to_correlation_matrix_f32(device const float* covMatrix [[buffer(0)]], device const uint* nFeatures [[buffer(1)]], device float* corMatrix [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint d = nFeatures[0];
  uint totalElems = (d * d);
  bool inBounds = (gid < totalElems);
  if (inBounds) {
    uint i = (gid / d);
    uint j = (gid % d);
    uint diagIdxI = ((i * d) + i);
    uint diagIdxJ = ((j * d) + j);
    float covIJ = covMatrix[gid];
    float covII = covMatrix[diagIdxI];
    float covJJ = covMatrix[diagIdxJ];
    float varProduct = (covII * covJJ);
    float denom = sqrt(varProduct);
    float corVal = (covIJ / denom);
    corMatrix[gid] = corVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_quantile_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_quantile_f32(device const float* sortedInput [[buffer(0)]], device const uint* count [[buffer(1)]], device const float* quantileP [[buffer(2)]], device float* result [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gidU32 = ((uint)(int(__gid.x)));
  if ((gidU32 == 0u)) {
    uint n = count[0u];
    float p = quantileP[0u];
    float pClamped = (((p < as_type<float>(0x00000000u))) ? (as_type<float>(0x00000000u)) : ((((p > as_type<float>(0x3f800000u))) ? (as_type<float>(0x3f800000u)) : (p))));
    uint nMinus1 = (n - 1u);
    float nMinus1Float = ((float)(nMinus1));
    float idx = (pClamped * nMinus1Float);
    float lowerFloat = floor(idx);
    uint lower = ((uint)(lowerFloat));
    uint upperCandidate = (lower + 1u);
    uint upper = (((upperCandidate < n)) ? (upperCandidate) : (nMinus1));
    float frac = (idx - lowerFloat);
    float valLower = sortedInput[lower];
    float valUpper = sortedInput[upper];
    float oneMinusFrac = (as_type<float>(0x3f800000u) - frac);
    float termLower = (oneMinusFrac * valLower);
    float termUpper = (frac * valUpper);
    float quantileVal = (termLower + termUpper);
    result[0u] = quantileVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_quantile_batch_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_quantile_batch_f32(device const float* sortedInput [[buffer(0)]], device const uint* count [[buffer(1)]], device const float* quantilePs [[buffer(2)]], device const uint* numQuantiles [[buffer(3)]], device float* results [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gidU32 = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint nq = numQuantiles[0u];
  bool inBounds = (gidU32 < nq);
  if (inBounds) {
    float p = quantilePs[gidU32];
    float pClamped = (((p < as_type<float>(0x00000000u))) ? (as_type<float>(0x00000000u)) : ((((p > as_type<float>(0x3f800000u))) ? (as_type<float>(0x3f800000u)) : (p))));
    uint nMinus1 = (n - 1u);
    float nMinus1Float = ((float)(nMinus1));
    float idx = (pClamped * nMinus1Float);
    float lowerFloat = floor(idx);
    uint lower = ((uint)(lowerFloat));
    uint upperCandidate = (lower + 1u);
    uint upper = (((upperCandidate < n)) ? (upperCandidate) : (nMinus1));
    float frac = (idx - lowerFloat);
    float valLower = sortedInput[lower];
    float valUpper = sortedInput[upper];
    float oneMinusFrac = (as_type<float>(0x3f800000u) - frac);
    float termLower = (oneMinusFrac * valLower);
    float termUpper = (frac * valUpper);
    float quantileVal = (termLower + termUpper);
    results[gidU32] = quantileVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_median_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_median_f32(device const float* sortedInput [[buffer(0)]], device const uint* count [[buffer(1)]], device float* median [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gidU32 = ((uint)(int(__gid.x)));
  if ((gidU32 == 0u)) {
    uint n = count[0u];
    uint nMinus1 = (n - 1u);
    float nMinus1Float = ((float)(nMinus1));
    float idx = (as_type<float>(0x3f000000u) * nMinus1Float);
    float lowerFloat = floor(idx);
    uint lower = ((uint)(lowerFloat));
    uint upperCandidate = (lower + 1u);
    uint upper = (((upperCandidate < n)) ? (upperCandidate) : (nMinus1));
    float frac = (idx - lowerFloat);
    float valLower = sortedInput[lower];
    float valUpper = sortedInput[upper];
    float oneMinusFrac = (as_type<float>(0x3f800000u) - frac);
    float termLower = (oneMinusFrac * valLower);
    float termUpper = (frac * valUpper);
    float medianVal = (termLower + termUpper);
    median[0u] = medianVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_iqr_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_iqr_f32(device const float* sortedInput [[buffer(0)]], device const uint* count [[buffer(1)]], device float* iqr [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gidU32 = ((uint)(int(__gid.x)));
  if ((gidU32 == 0u)) {
    uint n = count[0u];
    uint nMinus1 = (n - 1u);
    float nMinus1Float = ((float)(nMinus1));
    float idxQ1 = (as_type<float>(0x3e800000u) * nMinus1Float);
    float lowerQ1Float = floor(idxQ1);
    uint lowerQ1 = ((uint)(lowerQ1Float));
    uint upperQ1Candidate = (lowerQ1 + 1u);
    uint upperQ1 = (((upperQ1Candidate < n)) ? (upperQ1Candidate) : (nMinus1));
    float fracQ1 = (idxQ1 - lowerQ1Float);
    float valLowerQ1 = sortedInput[lowerQ1];
    float valUpperQ1 = sortedInput[upperQ1];
    float oneMinusFracQ1 = (as_type<float>(0x3f800000u) - fracQ1);
    float q1 = ((oneMinusFracQ1 * valLowerQ1) + (fracQ1 * valUpperQ1));
    float idxQ3 = (as_type<float>(0x3f400000u) * nMinus1Float);
    float lowerQ3Float = floor(idxQ3);
    uint lowerQ3 = ((uint)(lowerQ3Float));
    uint upperQ3Candidate = (lowerQ3 + 1u);
    uint upperQ3 = (((upperQ3Candidate < n)) ? (upperQ3Candidate) : (nMinus1));
    float fracQ3 = (idxQ3 - lowerQ3Float);
    float valLowerQ3 = sortedInput[lowerQ3];
    float valUpperQ3 = sortedInput[upperQ3];
    float oneMinusFracQ3 = (as_type<float>(0x3f800000u) - fracQ3);
    float q3 = ((oneMinusFracQ3 * valLowerQ3) + (fracQ3 * valUpperQ3));
    float iqrVal = (q3 - q1);
    iqr[0u] = iqrVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_init_e_w_m_c_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_init_e_w_m_c_f32(device const float* alphaIn [[buffer(0)]], device float* alpha [[buffer(1)]], device float* nEff [[buffer(2)]], device float* meanX [[buffer(3)]], device float* meanY [[buffer(4)]], device float* Cxy [[buffer(5)]], device float* varX [[buffer(6)]], device float* varY [[buffer(7)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float a = alphaIn[0];
    alpha[0] = a;
    nEff[0] = as_type<float>(0x00000000u);
    meanX[0] = as_type<float>(0x00000000u);
    meanY[0] = as_type<float>(0x00000000u);
    Cxy[0] = as_type<float>(0x00000000u);
    varX[0] = as_type<float>(0x00000000u);
    varY[0] = as_type<float>(0x00000000u);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_init_e_w_m_c_from_halflife_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_init_e_w_m_c_from_halflife_f32(device const float* halflife [[buffer(0)]], device float* alpha [[buffer(1)]], device float* nEff [[buffer(2)]], device float* meanX [[buffer(3)]], device float* meanY [[buffer(4)]], device float* Cxy [[buffer(5)]], device float* varX [[buffer(6)]], device float* varY [[buffer(7)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float h = halflife[0];
    float ln2 = as_type<float>(0x3f317215u);
    float a = (ln2 / h);
    alpha[0] = a;
    nEff[0] = as_type<float>(0x00000000u);
    meanX[0] = as_type<float>(0x00000000u);
    meanY[0] = as_type<float>(0x00000000u);
    Cxy[0] = as_type<float>(0x00000000u);
    varX[0] = as_type<float>(0x00000000u);
    varY[0] = as_type<float>(0x00000000u);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_ewmc_update_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_ewmc_update_f32(device const float* x [[buffer(0)]], device const float* y [[buffer(1)]], device const float* alpha [[buffer(2)]], device float* nEff [[buffer(3)]], device float* meanX [[buffer(4)]], device float* meanY [[buffer(5)]], device float* Cxy [[buffer(6)]], device float* varX [[buffer(7)]], device float* varY [[buffer(8)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float xVal = x[0];
    float yVal = y[0];
    float a = alpha[0];
    float oldNEff = nEff[0];
    float oldMeanX = meanX[0];
    float oldMeanY = meanY[0];
    float oldCxy = Cxy[0];
    float oldVarX = varX[0];
    float oldVarY = varY[0];
    float omA = (as_type<float>(0x3f800000u) - a);
    float omANEff = (omA * oldNEff);
    float newNEff = (a + omANEff);
    float deltaX = (xVal - oldMeanX);
    float deltaY = (yVal - oldMeanY);
    float aDeltaX = (a * deltaX);
    float newMeanX = (oldMeanX + aDeltaX);
    float aDeltaY = (a * deltaY);
    float newMeanY = (oldMeanY + aDeltaY);
    float deltaXNew = (xVal - newMeanX);
    float deltaYNew = (yVal - newMeanY);
    float dxDyNew = (deltaX * deltaYNew);
    float aDxDyNew = (a * dxDyNew);
    float cxySum = (oldCxy + aDxDyNew);
    float newCxy = (omA * cxySum);
    float dxDxNew = (deltaX * deltaXNew);
    float aDxDxNew = (a * dxDxNew);
    float varXSum = (oldVarX + aDxDxNew);
    float newVarX = (omA * varXSum);
    float dyDyNew = (deltaY * deltaYNew);
    float aDyDyNew = (a * dyDyNew);
    float varYSum = (oldVarY + aDyDyNew);
    float newVarY = (omA * varYSum);
    nEff[0] = newNEff;
    meanX[0] = newMeanX;
    meanY[0] = newMeanY;
    Cxy[0] = newCxy;
    varX[0] = newVarX;
    varY[0] = newVarY;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_ewmc_extract_covariance_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_ewmc_extract_covariance_f32(device const float* Cxy [[buffer(0)]], device const float* nEff [[buffer(1)]], device float* result [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float c = Cxy[0];
    float n = nEff[0];
    float eps = as_type<float>(0x2edbe6ffu);
    float safeN = (((n < eps)) ? (as_type<float>(0x3f800000u)) : (n));
    float cov = (c / safeN);
    float res = (((n < eps)) ? (as_type<float>(0x00000000u)) : (cov));
    result[0] = res;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_ewmc_extract_correlation_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_ewmc_extract_correlation_f32(device const float* Cxy [[buffer(0)]], device const float* varX [[buffer(1)]], device const float* varY [[buffer(2)]], device const float* nEff [[buffer(3)]], device float* result [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    float c = Cxy[0];
    float vx = varX[0];
    float vy = varY[0];
    float n = nEff[0];
    float vxvy = (vx * vy);
    float eps = as_type<float>(0x2edbe6ffu);
    float denom = sqrt(vxvy);
    float safeDenom = (((vxvy < eps)) ? (as_type<float>(0x3f800000u)) : (denom));
    float corr = (c / safeDenom);
    float res = (((vxvy < eps)) ? (as_type<float>(0x00000000u)) : (corr));
    result[0] = res;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_init_m_c_d_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_init_m_c_d_f32(device const float* thresholdIn [[buffer(0)]], device float* robustMeanX [[buffer(1)]], device float* robustMeanY [[buffer(2)]], device float* robustCov [[buffer(3)]], device float* determinant [[buffer(4)]], device float* threshold [[buffer(5)]], device uint* count [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    float thresh = thresholdIn[0];
    robustMeanX[0] = as_type<float>(0x00000000u);
    robustMeanY[0] = as_type<float>(0x00000000u);
    robustCov[0] = as_type<float>(0x00000000u);
    determinant[0] = as_type<float>(0x00000000u);
    threshold[0] = thresh;
    count[0] = 0u;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_mcd_compute_center_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_mcd_compute_center_f32(device const float* dataX [[buffer(0)]], device const float* dataY [[buffer(1)]], device const uint* mask [[buffer(2)]], device const uint* n [[buffer(3)]], device float* robustMeanX [[buffer(4)]], device float* robustMeanY [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float sdataX[256];
  threadgroup float sdataY[256];
  threadgroup uint scount[256];
  uint len = n[0];
  bool inBounds = (gid < len);
  if (inBounds) {
    uint m = mask[gid];
    float x = dataX[gid];
    float y = dataY[gid];
    float mF = ((float)(m));
    float maskedX = (x * mF);
    float maskedY = (y * mF);
    sdataX[lid] = maskedX;
    sdataY[lid] = maskedY;
    scount[lid] = m;
  } else {
    sdataX[lid] = as_type<float>(0x00000000u);
    sdataY[lid] = as_type<float>(0x00000000u);
    scount[lid] = 0u;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  uint ctr1ReductionStride = 128u;
  for (int ctr1ReductionStep = 0; ctr1ReductionStep < (int)(8); ++ctr1ReductionStep) {
    uint ctr1Stride = ctr1ReductionStride;
    if ((lid < ctr1Stride)) {
      float otherX = sdataX[(lid + ctr1Stride)];
      float otherY = sdataY[(lid + ctr1Stride)];
      uint otherC = scount[(lid + ctr1Stride)];
      float mineX = sdataX[lid];
      float mineY = sdataY[lid];
      uint mineC = scount[lid];
      float sumX = (mineX + otherX);
      float sumY = (mineY + otherY);
      uint sumC = (mineC + otherC);
      sdataX[lid] = sumX;
      sdataY[lid] = sumY;
      scount[lid] = sumC;
    }
    uint ctr1StrideToHalve = ctr1ReductionStride;
    uint ctr1NextStride = (ctr1StrideToHalve >> 1u);
    ctr1ReductionStride = ctr1NextStride;
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if ((lid == 0u)) {
    float totalX = sdataX[0];
    float totalY = sdataY[0];
    uint totalCount = scount[0];
    float countF = ((float)(totalCount));
    float safeCount = (((countF > as_type<float>(0x00000000u))) ? (countF) : (as_type<float>(0x3f800000u)));
    float meanX = (totalX / safeCount);
    float meanY = (totalY / safeCount);
    robustMeanX[0] = meanX;
    robustMeanY[0] = meanY;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_mcd_compute_covariance_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_mcd_compute_covariance_f32(device const float* dataX [[buffer(0)]], device const float* dataY [[buffer(1)]], device const uint* mask [[buffer(2)]], device const float* robustMeanX [[buffer(3)]], device const float* robustMeanY [[buffer(4)]], device const uint* n [[buffer(5)]], device float* robustCov [[buffer(6)]], device float* determinant [[buffer(7)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float scov[256];
  threadgroup float svarX[256];
  threadgroup float svarY[256];
  threadgroup uint scount[256];
  uint len = n[0];
  float mx = robustMeanX[0];
  float my = robustMeanY[0];
  bool inBounds = (gid < len);
  if (inBounds) {
    uint m = mask[gid];
    float x = dataX[gid];
    float y = dataY[gid];
    float mF = ((float)(m));
    float dx = (x - mx);
    float dy = (y - my);
    float covProd = (mF * (dx * dy));
    float varXProd = (mF * (dx * dx));
    float varYProd = (mF * (dy * dy));
    scov[lid] = covProd;
    svarX[lid] = varXProd;
    svarY[lid] = varYProd;
    scount[lid] = m;
  } else {
    scov[lid] = as_type<float>(0x00000000u);
    svarX[lid] = as_type<float>(0x00000000u);
    svarY[lid] = as_type<float>(0x00000000u);
    scount[lid] = 0u;
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  uint cov1ReductionStride = 128u;
  for (int cov1ReductionStep = 0; cov1ReductionStep < (int)(8); ++cov1ReductionStep) {
    uint cov1Stride = cov1ReductionStride;
    if ((lid < cov1Stride)) {
      float otherCov = scov[(lid + cov1Stride)];
      float otherVX = svarX[(lid + cov1Stride)];
      float otherVY = svarY[(lid + cov1Stride)];
      uint otherC = scount[(lid + cov1Stride)];
      float mineCov = scov[lid];
      float mineVX = svarX[lid];
      float mineVY = svarY[lid];
      uint mineC = scount[lid];
      float sumCov = (mineCov + otherCov);
      float sumVX = (mineVX + otherVX);
      float sumVY = (mineVY + otherVY);
      uint sumC = (mineC + otherC);
      scov[lid] = sumCov;
      svarX[lid] = sumVX;
      svarY[lid] = sumVY;
      scount[lid] = sumC;
    }
    uint cov1StrideToHalve = cov1ReductionStride;
    uint cov1NextStride = (cov1StrideToHalve >> 1u);
    cov1ReductionStride = cov1NextStride;
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if ((lid == 0u)) {
    float totalCov = scov[0];
    float totalVX = svarX[0];
    float totalVY = svarY[0];
    uint totalCount = scount[0];
    float countF = ((float)(totalCount));
    float safeCount = (((countF > as_type<float>(0x00000000u))) ? (countF) : (as_type<float>(0x3f800000u)));
    float covVal = (totalCov / safeCount);
    float varX = (totalVX / safeCount);
    float varY = (totalVY / safeCount);
    float covSq = (covVal * covVal);
    float det = ((varX * varY) - covSq);
    robustCov[0] = covVal;
    determinant[0] = det;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_mcd_mahalanobis_distance_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_mcd_mahalanobis_distance_f32(device const float* dataX [[buffer(0)]], device const float* dataY [[buffer(1)]], device const float* robustMeanX [[buffer(2)]], device const float* robustMeanY [[buffer(3)]], device const float* robustCov [[buffer(4)]], device const float* varX [[buffer(5)]], device const float* varY [[buffer(6)]], device const uint* n [[buffer(7)]], device float* distances [[buffer(8)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint len = n[0];
  bool inBounds = (gid < len);
  if (inBounds) {
    float mx = robustMeanX[0];
    float my = robustMeanY[0];
    float cov = robustCov[0];
    float vx = varX[0];
    float vy = varY[0];
    float x = dataX[gid];
    float y = dataY[gid];
    float dx = (x - mx);
    float dy = (y - my);
    float covSq = (cov * cov);
    float det = ((vx * vy) - covSq);
    float safeDet = (((det > as_type<float>(0x358637bdu))) ? (det) : (as_type<float>(0x3f800000u)));
    float invDet = (as_type<float>(0x3f800000u) / safeDet);
    float dx2 = (dx * dx);
    float dy2 = (dy * dy);
    float dxdy = (dx * dy);
    float term1 = (dx2 * vy);
    float term2 = (dy2 * vx);
    float term3 = (as_type<float>(0x40000000u) * (dxdy * cov));
    float num = ((term1 + term2) - term3);
    float dist2 = (num * invDet);
    distances[gid] = dist2;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_mcd_detect_outliers_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_mcd_detect_outliers_f32(device const float* distances [[buffer(0)]], device const float* threshold [[buffer(1)]], device const uint* n [[buffer(2)]], device uint* outlierFlags [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint len = n[0];
  bool inBounds = (gid < len);
  if (inBounds) {
    float thresh = threshold[0];
    float dist2 = distances[gid];
    bool isOutlier = (dist2 > thresh);
    uint flag = ((isOutlier) ? (1u) : (0u));
    outlierFlags[gid] = flag;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_mcd_extract_mean_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_mcd_extract_mean_f32(device const float* robustMeanX [[buffer(0)]], device const float* robustMeanY [[buffer(1)]], device float* meanX [[buffer(2)]], device float* meanY [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    float mx = robustMeanX[0];
    float my = robustMeanY[0];
    meanX[0] = mx;
    meanY[0] = my;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_init_moments_state_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_init_moments_state_f32(device uint* n [[buffer(0)]], device float* M1 [[buffer(1)]], device float* M2 [[buffer(2)]], device float* M3 [[buffer(3)]], device float* M4 [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    n[0] = 0u;
    M1[0] = as_type<float>(0x00000000u);
    M2[0] = as_type<float>(0x00000000u);
    M3[0] = as_type<float>(0x00000000u);
    M4[0] = as_type<float>(0x00000000u);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_moments_update_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_moments_update_f32(device const float* x [[buffer(0)]], device uint* n [[buffer(1)]], device float* M1 [[buffer(2)]], device float* M2 [[buffer(3)]], device float* M3 [[buffer(4)]], device float* M4 [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    uint nOld = n[0];
    float m1Old = M1[0];
    float m2Old = M2[0];
    float m3Old = M3[0];
    float m4Old = M4[0];
    float xVal = x[0];
    uint nNew = (nOld + 1u);
    float nNewF = ((float)(nNew));
    float nOldF = ((float)(nOld));
    float delta = (xVal - m1Old);
    float delta_n = (delta / nNewF);
    float delta_n2 = (delta_n * delta_n);
    float term1 = ((delta * delta_n) * nOldF);
    float m1New = (m1Old + delta_n);
    float nNew2 = (nNewF * nNewF);
    float threeNNew = (as_type<float>(0x40400000u) * nNewF);
    float nNew2MinusThreeN = (nNew2 - threeNNew);
    float m4Coeff = (nNew2MinusThreeN + as_type<float>(0x40400000u));
    float m4Term1 = ((term1 * delta_n2) * m4Coeff);
    float m4Term2 = ((as_type<float>(0x40c00000u) * delta_n2) * m2Old);
    float m4Term3 = ((as_type<float>(0x40800000u) * delta_n) * m3Old);
    float m4New = (((m4Old + m4Term1) + m4Term2) - m4Term3);
    float nNewMinus2 = (nNewF - as_type<float>(0x40000000u));
    float m3Term1 = ((term1 * delta_n) * nNewMinus2);
    float m3Term2 = ((as_type<float>(0x40400000u) * delta_n) * m2Old);
    float m3New = ((m3Old + m3Term1) - m3Term2);
    float m2New = (m2Old + term1);
    n[0] = nNew;
    M1[0] = m1New;
    M2[0] = m2New;
    M3[0] = m3New;
    M4[0] = m4New;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_moments_merge_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_moments_merge_f32(device const uint* nA [[buffer(0)]], device const float* M1A [[buffer(1)]], device const float* M2A [[buffer(2)]], device const float* M3A [[buffer(3)]], device const float* M4A [[buffer(4)]], device const uint* nB [[buffer(5)]], device const float* M1B [[buffer(6)]], device const float* M2B [[buffer(7)]], device const float* M3B [[buffer(8)]], device const float* M4B [[buffer(9)]], device uint* nOut [[buffer(10)]], device float* M1Out [[buffer(11)]], device float* M2Out [[buffer(12)]], device float* M3Out [[buffer(13)]], device float* M4Out [[buffer(14)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    uint nAVal = nA[0];
    float m1AVal = M1A[0];
    float m2AVal = M2A[0];
    float m3AVal = M3A[0];
    float m4AVal = M4A[0];
    uint nBVal = nB[0];
    float m1BVal = M1B[0];
    float m2BVal = M2B[0];
    float m3BVal = M3B[0];
    float m4BVal = M4B[0];
    uint nTotal = (nAVal + nBVal);
    float nAf = ((float)(nAVal));
    float nBf = ((float)(nBVal));
    float nTotalf = ((float)(nTotal));
    float nTotalSafe = (((nTotal == 0u)) ? (as_type<float>(0x3f800000u)) : (nTotalf));
    float delta = (m1BVal - m1AVal);
    float delta2 = (delta * delta);
    float delta3 = (delta2 * delta);
    float delta4 = (delta3 * delta);
    float nAnB = (nAf * nBf);
    float m1New = (m1AVal + (delta * (nBf / nTotalSafe)));
    float m2Term = ((delta2 * nAnB) / nTotalSafe);
    float m2New = ((m2AVal + m2BVal) + m2Term);
    float n2 = (nTotalSafe * nTotalSafe);
    float nAMinusNB = (nAf - nBf);
    float m3Term1 = (((delta3 * nAnB) * nAMinusNB) / n2);
    float nAM2B = (nAf * m2BVal);
    float nBM2A = (nBf * m2AVal);
    float m3Term2 = (((as_type<float>(0x40400000u) * delta) * (nAM2B - nBM2A)) / nTotalSafe);
    float m3New = (((m3AVal + m3BVal) + m3Term1) + m3Term2);
    float n3 = (n2 * nTotalSafe);
    float nA2 = (nAf * nAf);
    float nB2 = (nBf * nBf);
    float nA2MinusNAnB = (nA2 - nAnB);
    float m4Coeff = (nA2MinusNAnB + nB2);
    float m4Term1 = (((delta4 * nAnB) * m4Coeff) / n3);
    float nA2M2B = (nA2 * m2BVal);
    float nB2M2A = (nB2 * m2AVal);
    float m4Term2 = (((as_type<float>(0x40c00000u) * delta2) * (nA2M2B + nB2M2A)) / n2);
    float nAM3B = (nAf * m3BVal);
    float nBM3A = (nBf * m3AVal);
    float m4Term3 = (((as_type<float>(0x40800000u) * delta) * (nAM3B - nBM3A)) / nTotalSafe);
    float m4New = ((((m4AVal + m4BVal) + m4Term1) + m4Term2) + m4Term3);
    nOut[0] = nTotal;
    M1Out[0] = m1New;
    M2Out[0] = m2New;
    M3Out[0] = m3New;
    M4Out[0] = m4New;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_skewness_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_extract_skewness_f32(device const uint* n [[buffer(0)]], device const float* M2 [[buffer(1)]], device const float* M3 [[buffer(2)]], device float* skewness [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    uint nVal = n[0];
    float m2Val = M2[0];
    float m3Val = M3[0];
    float nf = ((float)(nVal));
    float sqrtN = sqrt(nf);
    float sqrtM2 = sqrt(m2Val);
    float m2Pow15 = (m2Val * sqrtM2);
    float denomSafe = (((m2Val == as_type<float>(0x00000000u))) ? (as_type<float>(0x3f800000u)) : (m2Pow15));
    float numerator = (sqrtN * m3Val);
    float skewRaw = (numerator / denomSafe);
    float skewResult = (((m2Val == as_type<float>(0x00000000u))) ? (as_type<float>(0x00000000u)) : (skewRaw));
    skewness[0] = skewResult;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_kurtosis_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_extract_kurtosis_f32(device const uint* n [[buffer(0)]], device const float* M2 [[buffer(1)]], device const float* M4 [[buffer(2)]], device float* kurtosis [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    uint nVal = n[0];
    float m2Val = M2[0];
    float m4Val = M4[0];
    float nf = ((float)(nVal));
    float m2Sq = (m2Val * m2Val);
    float denomSafe = (((m2Val == as_type<float>(0x00000000u))) ? (as_type<float>(0x3f800000u)) : (m2Sq));
    float numerator = (nf * m4Val);
    float kurtRaw = (numerator / denomSafe);
    float kurtResult = (((m2Val == as_type<float>(0x00000000u))) ? (as_type<float>(0x00000000u)) : (kurtRaw));
    kurtosis[0] = kurtResult;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_excess_kurtosis_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_extract_excess_kurtosis_f32(device const uint* n [[buffer(0)]], device const float* M2 [[buffer(1)]], device const float* M4 [[buffer(2)]], device float* excessKurtosis [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  if ((int(__gid.x) == 0)) {
    uint nVal = n[0];
    float m2Val = M2[0];
    float m4Val = M4[0];
    float nf = ((float)(nVal));
    float m2Sq = (m2Val * m2Val);
    float denomSafe = (((m2Val == as_type<float>(0x00000000u))) ? (as_type<float>(0x3f800000u)) : (m2Sq));
    float numerator = (nf * m4Val);
    float kurtRaw = (numerator / denomSafe);
    float excessRaw = (kurtRaw - as_type<float>(0x40400000u));
    float excessResult = (((m2Val == as_type<float>(0x00000000u))) ? (as_type<float>(0x00000000u)) : (excessRaw));
    excessKurtosis[0] = excessResult;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_contingency_matrix_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_contingency_matrix_f32(device const uint* trueLabels [[buffer(0)]], device const uint* predLabels [[buffer(1)]], device const uint* n [[buffer(2)]], device const uint* numTrueClasses [[buffer(3)]], device const uint* numPredClusters [[buffer(4)]], device float* contingency [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint nVal = n[0u];
  uint numPred = numPredClusters[0u];
  bool inBounds = (gid < nVal);
  if (inBounds) {
    uint trueLabel = trueLabels[gid];
    uint predLabel = predLabels[gid];
    uint contIdx = ((trueLabel * numPred) + predLabel);
    atomic_fetch_add_explicit((device atomic_float*)(&contingency[contIdx]), as_type<float>(0x3f800000u), memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_silhouette_score_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_silhouette_score_f32(device const float* distMatrix [[buffer(0)]], device const uint* labels [[buffer(1)]], device const uint* clusterCounts [[buffer(2)]], device const uint* n [[buffer(3)]], device const uint* k [[buffer(4)]], device float* silhouette [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint nVal = n[0u];
  uint kVal = k[0u];
  bool inBounds = (gid < nVal);
  if (inBounds) {
    uint myCluster = labels[gid];
    uint myClusterCount = clusterCounts[myCluster];
    uint rowBase = (gid * nVal);
    float intraSumAccum = as_type<float>(0x00000000u);
    uint intraCountAccum = 0u;
    for (int j = 0; j < (int)(nVal); ++j) {
      uint jU32 = ((uint)(j));
      uint otherCluster = labels[jU32];
      bool sameCluster = (otherCluster == myCluster);
      bool notSelf = (gid != jU32);
      bool shouldInclude = (sameCluster && notSelf);
      if (shouldInclude) {
        uint distIdx = (rowBase + jU32);
        float dist = distMatrix[distIdx];
        float curIntraSum = intraSumAccum;
        float newIntraSum = (curIntraSum + dist);
        intraSumAccum = newIntraSum;
        uint curIntraCount = intraCountAccum;
        uint newIntraCount = (curIntraCount + 1u);
        intraCountAccum = newIntraCount;
      }
    }
    float intraSum = intraSumAccum;
    uint intraCount = intraCountAccum;
    bool hasIntra = (intraCount > 0u);
    float intraCountF = ((float)(intraCount));
    float aVal = ((hasIntra) ? ((intraSum / intraCountF)) : (as_type<float>(0x00000000u)));
    float bValAccum = as_type<float>(0x7f7fffffu);
    float interSumAccum = as_type<float>(0x00000000u);
    uint interCountAccum = 0u;
    for (int c = 0; c < (int)(kVal); ++c) {
      uint cU32 = ((uint)(c));
      bool isOtherCluster = (cU32 != myCluster);
      if (isOtherCluster) {
        interSumAccum = as_type<float>(0x00000000u);
        interCountAccum = 0u;
        for (int j = 0; j < (int)(nVal); ++j) {
          uint jU32 = ((uint)(j));
          uint otherClusterJ = labels[jU32];
          bool inTargetCluster = (otherClusterJ == cU32);
          if (inTargetCluster) {
            uint distIdx = (rowBase + jU32);
            float dist = distMatrix[distIdx];
            float curInterSum = interSumAccum;
            float newInterSum = (curInterSum + dist);
            interSumAccum = newInterSum;
            uint curInterCount = interCountAccum;
            uint newInterCount = (curInterCount + 1u);
            interCountAccum = newInterCount;
          }
        }
        float interSum = interSumAccum;
        uint interCount = interCountAccum;
        bool hasInter = (interCount > 0u);
        if (hasInter) {
          float interCountF = ((float)(interCount));
          float meanDist = (interSum / interCountF);
          float curBVal = bValAccum;
          bool isBetter = (meanDist < curBVal);
          float newBVal = ((isBetter) ? (meanDist) : (curBVal));
          bValAccum = newBVal;
        }
      }
    }
    float bVal = bValAccum;
    float maxAB = (((aVal > bVal)) ? (aVal) : (bVal));
    float diff = (bVal - aVal);
    bool maxIsZero = (maxAB == as_type<float>(0x00000000u));
    float score = ((maxIsZero) ? (as_type<float>(0x00000000u)) : ((diff / maxAB)));
    silhouette[gid] = score;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_adjusted_rand_index_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_adjusted_rand_index_f32(device const float* contingency [[buffer(0)]], device const float* rowSums [[buffer(1)]], device const float* colSums [[buffer(2)]], device const float* n [[buffer(3)]], device const uint* numTrue [[buffer(4)]], device const uint* numPred [[buffer(5)]], device float* ari [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float sumNij[256];
  threadgroup float sumAi[256];
  threadgroup float sumBj[256];
  float nVal = n[0u];
  uint numTrueVal = numTrue[0u];
  uint numPredVal = numPred[0u];
  uint totalCells = (numTrueVal * numPredVal);
  float localSumNijAccum = as_type<float>(0x00000000u);
  for (int idx = 0; idx < (int)(totalCells); ++idx) {
    uint idxU32 = ((uint)(idx));
    bool shouldProcess = ((idxU32 % 256u) == lid);
    if (shouldProcess) {
      float nij = contingency[idxU32];
      float nijM1 = (nij - as_type<float>(0x3f800000u));
      float choose2 = ((nij * nijM1) / as_type<float>(0x40000000u));
      float curSumNij = localSumNijAccum;
      float newSumNij = (curSumNij + choose2);
      localSumNijAccum = newSumNij;
    }
  }
  float localSumNij = localSumNijAccum;
  float localSumAiAccum = as_type<float>(0x00000000u);
  for (int i = 0; i < (int)(numTrueVal); ++i) {
    uint iU32 = ((uint)(i));
    bool shouldProcess = ((iU32 % 256u) == lid);
    if (shouldProcess) {
      float ai = rowSums[iU32];
      float aiM1 = (ai - as_type<float>(0x3f800000u));
      float choose2Ai = ((ai * aiM1) / as_type<float>(0x40000000u));
      float curSumAi = localSumAiAccum;
      float newSumAi = (curSumAi + choose2Ai);
      localSumAiAccum = newSumAi;
    }
  }
  float localSumAi = localSumAiAccum;
  float localSumBjAccum = as_type<float>(0x00000000u);
  for (int j = 0; j < (int)(numPredVal); ++j) {
    uint jU32 = ((uint)(j));
    bool shouldProcess = ((jU32 % 256u) == lid);
    if (shouldProcess) {
      float bj = colSums[jU32];
      float bjM1 = (bj - as_type<float>(0x3f800000u));
      float choose2Bj = ((bj * bjM1) / as_type<float>(0x40000000u));
      float curSumBj = localSumBjAccum;
      float newSumBj = (curSumBj + choose2Bj);
      localSumBjAccum = newSumBj;
    }
  }
  float localSumBj = localSumBjAccum;
  sumNij[lid] = localSumNij;
  sumAi[lid] = localSumAi;
  sumBj[lid] = localSumBj;
  threadgroup_barrier(mem_flags::mem_threadgroup);
  for (int stride = 0; stride < (int)(128u); ++stride) {
    uint strideU32 = ((uint)(stride));
    if ((lid < strideU32)) {
      float otherNij = sumNij[(lid + strideU32)];
      float mineNij = sumNij[lid];
      sumNij[lid] = (mineNij + otherNij);
      float otherAi = sumAi[(lid + strideU32)];
      float mineAi = sumAi[lid];
      sumAi[lid] = (mineAi + otherAi);
      float otherBj = sumBj[(lid + strideU32)];
      float mineBj = sumBj[lid];
      sumBj[lid] = (mineBj + otherBj);
    }
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if ((lid == 0u)) {
    float sumNijFinal = sumNij[0u];
    float sumAiFinal = sumAi[0u];
    float sumBjFinal = sumBj[0u];
    float nM1 = (nVal - as_type<float>(0x3f800000u));
    float nChoose2 = ((nVal * nM1) / as_type<float>(0x40000000u));
    float expectedIdx = ((sumAiFinal * sumBjFinal) / nChoose2);
    float maxIdx = ((sumAiFinal + sumBjFinal) / as_type<float>(0x40000000u));
    float numerator = (sumNijFinal - expectedIdx);
    float denominator = (maxIdx - expectedIdx);
    bool denomIsZero = (denominator == as_type<float>(0x00000000u));
    float ariVal = ((denomIsZero) ? (as_type<float>(0x3f800000u)) : ((numerator / denominator)));
    ari[0u] = ariVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_normalized_mutual_info_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_normalized_mutual_info_f32(device const float* contingency [[buffer(0)]], device const float* rowSums [[buffer(1)]], device const float* colSums [[buffer(2)]], device const float* n [[buffer(3)]], device const uint* numTrue [[buffer(4)]], device const uint* numPred [[buffer(5)]], device float* nmi [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float miSum[256];
  threadgroup float hySum[256];
  threadgroup float hcSum[256];
  float nVal = n[0u];
  uint numTrueVal = numTrue[0u];
  uint numPredVal = numPred[0u];
  uint totalCells = (numTrueVal * numPredVal);
  float localMIAccum = as_type<float>(0x00000000u);
  for (int idx = 0; idx < (int)(totalCells); ++idx) {
    uint idxU32 = ((uint)(idx));
    bool shouldProcess = ((idxU32 % 256u) == lid);
    if (shouldProcess) {
      uint i = (idxU32 / numPredVal);
      uint j = (idxU32 % numPredVal);
      float nij = contingency[idxU32];
      float ai = rowSums[i];
      float bj = colSums[j];
      bool nijGtZero = (nij > as_type<float>(0x00000000u));
      if (nijGtZero) {
        float pij = (nij / nVal);
        float aibj = (ai * bj);
        float ratio = ((nVal * nij) / aibj);
        float logRatio = log(ratio);
        float termMI = (pij * logRatio);
        float curMI = localMIAccum;
        float newMI = (curMI + termMI);
        localMIAccum = newMI;
      }
    }
  }
  float localMI = localMIAccum;
  float localHYAccum = as_type<float>(0x00000000u);
  for (int i = 0; i < (int)(numTrueVal); ++i) {
    uint iU32 = ((uint)(i));
    bool shouldProcess = ((iU32 % 256u) == lid);
    if (shouldProcess) {
      float aiHY = rowSums[iU32];
      bool aiGtZero = (aiHY > as_type<float>(0x00000000u));
      if (aiGtZero) {
        float pi = (aiHY / nVal);
        float logPi = log(pi);
        float termHY = (pi * logPi);
        float curHY = localHYAccum;
        float newHY = (curHY - termHY);
        localHYAccum = newHY;
      }
    }
  }
  float localHY = localHYAccum;
  float localHCAccum = as_type<float>(0x00000000u);
  for (int j = 0; j < (int)(numPredVal); ++j) {
    uint jU32 = ((uint)(j));
    bool shouldProcess = ((jU32 % 256u) == lid);
    if (shouldProcess) {
      float bjHC = colSums[jU32];
      bool bjGtZero = (bjHC > as_type<float>(0x00000000u));
      if (bjGtZero) {
        float pj = (bjHC / nVal);
        float logPj = log(pj);
        float termHC = (pj * logPj);
        float curHC = localHCAccum;
        float newHC = (curHC - termHC);
        localHCAccum = newHC;
      }
    }
  }
  float localHC = localHCAccum;
  miSum[lid] = localMI;
  hySum[lid] = localHY;
  hcSum[lid] = localHC;
  threadgroup_barrier(mem_flags::mem_threadgroup);
  for (int stride = 0; stride < (int)(128u); ++stride) {
    uint strideU32 = ((uint)(stride));
    if ((lid < strideU32)) {
      float otherMI = miSum[(lid + strideU32)];
      float mineMI = miSum[lid];
      miSum[lid] = (mineMI + otherMI);
      float otherHY = hySum[(lid + strideU32)];
      float mineHY = hySum[lid];
      hySum[lid] = (mineHY + otherHY);
      float otherHC = hcSum[(lid + strideU32)];
      float mineHC = hcSum[lid];
      hcSum[lid] = (mineHC + otherHC);
    }
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if ((lid == 0u)) {
    float miFinal = miSum[0u];
    float hyFinal = hySum[0u];
    float hcFinal = hcSum[0u];
    float hSum = (hyFinal + hcFinal);
    bool hSumIsZero = (hSum == as_type<float>(0x00000000u));
    float nmiVal = ((hSumIsZero) ? (as_type<float>(0x3f800000u)) : (((as_type<float>(0x40000000u) * miFinal) / hSum)));
    nmi[0u] = nmiVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_homogeneity_score_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_homogeneity_score_f32(device const float* contingency [[buffer(0)]], device const float* rowSums [[buffer(1)]], device const float* colSums [[buffer(2)]], device const float* n [[buffer(3)]], device const uint* numTrue [[buffer(4)]], device const uint* numPred [[buffer(5)]], device float* homogeneity [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float hycSum[256];
  threadgroup float hySum[256];
  float nVal = n[0u];
  uint numTrueVal = numTrue[0u];
  uint numPredVal = numPred[0u];
  uint totalCells = (numTrueVal * numPredVal);
  float localHYCAccum = as_type<float>(0x00000000u);
  for (int idx = 0; idx < (int)(totalCells); ++idx) {
    uint idxU32 = ((uint)(idx));
    bool shouldProcess = ((idxU32 % 256u) == lid);
    if (shouldProcess) {
      uint j = (idxU32 % numPredVal);
      float nij = contingency[idxU32];
      float bj = colSums[j];
      bool nijGtZero = (nij > as_type<float>(0x00000000u));
      bool bjGtZero = (bj > as_type<float>(0x00000000u));
      bool bothGtZero = (nijGtZero && bjGtZero);
      if (bothGtZero) {
        float pij = (nij / nVal);
        float condProb = (nij / bj);
        float logCondProb = log(condProb);
        float termHYC = (pij * logCondProb);
        float curHYC = localHYCAccum;
        float newHYC = (curHYC - termHYC);
        localHYCAccum = newHYC;
      }
    }
  }
  float localHYC = localHYCAccum;
  float localHYAccumHom = as_type<float>(0x00000000u);
  for (int i = 0; i < (int)(numTrueVal); ++i) {
    uint iU32 = ((uint)(i));
    bool shouldProcess = ((iU32 % 256u) == lid);
    if (shouldProcess) {
      float ai = rowSums[iU32];
      bool aiGtZero = (ai > as_type<float>(0x00000000u));
      if (aiGtZero) {
        float pi = (ai / nVal);
        float logPi = log(pi);
        float termHY = (pi * logPi);
        float curHY = localHYAccumHom;
        float newHY = (curHY - termHY);
        localHYAccumHom = newHY;
      }
    }
  }
  float localHY = localHYAccumHom;
  hycSum[lid] = localHYC;
  hySum[lid] = localHY;
  threadgroup_barrier(mem_flags::mem_threadgroup);
  for (int stride = 0; stride < (int)(128u); ++stride) {
    uint strideU32 = ((uint)(stride));
    if ((lid < strideU32)) {
      float otherHYC = hycSum[(lid + strideU32)];
      float mineHYC = hycSum[lid];
      hycSum[lid] = (mineHYC + otherHYC);
      float otherHY = hySum[(lid + strideU32)];
      float mineHY = hySum[lid];
      hySum[lid] = (mineHY + otherHY);
    }
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if ((lid == 0u)) {
    float hycFinal = hycSum[0u];
    float hyFinal = hySum[0u];
    bool hyIsZero = (hyFinal == as_type<float>(0x00000000u));
    float homoVal = ((hyIsZero) ? (as_type<float>(0x3f800000u)) : ((as_type<float>(0x3f800000u) - (hycFinal / hyFinal))));
    homogeneity[0u] = homoVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_completeness_score_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_completeness_score_f32(device const float* contingency [[buffer(0)]], device const float* rowSums [[buffer(1)]], device const float* colSums [[buffer(2)]], device const float* n [[buffer(3)]], device const uint* numTrue [[buffer(4)]], device const uint* numPred [[buffer(5)]], device float* completeness [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float hcySum[256];
  threadgroup float hcSum[256];
  float nVal = n[0u];
  uint numTrueVal = numTrue[0u];
  uint numPredVal = numPred[0u];
  uint totalCells = (numTrueVal * numPredVal);
  float localHCYAccum = as_type<float>(0x00000000u);
  for (int idx = 0; idx < (int)(totalCells); ++idx) {
    uint idxU32 = ((uint)(idx));
    bool shouldProcess = ((idxU32 % 256u) == lid);
    if (shouldProcess) {
      uint i = (idxU32 / numPredVal);
      float nij = contingency[idxU32];
      float ai = rowSums[i];
      bool nijGtZero = (nij > as_type<float>(0x00000000u));
      bool aiGtZero = (ai > as_type<float>(0x00000000u));
      bool bothGtZero = (nijGtZero && aiGtZero);
      if (bothGtZero) {
        float pij = (nij / nVal);
        float condProb = (nij / ai);
        float logCondProb = log(condProb);
        float termHCY = (pij * logCondProb);
        float curHCY = localHCYAccum;
        float newHCY = (curHCY - termHCY);
        localHCYAccum = newHCY;
      }
    }
  }
  float localHCY = localHCYAccum;
  float localHCAccumComp = as_type<float>(0x00000000u);
  for (int j = 0; j < (int)(numPredVal); ++j) {
    uint jU32 = ((uint)(j));
    bool shouldProcess = ((jU32 % 256u) == lid);
    if (shouldProcess) {
      float bj = colSums[jU32];
      bool bjGtZero = (bj > as_type<float>(0x00000000u));
      if (bjGtZero) {
        float pj = (bj / nVal);
        float logPj = log(pj);
        float termHC = (pj * logPj);
        float curHC = localHCAccumComp;
        float newHC = (curHC - termHC);
        localHCAccumComp = newHC;
      }
    }
  }
  float localHC = localHCAccumComp;
  hcySum[lid] = localHCY;
  hcSum[lid] = localHC;
  threadgroup_barrier(mem_flags::mem_threadgroup);
  for (int stride = 0; stride < (int)(128u); ++stride) {
    uint strideU32 = ((uint)(stride));
    if ((lid < strideU32)) {
      float otherHCY = hcySum[(lid + strideU32)];
      float mineHCY = hcySum[lid];
      hcySum[lid] = (mineHCY + otherHCY);
      float otherHC = hcSum[(lid + strideU32)];
      float mineHC = hcSum[lid];
      hcSum[lid] = (mineHC + otherHC);
    }
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if ((lid == 0u)) {
    float hcyFinal = hcySum[0u];
    float hcFinal = hcSum[0u];
    bool hcIsZero = (hcFinal == as_type<float>(0x00000000u));
    float compVal = ((hcIsZero) ? (as_type<float>(0x3f800000u)) : ((as_type<float>(0x3f800000u) - (hcyFinal / hcFinal))));
    completeness[0u] = compVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_v_measure_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_v_measure_f32(device const float* homogeneity [[buffer(0)]], device const float* completeness [[buffer(1)]], device float* vMeasure [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  if ((gid == 0u)) {
    float h = homogeneity[0u];
    float c = completeness[0u];
    float hPlusC = (h + c);
    bool sumIsZero = (hPlusC == as_type<float>(0x00000000u));
    float hTimesC = (h * c);
    float twoHC = (as_type<float>(0x40000000u) * hTimesC);
    float vVal = ((sumIsZero) ? (as_type<float>(0x00000000u)) : ((twoHC / hPlusC)));
    vMeasure[0u] = vVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_r2_score_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_r2_score_f32(device const float* y_true [[buffer(0)]], device const float* y_pred [[buffer(1)]], device const float* y_mean [[buffer(2)]], device const uint* n [[buffer(3)]], device float* result [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float sdata_res[256];
  threadgroup float sdata_tot[256];
  uint countVal = n[0];
  float meanVal = y_mean[0];
  bool inBounds = (gid < countVal);
  float trueVal = y_true[gid];
  float predVal = y_pred[gid];
  float diffRes = (trueVal - predVal);
  float sqRes = (diffRes * diffRes);
  float diffTot = (trueVal - meanVal);
  float sqTot = (diffTot * diffTot);
  if (inBounds) {
    sdata_res[lid] = sqRes;
    sdata_tot[lid] = sqTot;
  } else {
    sdata_res[lid] = as_type<float>(0x00000000u);
    sdata_tot[lid] = as_type<float>(0x00000000u);
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  uint r2F32_reductionStride = 128u;
  for (int reductionStep = 0; reductionStep < (int)(8); ++reductionStep) {
    uint r2F32_stride = r2F32_reductionStride;
    if ((lid < r2F32_stride)) {
      uint r2F32_partnerIdx = (lid + r2F32_stride);
      float otherRes = sdata_res[r2F32_partnerIdx];
      float mineRes = sdata_res[lid];
      float sumRes = (mineRes + otherRes);
      sdata_res[lid] = sumRes;
      float otherTot = sdata_tot[r2F32_partnerIdx];
      float mineTot = sdata_tot[lid];
      float sumTot = (mineTot + otherTot);
      sdata_tot[lid] = sumTot;
    }
    uint r2F32_strideToHalve = r2F32_reductionStride;
    uint r2F32_nextStride = (r2F32_strideToHalve >> 1u);
    r2F32_reductionStride = r2F32_nextStride;
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  uint zeroU32 = ((uint)(0));
  if ((lid == zeroU32)) {
    float ssRes = sdata_res[0];
    float ssTot = sdata_tot[0];
    float ratio = (ssRes / ssTot);
    float r2Val = (as_type<float>(0x3f800000u) - ratio);
    result[0] = r2Val;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_mse_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_mse_f32(device const float* y_true [[buffer(0)]], device const float* y_pred [[buffer(1)]], device const uint* n [[buffer(2)]], device float* result [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float sdata[256];
  uint countVal = n[0];
  bool inBounds = (gid < countVal);
  float trueVal = y_true[gid];
  float predVal = y_pred[gid];
  float diff = (trueVal - predVal);
  float sqErr = (diff * diff);
  if (inBounds) {
    sdata[lid] = sqErr;
  } else {
    sdata[lid] = as_type<float>(0x00000000u);
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  uint mseF32_reductionStride = 128u;
  for (int reductionStep = 0; reductionStep < (int)(8); ++reductionStep) {
    uint mseF32_stride = mseF32_reductionStride;
    if ((lid < mseF32_stride)) {
      uint mseF32_partnerIdx = (lid + mseF32_stride);
      float other = sdata[mseF32_partnerIdx];
      float mine = sdata[lid];
      float sum = (mine + other);
      sdata[lid] = sum;
    }
    uint mseF32_strideToHalve = mseF32_reductionStride;
    uint mseF32_nextStride = (mseF32_strideToHalve >> 1u);
    mseF32_reductionStride = mseF32_nextStride;
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  uint zeroU32 = ((uint)(0));
  if ((lid == zeroU32)) {
    float totalSum = sdata[0];
    float countFloat = ((float)(countVal));
    float mseVal = (totalSum / countFloat);
    result[0] = mseVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_rmse_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_rmse_f32(device const float* mse [[buffer(0)]], device float* rmse [[buffer(1)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint zeroU32 = ((uint)(0));
  if ((gid == zeroU32)) {
    float mseVal = mse[0];
    float rmseVal = sqrt(mseVal);
    rmse[0] = rmseVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_mae_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_mae_f32(device const float* y_true [[buffer(0)]], device const float* y_pred [[buffer(1)]], device const uint* n [[buffer(2)]], device float* result [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float sdata[256];
  uint countVal = n[0];
  bool inBounds = (gid < countVal);
  float trueVal = y_true[gid];
  float predVal = y_pred[gid];
  float diff = (trueVal - predVal);
  float absErr = abs(diff);
  if (inBounds) {
    sdata[lid] = absErr;
  } else {
    sdata[lid] = as_type<float>(0x00000000u);
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  uint maeF32_reductionStride = 128u;
  for (int reductionStep = 0; reductionStep < (int)(8); ++reductionStep) {
    uint maeF32_stride = maeF32_reductionStride;
    if ((lid < maeF32_stride)) {
      uint maeF32_partnerIdx = (lid + maeF32_stride);
      float other = sdata[maeF32_partnerIdx];
      float mine = sdata[lid];
      float sum = (mine + other);
      sdata[lid] = sum;
    }
    uint maeF32_strideToHalve = maeF32_reductionStride;
    uint maeF32_nextStride = (maeF32_strideToHalve >> 1u);
    maeF32_reductionStride = maeF32_nextStride;
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  uint zeroU32 = ((uint)(0));
  if ((lid == zeroU32)) {
    float totalSum = sdata[0];
    float countFloat = ((float)(countVal));
    float maeVal = (totalSum / countFloat);
    result[0] = maeVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_mape_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_mape_f32(device const float* y_true [[buffer(0)]], device const float* y_pred [[buffer(1)]], device const uint* n [[buffer(2)]], device float* result [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float sdata[256];
  uint countVal = n[0];
  bool inBounds = (gid < countVal);
  float trueVal = y_true[gid];
  float predVal = y_pred[gid];
  float diff = (trueVal - predVal);
  float absErr = abs(diff);
  float absTrueVal = abs(trueVal);
  float pctErr = (absErr / absTrueVal);
  if (inBounds) {
    sdata[lid] = pctErr;
  } else {
    sdata[lid] = as_type<float>(0x00000000u);
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  uint mapeF32_reductionStride = 128u;
  for (int reductionStep = 0; reductionStep < (int)(8); ++reductionStep) {
    uint mapeF32_stride = mapeF32_reductionStride;
    if ((lid < mapeF32_stride)) {
      uint mapeF32_partnerIdx = (lid + mapeF32_stride);
      float other = sdata[mapeF32_partnerIdx];
      float mine = sdata[lid];
      float sum = (mine + other);
      sdata[lid] = sum;
    }
    uint mapeF32_strideToHalve = mapeF32_reductionStride;
    uint mapeF32_nextStride = (mapeF32_strideToHalve >> 1u);
    mapeF32_reductionStride = mapeF32_nextStride;
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  uint zeroU32 = ((uint)(0));
  if ((lid == zeroU32)) {
    float totalSum = sdata[0];
    float countFloat = ((float)(countVal));
    float avgPctErr = (totalSum / countFloat);
    float mapeVal = (as_type<float>(0x42c80000u) * avgPctErr);
    result[0] = mapeVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_explained_variance_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_explained_variance_f32(device const float* y_true [[buffer(0)]], device const float* y_pred [[buffer(1)]], device const float* mean_residuals [[buffer(2)]], device const float* mean_true [[buffer(3)]], device const uint* n [[buffer(4)]], device float* result [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float sdata_var_res[256];
  threadgroup float sdata_var_true[256];
  uint countVal = n[0];
  float meanResVal = mean_residuals[0];
  float meanTrueVal = mean_true[0];
  bool inBounds = (gid < countVal);
  float trueVal = y_true[gid];
  float predVal = y_pred[gid];
  float residual = (trueVal - predVal);
  float resDiff = (residual - meanResVal);
  float sqResDiff = (resDiff * resDiff);
  float trueDiff = (trueVal - meanTrueVal);
  float sqTrueDiff = (trueDiff * trueDiff);
  if (inBounds) {
    sdata_var_res[lid] = sqResDiff;
    sdata_var_true[lid] = sqTrueDiff;
  } else {
    sdata_var_res[lid] = as_type<float>(0x00000000u);
    sdata_var_true[lid] = as_type<float>(0x00000000u);
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  uint evF32_reductionStride = 128u;
  for (int reductionStep = 0; reductionStep < (int)(8); ++reductionStep) {
    uint evF32_stride = evF32_reductionStride;
    if ((lid < evF32_stride)) {
      uint evF32_partnerIdx = (lid + evF32_stride);
      float otherRes = sdata_var_res[evF32_partnerIdx];
      float mineRes = sdata_var_res[lid];
      float sumRes = (mineRes + otherRes);
      sdata_var_res[lid] = sumRes;
      float otherTrue = sdata_var_true[evF32_partnerIdx];
      float mineTrue = sdata_var_true[lid];
      float sumTrue = (mineTrue + otherTrue);
      sdata_var_true[lid] = sumTrue;
    }
    uint evF32_strideToHalve = evF32_reductionStride;
    uint evF32_nextStride = (evF32_strideToHalve >> 1u);
    evF32_reductionStride = evF32_nextStride;
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  uint zeroU32 = ((uint)(0));
  if ((lid == zeroU32)) {
    float sumSqRes = sdata_var_res[0];
    float sumSqTrue = sdata_var_true[0];
    float countFloat = ((float)(countVal));
    float varRatio = (sumSqRes / sumSqTrue);
    float evVal = (as_type<float>(0x3f800000u) - varRatio);
    result[0] = evVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_entropy_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_entropy_f32(device const float* p [[buffer(0)]], device const uint* n [[buffer(1)]], device float* result [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float sdata[256];
  uint nVal = n[0u];
  bool inBounds = (gid < nVal);
  float pVal = p[gid];
  bool pPositive = (pVal > as_type<float>(0x00000000u));
  float logP = log(pVal);
  float pLogP = (pVal * logP);
  float contrib = ((pPositive) ? ((as_type<float>(0xbf800000u) * pLogP)) : (as_type<float>(0x00000000u)));
  if (inBounds) {
    sdata[lid] = contrib;
  } else {
    sdata[lid] = as_type<float>(0x00000000u);
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  uint e32_reductionStride = 128u;
  for (int e32_reductionStep = 0; e32_reductionStep < (int)(8); ++e32_reductionStep) {
    uint e32_stride = e32_reductionStride;
    if ((lid < e32_stride)) {
      float e32_other = sdata[(lid + e32_stride)];
      float e32_mine = sdata[lid];
      float e32_sum = (e32_mine + e32_other);
      sdata[lid] = e32_sum;
    }
    uint e32_strideToHalve = e32_reductionStride;
    uint e32_nextStride = (e32_strideToHalve >> 1u);
    e32_reductionStride = e32_nextStride;
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if ((lid == 0u)) {
    result[0u] = sdata[0u];
  }
}

)kernel";
const char* k_metal_navatala_dataframe_cross_entropy_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_cross_entropy_f32(device const float* p [[buffer(0)]], device const float* q [[buffer(1)]], device const uint* n [[buffer(2)]], device float* result [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float sdata[256];
  uint nVal = n[0u];
  bool inBounds = (gid < nVal);
  float pVal = p[gid];
  float qVal = q[gid];
  bool pPositive = (pVal > as_type<float>(0x00000000u));
  float logQ = log(qVal);
  float pLogQ = (pVal * logQ);
  float contrib = ((pPositive) ? ((as_type<float>(0xbf800000u) * pLogQ)) : (as_type<float>(0x00000000u)));
  if (inBounds) {
    sdata[lid] = contrib;
  } else {
    sdata[lid] = as_type<float>(0x00000000u);
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  uint ce32_reductionStride = 128u;
  for (int ce32_reductionStep = 0; ce32_reductionStep < (int)(8); ++ce32_reductionStep) {
    uint ce32_stride = ce32_reductionStride;
    if ((lid < ce32_stride)) {
      float ce32_other = sdata[(lid + ce32_stride)];
      float ce32_mine = sdata[lid];
      float ce32_sum = (ce32_mine + ce32_other);
      sdata[lid] = ce32_sum;
    }
    uint ce32_strideToHalve = ce32_reductionStride;
    uint ce32_nextStride = (ce32_strideToHalve >> 1u);
    ce32_reductionStride = ce32_nextStride;
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if ((lid == 0u)) {
    result[0u] = sdata[0u];
  }
}

)kernel";
const char* k_metal_navatala_dataframe_kl_divergence_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_kl_divergence_f32(device const float* p [[buffer(0)]], device const float* q [[buffer(1)]], device const uint* n [[buffer(2)]], device float* result [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lid = ((uint)(int(__tid.x)));
  threadgroup float sdata[256];
  uint nVal = n[0u];
  bool inBounds = (gid < nVal);
  float pVal = p[gid];
  float qVal = q[gid];
  bool pPositive = (pVal > as_type<float>(0x00000000u));
  float logP = log(pVal);
  float logQ = log(qVal);
  float logRatio = (logP - logQ);
  float pLogRatio = (pVal * logRatio);
  float contrib = ((pPositive) ? (pLogRatio) : (as_type<float>(0x00000000u)));
  if (inBounds) {
    sdata[lid] = contrib;
  } else {
    sdata[lid] = as_type<float>(0x00000000u);
  }
  threadgroup_barrier(mem_flags::mem_threadgroup);
  uint kl32_reductionStride = 128u;
  for (int kl32_reductionStep = 0; kl32_reductionStep < (int)(8); ++kl32_reductionStep) {
    uint kl32_stride = kl32_reductionStride;
    if ((lid < kl32_stride)) {
      float kl32_other = sdata[(lid + kl32_stride)];
      float kl32_mine = sdata[lid];
      float kl32_sum = (kl32_mine + kl32_other);
      sdata[lid] = kl32_sum;
    }
    uint kl32_strideToHalve = kl32_reductionStride;
    uint kl32_nextStride = (kl32_strideToHalve >> 1u);
    kl32_reductionStride = kl32_nextStride;
    threadgroup_barrier(mem_flags::mem_threadgroup);
  }
  if ((lid == 0u)) {
    result[0u] = sdata[0u];
  }
}

)kernel";
const char* k_metal_navatala_dataframe_mutual_information_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_mutual_information_f32(device const float* hX [[buffer(0)]], device const float* hY [[buffer(1)]], device const float* hXY [[buffer(2)]], device float* result [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gidU32 = ((uint)(int(__gid.x)));
  if ((gidU32 == 0u)) {
    float hXVal = hX[0u];
    float hYVal = hY[0u];
    float hXYVal = hXY[0u];
    float sum = (hXVal + hYVal);
    float mi = (sum - hXYVal);
    result[0u] = mi;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_conditional_entropy_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_conditional_entropy_f32(device const float* hX [[buffer(0)]], device const float* hXY [[buffer(1)]], device float* result [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gidU32 = ((uint)(int(__gid.x)));
  if ((gidU32 == 0u)) {
    float hXVal = hX[0u];
    float hXYVal = hXY[0u];
    float condH = (hXYVal - hXVal);
    result[0u] = condH;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_permute_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_permute_f32(device float* data [[buffer(0)]], device const uint* random [[buffer(1)]], device const uint* n [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint nVal = n[0u];
  bool isThread0 = (gid == 0u);
  if (isThread0) {
    for (int i = 0; i < (int)(nVal); ++i) {
      uint iU32 = ((uint)(i));
      uint idx = ((nVal - 1u) - iU32);
      bool shouldSwap = (idx > 0u);
      if (shouldSwap) {
        uint randIdx = ((nVal - 1u) - idx);
        uint randVal = random[randIdx];
        uint idxPlusOne = (idx + 1u);
        uint j = (randVal % idxPlusOne);
        float valIdx = data[idx];
        float valJ = data[j];
        data[idx] = valJ;
        data[j] = valIdx;
      }
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_shuffle_rows_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_shuffle_rows_f32(device const float* _input [[buffer(0)]], device const uint* random [[buffer(1)]], device const uint* m [[buffer(2)]], device const uint* n [[buffer(3)]], device float* _output [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint mVal = m[0u];
  uint nVal = n[0u];
  bool inBounds = (gid < mVal);
  if (inBounds) {
    uint randVal = random[gid];
    uint srcRow = (randVal % mVal);
    for (int col = 0; col < (int)(nVal); ++col) {
      uint colU32 = ((uint)(col));
      uint srcIdx = ((srcRow * nVal) + colU32);
      uint dstIdx = ((gid * nVal) + colU32);
      float val = _input[srcIdx];
      _output[dstIdx] = val;
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_sample_without_replacement_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_sample_without_replacement_f32(device const float* data [[buffer(0)]], device const uint* random [[buffer(1)]], device const uint* n [[buffer(2)]], device const uint* k [[buffer(3)]], device float* _output [[buffer(4)]], device uint* indices [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint nVal = n[0u];
  uint kVal = k[0u];
  bool inBounds = (gid < kVal);
  if (inBounds) {
    uint randVal = random[gid];
    uint idx = (randVal % nVal);
    float val = data[idx];
    _output[gid] = val;
    indices[gid] = idx;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_sample_with_replacement_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_sample_with_replacement_f32(device const float* data [[buffer(0)]], device const uint* random [[buffer(1)]], device const uint* n [[buffer(2)]], device const uint* k [[buffer(3)]], device float* _output [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint nVal = n[0u];
  uint kVal = k[0u];
  bool inBounds = (gid < kVal);
  if (inBounds) {
    uint randVal = random[gid];
    uint idx = (randVal % nVal);
    float val = data[idx];
    _output[gid] = val;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_bootstrap_sample_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_bootstrap_sample_f32(device const float* data [[buffer(0)]], device const uint* random [[buffer(1)]], device const uint* n [[buffer(2)]], device float* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint nVal = n[0u];
  bool inBounds = (gid < nVal);
  if (inBounds) {
    uint randVal = random[gid];
    uint idx = (randVal % nVal);
    float val = data[idx];
    _output[gid] = val;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_select_top_k_per_row_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_select_top_k_per_row_f32(device const float* distances [[buffer(0)]], device const uint* q [[buffer(1)]], device const uint* n [[buffer(2)]], device const uint* k [[buffer(3)]], device float* outDistances [[buffer(4)]], device uint* outIndices [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint qVal = q[0];
  uint nVal = n[0];
  uint kVal = k[0];
  uint rowIdx = gid;
  bool inBounds = (rowIdx < qVal);
  if (inBounds) {
    uint inBase = (rowIdx * nVal);
    uint outBase = (rowIdx * kVal);
    for (int initIdx = 0; initIdx < (int)(kVal); ++initIdx) {
      uint initIdxU32 = ((uint)(initIdx));
      uint outDistIdx = (outBase + initIdxU32);
      outDistances[outDistIdx] = as_type<float>(0x7f7fc99eu);
      outIndices[outDistIdx] = 4294967295u;
    }
    for (int col = 0; col < (int)(nVal); ++col) {
      uint colU32 = ((uint)(col));
      uint inIdx = (inBase + colU32);
      float val = distances[inIdx];
      uint lastIdx = (kVal - 1u);
      uint lastOutIdx = (outBase + lastIdx);
      float kthDist = outDistances[lastOutIdx];
      bool shouldInsert = (val < kthDist);
      if (shouldInsert) {
        uint insertPosAccum = lastIdx;
        for (int scanIdx = 0; scanIdx < (int)(kVal); ++scanIdx) {
          uint scanIdxU32 = ((uint)(scanIdx));
          uint currentInsertPos = insertPosAccum;
          uint checkPos = (lastIdx - scanIdxU32);
          uint checkOutIdx = (outBase + checkPos);
          float checkDist = outDistances[checkOutIdx];
          bool isSmaller = (val < checkDist);
          uint newInsertPos = ((isSmaller) ? (checkPos) : (currentInsertPos));
          insertPosAccum = newInsertPos;
        }
        uint finalInsertPos = insertPosAccum;
        for (int shiftIdx = 0; shiftIdx < (int)(kVal); ++shiftIdx) {
          uint shiftIdxU32 = ((uint)(shiftIdx));
          uint shiftPos = (lastIdx - shiftIdxU32);
          bool shouldShift = (shiftPos > finalInsertPos);
          if (shouldShift) {
            uint srcPos = (shiftPos - 1u);
            uint srcOutIdx = (outBase + srcPos);
            uint dstOutIdx = (outBase + shiftPos);
            float srcDist = outDistances[srcOutIdx];
            uint srcIdx = outIndices[srcOutIdx];
            outDistances[dstOutIdx] = srcDist;
            outIndices[dstOutIdx] = srcIdx;
          }
        }
        uint insertOutIdx = (outBase + finalInsertPos);
        outDistances[insertOutIdx] = val;
        outIndices[insertOutIdx] = colU32;
      }
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_select_top_k_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_select_top_k_f32(device const float* values [[buffer(0)]], device const uint* count [[buffer(1)]], device const uint* k [[buffer(2)]], device float* outValues [[buffer(3)]], device uint* outIndices [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint countVal = count[0];
  uint kVal = k[0];
  bool isThread0 = (gid == 0u);
  if (isThread0) {
    for (int initIdx = 0; initIdx < (int)(kVal); ++initIdx) {
      uint initIdxU32 = ((uint)(initIdx));
      outValues[initIdxU32] = as_type<float>(0x7f7fc99eu);
      outIndices[initIdxU32] = 4294967295u;
    }
    for (int i = 0; i < (int)(countVal); ++i) {
      uint iU32 = ((uint)(i));
      float val = values[iU32];
      uint lastIdx = (kVal - 1u);
      float kthVal = outValues[lastIdx];
      bool shouldInsert = (val < kthVal);
      if (shouldInsert) {
        uint insertPosAccum = lastIdx;
        for (int scanIdx = 0; scanIdx < (int)(kVal); ++scanIdx) {
          uint scanIdxU32 = ((uint)(scanIdx));
          uint currentInsertPos = insertPosAccum;
          uint checkPos = (lastIdx - scanIdxU32);
          float checkVal = outValues[checkPos];
          bool isSmaller = (val < checkVal);
          uint newInsertPos = ((isSmaller) ? (checkPos) : (currentInsertPos));
          insertPosAccum = newInsertPos;
        }
        uint finalInsertPos = insertPosAccum;
        for (int shiftIdx = 0; shiftIdx < (int)(kVal); ++shiftIdx) {
          uint shiftIdxU32 = ((uint)(shiftIdx));
          uint shiftPos = (lastIdx - shiftIdxU32);
          bool shouldShift = (shiftPos > finalInsertPos);
          if (shouldShift) {
            uint srcPos = (shiftPos - 1u);
            float srcVal = outValues[srcPos];
            uint srcIdx = outIndices[srcPos];
            outValues[shiftPos] = srcVal;
            outIndices[shiftPos] = srcIdx;
          }
        }
        outValues[finalInsertPos] = val;
        outIndices[finalInsertPos] = iU32;
      }
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_arg_top_k_per_row_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_arg_top_k_per_row_f32(device const float* values [[buffer(0)]], device const uint* q [[buffer(1)]], device const uint* n [[buffer(2)]], device const uint* k [[buffer(3)]], device uint* outIndices [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint qVal = q[0];
  uint nVal = n[0];
  uint kVal = k[0];
  uint rowIdx = gid;
  bool inBounds = (rowIdx < qVal);
  if (inBounds) {
    uint inBase = (rowIdx * nVal);
    uint outBase = (rowIdx * kVal);
    float topKVal0 = as_type<float>(0x7f7fc99eu);
    for (int initIdx = 0; initIdx < (int)(kVal); ++initIdx) {
      uint initIdxU32 = ((uint)(initIdx));
      uint outIdx = (outBase + initIdxU32);
      outIndices[outIdx] = 4294967295u;
    }
    for (int col = 0; col < (int)(nVal); ++col) {
      uint colU32 = ((uint)(col));
      uint inIdx = (inBase + colU32);
      float val = values[inIdx];
      uint lastIdx = (kVal - 1u);
      uint lastOutIdx = (outBase + lastIdx);
      uint kthIdx = outIndices[lastOutIdx];
      bool kthIdxValid = (kthIdx != 4294967295u);
      uint kthInIdx = (inBase + kthIdx);
      float kthValIfValid = values[kthInIdx];
      float kthVal = ((kthIdxValid) ? (kthValIfValid) : (as_type<float>(0x7f7fc99eu)));
      bool shouldInsert = (val < kthVal);
      if (shouldInsert) {
        uint insertPosAccum = lastIdx;
        for (int scanIdx = 0; scanIdx < (int)(kVal); ++scanIdx) {
          uint scanIdxU32 = ((uint)(scanIdx));
          uint currentInsertPos = insertPosAccum;
          uint checkPos = (lastIdx - scanIdxU32);
          uint checkOutIdx = (outBase + checkPos);
          uint checkIdx = outIndices[checkOutIdx];
          bool checkIdxValid = (checkIdx != 4294967295u);
          uint checkInIdx = (inBase + checkIdx);
          float checkValIfValid = values[checkInIdx];
          float checkVal = ((checkIdxValid) ? (checkValIfValid) : (as_type<float>(0x7f7fc99eu)));
          bool isSmaller = (val < checkVal);
          uint newInsertPos = ((isSmaller) ? (checkPos) : (currentInsertPos));
          insertPosAccum = newInsertPos;
        }
        uint finalInsertPos = insertPosAccum;
        for (int shiftIdx = 0; shiftIdx < (int)(kVal); ++shiftIdx) {
          uint shiftIdxU32 = ((uint)(shiftIdx));
          uint shiftPos = (lastIdx - shiftIdxU32);
          bool shouldShift = (shiftPos > finalInsertPos);
          if (shouldShift) {
            uint srcPos = (shiftPos - 1u);
            uint srcOutIdx = (outBase + srcPos);
            uint dstOutIdx = (outBase + shiftPos);
            uint srcIdx = outIndices[srcOutIdx];
            outIndices[dstOutIdx] = srcIdx;
          }
        }
        uint insertOutIdx = (outBase + finalInsertPos);
        outIndices[insertOutIdx] = colU32;
      }
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_arg_top_k_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_arg_top_k_f32(device const float* values [[buffer(0)]], device const uint* count [[buffer(1)]], device const uint* k [[buffer(2)]], device uint* outIndices [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint countVal = count[0];
  uint kVal = k[0];
  bool isThread0 = (gid == 0u);
  if (isThread0) {
    for (int initIdx = 0; initIdx < (int)(kVal); ++initIdx) {
      uint initIdxU32 = ((uint)(initIdx));
      outIndices[initIdxU32] = 4294967295u;
    }
    for (int i = 0; i < (int)(countVal); ++i) {
      uint iU32 = ((uint)(i));
      float val = values[iU32];
      uint lastIdx = (kVal - 1u);
      uint kthIdx = outIndices[lastIdx];
      bool kthIdxValid = (kthIdx != 4294967295u);
      float kthValIfValid = values[kthIdx];
      float kthVal = ((kthIdxValid) ? (kthValIfValid) : (as_type<float>(0x7f7fc99eu)));
      bool shouldInsert = (val < kthVal);
      if (shouldInsert) {
        uint insertPosAccum = lastIdx;
        for (int scanIdx = 0; scanIdx < (int)(kVal); ++scanIdx) {
          uint scanIdxU32 = ((uint)(scanIdx));
          uint currentInsertPos = insertPosAccum;
          uint checkPos = (lastIdx - scanIdxU32);
          uint checkIdx = outIndices[checkPos];
          bool checkIdxValid = (checkIdx != 4294967295u);
          float checkValIfValid = values[checkIdx];
          float checkVal = ((checkIdxValid) ? (checkValIfValid) : (as_type<float>(0x7f7fc99eu)));
          bool isSmaller = (val < checkVal);
          uint newInsertPos = ((isSmaller) ? (checkPos) : (currentInsertPos));
          insertPosAccum = newInsertPos;
        }
        uint finalInsertPos = insertPosAccum;
        for (int shiftIdx = 0; shiftIdx < (int)(kVal); ++shiftIdx) {
          uint shiftIdxU32 = ((uint)(shiftIdx));
          uint shiftPos = (lastIdx - shiftIdxU32);
          bool shouldShift = (shiftPos > finalInsertPos);
          if (shouldShift) {
            uint srcPos = (shiftPos - 1u);
            uint srcIdx = outIndices[srcPos];
            outIndices[shiftPos] = srcIdx;
          }
        }
        outIndices[finalInsertPos] = iU32;
      }
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_partial_sort_per_row_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_partial_sort_per_row_f32(device const float* values [[buffer(0)]], device const uint* q [[buffer(1)]], device const uint* n [[buffer(2)]], device const uint* k [[buffer(3)]], device float* outValues [[buffer(4)]], device uint* outIndices [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint qVal = q[0];
  uint nVal = n[0];
  uint kVal = k[0];
  uint rowIdx = gid;
  bool inBounds = (rowIdx < qVal);
  if (inBounds) {
    uint inBase = (rowIdx * nVal);
    uint outBase = (rowIdx * kVal);
    for (int initIdx = 0; initIdx < (int)(kVal); ++initIdx) {
      uint initIdxU32 = ((uint)(initIdx));
      uint outIdx = (outBase + initIdxU32);
      outValues[outIdx] = as_type<float>(0x7f7fc99eu);
      outIndices[outIdx] = 4294967295u;
    }
    for (int col = 0; col < (int)(nVal); ++col) {
      uint colU32 = ((uint)(col));
      uint inIdx = (inBase + colU32);
      float val = values[inIdx];
      uint lastIdx = (kVal - 1u);
      uint lastOutIdx = (outBase + lastIdx);
      float kthVal = outValues[lastOutIdx];
      bool shouldInsert = (val < kthVal);
      if (shouldInsert) {
        uint insertPosAccum = lastIdx;
        for (int scanIdx = 0; scanIdx < (int)(kVal); ++scanIdx) {
          uint scanIdxU32 = ((uint)(scanIdx));
          uint currentInsertPos = insertPosAccum;
          uint checkPos = (lastIdx - scanIdxU32);
          uint checkOutIdx = (outBase + checkPos);
          float checkVal = outValues[checkOutIdx];
          bool isSmaller = (val < checkVal);
          uint newInsertPos = ((isSmaller) ? (checkPos) : (currentInsertPos));
          insertPosAccum = newInsertPos;
        }
        uint finalInsertPos = insertPosAccum;
        for (int shiftIdx = 0; shiftIdx < (int)(kVal); ++shiftIdx) {
          uint shiftIdxU32 = ((uint)(shiftIdx));
          uint shiftPos = (lastIdx - shiftIdxU32);
          bool shouldShift = (shiftPos > finalInsertPos);
          if (shouldShift) {
            uint srcPos = (shiftPos - 1u);
            uint srcOutIdx = (outBase + srcPos);
            uint dstOutIdx = (outBase + shiftPos);
            float srcVal = outValues[srcOutIdx];
            uint srcIdx = outIndices[srcOutIdx];
            outValues[dstOutIdx] = srcVal;
            outIndices[dstOutIdx] = srcIdx;
          }
        }
        uint insertOutIdx = (outBase + finalInsertPos);
        outValues[insertOutIdx] = val;
        outIndices[insertOutIdx] = colU32;
      }
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_group_by_sum_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_group_by_sum_i32(device const int* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* groupIds [[buffer(2)]], device const uint* count [[buffer(3)]], device int* _output [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      int val = data[gid];
      uint groupId = groupIds[gid];
      atomic_fetch_add_explicit((device atomic_int*)(&_output[groupId]), val, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_group_by_sum_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_group_by_sum_i64(device const long* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* groupIds [[buffer(2)]], device const uint* count [[buffer(3)]], device long* _output [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      long val = data[gid];
      uint groupId = groupIds[gid];
      atomic_fetch_add_explicit((device atomic_int*) /* metal atomic: unknown elem type */(&_output[groupId]), val, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_group_by_count = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_group_by_count(device const uint* validity [[buffer(0)]], device const uint* groupIds [[buffer(1)]], device const uint* count [[buffer(2)]], device uint* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      uint groupId = groupIds[gid];
      atomic_fetch_add_explicit((device atomic_uint*)(&_output[groupId]), 1u, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_group_by_min_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_group_by_min_i32(device const int* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* groupIds [[buffer(2)]], device const uint* count [[buffer(3)]], device int* _output [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      int val = data[gid];
      uint groupId = groupIds[gid];
      atomic_fetch_min_explicit((device atomic_int*)(&_output[groupId]), val, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_group_by_max_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_group_by_max_i32(device const int* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* groupIds [[buffer(2)]], device const uint* count [[buffer(3)]], device int* _output [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      int val = data[gid];
      uint groupId = groupIds[gid];
      atomic_fetch_max_explicit((device atomic_int*)(&_output[groupId]), val, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_group_by_sum_for_mean_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_group_by_sum_for_mean_f32(device const float* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* groupIds [[buffer(2)]], device const uint* count [[buffer(3)]], device long* sumOutput [[buffer(4)]], device uint* countOutput [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      float val = data[gid];
      uint groupId = groupIds[gid];
      float scaleFactor = as_type<float>(0x49800000u);
      float scaledVal = (val * scaleFactor);
      long scaledInt = ((long)(scaledVal));
      atomic_fetch_add_explicit((device atomic_int*) /* metal atomic: unknown elem type */(&sumOutput[groupId]), scaledInt, memory_order_relaxed);
      atomic_fetch_add_explicit((device atomic_uint*)(&countOutput[groupId]), 1u, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_group_by_nunique_helper_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_group_by_nunique_helper_i32(device const int* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* groupIds [[buffer(2)]], device const uint* count [[buffer(3)]], device uint* hashOutput [[buffer(4)]], device uint* validOutput [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int val = data[gid];
    uint groupId = groupIds[gid];
    uint k = ((uint)(val));
    uint c1 = 3432918353u;
    uint c2 = 461845907u;
    uint k1 = (k * c1);
    uint k2 = ((k1 << 15u) | (k1 >> 17u));
    uint k3 = (k2 * c2);
    uint h1 = (groupId ^ k3);
    uint h2 = ((h1 << 13u) | (h1 >> 19u));
    uint h3 = ((h2 * 5u) + 3864292196u);
    uint h4 = (h3 ^ 4u);
    uint h5 = (h4 ^ (h4 >> 16u));
    uint h6 = (h5 * 2246822507u);
    uint h7 = (h6 ^ (h6 >> 13u));
    uint h8 = (h7 * 3266489909u);
    uint hash = (h8 ^ (h8 >> 16u));
    uint outHash = ((isValid) ? (hash) : (0u));
    hashOutput[gid] = outHash;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&validOutput[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_group_by_nunique_helper_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_group_by_nunique_helper_i64(device const long* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* groupIds [[buffer(2)]], device const uint* count [[buffer(3)]], device uint* hashOutput [[buffer(4)]], device uint* validOutput [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    long val = data[gid];
    uint groupId = groupIds[gid];
    uint kLow = ((uint)(val));
    uint kHigh = ((uint)((val >> 32)));
    uint c1 = 3432918353u;
    uint c2 = 461845907u;
    uint k1a = (kLow * c1);
    uint k1b = ((k1a << 15u) | (k1a >> 17u));
    uint k1c = (k1b * c2);
    uint k2a = (kHigh * c1);
    uint k2b = ((k2a << 15u) | (k2a >> 17u));
    uint k2c = (k2b * c2);
    uint h1 = (groupId ^ k1c);
    uint h2 = (h1 ^ k2c);
    uint h3 = ((h2 << 13u) | (h2 >> 19u));
    uint h4 = ((h3 * 5u) + 3864292196u);
    uint h5 = (h4 ^ 8u);
    uint h6 = (h5 ^ (h5 >> 16u));
    uint h7 = (h6 * 2246822507u);
    uint h8 = (h7 ^ (h7 >> 13u));
    uint h9 = (h8 * 3266489909u);
    uint hash = (h9 ^ (h9 >> 16u));
    uint outHash = ((isValid) ? (hash) : (0u));
    hashOutput[gid] = outHash;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&validOutput[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_group_by_sorted_sum = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_group_by_sorted_sum(device const int* sortedKeys [[buffer(0)]], device const float* sortedData [[buffer(1)]], device const uint* validity [[buffer(2)]], device const uint* count [[buffer(3)]], device float* groupSums [[buffer(4)]], device int* groupBounds [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int currentKey = sortedKeys[gid];
    float currentData = sortedData[gid];
    bool isFirst = (gid == 0u);
    uint prevIdx = (gid - 1u);
    uint safePrevIdx = ((isFirst) ? (gid) : (prevIdx));
    int prevKey = sortedKeys[safePrevIdx];
    bool keyChanged = (currentKey != prevKey);
    bool isBoundary = (isFirst || keyChanged);
    uint groupIdx = ((uint)(currentKey));
    if (isValid) {
      float scaleFactor = as_type<float>(0x49800000u);
      float scaledVal = (currentData * scaleFactor);
      int scaledInt = ((int)(scaledVal));
      atomic_fetch_add_explicit((device atomic_float*)(&groupSums[groupIdx]), scaledInt, memory_order_relaxed);
    }
    if (isBoundary) {
      int boundaryIdx = ((int)(gid));
      groupBounds[groupIdx] = boundaryIdx;
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_group_by_sorted_count = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_group_by_sorted_count(device const int* sortedKeys [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device uint* groupCounts [[buffer(3)]], device int* groupBounds [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int currentKey = sortedKeys[gid];
    bool isFirst = (gid == 0u);
    uint prevIdx = (gid - 1u);
    uint safePrevIdx = ((isFirst) ? (gid) : (prevIdx));
    int prevKey = sortedKeys[safePrevIdx];
    bool keyChanged = (currentKey != prevKey);
    bool isBoundary = (isFirst || keyChanged);
    uint groupIdx = ((uint)(currentKey));
    if (isValid) {
      atomic_fetch_add_explicit((device atomic_uint*)(&groupCounts[groupIdx]), 1u, memory_order_relaxed);
    }
    if (isBoundary) {
      int boundaryIdx = ((int)(gid));
      groupBounds[groupIdx] = boundaryIdx;
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_column_sum_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_column_sum_i32(device const int* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device int* result [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      int val = data[gid];
      atomic_fetch_add_explicit((device atomic_int*)(&result[0u]), val, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_column_sum_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_column_sum_i64(device const long* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device long* result [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      long val = data[gid];
      atomic_fetch_add_explicit((device atomic_int*) /* metal atomic: unknown elem type */(&result[0u]), val, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_column_sum_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_column_sum_f32(device const float* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device long* result [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      float val = data[gid];
      float scaleFactor = as_type<float>(0x49800000u);
      float scaledVal = (val * scaleFactor);
      long scaledInt = ((long)(scaledVal));
      atomic_fetch_add_explicit((device atomic_int*) /* metal atomic: unknown elem type */(&result[0u]), scaledInt, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_column_min_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_column_min_i32(device const int* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device int* result [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      int val = data[gid];
      atomic_fetch_min_explicit((device atomic_int*)(&result[0u]), val, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_column_max_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_column_max_i32(device const int* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device int* result [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      int val = data[gid];
      atomic_fetch_max_explicit((device atomic_int*)(&result[0u]), val, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_column_count = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_column_count(device const uint* validity [[buffer(0)]], device const uint* count [[buffer(1)]], device uint* result [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      atomic_fetch_add_explicit((device atomic_uint*)(&result[0u]), 1u, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_column_mean_helper_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_column_mean_helper_f32(device const float* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device long* sumResult [[buffer(3)]], device uint* countResult [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      float val = data[gid];
      float scaleFactor = as_type<float>(0x49800000u);
      float scaledVal = (val * scaleFactor);
      long scaledInt = ((long)(scaledVal));
      atomic_fetch_add_explicit((device atomic_int*) /* metal atomic: unknown elem type */(&sumResult[0u]), scaledInt, memory_order_relaxed);
      atomic_fetch_add_explicit((device atomic_uint*)(&countResult[0u]), 1u, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_column_variance_helper_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_column_variance_helper_f32(device const float* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device long* sumResult [[buffer(3)]], device long* sumSqResult [[buffer(4)]], device uint* countResult [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      float val = data[gid];
      float scaleFactor = as_type<float>(0x47800000u);
      float scaledVal = (val * scaleFactor);
      long scaledInt = ((long)(scaledVal));
      float valSq = (val * val);
      float scaledSq = (valSq * scaleFactor);
      long scaledSqInt = ((long)(scaledSq));
      atomic_fetch_add_explicit((device atomic_int*) /* metal atomic: unknown elem type */(&sumResult[0u]), scaledInt, memory_order_relaxed);
      atomic_fetch_add_explicit((device atomic_int*) /* metal atomic: unknown elem type */(&sumSqResult[0u]), scaledSqInt, memory_order_relaxed);
      atomic_fetch_add_explicit((device atomic_uint*)(&countResult[0u]), 1u, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_column_arg_min_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_column_arg_min_i32(device const int* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device int* minVal [[buffer(3)]], device uint* minIdx [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      int val = data[gid];
      atomic_fetch_min_explicit((device atomic_int*)(&minVal[0u]), val, memory_order_relaxed);
      int currMin = minVal[0u];
      bool isMin = (val == currMin);
      if (isMin) {
        atomic_fetch_min_explicit((device atomic_uint*)(&minIdx[0u]), gid, memory_order_relaxed);
      }
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_column_arg_max_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_column_arg_max_i32(device const int* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device int* maxVal [[buffer(3)]], device uint* maxIdx [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      int val = data[gid];
      atomic_fetch_max_explicit((device atomic_int*)(&maxVal[0u]), val, memory_order_relaxed);
      int currMax = maxVal[0u];
      bool isMax = (val == currMax);
      if (isMax) {
        atomic_fetch_min_explicit((device atomic_uint*)(&maxIdx[0u]), gid, memory_order_relaxed);
      }
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_column_arg_min_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_column_arg_min_f32(device const float* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device int* minValBits [[buffer(3)]], device uint* minIdx [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      float val = data[gid];
      int valBits = ((int)(val));
      atomic_fetch_min_explicit((device atomic_int*)(&minValBits[0u]), valBits, memory_order_relaxed);
      int currMinBits = minValBits[0u];
      bool isMin = (valBits == currMinBits);
      if (isMin) {
        atomic_fetch_min_explicit((device atomic_uint*)(&minIdx[0u]), gid, memory_order_relaxed);
      }
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_column_arg_max_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_column_arg_max_f32(device const float* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device int* maxValBits [[buffer(3)]], device uint* maxIdx [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      float val = data[gid];
      int valBits = ((int)(val));
      atomic_fetch_max_explicit((device atomic_int*)(&maxValBits[0u]), valBits, memory_order_relaxed);
      int currMaxBits = maxValBits[0u];
      bool isMax = (valBits == currMaxBits);
      if (isMax) {
        atomic_fetch_min_explicit((device atomic_uint*)(&maxIdx[0u]), gid, memory_order_relaxed);
      }
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_column_std_helper = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_column_std_helper(device const float* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device const float* mean [[buffer(3)]], device long* sumSqDiffResult [[buffer(4)]], device uint* countResult [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      float val = data[gid];
      float meanVal = mean[0u];
      float diff = (val - meanVal);
      float diffSq = (diff * diff);
      float scaleFactor = as_type<float>(0x47800000u);
      float scaledSq = (diffSq * scaleFactor);
      long scaledSqInt = ((long)(scaledSq));
      atomic_fetch_add_explicit((device atomic_int*) /* metal atomic: unknown elem type */(&sumSqDiffResult[0u]), scaledSqInt, memory_order_relaxed);
      atomic_fetch_add_explicit((device atomic_uint*)(&countResult[0u]), 1u, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_column_product_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_column_product_i32(device const int* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device long* sumLogResult [[buffer(3)]], device uint* negCount [[buffer(4)]], device uint* zeroFlag [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      int val = data[gid];
      bool isZero = (val == 0);
      if (isZero) {
        atomic_fetch_max_explicit((device atomic_uint*)(&zeroFlag[0u]), 1u, memory_order_relaxed);
      } else {
        bool isNeg = (val < 0);
        if (isNeg) {
          atomic_fetch_add_explicit((device atomic_uint*)(&negCount[0u]), 1u, memory_order_relaxed);
        }
        int absVal = ((isNeg) ? ((0 - val)) : (val));
        float absF = ((float)(absVal));
        float logVal = log(absF);
        float scaledLog = (logVal * as_type<float>(0x49742400u));
        long logFixed = ((long)(scaledLog));
        atomic_fetch_add_explicit((device atomic_int*) /* metal atomic: unknown elem type */(&sumLogResult[0u]), logFixed, memory_order_relaxed);
      }
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_column_product_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_column_product_f32(device const float* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device long* sumLogResult [[buffer(3)]], device uint* negCount [[buffer(4)]], device uint* zeroFlag [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      float val = data[gid];
      bool isZero = (val == as_type<float>(0x00000000u));
      if (isZero) {
        atomic_fetch_max_explicit((device atomic_uint*)(&zeroFlag[0u]), 1u, memory_order_relaxed);
      } else {
        bool isNeg = (val < as_type<float>(0x00000000u));
        if (isNeg) {
          atomic_fetch_add_explicit((device atomic_uint*)(&negCount[0u]), 1u, memory_order_relaxed);
        }
        float absVal = abs(val);
        float logVal = log(absVal);
        float scaleFactor = as_type<float>(0x49800000u);
        float scaledLog = (logVal * scaleFactor);
        long scaledLogInt = ((long)(scaledLog));
        atomic_fetch_add_explicit((device atomic_int*) /* metal atomic: unknown elem type */(&sumLogResult[0u]), scaledLogInt, memory_order_relaxed);
      }
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_rolling_sum_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_rolling_sum_f32(device const float* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* windowSize [[buffer(2)]], device const uint* minPeriods [[buffer(3)]], device const uint* count [[buffer(4)]], device float* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint w = windowSize[0u];
  uint minP = minPeriods[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int windowStartRaw = (((int)(gid)) - (((int)(w)) - 1));
    int windowStartClamped = (((windowStartRaw < 0)) ? (0) : (windowStartRaw));
    uint windowStart = ((uint)(windowStartClamped));
    float val = data[gid];
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    float outVal = ((isValid) ? (val) : (as_type<float>(0x00000000u)));
    _output[gid] = outVal;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_rolling_count = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_rolling_count(device const uint* validity [[buffer(0)]], device const uint* windowSize [[buffer(1)]], device const uint* count [[buffer(2)]], device uint* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    _output[gid] = validBit;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_rolling_min_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_rolling_min_i32(device const int* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* windowSize [[buffer(2)]], device const uint* count [[buffer(3)]], device int* _output [[buffer(4)]], device uint* outputValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int val = data[gid];
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int outVal = ((isValid) ? (val) : (2147483647));
    _output[gid] = outVal;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_rolling_max_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_rolling_max_i32(device const int* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* windowSize [[buffer(2)]], device const uint* count [[buffer(3)]], device int* _output [[buffer(4)]], device uint* outputValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int val = data[gid];
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int outVal = ((isValid) ? (val) : (-2147483648));
    _output[gid] = outVal;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_rolling_mean_helper_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_rolling_mean_helper_f32(device const float* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* windowSize [[buffer(2)]], device const uint* count [[buffer(3)]], device long* sumOutput [[buffer(4)]], device uint* countOutput [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = data[gid];
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    float scaleFactor = as_type<float>(0x49800000u);
    float scaledVal = (val * scaleFactor);
    long scaledInt = ((long)(scaledVal));
    long sumVal = ((isValid) ? (scaledInt) : (0));
    uint cntVal = ((isValid) ? (1u) : (0u));
    sumOutput[gid] = sumVal;
    countOutput[gid] = cntVal;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_rolling_variance_helper_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_rolling_variance_helper_f32(device const float* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* windowSize [[buffer(2)]], device const uint* count [[buffer(3)]], device long* sumOutput [[buffer(4)]], device long* sumSqOutput [[buffer(5)]], device uint* countOutput [[buffer(6)]], device uint* outputValid [[buffer(7)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = data[gid];
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    float scaleFactor = as_type<float>(0x47800000u);
    float scaledVal = (val * scaleFactor);
    long scaledInt = ((long)(scaledVal));
    long sumVal = ((isValid) ? (scaledInt) : (0));
    float valSq = (val * val);
    float scaledSq = (valSq * scaleFactor);
    long scaledSqInt = ((long)(scaledSq));
    long sumSqVal = ((isValid) ? (scaledSqInt) : (0));
    uint cntVal = ((isValid) ? (1u) : (0u));
    sumOutput[gid] = sumVal;
    sumSqOutput[gid] = sumSqVal;
    countOutput[gid] = cntVal;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_expanding_sum_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_expanding_sum_f32(device const float* data [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device float* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float acc = as_type<float>(0x00000000u);
    uint loopEnd = (gid + 1u);
    float val = data[gid];
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    bool isFirstPos = (gid == 0u);
    uint prevIdx = ((isFirstPos) ? (0u) : ((gid - 1u)));
    float contribution = ((isValid) ? (val) : (as_type<float>(0x00000000u)));
    _output[gid] = contribution;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_expanding_count_valid = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_expanding_count_valid(device const uint* validity [[buffer(0)]], device const uint* count [[buffer(1)]], device uint* _output [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    _output[gid] = validBit;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_compute_quantile_indices = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_compute_quantile_indices(device const uint* quantile [[buffer(0)]], device const uint* count [[buffer(1)]], device uint* floorIdx [[buffer(2)]], device uint* ceilIdx [[buffer(3)]], device uint* fraction [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint q = quantile[0u];
  uint n = count[0u];
  uint nMinus1 = (n - 1u);
  uint posScaled = (q * nMinus1);
  uint floor = (posScaled / 1000u);
  uint ceilRaw = (floor + 1u);
  uint ceil = (((ceilRaw < n)) ? (ceilRaw) : (nMinus1));
  uint frac = (posScaled % 1000u);
  floorIdx[0u] = floor;
  ceilIdx[0u] = ceil;
  fraction[0u] = frac;
}

)kernel";
const char* k_metal_navatala_dataframe_select_quantile_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_select_quantile_f32(device const float* sortedData [[buffer(0)]], device const uint* floorIdx [[buffer(1)]], device const uint* ceilIdx [[buffer(2)]], device const uint* fraction [[buffer(3)]], device float* result [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint floor = floorIdx[0u];
  uint ceil = ceilIdx[0u];
  uint frac = fraction[0u];
  float floorVal = sortedData[floor];
  float ceilVal = sortedData[ceil];
  float diff = (ceilVal - floorVal);
  float fracF = ((float)(frac));
  float scaledDiff = (diff * fracF);
  float interpVal = (scaledDiff / as_type<float>(0x447a0000u));
  float quantileVal = (floorVal + interpVal);
  result[0u] = quantileVal;
}

)kernel";
const char* k_metal_navatala_dataframe_select_quantile_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_select_quantile_i32(device const int* sortedData [[buffer(0)]], device const uint* floorIdx [[buffer(1)]], device int* result [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint floor = floorIdx[0u];
  int val = sortedData[floor];
  result[0u] = val;
}

)kernel";
const char* k_metal_navatala_dataframe_batch_quantile_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_batch_quantile_f32(device const float* sortedData [[buffer(0)]], device const uint* quantiles [[buffer(1)]], device const uint* dataCount [[buffer(2)]], device const uint* numQuantiles [[buffer(3)]], device float* results [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint nQ = numQuantiles[0u];
  uint nData = dataCount[0u];
  bool inBounds = (gid < nQ);
  if (inBounds) {
    uint q = quantiles[gid];
    uint nMinus1 = (nData - 1u);
    uint posScaled = (q * nMinus1);
    uint floor = (posScaled / 1000u);
    uint ceilRaw = (floor + 1u);
    uint ceil = (((ceilRaw < nData)) ? (ceilRaw) : (nMinus1));
    uint frac = (posScaled % 1000u);
    float floorVal = sortedData[floor];
    float ceilVal = sortedData[ceil];
    float diff = (ceilVal - floorVal);
    float fracF = ((float)(frac));
    float scaledDiff = (diff * fracF);
    float interpVal = (scaledDiff / as_type<float>(0x447a0000u));
    float quantileVal = (floorVal + interpVal);
    results[gid] = quantileVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_compute_median_indices = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_compute_median_indices(device const uint* count [[buffer(0)]], device uint* floorIdx [[buffer(1)]], device uint* ceilIdx [[buffer(2)]], device uint* isEven [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint n = count[0u];
  uint mod2 = (n % 2u);
  bool even = (mod2 == 0u);
  uint _half = (n / 2u);
  uint floor = ((even) ? ((_half - 1u)) : (_half));
  uint ceil = _half;
  floorIdx[0u] = floor;
  ceilIdx[0u] = ceil;
  uint evenFlag = ((even) ? (1u) : (0u));
  isEven[0u] = evenFlag;
}

)kernel";
const char* k_metal_navatala_dataframe_select_median_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_select_median_f32(device const float* sortedData [[buffer(0)]], device const uint* floorIdx [[buffer(1)]], device const uint* ceilIdx [[buffer(2)]], device const uint* isEven [[buffer(3)]], device float* result [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint floor = floorIdx[0u];
  uint ceil = ceilIdx[0u];
  uint even = isEven[0u];
  bool isEvenBool = (even == 1u);
  float floorVal = sortedData[floor];
  float ceilVal = sortedData[ceil];
  float sum = (floorVal + ceilVal);
  float avg = (sum / as_type<float>(0x40000000u));
  float median = ((isEvenBool) ? (avg) : (floorVal));
  result[0u] = median;
}

)kernel";
const char* k_metal_navatala_dataframe_add_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_add_f32(device const float* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const float* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device float* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float lval = left[gid];
    float rval = right[gid];
    float result = (lval + rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_add_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_add_i32(device const int* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const int* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device int* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int lval = left[gid];
    int rval = right[gid];
    int result = (lval + rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_sub_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_sub_f32(device const float* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const float* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device float* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float lval = left[gid];
    float rval = right[gid];
    float result = (lval - rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_mul_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_mul_f32(device const float* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const float* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device float* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float lval = left[gid];
    float rval = right[gid];
    float result = (lval * rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_div_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_div_f32(device const float* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const float* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device float* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float lval = left[gid];
    float rval = right[gid];
    float result = (lval / rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_mod_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_mod_i32(device const int* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const int* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device int* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int lval = left[gid];
    int rval = right[gid];
    int result = (lval % rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_sub_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_sub_i32(device const int* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const int* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device int* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int lval = left[gid];
    int rval = right[gid];
    int result = (lval - rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_mul_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_mul_i32(device const int* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const int* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device int* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int lval = left[gid];
    int rval = right[gid];
    int result = (lval * rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_div_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_div_i32(device const int* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const int* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device int* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int lval = left[gid];
    int rval = right[gid];
    int result = (lval / rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_add_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_add_i64(device const long* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const long* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device long* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    long lval = left[gid];
    long rval = right[gid];
    long result = (lval + rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_sub_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_sub_i64(device const long* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const long* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device long* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    long lval = left[gid];
    long rval = right[gid];
    long result = (lval - rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_mul_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_mul_i64(device const long* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const long* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device long* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    long lval = left[gid];
    long rval = right[gid];
    long result = (lval * rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_div_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_div_i64(device const long* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const long* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device long* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    long lval = left[gid];
    long rval = right[gid];
    long result = (lval / rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_mod_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_mod_i64(device const long* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const long* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device long* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    long lval = left[gid];
    long rval = right[gid];
    long result = (lval % rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_eq_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_eq_f32(device const float* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const float* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device uchar* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float lval = left[gid];
    float rval = right[gid];
    bool cmp = (lval == rval);
    uchar result = ((cmp) ? (((uchar)(1u))) : (((uchar)(0u))));
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_eq_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_eq_i32(device const int* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const int* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device uchar* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int lval = left[gid];
    int rval = right[gid];
    bool cmp = (lval == rval);
    uchar result = ((cmp) ? (((uchar)(1u))) : (((uchar)(0u))));
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_lt_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_lt_f32(device const float* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const float* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device uchar* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float lval = left[gid];
    float rval = right[gid];
    bool cmp = (lval < rval);
    uchar result = ((cmp) ? (((uchar)(1u))) : (((uchar)(0u))));
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gt_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_gt_f32(device const float* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const float* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device uchar* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float lval = left[gid];
    float rval = right[gid];
    bool cmp = (lval > rval);
    uchar result = ((cmp) ? (((uchar)(1u))) : (((uchar)(0u))));
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_le_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_le_f32(device const float* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const float* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device uchar* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float lval = left[gid];
    float rval = right[gid];
    bool cmp = (lval <= rval);
    uchar result = ((cmp) ? (((uchar)(1u))) : (((uchar)(0u))));
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_ge_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_ge_f32(device const float* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const float* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device uchar* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float lval = left[gid];
    float rval = right[gid];
    bool cmp = (lval >= rval);
    uchar result = ((cmp) ? (((uchar)(1u))) : (((uchar)(0u))));
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_ne_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_ne_f32(device const float* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const float* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device uchar* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float lval = left[gid];
    float rval = right[gid];
    bool cmp = (lval != rval);
    uchar result = ((cmp) ? (((uchar)(1u))) : (((uchar)(0u))));
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_ne_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_ne_i32(device const int* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const int* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device uchar* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int lval = left[gid];
    int rval = right[gid];
    bool cmp = (lval != rval);
    uchar result = ((cmp) ? (((uchar)(1u))) : (((uchar)(0u))));
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_lt_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_lt_i32(device const int* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const int* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device uchar* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int lval = left[gid];
    int rval = right[gid];
    bool cmp = (lval < rval);
    uchar result = ((cmp) ? (((uchar)(1u))) : (((uchar)(0u))));
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gt_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_gt_i32(device const int* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const int* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device uchar* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int lval = left[gid];
    int rval = right[gid];
    bool cmp = (lval > rval);
    uchar result = ((cmp) ? (((uchar)(1u))) : (((uchar)(0u))));
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_le_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_le_i32(device const int* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const int* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device uchar* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int lval = left[gid];
    int rval = right[gid];
    bool cmp = (lval <= rval);
    uchar result = ((cmp) ? (((uchar)(1u))) : (((uchar)(0u))));
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_ge_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_ge_i32(device const int* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const int* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device uchar* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int lval = left[gid];
    int rval = right[gid];
    bool cmp = (lval >= rval);
    uchar result = ((cmp) ? (((uchar)(1u))) : (((uchar)(0u))));
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_bitwise_and_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_bitwise_and_i32(device const int* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const int* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device int* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int lval = left[gid];
    int rval = right[gid];
    int result = (lval & rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_bitwise_and_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_bitwise_and_i64(device const long* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const long* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device long* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    long lval = left[gid];
    long rval = right[gid];
    long result = (lval & rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_bitwise_or_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_bitwise_or_i32(device const int* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const int* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device int* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int lval = left[gid];
    int rval = right[gid];
    int result = (lval | rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_bitwise_or_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_bitwise_or_i64(device const long* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const long* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device long* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    long lval = left[gid];
    long rval = right[gid];
    long result = (lval | rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_bitwise_xor_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_bitwise_xor_i32(device const int* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const int* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device int* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int lval = left[gid];
    int rval = right[gid];
    int result = (lval ^ rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_bitwise_xor_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_bitwise_xor_i64(device const long* left [[buffer(0)]], device const uint* leftValid [[buffer(1)]], device const long* right [[buffer(2)]], device const uint* rightValid [[buffer(3)]], device const uint* count [[buffer(4)]], device long* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    long lval = left[gid];
    long rval = right[gid];
    long result = (lval ^ rval);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint leftValidWord = leftValid[wordIdx];
    uint leftValidBit = ((leftValidWord >> bitIdx) & 1u);
    uint rightValidWord = rightValid[wordIdx];
    uint rightValidBit = ((rightValidWord >> bitIdx) & 1u);
    uint outValidBit = (leftValidBit & rightValidBit);
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_bitwise_not_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_bitwise_not_i32(device const int* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int val = _input[gid];
    int result = (val ^ -1);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_bitwise_not_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_bitwise_not_i64(device const long* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device long* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    long val = _input[gid];
    long result = (val ^ -1);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_hour_from_seconds = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_extract_hour_from_seconds(device const long* timestamps [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    long ts = timestamps[gid];
    long secsInDay = 86400;
    long secsInHour = 3600;
    long timeOfDay = (ts % secsInDay);
    long absTimeOfDay = (((timeOfDay < 0)) ? ((timeOfDay + secsInDay)) : (timeOfDay));
    long hour = (absTimeOfDay / secsInHour);
    int hourI32 = ((int)(hour));
    int outVal = ((isValid) ? (hourI32) : (0));
    _output[gid] = outVal;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_minute_from_seconds = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_extract_minute_from_seconds(device const long* timestamps [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    long ts = timestamps[gid];
    long secsInDay = 86400;
    long secsInHour = 3600;
    long secsInMinute = 60;
    long timeOfDay = (ts % secsInDay);
    long absTimeOfDay = (((timeOfDay < 0)) ? ((timeOfDay + secsInDay)) : (timeOfDay));
    long inHour = (absTimeOfDay % secsInHour);
    long minute = (inHour / secsInMinute);
    int minuteI32 = ((int)(minute));
    int outVal = ((isValid) ? (minuteI32) : (0));
    _output[gid] = outVal;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_second_from_seconds = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_extract_second_from_seconds(device const long* timestamps [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    long ts = timestamps[gid];
    long secsInMinute = 60;
    long second = (ts % secsInMinute);
    long absSecond = (((second < 0)) ? ((second + secsInMinute)) : (second));
    int secondI32 = ((int)(absSecond));
    int outVal = ((isValid) ? (secondI32) : (0));
    _output[gid] = outVal;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_day_of_week_from_seconds = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_extract_day_of_week_from_seconds(device const long* timestamps [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    long ts = timestamps[gid];
    long secsInDay = 86400;
    long days = (ts / secsInDay);
    long daysPlus4 = (days + 4);
    long dow = (daysPlus4 % 7);
    long absDow = (((dow < 0)) ? ((dow + 7)) : (dow));
    int dowI32 = ((int)(absDow));
    int outVal = ((isValid) ? (dowI32) : (0));
    _output[gid] = outVal;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_year_from_seconds = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_extract_year_from_seconds(device const long* timestamps [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    long ts = timestamps[gid];
    long secsInDay = 86400;
    long rawDays = (ts / secsInDay);
    long remainder = (ts % secsInDay);
    bool needsAdjust = ((ts < 0) & (remainder != 0));
    long days = ((needsAdjust) ? ((rawDays - 1)) : (rawDays));
    long daysFromYear0 = (days + 719528);
    long cycles400 = (daysFromYear0 / 146097);
    long rem400 = (daysFromYear0 % 146097);
    long cycles100 = (rem400 / 36524);
    long adj100 = (((cycles100 == 4)) ? (3) : (cycles100));
    long rem100 = (rem400 - (adj100 * 36524));
    long cycles4 = (rem100 / 1461);
    long rem4 = (rem100 % 1461);
    long years1 = (rem4 / 365);
    long adjYears1 = (((years1 == 4)) ? (3) : (years1));
    long year = ((((cycles400 * 400) + (adj100 * 100)) + (cycles4 * 4)) + adjYears1);
    int yearI32 = ((int)(year));
    int outVal = ((isValid) ? (yearI32) : (0));
    _output[gid] = outVal;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_month_from_seconds = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_extract_month_from_seconds(device const long* timestamps [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    long ts = timestamps[gid];
    long secsInDay = 86400;
    long rawDays = (ts / secsInDay);
    long remainder = (ts % secsInDay);
    bool needsAdjust = ((ts < 0) & (remainder != 0));
    long days = ((needsAdjust) ? ((rawDays - 1)) : (rawDays));
    long daysFromYear0 = (days + 719528);
    long cycles400 = (daysFromYear0 / 146097);
    long rem400 = (daysFromYear0 % 146097);
    long cycles100 = (rem400 / 36524);
    long adj100 = (((cycles100 == 4)) ? (3) : (cycles100));
    long rem100 = (rem400 - (adj100 * 36524));
    long cycles4 = (rem100 / 1461);
    long rem4 = (rem100 % 1461);
    long years1 = (rem4 / 365);
    long adjYears1 = (((years1 == 4)) ? (3) : (years1));
    long year = ((((cycles400 * 400) + (adj100 * 100)) + (cycles4 * 4)) + adjYears1);
    long dayOfYear = (rem4 - (adjYears1 * 365));
    bool div4 = ((year % 4) == 0);
    bool div100 = ((year % 100) == 0);
    bool div400 = ((year % 400) == 0);
    bool isLeap = (div4 && ((!div100) || div400));
    long doy1 = (dayOfYear + 1);
    long leapAdj = ((isLeap) ? (1) : (0));
    bool m12 = (doy1 >= (335 + leapAdj));
    bool m11 = (doy1 >= (305 + leapAdj));
    bool m10 = (doy1 >= (274 + leapAdj));
    bool m9 = (doy1 >= (244 + leapAdj));
    bool m8 = (doy1 >= (213 + leapAdj));
    bool m7 = (doy1 >= (182 + leapAdj));
    bool m6 = (doy1 >= (152 + leapAdj));
    bool m5 = (doy1 >= (121 + leapAdj));
    bool m4 = (doy1 >= (91 + leapAdj));
    bool m3 = (doy1 >= 60);
    bool m2 = (doy1 >= 32);
    long month = ((m12) ? (12) : (((m11) ? (11) : (((m10) ? (10) : (((m9) ? (9) : (((m8) ? (8) : (((m7) ? (7) : (((m6) ? (6) : (((m5) ? (5) : (((m4) ? (4) : (((m3) ? (3) : (((m2) ? (2) : (1))))))))))))))))))))));
    int monthI32 = ((int)(month));
    int outVal = ((isValid) ? (monthI32) : (0));
    _output[gid] = outVal;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_day_from_seconds = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_extract_day_from_seconds(device const long* timestamps [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    long ts = timestamps[gid];
    long secsInDay = 86400;
    long rawDays = (ts / secsInDay);
    long remainder = (ts % secsInDay);
    bool needsAdjust = ((ts < 0) & (remainder != 0));
    long days = ((needsAdjust) ? ((rawDays - 1)) : (rawDays));
    long daysFromYear0 = (days + 719528);
    long cycles400 = (daysFromYear0 / 146097);
    long rem400 = (daysFromYear0 % 146097);
    long cycles100 = (rem400 / 36524);
    long adj100 = (((cycles100 == 4)) ? (3) : (cycles100));
    long rem100 = (rem400 - (adj100 * 36524));
    long cycles4 = (rem100 / 1461);
    long rem4 = (rem100 % 1461);
    long years1 = (rem4 / 365);
    long adjYears1 = (((years1 == 4)) ? (3) : (years1));
    long year = ((((cycles400 * 400) + (adj100 * 100)) + (cycles4 * 4)) + adjYears1);
    long dayOfYear = (rem4 - (adjYears1 * 365));
    bool div4 = ((year % 4) == 0);
    bool div100 = ((year % 100) == 0);
    bool div400 = ((year % 400) == 0);
    bool isLeap = (div4 && ((!div100) || div400));
    long doy1 = (dayOfYear + 1);
    long leapAdj = ((isLeap) ? (1) : (0));
    bool m12 = (doy1 >= (335 + leapAdj));
    bool m11 = (doy1 >= (305 + leapAdj));
    bool m10 = (doy1 >= (274 + leapAdj));
    bool m9 = (doy1 >= (244 + leapAdj));
    bool m8 = (doy1 >= (213 + leapAdj));
    bool m7 = (doy1 >= (182 + leapAdj));
    bool m6 = (doy1 >= (152 + leapAdj));
    bool m5 = (doy1 >= (121 + leapAdj));
    bool m4 = (doy1 >= (91 + leapAdj));
    bool m3 = (doy1 >= (60 + leapAdj));
    bool m2 = (doy1 >= 32);
    long monthStart = ((m12) ? ((335 + leapAdj)) : (((m11) ? ((305 + leapAdj)) : (((m10) ? ((274 + leapAdj)) : (((m9) ? ((244 + leapAdj)) : (((m8) ? ((213 + leapAdj)) : (((m7) ? ((182 + leapAdj)) : (((m6) ? ((152 + leapAdj)) : (((m5) ? ((121 + leapAdj)) : (((m4) ? ((91 + leapAdj)) : (((m3) ? ((60 + leapAdj)) : (((m2) ? (32) : (1))))))))))))))))))))));
    long dayOfMonth = ((doy1 - monthStart) + 1);
    int dayI32 = ((int)(dayOfMonth));
    int outVal = ((isValid) ? (dayI32) : (0));
    _output[gid] = outVal;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_extract_day_of_year_from_seconds = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_extract_day_of_year_from_seconds(device const long* timestamps [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    long ts = timestamps[gid];
    long secsInDay = 86400;
    long rawDays = (ts / secsInDay);
    long remainder = (ts % secsInDay);
    bool needsAdjust = ((ts < 0) & (remainder != 0));
    long days = ((needsAdjust) ? ((rawDays - 1)) : (rawDays));
    long daysFromYear0 = (days + 719528);
    long rem400 = (daysFromYear0 % 146097);
    long cycles100 = (rem400 / 36524);
    long adj100 = (((cycles100 == 4)) ? (3) : (cycles100));
    long rem100 = (rem400 - (adj100 * 36524));
    long rem4 = (rem100 % 1461);
    long years1 = (rem4 / 365);
    long adjYears1 = (((years1 == 4)) ? (3) : (years1));
    long dayOfYear0 = (rem4 - (adjYears1 * 365));
    long dayOfYear = (dayOfYear0 + 1);
    int doyI32 = ((int)(dayOfYear));
    int outVal = ((isValid) ? (doyI32) : (0));
    _output[gid] = outVal;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_add_days_to_timestamp = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_add_days_to_timestamp(device const long* timestamps [[buffer(0)]], device const int* days [[buffer(1)]], device const uint* validity [[buffer(2)]], device const uint* count [[buffer(3)]], device long* _output [[buffer(4)]], device uint* outputValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    long ts = timestamps[gid];
    int daysToAdd = days[gid];
    long daysI64 = ((long)(daysToAdd));
    long usPerDay = 86400000000;
    long deltaUs = (daysI64 * usPerDay);
    long result = (ts + deltaUs);
    long outVal = ((isValid) ? (result) : (0));
    _output[gid] = outVal;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_add_months_to_timestamp = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_add_months_to_timestamp(device const long* timestamps [[buffer(0)]], device const int* months [[buffer(1)]], device const uint* validity [[buffer(2)]], device const uint* count [[buffer(3)]], device long* _output [[buffer(4)]], device uint* outputValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    long ts = timestamps[gid];
    int monthsToAdd = months[gid];
    long monthsI64 = ((long)(monthsToAdd));
    long usPerMonth = 2592000000000;
    long deltaUs = (monthsI64 * usPerMonth);
    long result = (ts + deltaUs);
    long outVal = ((isValid) ? (result) : (0));
    _output[gid] = outVal;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_subtract_timestamps = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_subtract_timestamps(device const long* timestamps1 [[buffer(0)]], device const long* timestamps2 [[buffer(1)]], device const uint* validity1 [[buffer(2)]], device const uint* validity2 [[buffer(3)]], device const uint* count [[buffer(4)]], device long* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord1 = validity1[wordIdx];
    uint validBit1 = ((validWord1 >> bitIdx) & 1u);
    uint validWord2 = validity2[wordIdx];
    uint validBit2 = ((validWord2 >> bitIdx) & 1u);
    uint bothValid = (validBit1 & validBit2);
    bool isValid = (bothValid == 1u);
    long ts1 = timestamps1[gid];
    long ts2 = timestamps2[gid];
    long diff = (ts2 - ts1);
    long outVal = ((isValid) ? (diff) : (0));
    _output[gid] = outVal;
    uint outBit = (bothValid << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_timestamp_to_date_parts = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_timestamp_to_date_parts(device const long* timestamps [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device int* years [[buffer(3)]], device int* months [[buffer(4)]], device int* daysOut [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    long tsUs = timestamps[gid];
    long usPerSec = 1000000;
    long ts = (tsUs / usPerSec);
    long secsInDay = 86400;
    long rawDays = (ts / secsInDay);
    long remainder = (ts % secsInDay);
    bool needsAdjust = ((ts < 0) & (remainder != 0));
    long daysSinceEpoch = ((needsAdjust) ? ((rawDays - 1)) : (rawDays));
    long daysFromYear0 = (daysSinceEpoch + 719528);
    long cycles400 = (daysFromYear0 / 146097);
    long rem400 = (daysFromYear0 % 146097);
    long cycles100 = (rem400 / 36524);
    long adj100 = (((cycles100 == 4)) ? (3) : (cycles100));
    long rem100 = (rem400 - (adj100 * 36524));
    long cycles4 = (rem100 / 1461);
    long rem4 = (rem100 % 1461);
    long years1 = (rem4 / 365);
    long adjYears1 = (((years1 == 4)) ? (3) : (years1));
    long year = ((((cycles400 * 400) + (adj100 * 100)) + (cycles4 * 4)) + adjYears1);
    long dayOfYear = (rem4 - (adjYears1 * 365));
    bool div4 = ((year % 4) == 0);
    bool div100 = ((year % 100) == 0);
    bool div400 = ((year % 400) == 0);
    bool isLeap = (div4 && ((!div100) || div400));
    long doy1 = (dayOfYear + 1);
    long leapAdj = ((isLeap) ? (1) : (0));
    bool m12 = (doy1 >= (335 + leapAdj));
    bool m11 = (doy1 >= (305 + leapAdj));
    bool m10 = (doy1 >= (274 + leapAdj));
    bool m9 = (doy1 >= (244 + leapAdj));
    bool m8 = (doy1 >= (213 + leapAdj));
    bool m7 = (doy1 >= (182 + leapAdj));
    bool m6 = (doy1 >= (152 + leapAdj));
    bool m5 = (doy1 >= (121 + leapAdj));
    bool m4 = (doy1 >= (91 + leapAdj));
    bool m3 = (doy1 >= (60 + leapAdj));
    bool m2 = (doy1 >= 32);
    long month = ((m12) ? (12) : (((m11) ? (11) : (((m10) ? (10) : (((m9) ? (9) : (((m8) ? (8) : (((m7) ? (7) : (((m6) ? (6) : (((m5) ? (5) : (((m4) ? (4) : (((m3) ? (3) : (((m2) ? (2) : (1))))))))))))))))))))));
    long monthStart = ((m12) ? ((335 + leapAdj)) : (((m11) ? ((305 + leapAdj)) : (((m10) ? ((274 + leapAdj)) : (((m9) ? ((244 + leapAdj)) : (((m8) ? ((213 + leapAdj)) : (((m7) ? ((182 + leapAdj)) : (((m6) ? ((152 + leapAdj)) : (((m5) ? ((121 + leapAdj)) : (((m4) ? ((91 + leapAdj)) : (((m3) ? ((60 + leapAdj)) : (((m2) ? (32) : (1))))))))))))))))))))));
    long dayOfMonth = ((doy1 - monthStart) + 1);
    int yearI32 = ((int)(year));
    int monthI32 = ((int)(month));
    int dayI32 = ((int)(dayOfMonth));
    int outYear = ((isValid) ? (yearI32) : (0));
    int outMonth = ((isValid) ? (monthI32) : (0));
    int outDay = ((isValid) ? (dayI32) : (0));
    years[gid] = outYear;
    months[gid] = outMonth;
    daysOut[gid] = outDay;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_date_parts_to_timestamp = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_date_parts_to_timestamp(device const int* yearsIn [[buffer(0)]], device const int* monthsIn [[buffer(1)]], device const int* daysIn [[buffer(2)]], device const uint* validity [[buffer(3)]], device const uint* count [[buffer(4)]], device long* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int yearI32 = yearsIn[gid];
    int monthI32 = monthsIn[gid];
    int dayI32 = daysIn[gid];
    long year = ((long)(yearI32));
    long month = ((long)(monthI32));
    long day = ((long)(dayI32));
    bool div4 = ((year % 4) == 0);
    bool div100 = ((year % 100) == 0);
    bool div400 = ((year % 400) == 0);
    bool isLeap = (div4 && ((!div100) || div400));
    long leapAdj = ((isLeap) ? (1) : (0));
    bool isJan = (month == 1);
    bool isFeb = (month == 2);
    bool isMar = (month == 3);
    bool isApr = (month == 4);
    bool isMay = (month == 5);
    bool isJun = (month == 6);
    bool isJul = (month == 7);
    bool isAug = (month == 8);
    bool isSep = (month == 9);
    bool isOct = (month == 10);
    bool isNov = (month == 11);
    long daysBeforeMonth = ((isJan) ? (0) : (((isFeb) ? (31) : (((isMar) ? ((59 + leapAdj)) : (((isApr) ? ((90 + leapAdj)) : (((isMay) ? ((120 + leapAdj)) : (((isJun) ? ((151 + leapAdj)) : (((isJul) ? ((181 + leapAdj)) : (((isAug) ? ((212 + leapAdj)) : (((isSep) ? ((243 + leapAdj)) : (((isOct) ? ((273 + leapAdj)) : (((isNov) ? ((304 + leapAdj)) : ((334 + leapAdj)))))))))))))))))))))));
    long dayOfYear = (daysBeforeMonth + (day - 1));
    long yearM1 = (year - 1);
    long baseDays = (yearM1 * 365);
    long leap4 = (yearM1 / 4);
    long leap100 = (yearM1 / 100);
    long leap400 = (yearM1 / 400);
    long daysFromYear0 = (((baseDays + leap4) - leap100) + leap400);
    long totalDaysFromYear0 = (daysFromYear0 + dayOfYear);
    long daysSinceEpoch = (totalDaysFromYear0 - 719527);
    long usPerDay = 86400000000;
    long tsUs = (daysSinceEpoch * usPerDay);
    long outVal = ((isValid) ? (tsUs) : (0));
    _output[gid] = outVal;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_convert_timestamp_precision = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_convert_timestamp_precision(device const long* timestamps [[buffer(0)]], device const long* scaleFactor [[buffer(1)]], device const uint* validity [[buffer(2)]], device const uint* count [[buffer(3)]], device long* _output [[buffer(4)]], device uint* outputValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  long scale = scaleFactor[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    long ts = timestamps[gid];
    bool isMultiply = (scale > 0);
    long negScale = (0 - scale);
    long absScale = ((isMultiply) ? (scale) : (negScale));
    long multiplied = (ts * absScale);
    long divided = (ts / absScale);
    long result = ((isMultiply) ? (multiplied) : (divided));
    long outVal = ((isValid) ? (result) : (0));
    _output[gid] = outVal;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_combine_hash_values = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_combine_hash_values(device const uint* hashes1 [[buffer(0)]], device const uint* hashes2 [[buffer(1)]], device const uint* count [[buffer(2)]], device uint* combined [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint h1 = hashes1[gid];
    uint h2 = hashes2[gid];
    uint golden = 2654435769u;
    uint h2Left = (h2 << 5u);
    uint h2Right = (h2 >> 27u);
    uint h2Rotated = (h2Left | h2Right);
    uint h2WithGolden = (h2Rotated + golden);
    uint result = (h1 ^ h2WithGolden);
    combined[gid] = result;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_hash_column_row = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_hash_column_row(device const uint* colHashes1 [[buffer(0)]], device const uint* colHashes2 [[buffer(1)]], device const uint* count [[buffer(2)]], device uint* rowHashes [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint h1 = colHashes1[gid];
    uint h2 = colHashes2[gid];
    uint c1 = 3432918353u;
    uint c2 = 461845907u;
    uint h1Left = (h1 << 13u);
    uint h1Right = (h1 >> 19u);
    uint h1Rotated = (h1Left | h1Right);
    uint combined1 = (h1Rotated ^ h2);
    uint mixed1 = (combined1 * c1);
    uint m1Left = (mixed1 << 15u);
    uint m1Right = (mixed1 >> 17u);
    uint m1Rotated = (m1Left | m1Right);
    uint mixed2 = (m1Rotated * c2);
    uint finalMix = (mixed2 ^ (mixed2 >> 16u));
    rowHashes[gid] = finalMix;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_md5_hash = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_md5_hash(device const uchar* _input [[buffer(0)]], device const uint* inputLength [[buffer(1)]], device uchar* _output [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {

}

)kernel";
const char* k_metal_navatala_dataframe_sha256_hash = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_sha256_hash(device const uchar* _input [[buffer(0)]], device const uint* inputLength [[buffer(1)]], device uchar* _output [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {

}

)kernel";
const char* k_metal_navatala_dataframe_identity_hash_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_identity_hash_i32(device const int* keys [[buffer(0)]], device const uint* count [[buffer(1)]], device uint* hashes [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int key = keys[gid];
    uint hash = ((uint)(key));
    hashes[gid] = hash;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_identity_hash_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_identity_hash_i64(device const long* keys [[buffer(0)]], device const uint* count [[buffer(1)]], device ulong* hashes [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    long key = keys[gid];
    ulong hash = ((ulong)(key));
    hashes[gid] = hash;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_murmur3_hash32_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_murmur3_hash32_i32(device const int* keys [[buffer(0)]], device const uint* seed [[buffer(1)]], device const uint* count [[buffer(2)]], device uint* hashes [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint s = seed[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int key = keys[gid];
    uint k = ((uint)(key));
    uint c1 = 3432918353u;
    uint c2 = 461845907u;
    uint k1 = (k * c1);
    uint k2 = ((k1 << 15u) | (k1 >> 17u));
    uint k3 = (k2 * c2);
    uint h1 = (s ^ k3);
    uint h2 = ((h1 << 13u) | (h1 >> 19u));
    uint h3 = ((h2 * 5u) + 3864292196u);
    uint h4 = (h3 ^ 4u);
    uint h5 = (h4 ^ (h4 >> 16u));
    uint h6 = (h5 * 2246822507u);
    uint h7 = (h6 ^ (h6 >> 13u));
    uint h8 = (h7 * 3266489909u);
    uint hash = (h8 ^ (h8 >> 16u));
    hashes[gid] = hash;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_murmur3_hash32_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_murmur3_hash32_i64(device const long* keys [[buffer(0)]], device const uint* seed [[buffer(1)]], device const uint* count [[buffer(2)]], device uint* hashes [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint s = seed[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    long key = keys[gid];
    uint kLow = ((uint)(key));
    uint kHigh = ((uint)((key >> 32)));
    uint c1 = 3432918353u;
    uint c2 = 461845907u;
    uint k1a = (kLow * c1);
    uint k1b = ((k1a << 15u) | (k1a >> 17u));
    uint k1c = (k1b * c2);
    uint h1a = (s ^ k1c);
    uint h1b = ((h1a << 13u) | (h1a >> 19u));
    uint h1c = ((h1b * 5u) + 3864292196u);
    uint k2a = (kHigh * c1);
    uint k2b = ((k2a << 15u) | (k2a >> 17u));
    uint k2c = (k2b * c2);
    uint h2a = (h1c ^ k2c);
    uint h2b = ((h2a << 13u) | (h2a >> 19u));
    uint h2c = ((h2b * 5u) + 3864292196u);
    uint h3 = (h2c ^ 8u);
    uint h4 = (h3 ^ (h3 >> 16u));
    uint h5 = (h4 * 2246822507u);
    uint h6 = (h5 ^ (h5 >> 13u));
    uint h7 = (h6 * 3266489909u);
    uint hash = (h7 ^ (h7 >> 16u));
    hashes[gid] = hash;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_cross_join_compute_output_size = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_cross_join_compute_output_size(device const uint* leftCount [[buffer(0)]], device const uint* rightCount [[buffer(1)]], device ulong* outputSize [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint left = leftCount[0u];
  uint right = rightCount[0u];
  ulong left64 = ((ulong)(left));
  ulong right64 = ((ulong)(right));
  ulong size = (left64 * right64);
  outputSize[0u] = size;
}

)kernel";
const char* k_metal_navatala_dataframe_cross_join_gather_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_cross_join_gather_i32(device const int* leftData [[buffer(0)]], device const int* rightData [[buffer(1)]], device const uint* leftCount [[buffer(2)]], device const uint* rightCount [[buffer(3)]], device const ulong* outputCount [[buffer(4)]], device int* leftOutput [[buffer(5)]], device int* rightOutput [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  ulong gid = ((ulong)(int(__gid.x)));
  ulong n = outputCount[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint rCount = rightCount[0u];
    ulong rCount64 = ((ulong)(rCount));
    ulong leftIdx64 = (gid / rCount64);
    ulong rightIdx64 = (gid % rCount64);
    uint leftIdx = ((uint)(leftIdx64));
    uint rightIdx = ((uint)(rightIdx64));
    int leftVal = leftData[leftIdx];
    int rightVal = rightData[rightIdx];
    uint outIdx = ((uint)(gid));
    leftOutput[outIdx] = leftVal;
    rightOutput[outIdx] = rightVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_hash_keys_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_hash_keys_i32(device const int* keys [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device const uint* tableSize [[buffer(3)]], device uint* hashes [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint tSize = tableSize[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int key = keys[gid];
    uint k1 = ((uint)(key));
    uint k2 = (k1 * 3432918353u);
    uint k3 = ((k2 << 15u) | (k2 >> 17u));
    uint k4 = (k3 * 461845907u);
    uint h1 = (k4 ^ 2246822507u);
    uint h2 = (h1 ^ (h1 >> 16u));
    uint h3 = (h2 * 3266489909u);
    uint h4 = (h3 ^ (h3 >> 13u));
    uint hashIdx = (h4 % tSize);
    uint outHash = ((isValid) ? (hashIdx) : (2147483647u));
    hashes[gid] = outHash;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_build_hash_table_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_build_hash_table_i32(device const int* keys [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* hashes [[buffer(2)]], device const uint* count [[buffer(3)]], device const uint* tableSize [[buffer(4)]], device atomic_int* tableKeys [[buffer(5)]], device uint* tableRowIds [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint tSize = tableSize[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    if (isValid) {
      int key = keys[gid];
      uint startIdx = hashes[gid];
      uint probeIdx = startIdx;
      bool inserted = false;
      for (int __iter = 0; __iter < 65536; ++__iter) {
        if (!((!inserted))) break;
        uint curIdx = probeIdx;
        int oldKey = gpu_atomic_cas_int((device atomic_int*)(&(tableKeys[curIdx])), (int)(2147483647), (int)(key));
        bool gotSlot = (oldKey == 2147483647);
        if (gotSlot) {
          tableRowIds[curIdx] = gid;
          inserted = true;
        } else {
          uint nextIdx = ((curIdx + 1u) % tSize);
          probeIdx = nextIdx;
        }
      }
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_probe_hash_table_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_probe_hash_table_i32(device const int* rightKeys [[buffer(0)]], device const uint* rightValidity [[buffer(1)]], device const uint* rightHashes [[buffer(2)]], device const uint* rightCount [[buffer(3)]], device const int* tableKeys [[buffer(4)]], device const uint* tableRowIds [[buffer(5)]], device const uint* tableSize [[buffer(6)]], device int* leftMatches [[buffer(7)]], device uint* rightMatches [[buffer(8)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = rightCount[0u];
  uint tSize = tableSize[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = rightValidity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int rightKey = rightKeys[gid];
    uint startIdx = rightHashes[gid];
    uint probeIdx = startIdx;
    bool foundMatch = false;
    bool done = (!isValid);
    uint matchedRowId = 0u;
    for (int __iter = 0; __iter < 65536; ++__iter) {
      if (!((!done))) break;
      uint curIdx = probeIdx;
      int tableKey = tableKeys[curIdx];
      bool isEmpty = (tableKey == 2147483647);
      bool keyMatch = (rightKey == tableKey);
      if (isEmpty) {
        done = true;
      } else {
        if (keyMatch) {
          uint rowId = tableRowIds[curIdx];
          matchedRowId = rowId;
          foundMatch = true;
          done = true;
        } else {
          uint nextIdx = ((curIdx + 1u) % tSize);
          probeIdx = nextIdx;
        }
      }
    }
    int leftMatch = ((foundMatch) ? (((int)(matchedRowId))) : (-1));
    leftMatches[gid] = leftMatch;
    rightMatches[gid] = gid;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gather_join_results_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_gather_join_results_i32(device const int* leftData [[buffer(0)]], device const int* leftIndices [[buffer(1)]], device const uint* outputCount [[buffer(2)]], device int* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = outputCount[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int leftIdx = leftIndices[gid];
    bool hasLeftMatch = (leftIdx >= 0);
    int leftVal = ((hasLeftMatch) ? (leftData[((uint)(leftIdx))]) : (0));
    _output[gid] = leftVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gather_join_results_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_gather_join_results_f32(device const float* leftData [[buffer(0)]], device const int* leftIndices [[buffer(1)]], device const uint* outputCount [[buffer(2)]], device float* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = outputCount[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int leftIdx = leftIndices[gid];
    bool hasLeftMatch = (leftIdx >= 0);
    float leftVal = ((hasLeftMatch) ? (leftData[((uint)(leftIdx))]) : (as_type<float>(0x00000000u)));
    _output[gid] = leftVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_mark_left_matched = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_mark_left_matched(device const int* leftMatches [[buffer(0)]], device const uint* probeCount [[buffer(1)]], device uint* leftMatched [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = probeCount[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int leftIdx = leftMatches[gid];
    bool hasMatch = (leftIdx >= 0);
    if (hasMatch) {
      uint leftIdxU = ((uint)(leftIdx));
      uint wordIdx = (leftIdxU / 32u);
      uint bitIdx = (leftIdxU % 32u);
      uint bitMask = (1u << bitIdx);
      atomic_fetch_or_explicit((device atomic_uint*)(&leftMatched[wordIdx]), bitMask, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_filter_semi_join = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_filter_semi_join(device const int* leftMatches [[buffer(0)]], device const uint* count [[buffer(1)]], device uint* semiFlag [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int leftIdx = leftMatches[gid];
    bool hasMatch = (leftIdx >= 0);
    uint flag = ((hasMatch) ? (1u) : (0u));
    semiFlag[gid] = flag;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_filter_anti_join = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_filter_anti_join(device const int* leftMatches [[buffer(0)]], device const uint* count [[buffer(1)]], device uint* antiFlag [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int leftIdx = leftMatches[gid];
    bool hasMatch = (leftIdx >= 0);
    uint flag = ((hasMatch) ? (0u) : (1u));
    antiFlag[gid] = flag;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_count_unmatched_left = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_count_unmatched_left(device const uint* leftMatched [[buffer(0)]], device const uint* leftRowCount [[buffer(1)]], device uint* unmatchedCount [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = leftRowCount[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint word = leftMatched[wordIdx];
    uint bit = ((word >> bitIdx) & 1u);
    bool isMatched = (bit == 1u);
    if ((isMatched == false)) {
      atomic_fetch_add_explicit((device atomic_uint*)(&unmatchedCount[0u]), 1u, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gather_unmatched_left_indices = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_gather_unmatched_left_indices(device const uint* leftMatched [[buffer(0)]], device const uint* leftRowCount [[buffer(1)]], device uint* outputOffset [[buffer(2)]], device uint* unmatchedIndices [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = leftRowCount[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint word = leftMatched[wordIdx];
    uint bit = ((word >> bitIdx) & 1u);
    bool isMatched = (bit == 1u);
    if ((isMatched == false)) {
      unmatchedIndices[gid] = gid;
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gather_left_join_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_gather_left_join_i32(device const int* leftData [[buffer(0)]], device const uint* leftValidity [[buffer(1)]], device const int* leftIndices [[buffer(2)]], device const uint* outputCount [[buffer(3)]], device int* _output [[buffer(4)]], device uint* outputValidity [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = outputCount[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int leftIdx = leftIndices[gid];
    bool hasMatch = (leftIdx >= 0);
    uint outWordIdx = (gid / 32u);
    uint outBitIdx = (gid % 32u);
    if (hasMatch) {
      uint leftIdxU = ((uint)(leftIdx));
      int val = leftData[leftIdxU];
      uint srcWordIdx = (leftIdxU / 32u);
      uint srcBitIdx = (leftIdxU % 32u);
      uint srcWord = leftValidity[srcWordIdx];
      uint srcBit = ((srcWord >> srcBitIdx) & 1u);
      _output[gid] = val;
      uint outBit = (srcBit << outBitIdx);
      atomic_fetch_or_explicit((device atomic_uint*)(&outputValidity[outWordIdx]), outBit, memory_order_relaxed);
    } else {
      _output[gid] = 0;
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gather_left_join_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_gather_left_join_f32(device const float* leftData [[buffer(0)]], device const uint* leftValidity [[buffer(1)]], device const int* leftIndices [[buffer(2)]], device const uint* outputCount [[buffer(3)]], device float* _output [[buffer(4)]], device uint* outputValidity [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = outputCount[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int leftIdx = leftIndices[gid];
    bool hasMatch = (leftIdx >= 0);
    uint outWordIdx = (gid / 32u);
    uint outBitIdx = (gid % 32u);
    if (hasMatch) {
      uint leftIdxU = ((uint)(leftIdx));
      float val = leftData[leftIdxU];
      uint srcWordIdx = (leftIdxU / 32u);
      uint srcBitIdx = (leftIdxU % 32u);
      uint srcWord = leftValidity[srcWordIdx];
      uint srcBit = ((srcWord >> srcBitIdx) & 1u);
      _output[gid] = val;
      uint outBit = (srcBit << outBitIdx);
      atomic_fetch_or_explicit((device atomic_uint*)(&outputValidity[outWordIdx]), outBit, memory_order_relaxed);
    } else {
      _output[gid] = as_type<float>(0x00000000u);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gather_right_join_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_gather_right_join_i32(device const int* rightData [[buffer(0)]], device const uint* rightValidity [[buffer(1)]], device const int* rightIndices [[buffer(2)]], device const uint* outputCount [[buffer(3)]], device int* _output [[buffer(4)]], device uint* outputValidity [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = outputCount[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int rightIdx = rightIndices[gid];
    bool hasMatch = (rightIdx >= 0);
    uint outWordIdx = (gid / 32u);
    uint outBitIdx = (gid % 32u);
    if (hasMatch) {
      uint rightIdxU = ((uint)(rightIdx));
      int val = rightData[rightIdxU];
      uint srcWordIdx = (rightIdxU / 32u);
      uint srcBitIdx = (rightIdxU % 32u);
      uint srcWord = rightValidity[srcWordIdx];
      uint srcBit = ((srcWord >> srcBitIdx) & 1u);
      _output[gid] = val;
      uint outBit = (srcBit << outBitIdx);
      atomic_fetch_or_explicit((device atomic_uint*)(&outputValidity[outWordIdx]), outBit, memory_order_relaxed);
    } else {
      _output[gid] = 0;
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gather_right_join_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_gather_right_join_f32(device const float* rightData [[buffer(0)]], device const uint* rightValidity [[buffer(1)]], device const int* rightIndices [[buffer(2)]], device const uint* outputCount [[buffer(3)]], device float* _output [[buffer(4)]], device uint* outputValidity [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = outputCount[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int rightIdx = rightIndices[gid];
    bool hasMatch = (rightIdx >= 0);
    uint outWordIdx = (gid / 32u);
    uint outBitIdx = (gid % 32u);
    if (hasMatch) {
      uint rightIdxU = ((uint)(rightIdx));
      float val = rightData[rightIdxU];
      uint srcWordIdx = (rightIdxU / 32u);
      uint srcBitIdx = (rightIdxU % 32u);
      uint srcWord = rightValidity[srcWordIdx];
      uint srcBit = ((srcWord >> srcBitIdx) & 1u);
      _output[gid] = val;
      uint outBit = (srcBit << outBitIdx);
      atomic_fetch_or_explicit((device atomic_uint*)(&outputValidity[outWordIdx]), outBit, memory_order_relaxed);
    } else {
      _output[gid] = as_type<float>(0x00000000u);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_count_unmatched_right = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_count_unmatched_right(device const uint* rightMatched [[buffer(0)]], device const uint* rightRowCount [[buffer(1)]], device uint* unmatchedCount [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = rightRowCount[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint word = rightMatched[wordIdx];
    uint bit = ((word >> bitIdx) & 1u);
    bool isMatched = (bit == 1u);
    if ((isMatched == false)) {
      atomic_fetch_add_explicit((device atomic_uint*)(&unmatchedCount[0u]), 1u, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gather_unmatched_right_indices = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_gather_unmatched_right_indices(device const uint* rightMatched [[buffer(0)]], device const uint* rightRowCount [[buffer(1)]], device uint* outputOffset [[buffer(2)]], device uint* unmatchedIndices [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = rightRowCount[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint word = rightMatched[wordIdx];
    uint bit = ((word >> bitIdx) & 1u);
    bool isMatched = (bit == 1u);
    if ((isMatched == false)) {
      unmatchedIndices[gid] = gid;
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_nested_loop_join_probe = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_nested_loop_join_probe(device const int* leftKeys [[buffer(0)]], device const int* rightKeys [[buffer(1)]], device const uint* leftCount [[buffer(2)]], device const uint* rightCount [[buffer(3)]], device const uint* maxMatches [[buffer(4)]], device uint* matchCount [[buffer(5)]], device int* matchLeft [[buffer(6)]], device int* matchRight [[buffer(7)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint lCount = leftCount[0u];
  uint rCount = rightCount[0u];
  uint maxM = maxMatches[0u];
  bool inBounds = (gid < lCount);
  if (inBounds) {
    int leftKey = leftKeys[gid];
    for (int rightIdx = 0; rightIdx < (int)(rCount); ++rightIdx) {
      int rightKey = rightKeys[rightIdx];
      bool isMatch = (leftKey == rightKey);
      if (isMatch) {
        atomic_fetch_add_explicit((device atomic_uint*)(&matchCount[0u]), 1u, memory_order_relaxed);
        uint outPos = matchCount[0u];
        uint outPosAdj = (outPos - 1u);
        bool canStore = (outPosAdj < maxM);
        if (canStore) {
          int leftIdxI32 = ((int)(gid));
          int rightIdxI32 = ((int)(rightIdx));
          matchLeft[outPosAdj] = leftIdxI32;
          matchRight[outPosAdj] = rightIdxI32;
        }
      }
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_nested_loop_join_gather_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_nested_loop_join_gather_i32(device const int* leftData [[buffer(0)]], device const int* rightData [[buffer(1)]], device const int* leftIndices [[buffer(2)]], device const int* rightIndices [[buffer(3)]], device const uint* count [[buffer(4)]], device int* leftOutput [[buffer(5)]], device int* rightOutput [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int leftIdx = leftIndices[gid];
    int rightIdx = rightIndices[gid];
    uint leftIdxU = ((uint)(leftIdx));
    uint rightIdxU = ((uint)(rightIdx));
    int leftVal = leftData[leftIdxU];
    int rightVal = rightData[rightIdxU];
    leftOutput[gid] = leftVal;
    rightOutput[gid] = rightVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_sort_merge_join_probe = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_sort_merge_join_probe(device const int* leftKeys [[buffer(0)]], device const uint* leftValidity [[buffer(1)]], device const uint* leftCount [[buffer(2)]], device const int* rightKeys [[buffer(3)]], device const uint* rightValidity [[buffer(4)]], device const uint* rightCount [[buffer(5)]], device int* leftMatches [[buffer(6)]], device int* rightMatches [[buffer(7)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint nLeft = leftCount[0u];
  uint nRight = rightCount[0u];
  bool inBounds = (gid < nLeft);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = leftValidity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isLeftValid = (validBit == 1u);
    int leftKey = leftKeys[gid];
    uint lo0 = 0u;
    uint hi0 = nRight;
    uint mid1 = ((lo0 + hi0) / 2u);
    int midKey1 = (((mid1 < nRight)) ? (rightKeys[mid1]) : (0));
    bool goLeft1 = (leftKey < midKey1);
    uint lo1 = ((goLeft1) ? (lo0) : ((mid1 + 1u)));
    uint hi1 = ((goLeft1) ? (mid1) : (hi0));
    uint mid2 = ((lo1 + hi1) / 2u);
    int midKey2 = (((mid2 < nRight)) ? (rightKeys[mid2]) : (0));
    bool goLeft2 = (leftKey < midKey2);
    uint lo2 = ((goLeft2) ? (lo1) : ((mid2 + 1u)));
    uint hi2 = ((goLeft2) ? (mid2) : (hi1));
    uint mid3 = ((lo2 + hi2) / 2u);
    int midKey3 = (((mid3 < nRight)) ? (rightKeys[mid3]) : (0));
    bool goLeft3 = (leftKey < midKey3);
    uint lo3 = ((goLeft3) ? (lo2) : ((mid3 + 1u)));
    uint hi3 = ((goLeft3) ? (mid3) : (hi2));
    uint mid4 = ((lo3 + hi3) / 2u);
    int midKey4 = (((mid4 < nRight)) ? (rightKeys[mid4]) : (0));
    bool goLeft4 = (leftKey < midKey4);
    uint lo4 = ((goLeft4) ? (lo3) : ((mid4 + 1u)));
    uint hi4 = ((goLeft4) ? (mid4) : (hi3));
    uint mid5 = ((lo4 + hi4) / 2u);
    int midKey5 = (((mid5 < nRight)) ? (rightKeys[mid5]) : (0));
    bool goLeft5 = (leftKey < midKey5);
    uint lo5 = ((goLeft5) ? (lo4) : ((mid5 + 1u)));
    uint hi5 = ((goLeft5) ? (mid5) : (hi4));
    uint mid6 = ((lo5 + hi5) / 2u);
    int midKey6 = (((mid6 < nRight)) ? (rightKeys[mid6]) : (0));
    bool goLeft6 = (leftKey < midKey6);
    uint lo6 = ((goLeft6) ? (lo5) : ((mid6 + 1u)));
    uint hi6 = ((goLeft6) ? (mid6) : (hi5));
    uint mid7 = ((lo6 + hi6) / 2u);
    int midKey7 = (((mid7 < nRight)) ? (rightKeys[mid7]) : (0));
    bool goLeft7 = (leftKey < midKey7);
    uint lo7 = ((goLeft7) ? (lo6) : ((mid7 + 1u)));
    uint hi7 = ((goLeft7) ? (mid7) : (hi6));
    uint mid8 = ((lo7 + hi7) / 2u);
    int midKey8 = (((mid8 < nRight)) ? (rightKeys[mid8]) : (0));
    bool goLeft8 = (leftKey < midKey8);
    uint lo8 = ((goLeft8) ? (lo7) : ((mid8 + 1u)));
    uint hi8 = ((goLeft8) ? (mid8) : (hi7));
    uint foundIdx = lo8;
    bool foundInRange = (foundIdx < nRight);
    int foundKey = ((foundInRange) ? (rightKeys[foundIdx]) : (0));
    bool isMatch = (foundInRange && (leftKey == foundKey));
    uint rightWordIdx = (foundIdx / 32u);
    uint rightBitIdx = (foundIdx % 32u);
    uint rightValidWord = ((foundInRange) ? (rightValidity[rightWordIdx]) : (0u));
    uint rightValidBit = ((rightValidWord >> rightBitIdx) & 1u);
    bool isRightValid = (rightValidBit == 1u);
    bool hasValidMatch = (isLeftValid && (isRightValid && isMatch));
    int leftOut = ((int)(gid));
    leftMatches[gid] = leftOut;
    int rightOut = ((hasValidMatch) ? (((int)(foundIdx))) : (-1));
    rightMatches[gid] = rightOut;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_sort_merge_join_gather_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_sort_merge_join_gather_i32(device const int* leftData [[buffer(0)]], device const int* rightData [[buffer(1)]], device const int* leftIndices [[buffer(2)]], device const int* rightIndices [[buffer(3)]], device const uint* outputCount [[buffer(4)]], device int* leftOutput [[buffer(5)]], device int* rightOutput [[buffer(6)]], device uint* outputValid [[buffer(7)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = outputCount[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int leftIdx = leftIndices[gid];
    int rightIdx = rightIndices[gid];
    bool hasLeftMatch = (leftIdx >= 0);
    bool hasRightMatch = (rightIdx >= 0);
    bool hasBothMatches = (hasLeftMatch && hasRightMatch);
    uint leftIdxU = ((uint)(leftIdx));
    uint rightIdxU = ((uint)(rightIdx));
    int leftVal = ((hasLeftMatch) ? (leftData[leftIdxU]) : (0));
    int rightVal = ((hasRightMatch) ? (rightData[rightIdxU]) : (0));
    leftOutput[gid] = leftVal;
    rightOutput[gid] = rightVal;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validBit = ((hasBothMatches) ? ((1u << bitIdx)) : (0u));
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), validBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_merge_step_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_merge_step_i32(device const int* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device const uint* blockSize [[buffer(2)]], device int* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint bSize = blockSize[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint mergeBlockSize = (bSize * 2u);
    uint mergeBlockIdx = (gid / mergeBlockSize);
    uint posInMerge = (gid % mergeBlockSize);
    uint leftStart = (mergeBlockIdx * mergeBlockSize);
    uint rightStart = (leftStart + bSize);
    uint leftEnd = (((rightStart < n)) ? (rightStart) : (n));
    uint rightEnd = ((((leftStart + mergeBlockSize) < n)) ? ((leftStart + mergeBlockSize)) : (n));
    uint leftSize = (leftEnd - leftStart);
    uint rightSize = (rightEnd - rightStart);
    bool leftDone = (posInMerge >= leftSize);
    bool rightAvail = (posInMerge < (leftSize + rightSize));
    uint leftIdx = (((posInMerge < leftSize)) ? (posInMerge) : ((leftSize - 1u)));
    uint rightIdx = ((leftDone) ? ((posInMerge - leftSize)) : (0u));
    uint absLeftIdx = (leftStart + leftIdx);
    uint absRightIdx = (rightStart + rightIdx);
    int leftVal = _input[absLeftIdx];
    int rightVal = (((absRightIdx < rightEnd)) ? (_input[absRightIdx]) : (2147483647));
    bool rightExhausted = (absRightIdx >= rightEnd);
    bool leftSmaller = (leftVal <= rightVal);
    bool takeLeft = ((!leftDone) && (rightExhausted || leftSmaller));
    int outVal = ((takeLeft) ? (leftVal) : (rightVal));
    _output[gid] = outVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_merge_step_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_merge_step_f32(device const float* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device const uint* blockSize [[buffer(2)]], device float* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint bSize = blockSize[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint mergeBlockSize = (bSize * 2u);
    uint mergeBlockIdx = (gid / mergeBlockSize);
    uint posInMerge = (gid % mergeBlockSize);
    uint leftStart = (mergeBlockIdx * mergeBlockSize);
    uint rightStart = (leftStart + bSize);
    uint leftEnd = (((rightStart < n)) ? (rightStart) : (n));
    uint rightEnd = ((((leftStart + mergeBlockSize) < n)) ? ((leftStart + mergeBlockSize)) : (n));
    uint leftSize = (leftEnd - leftStart);
    uint rightSize = (rightEnd - rightStart);
    bool leftDone = (posInMerge >= leftSize);
    uint leftIdx = (((posInMerge < leftSize)) ? (posInMerge) : ((leftSize - 1u)));
    uint rightIdx = ((leftDone) ? ((posInMerge - leftSize)) : (0u));
    uint absLeftIdx = (leftStart + leftIdx);
    uint absRightIdx = (rightStart + rightIdx);
    float leftVal = _input[absLeftIdx];
    float rightVal = (((absRightIdx < rightEnd)) ? (_input[absRightIdx]) : (as_type<float>(0x7f7fffffu)));
    bool rightExhausted = (absRightIdx >= rightEnd);
    bool leftSmaller = (leftVal <= rightVal);
    bool takeLeft = ((!leftDone) && (rightExhausted || leftSmaller));
    float outVal = ((takeLeft) ? (leftVal) : (rightVal));
    _output[gid] = outVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_merge_step_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_merge_step_i64(device const long* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device const uint* blockSize [[buffer(2)]], device long* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint bSize = blockSize[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint mergeBlockSize = (bSize * 2u);
    uint mergeBlockIdx = (gid / mergeBlockSize);
    uint posInMerge = (gid % mergeBlockSize);
    uint leftStart = (mergeBlockIdx * mergeBlockSize);
    uint rightStart = (leftStart + bSize);
    uint leftEnd = (((rightStart < n)) ? (rightStart) : (n));
    uint rightEnd = ((((leftStart + mergeBlockSize) < n)) ? ((leftStart + mergeBlockSize)) : (n));
    uint leftSize = (leftEnd - leftStart);
    uint rightSize = (rightEnd - rightStart);
    bool leftDone = (posInMerge >= leftSize);
    uint leftIdx = (((posInMerge < leftSize)) ? (posInMerge) : ((leftSize - 1u)));
    uint rightIdx = ((leftDone) ? ((posInMerge - leftSize)) : (0u));
    uint absLeftIdx = (leftStart + leftIdx);
    uint absRightIdx = (rightStart + rightIdx);
    long leftVal = _input[absLeftIdx];
    long rightVal = (((absRightIdx < rightEnd)) ? (_input[absRightIdx]) : (9223372036854775807));
    bool rightExhausted = (absRightIdx >= rightEnd);
    bool leftSmaller = (leftVal <= rightVal);
    bool takeLeft = ((!leftDone) && (rightExhausted || leftSmaller));
    long outVal = ((takeLeft) ? (leftVal) : (rightVal));
    _output[gid] = outVal;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_radix_scatter_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_radix_scatter_i32(device const int* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device const uint* bitOffset [[buffer(2)]], device const uint* prefixSums [[buffer(3)]], device int* _output [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint bits = bitOffset[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int val = _input[gid];
    uint valU = ((uint)(val));
    uint signFlipped = (valU ^ 2147483648u);
    uint digit = ((signFlipped >> bits) & 255u);
    uint destBase = prefixSums[digit];
    _output[destBase] = val;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_radix_scatter_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_radix_scatter_i64(device const long* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device const uint* bitOffset [[buffer(2)]], device const uint* prefixSums [[buffer(3)]], device long* _output [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint bits = bitOffset[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    long val = _input[gid];
    long signFlipped = (val ^ 9223372036854775808);
    uint bitsU32 = bits;
    long shifted = (signFlipped >> ((long)(bitsU32)));
    uint digit = ((uint)((shifted & 255)));
    uint destBase = prefixSums[digit];
    _output[destBase] = val;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_radix_scatter_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_radix_scatter_f32(device const float* _input [[buffer(0)]], device const uint* count [[buffer(1)]], device const uint* bitOffset [[buffer(2)]], device const uint* prefixSums [[buffer(3)]], device float* _output [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint bits = bitOffset[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = _input[gid];
    float scaled = (val * as_type<float>(0x4b800000u));
    int sortKeyRaw = ((int)(scaled));
    uint sortKey = ((uint)((sortKeyRaw + 2147483648)));
    uint digit = ((sortKey >> bits) & 255u);
    uint destBase = prefixSums[digit];
    _output[destBase] = val;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_len = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_len(device const int* offsets [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    int outLen = ((isValid) ? (length) : (0));
    _output[gid] = outLen;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_upper_chars = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_upper_chars(device const char* chars [[buffer(0)]], device const uint* count [[buffer(1)]], device char* _output [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    char c = chars[gid];
    int cInt = ((int)(c));
    bool isLower = ((cInt >= 97) && (cInt <= 122));
    int upperInt = ((isLower) ? ((cInt - 32)) : (cInt));
    char upperChar = ((char)(upperInt));
    _output[gid] = upperChar;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_lower_chars = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_lower_chars(device const char* chars [[buffer(0)]], device const uint* count [[buffer(1)]], device char* _output [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    char c = chars[gid];
    int cInt = ((int)(c));
    bool isUpper = ((cInt >= 65) && (cInt <= 90));
    int lowerInt = ((isUpper) ? ((cInt + 32)) : (cInt));
    char lowerChar = ((char)(lowerInt));
    _output[gid] = lowerChar;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_starts_with_char = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_starts_with_char(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const char* targetChar [[buffer(3)]], device const uint* count [[buffer(4)]], device uint* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  char target = targetChar[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    bool hasChars = (length > 0);
    uint firstCharIdx = ((uint)(startOffset));
    char firstChar = chars[firstCharIdx];
    bool matches = (hasChars && (firstChar == target));
    uint result = (((isValid && matches)) ? (1u) : (0u));
    _output[gid] = result;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_capitalize_first_char = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_capitalize_first_char(device const char* chars [[buffer(0)]], device const int* offsets [[buffer(1)]], device const uint* validity [[buffer(2)]], device const uint* count [[buffer(3)]], device char* outputChars [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    bool hasChars = (length > 0);
    uint firstCharIdx = ((uint)(startOffset));
    char firstChar = chars[firstCharIdx];
    int charInt = ((int)(firstChar));
    bool isLower = ((charInt >= 97) && (charInt <= 122));
    int upperInt = ((isLower) ? ((charInt - 32)) : (charInt));
    char upperChar = ((char)(upperInt));
    bool shouldWrite = (isValid && hasChars);
    if (shouldWrite) {
      outputChars[firstCharIdx] = upperChar;
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_title_case_char = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_title_case_char(device const char* chars [[buffer(0)]], device const int* offsets [[buffer(1)]], device const uint* prevIsSpace [[buffer(2)]], device const uint* charCount [[buffer(3)]], device char* outputChars [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = charCount[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    char c = chars[gid];
    int cInt = ((int)(c));
    uint prevSpace = prevIsSpace[gid];
    bool afterSpace = (prevSpace == 1u);
    bool isLower = ((cInt >= 97) && (cInt <= 122));
    bool shouldCapitalize = (afterSpace && isLower);
    int upperInt = ((shouldCapitalize) ? ((cInt - 32)) : (cInt));
    char outChar = ((char)(upperInt));
    outputChars[gid] = outChar;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_swap_case_char = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_swap_case_char(device const char* chars [[buffer(0)]], device const uint* charCount [[buffer(1)]], device char* outputChars [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = charCount[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    char c = chars[gid];
    int cInt = ((int)(c));
    bool isUpper = ((cInt >= 65) && (cInt <= 90));
    bool isLower = ((cInt >= 97) && (cInt <= 122));
    int toLower = (cInt + 32);
    int toUpper = (cInt - 32);
    int swappedInt = ((isUpper) ? (toLower) : (((isLower) ? (toUpper) : (cInt))));
    char outChar = ((char)(swappedInt));
    outputChars[gid] = outChar;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_normalize_whitespace_char = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_normalize_whitespace_char(device const char* chars [[buffer(0)]], device const uint* prevIsSpace [[buffer(1)]], device const uint* charCount [[buffer(2)]], device uint* shouldKeep [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = charCount[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    char c = chars[gid];
    int cInt = ((int)(c));
    bool isSpace = (cInt == 32);
    bool isTab = (cInt == 9);
    bool isNewline = (cInt == 10);
    bool isCR = (cInt == 13);
    bool isWhitespace = (isSpace || (isTab || (isNewline || isCR)));
    uint prevSpace = prevIsSpace[gid];
    bool prevWasSpace = (prevSpace == 1u);
    bool notWhitespace = (isWhitespace == false);
    bool firstWhitespace = (isWhitespace && (prevWasSpace == false));
    bool keep = (notWhitespace || firstWhitespace);
    uint keepFlag = ((keep) ? (1u) : (0u));
    shouldKeep[gid] = keepFlag;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_is_digit = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_is_digit(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const uint* count [[buffer(3)]], device uint* result [[buffer(4)]], device uint* outputValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    bool hasChars = (length > 0);
    uint firstIdx = ((uint)(startOffset));
    char firstChar = ((hasChars) ? (chars[firstIdx]) : (0));
    int firstInt = ((int)(firstChar));
    bool isFirstDigit = ((firstInt >= 48) && (firstInt <= 57));
    uint _out = (((isValid && (hasChars && isFirstDigit))) ? (1u) : (0u));
    result[gid] = _out;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_is_alpha = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_is_alpha(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const uint* count [[buffer(3)]], device uint* result [[buffer(4)]], device uint* outputValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    bool hasChars = (length > 0);
    uint firstIdx = ((uint)(startOffset));
    char firstChar = ((hasChars) ? (chars[firstIdx]) : (0));
    int firstInt = ((int)(firstChar));
    bool isUpper = ((firstInt >= 65) && (firstInt <= 90));
    bool isLower = ((firstInt >= 97) && (firstInt <= 122));
    bool isFirstAlpha = (isUpper || isLower);
    uint _out = (((isValid && (hasChars && isFirstAlpha))) ? (1u) : (0u));
    result[gid] = _out;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_is_alnum = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_is_alnum(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const uint* count [[buffer(3)]], device uint* result [[buffer(4)]], device uint* outputValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    bool hasChars = (length > 0);
    uint firstIdx = ((uint)(startOffset));
    char firstChar = ((hasChars) ? (chars[firstIdx]) : (0));
    int firstInt = ((int)(firstChar));
    bool isDigit = ((firstInt >= 48) && (firstInt <= 57));
    bool isUpper = ((firstInt >= 65) && (firstInt <= 90));
    bool isLower = ((firstInt >= 97) && (firstInt <= 122));
    bool isAlnum = (isDigit || (isUpper || isLower));
    uint _out = (((isValid && (hasChars && isAlnum))) ? (1u) : (0u));
    result[gid] = _out;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_is_space = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_is_space(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const uint* count [[buffer(3)]], device uint* result [[buffer(4)]], device uint* outputValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    bool hasChars = (length > 0);
    uint firstIdx = ((uint)(startOffset));
    char firstChar = ((hasChars) ? (chars[firstIdx]) : (0));
    int firstInt = ((int)(firstChar));
    bool isSpace = (firstInt == 32);
    bool isTab = (firstInt == 9);
    bool isNewline = (firstInt == 10);
    bool isCR = (firstInt == 13);
    bool isWhitespace = (isSpace || (isTab || (isNewline || isCR)));
    uint _out = (((isValid && (hasChars && isWhitespace))) ? (1u) : (0u));
    result[gid] = _out;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_is_upper = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_is_upper(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const uint* count [[buffer(3)]], device uint* result [[buffer(4)]], device uint* outputValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    bool hasChars = (length > 0);
    uint firstIdx = ((uint)(startOffset));
    char firstChar = ((hasChars) ? (chars[firstIdx]) : (0));
    int firstInt = ((int)(firstChar));
    bool isFirstUpper = ((firstInt >= 65) && (firstInt <= 90));
    uint _out = (((isValid && (hasChars && isFirstUpper))) ? (1u) : (0u));
    result[gid] = _out;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_is_lower = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_is_lower(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const uint* count [[buffer(3)]], device uint* result [[buffer(4)]], device uint* outputValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    bool hasChars = (length > 0);
    uint firstIdx = ((uint)(startOffset));
    char firstChar = ((hasChars) ? (chars[firstIdx]) : (0));
    int firstInt = ((int)(firstChar));
    bool isFirstLower = ((firstInt >= 97) && (firstInt <= 122));
    uint _out = (((isValid && (hasChars && isFirstLower))) ? (1u) : (0u));
    result[gid] = _out;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_concat_pair_offsets = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_concat_pair_offsets(device const int* offsetsA [[buffer(0)]], device const int* offsetsB [[buffer(1)]], device const uint* validityA [[buffer(2)]], device const uint* validityB [[buffer(3)]], device const uint* count [[buffer(4)]], device int* outputLengths [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWordA = validityA[wordIdx];
    uint validBitA = ((validWordA >> bitIdx) & 1u);
    bool isValidA = (validBitA == 1u);
    uint validWordB = validityB[wordIdx];
    uint validBitB = ((validWordB >> bitIdx) & 1u);
    bool isValidB = (validBitB == 1u);
    bool bothValid = (isValidA && isValidB);
    int startA = offsetsA[gid];
    uint nextIdx = (gid + 1u);
    int endA = offsetsA[nextIdx];
    int lenA = (endA - startA);
    int startB = offsetsB[gid];
    int endB = offsetsB[nextIdx];
    int lenB = (endB - startB);
    int totalLen = (lenA + lenB);
    int outLen = ((bothValid) ? (totalLen) : (0));
    outputLengths[gid] = outLen;
    uint outValidBit = ((bothValid) ? (1u) : (0u));
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_concat_copy_chars = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_concat_copy_chars(device const char* charsA [[buffer(0)]], device const char* charsB [[buffer(1)]], device const int* offsetsA [[buffer(2)]], device const int* offsetsB [[buffer(3)]], device const int* outputOffsets [[buffer(4)]], device const uint* count [[buffer(5)]], device char* outputChars [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int srcStartA = offsetsA[gid];
    uint nextIdx = (gid + 1u);
    int srcEndA = offsetsA[nextIdx];
    int lenA = (srcEndA - srcStartA);
    int srcStartB = offsetsB[gid];
    int srcEndB = offsetsB[nextIdx];
    int lenB = (srcEndB - srcStartB);
    int dstStart = outputOffsets[gid];
    bool hasCharA = (lenA > 0);
    uint srcIdxA0 = ((uint)(srcStartA));
    char charA0 = charsA[srcIdxA0];
    uint dstIdxA0 = ((uint)(dstStart));
    if (hasCharA) {
      outputChars[dstIdxA0] = charA0;
    }
    bool hasCharA1 = (lenA > 1);
    uint srcIdxA1 = (srcIdxA0 + 1u);
    char charA1 = charsA[srcIdxA1];
    uint dstIdxA1 = (dstIdxA0 + 1u);
    if (hasCharA1) {
      outputChars[dstIdxA1] = charA1;
    }
    bool hasCharA2 = (lenA > 2);
    uint srcIdxA2 = (srcIdxA0 + 2u);
    char charA2 = charsA[srcIdxA2];
    uint dstIdxA2 = (dstIdxA0 + 2u);
    if (hasCharA2) {
      outputChars[dstIdxA2] = charA2;
    }
    bool hasCharA3 = (lenA > 3);
    uint srcIdxA3 = (srcIdxA0 + 3u);
    char charA3 = charsA[srcIdxA3];
    uint dstIdxA3 = (dstIdxA0 + 3u);
    if (hasCharA3) {
      outputChars[dstIdxA3] = charA3;
    }
    int dstStartB = (dstStart + lenA);
    bool hasCharB = (lenB > 0);
    uint srcIdxB0 = ((uint)(srcStartB));
    char charB0 = charsB[srcIdxB0];
    uint dstIdxB0 = ((uint)(dstStartB));
    if (hasCharB) {
      outputChars[dstIdxB0] = charB0;
    }
    bool hasCharB1 = (lenB > 1);
    uint srcIdxB1 = (srcIdxB0 + 1u);
    char charB1 = charsB[srcIdxB1];
    uint dstIdxB1 = (dstIdxB0 + 1u);
    if (hasCharB1) {
      outputChars[dstIdxB1] = charB1;
    }
    bool hasCharB2 = (lenB > 2);
    uint srcIdxB2 = (srcIdxB0 + 2u);
    char charB2 = charsB[srcIdxB2];
    uint dstIdxB2 = (dstIdxB0 + 2u);
    if (hasCharB2) {
      outputChars[dstIdxB2] = charB2;
    }
    bool hasCharB3 = (lenB > 3);
    uint srcIdxB3 = (srcIdxB0 + 3u);
    char charB3 = charsB[srcIdxB3];
    uint dstIdxB3 = (dstIdxB0 + 3u);
    if (hasCharB3) {
      outputChars[dstIdxB3] = charB3;
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_join_with_sep_offsets = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_join_with_sep_offsets(device const int* offsets [[buffer(0)]], device const uint* validity [[buffer(1)]], device const uint* count [[buffer(2)]], device const int* sepLen [[buffer(3)]], device int* totalLength [[buffer(4)]], device uint* validCount [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  int separatorLen = sepLen[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int strLen = (endOffset - startOffset);
    if (isValid) {
      atomic_fetch_add_explicit((device atomic_int*)(&totalLength[0u]), strLen, memory_order_relaxed);
      atomic_fetch_add_explicit((device atomic_uint*)(&validCount[0u]), 1u, memory_order_relaxed);
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_join_copy_with_sep = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_join_copy_with_sep(device const char* chars [[buffer(0)]], device const int* offsets [[buffer(1)]], device const uint* validity [[buffer(2)]], device const char* sepChar [[buffer(3)]], device const uint* count [[buffer(4)]], device const int* outputPositions [[buffer(5)]], device char* outputChars [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  char separator = sepChar[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int srcStart = offsets[gid];
    uint nextIdx = (gid + 1u);
    int srcEnd = offsets[nextIdx];
    int strLen = (srcEnd - srcStart);
    if (isValid) {
      int outPos = outputPositions[gid];
      bool needSep = (outPos > 0);
      int sepPos = (outPos - 1);
      uint sepIdx = ((uint)(sepPos));
      if (needSep) {
        outputChars[sepIdx] = separator;
      }
      uint charStartIdx = ((uint)(outPos));
      uint srcStartU = ((uint)(srcStart));
      bool hasChar0 = (strLen > 0);
      uint srcIdx0 = srcStartU;
      char char0 = chars[srcIdx0];
      uint dstIdx0 = charStartIdx;
      if (hasChar0) {
        outputChars[dstIdx0] = char0;
      }
      bool hasChar1 = (strLen > 1);
      uint srcIdx1 = (srcStartU + 1u);
      char char1 = chars[srcIdx1];
      uint dstIdx1 = (charStartIdx + 1u);
      if (hasChar1) {
        outputChars[dstIdx1] = char1;
      }
      bool hasChar2 = (strLen > 2);
      uint srcIdx2 = (srcStartU + 2u);
      char char2 = chars[srcIdx2];
      uint dstIdx2 = (charStartIdx + 2u);
      if (hasChar2) {
        outputChars[dstIdx2] = char2;
      }
      bool hasChar3 = (strLen > 3);
      uint srcIdx3 = (srcStartU + 3u);
      char char3 = chars[srcIdx3];
      uint dstIdx3 = (charStartIdx + 3u);
      if (hasChar3) {
        outputChars[dstIdx3] = char3;
      }
    }
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_compare = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_compare(device const int* offsetsA [[buffer(0)]], device const int* offsetsB [[buffer(1)]], device const char* charsA [[buffer(2)]], device const char* charsB [[buffer(3)]], device const uint* validityA [[buffer(4)]], device const uint* validityB [[buffer(5)]], device const uint* count [[buffer(6)]], device int* result [[buffer(7)]], device uint* outputValid [[buffer(8)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWordA = validityA[wordIdx];
    uint validBitA = ((validWordA >> bitIdx) & 1u);
    bool isValidA = (validBitA == 1u);
    uint validWordB = validityB[wordIdx];
    uint validBitB = ((validWordB >> bitIdx) & 1u);
    bool isValidB = (validBitB == 1u);
    bool bothValid = (isValidA && isValidB);
    int startA = offsetsA[gid];
    uint nextIdx = (gid + 1u);
    int endA = offsetsA[nextIdx];
    int lenA = (endA - startA);
    int startB = offsetsB[gid];
    int endB = offsetsB[nextIdx];
    int lenB = (endB - startB);
    bool hasCharsA = (lenA > 0);
    bool hasCharsB = (lenB > 0);
    uint idxA = ((uint)(startA));
    uint idxB = ((uint)(startB));
    char charA = ((hasCharsA) ? (charsA[idxA]) : (0));
    char charB = ((hasCharsB) ? (charsB[idxB]) : (0));
    int charAInt = ((int)(charA));
    int charBInt = ((int)(charB));
    bool charLt = (charAInt < charBInt);
    bool charGt = (charAInt > charBInt);
    bool charEq = (charAInt == charBInt);
    bool lenLt = (lenA < lenB);
    bool lenGt = (lenA > lenB);
    int cmpResult = ((charLt) ? (-1) : (((charGt) ? (1) : (((lenLt) ? (-1) : (((lenGt) ? (1) : (0))))))));
    int _out = ((bothValid) ? (cmpResult) : (0));
    result[gid] = _out;
    uint outValidBit = ((bothValid) ? (1u) : (0u));
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_equals = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_equals(device const int* offsetsA [[buffer(0)]], device const int* offsetsB [[buffer(1)]], device const char* charsA [[buffer(2)]], device const char* charsB [[buffer(3)]], device const uint* validityA [[buffer(4)]], device const uint* validityB [[buffer(5)]], device const uint* count [[buffer(6)]], device uint* result [[buffer(7)]], device uint* outputValid [[buffer(8)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWordA = validityA[wordIdx];
    uint validBitA = ((validWordA >> bitIdx) & 1u);
    bool isValidA = (validBitA == 1u);
    uint validWordB = validityB[wordIdx];
    uint validBitB = ((validWordB >> bitIdx) & 1u);
    bool isValidB = (validBitB == 1u);
    bool bothValid = (isValidA && isValidB);
    int startA = offsetsA[gid];
    uint nextIdx = (gid + 1u);
    int endA = offsetsA[nextIdx];
    int lenA = (endA - startA);
    int startB = offsetsB[gid];
    int endB = offsetsB[nextIdx];
    int lenB = (endB - startB);
    bool sameLen = (lenA == lenB);
    bool hasChars = (lenA > 0);
    uint idxA = ((uint)(startA));
    uint idxB = ((uint)(startB));
    char charA = ((hasChars) ? (charsA[idxA]) : (0));
    char charB = ((hasChars) ? (charsB[idxB]) : (0));
    bool firstMatch = ((!hasChars) || (charA == charB));
    bool isEqual = (sameLen && firstMatch);
    uint _out = (((bothValid && isEqual)) ? (1u) : (0u));
    result[gid] = _out;
    uint outValidBit = ((bothValid) ? (1u) : (0u));
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_less_than = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_less_than(device const int* offsetsA [[buffer(0)]], device const int* offsetsB [[buffer(1)]], device const char* charsA [[buffer(2)]], device const char* charsB [[buffer(3)]], device const uint* validityA [[buffer(4)]], device const uint* validityB [[buffer(5)]], device const uint* count [[buffer(6)]], device uint* result [[buffer(7)]], device uint* outputValid [[buffer(8)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWordA = validityA[wordIdx];
    uint validBitA = ((validWordA >> bitIdx) & 1u);
    bool isValidA = (validBitA == 1u);
    uint validWordB = validityB[wordIdx];
    uint validBitB = ((validWordB >> bitIdx) & 1u);
    bool isValidB = (validBitB == 1u);
    bool bothValid = (isValidA && isValidB);
    int startA = offsetsA[gid];
    uint nextIdx = (gid + 1u);
    int endA = offsetsA[nextIdx];
    int lenA = (endA - startA);
    int startB = offsetsB[gid];
    int endB = offsetsB[nextIdx];
    int lenB = (endB - startB);
    bool hasCharsA = (lenA > 0);
    bool hasCharsB = (lenB > 0);
    uint idxA = ((uint)(startA));
    uint idxB = ((uint)(startB));
    char charA = ((hasCharsA) ? (charsA[idxA]) : (0));
    char charB = ((hasCharsB) ? (charsB[idxB]) : (0));
    int charAInt = ((int)(charA));
    int charBInt = ((int)(charB));
    bool charLt = (charAInt < charBInt);
    bool charEq = (charAInt == charBInt);
    bool lenLt = (lenA < lenB);
    bool isLt = (charLt || (charEq && lenLt));
    uint _out = (((bothValid && isLt)) ? (1u) : (0u));
    result[gid] = _out;
    uint outValidBit = ((bothValid) ? (1u) : (0u));
    uint outBit = (outValidBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_strip_leading_count = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_strip_leading_count(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const uint* count [[buffer(3)]], device int* leadingSpaces [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    int leading = 0;
    bool hasLen1 = (length >= 1);
    uint idx0 = ((uint)(startOffset));
    char c0 = chars[idx0];
    bool isSpace0 = (((int)(c0)) == 32);
    int add0 = (((hasLen1 && isSpace0)) ? (1) : (0));
    int result = ((isValid) ? (add0) : (0));
    leadingSpaces[gid] = result;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_strip_trailing_count = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_strip_trailing_count(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const uint* count [[buffer(3)]], device int* trailingSpaces [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    bool hasLen1 = (length >= 1);
    bool hasLen2 = (length >= 2);
    bool hasLen3 = (length >= 3);
    bool hasLen4 = (length >= 4);
    uint idx0 = ((uint)((endOffset - 1)));
    uint idx1 = ((uint)((endOffset - 2)));
    uint idx2 = ((uint)((endOffset - 3)));
    uint idx3 = ((uint)((endOffset - 4)));
    char c0 = ((hasLen1) ? (chars[idx0]) : (0));
    char c1 = ((hasLen2) ? (chars[idx1]) : (0));
    char c2 = ((hasLen3) ? (chars[idx2]) : (0));
    char c3 = ((hasLen4) ? (chars[idx3]) : (0));
    int c0Int = ((int)(c0));
    int c1Int = ((int)(c1));
    int c2Int = ((int)(c2));
    int c3Int = ((int)(c3));
    bool isWs0 = (((c0Int == 32) || (c0Int == 9)) || ((c0Int == 10) || (c0Int == 13)));
    bool isWs1 = (((c1Int == 32) || (c1Int == 9)) || ((c1Int == 10) || (c1Int == 13)));
    bool isWs2 = (((c2Int == 32) || (c2Int == 9)) || ((c2Int == 10) || (c2Int == 13)));
    bool isWs3 = (((c3Int == 32) || (c3Int == 9)) || ((c3Int == 10) || (c3Int == 13)));
    int trail0 = (((hasLen1 && isWs0)) ? (1) : (0));
    int trail1 = ((((hasLen2 && isWs0) && isWs1)) ? (1) : (0));
    int trail2 = (((((hasLen3 && isWs0) && isWs1) && isWs2)) ? (1) : (0));
    int trail3 = ((((((hasLen4 && isWs0) && isWs1) && isWs2) && isWs3)) ? (1) : (0));
    int trailCount = (trail0 + (trail1 + (trail2 + trail3)));
    int result = ((isValid) ? (trailCount) : (0));
    trailingSpaces[gid] = result;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_strip_both_count = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_strip_both_count(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const uint* count [[buffer(3)]], device int* leadingCounts [[buffer(4)]], device int* trailingCounts [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    bool hasLen1 = (length >= 1);
    bool hasLen2 = (length >= 2);
    bool hasLen3 = (length >= 3);
    bool hasLen4 = (length >= 4);
    uint idxL0 = ((uint)(startOffset));
    uint idxL1 = (idxL0 + 1u);
    uint idxL2 = (idxL0 + 2u);
    uint idxL3 = (idxL0 + 3u);
    uint idxT0 = ((uint)((endOffset - 1)));
    uint idxT1 = ((uint)((endOffset - 2)));
    uint idxT2 = ((uint)((endOffset - 3)));
    uint idxT3 = ((uint)((endOffset - 4)));
    char cL0 = ((hasLen1) ? (chars[idxL0]) : (0));
    char cL1 = ((hasLen2) ? (chars[idxL1]) : (0));
    char cL2 = ((hasLen3) ? (chars[idxL2]) : (0));
    char cL3 = ((hasLen4) ? (chars[idxL3]) : (0));
    char cT0 = ((hasLen1) ? (chars[idxT0]) : (0));
    char cT1 = ((hasLen2) ? (chars[idxT1]) : (0));
    char cT2 = ((hasLen3) ? (chars[idxT2]) : (0));
    char cT3 = ((hasLen4) ? (chars[idxT3]) : (0));
    int cL0Int = ((int)(cL0));
    int cL1Int = ((int)(cL1));
    int cL2Int = ((int)(cL2));
    int cL3Int = ((int)(cL3));
    int cT0Int = ((int)(cT0));
    int cT1Int = ((int)(cT1));
    int cT2Int = ((int)(cT2));
    int cT3Int = ((int)(cT3));
    bool isWsL0 = (((cL0Int == 32) || (cL0Int == 9)) || ((cL0Int == 10) || (cL0Int == 13)));
    bool isWsL1 = (((cL1Int == 32) || (cL1Int == 9)) || ((cL1Int == 10) || (cL1Int == 13)));
    bool isWsL2 = (((cL2Int == 32) || (cL2Int == 9)) || ((cL2Int == 10) || (cL2Int == 13)));
    bool isWsL3 = (((cL3Int == 32) || (cL3Int == 9)) || ((cL3Int == 10) || (cL3Int == 13)));
    bool isWsT0 = (((cT0Int == 32) || (cT0Int == 9)) || ((cT0Int == 10) || (cT0Int == 13)));
    bool isWsT1 = (((cT1Int == 32) || (cT1Int == 9)) || ((cT1Int == 10) || (cT1Int == 13)));
    bool isWsT2 = (((cT2Int == 32) || (cT2Int == 9)) || ((cT2Int == 10) || (cT2Int == 13)));
    bool isWsT3 = (((cT3Int == 32) || (cT3Int == 9)) || ((cT3Int == 10) || (cT3Int == 13)));
    int lead0 = (((hasLen1 && isWsL0)) ? (1) : (0));
    int lead1 = ((((hasLen2 && isWsL0) && isWsL1)) ? (1) : (0));
    int lead2 = (((((hasLen3 && isWsL0) && isWsL1) && isWsL2)) ? (1) : (0));
    int lead3 = ((((((hasLen4 && isWsL0) && isWsL1) && isWsL2) && isWsL3)) ? (1) : (0));
    int trail0 = (((hasLen1 && isWsT0)) ? (1) : (0));
    int trail1 = ((((hasLen2 && isWsT0) && isWsT1)) ? (1) : (0));
    int trail2 = (((((hasLen3 && isWsT0) && isWsT1) && isWsT2)) ? (1) : (0));
    int trail3 = ((((((hasLen4 && isWsT0) && isWsT1) && isWsT2) && isWsT3)) ? (1) : (0));
    int leadCount = (lead0 + (lead1 + (lead2 + lead3)));
    int trailCount = (trail0 + (trail1 + (trail2 + trail3)));
    int resultLead = ((isValid) ? (leadCount) : (0));
    int resultTrail = ((isValid) ? (trailCount) : (0));
    leadingCounts[gid] = resultLead;
    trailingCounts[gid] = resultTrail;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_slice_bounds = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_slice_bounds(device const int* offsets [[buffer(0)]], device const uint* validity [[buffer(1)]], device const int* sliceStart [[buffer(2)]], device const int* sliceLen [[buffer(3)]], device const uint* count [[buffer(4)]], device int* newStartOffsets [[buffer(5)]], device int* newLengths [[buffer(6)]], device uint* outputValid [[buffer(7)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  int start = sliceStart[0u];
  int maxLen = sliceLen[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int strStart = offsets[gid];
    uint nextIdx = (gid + 1u);
    int strEnd = offsets[nextIdx];
    int strLen = (strEnd - strStart);
    int clampedStart = (((start < strLen)) ? (start) : (strLen));
    int clampedStartNonNeg = (((clampedStart >= 0)) ? (clampedStart) : (0));
    int remaining = (strLen - clampedStartNonNeg);
    int actualLen = (((maxLen < remaining)) ? (maxLen) : (remaining));
    int actualLenNonNeg = (((actualLen >= 0)) ? (actualLen) : (0));
    int newStart = (strStart + clampedStartNonNeg);
    int outStart = ((isValid) ? (newStart) : (0));
    int outLen = ((isValid) ? (actualLenNonNeg) : (0));
    newStartOffsets[gid] = outStart;
    newLengths[gid] = outLen;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_contains_char = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_contains_char(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const char* targetChar [[buffer(3)]], device const uint* count [[buffer(4)]], device uint* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  char target = targetChar[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    uint startU = ((uint)(startOffset));
    uint endU = ((uint)(endOffset));
    uint len = (endU - startU);
    bool hasLen1 = (len >= 1u);
    uint idx0 = startU;
    char c0 = chars[idx0];
    bool match0 = (hasLen1 && (c0 == target));
    bool hasLen2 = (len >= 2u);
    uint idx1 = (startU + 1u);
    char c1 = chars[idx1];
    bool match1 = (hasLen2 && (c1 == target));
    bool hasLen3 = (len >= 3u);
    uint idx2 = (startU + 2u);
    char c2 = chars[idx2];
    bool match2 = (hasLen3 && (c2 == target));
    bool hasLen4 = (len >= 4u);
    uint idx3 = (startU + 3u);
    char c3 = chars[idx3];
    bool match3 = (hasLen4 && (c3 == target));
    bool anyMatch = (match0 || (match1 || (match2 || match3)));
    uint result = (((isValid && anyMatch)) ? (1u) : (0u));
    _output[gid] = result;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_ends_with_char = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_ends_with_char(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const char* targetChar [[buffer(3)]], device const uint* count [[buffer(4)]], device uint* _output [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  char target = targetChar[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    bool hasChars = (length > 0);
    uint lastCharIdx = ((uint)((endOffset - 1)));
    char lastChar = chars[lastCharIdx];
    bool matches = (hasChars && (lastChar == target));
    uint result = (((isValid && matches)) ? (1u) : (0u));
    _output[gid] = result;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_pad_left_calc = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_pad_left_calc(device const int* offsets [[buffer(0)]], device const uint* validity [[buffer(1)]], device const int* targetWidth [[buffer(2)]], device const uint* count [[buffer(3)]], device int* paddingNeeded [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  int width = targetWidth[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    int diff = (width - length);
    int padding = (((diff > 0)) ? (diff) : (0));
    int result = ((isValid) ? (padding) : (0));
    paddingNeeded[gid] = result;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_pad_center = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_pad_center(device const int* offsets [[buffer(0)]], device const uint* validity [[buffer(1)]], device const int* targetWidth [[buffer(2)]], device const uint* count [[buffer(3)]], device int* leftPad [[buffer(4)]], device int* rightPad [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  int width = targetWidth[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    int diff = (width - length);
    int totalPad = (((diff > 0)) ? (diff) : (0));
    int leftPadVal = (totalPad / 2);
    int rightPadVal = (totalPad - leftPadVal);
    int resultLeft = ((isValid) ? (leftPadVal) : (0));
    int resultRight = ((isValid) ? (rightPadVal) : (0));
    leftPad[gid] = resultLeft;
    rightPad[gid] = resultRight;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_zfill = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_zfill(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const int* targetWidth [[buffer(3)]], device const uint* count [[buffer(4)]], device int* zeroCount [[buffer(5)]], device int* hasSign [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  int width = targetWidth[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    bool hasChars = (length > 0);
    uint firstIdx = ((uint)(startOffset));
    char firstChar = ((hasChars) ? (chars[firstIdx]) : (0));
    int firstCharInt = ((int)(firstChar));
    bool isPlus = (firstCharInt == 43);
    bool isMinus = (firstCharInt == 45);
    bool hasSignChar = (isPlus || isMinus);
    int diff = (width - length);
    int zerosNeeded = (((diff > 0)) ? (diff) : (0));
    int resultZeros = ((isValid) ? (zerosNeeded) : (0));
    int resultSign = (((isValid && hasSignChar)) ? (1) : (0));
    zeroCount[gid] = resultZeros;
    hasSign[gid] = resultSign;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_wrap_bounds = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_wrap_bounds(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const int* maxWidth [[buffer(3)]], device const uint* count [[buffer(4)]], device int* wrapPos [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  int width = maxWidth[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    bool needsWrap = (length > width);
    int widthBound = (((width < length)) ? (width) : (length));
    int pos0 = (widthBound - 1);
    int pos1 = (widthBound - 2);
    int pos2 = (widthBound - 3);
    int pos3 = (widthBound - 4);
    bool hasPos0 = (pos0 >= 0);
    bool hasPos1 = (pos1 >= 0);
    bool hasPos2 = (pos2 >= 0);
    bool hasPos3 = (pos3 >= 0);
    uint idx0 = ((uint)((startOffset + pos0)));
    uint idx1 = ((uint)((startOffset + pos1)));
    uint idx2 = ((uint)((startOffset + pos2)));
    uint idx3 = ((uint)((startOffset + pos3)));
    char c0 = ((hasPos0) ? (chars[idx0]) : (0));
    char c1 = ((hasPos1) ? (chars[idx1]) : (0));
    char c2 = ((hasPos2) ? (chars[idx2]) : (0));
    char c3 = ((hasPos3) ? (chars[idx3]) : (0));
    bool isSpace0 = (((int)(c0)) == 32);
    bool isSpace1 = (((int)(c1)) == 32);
    bool isSpace2 = (((int)(c2)) == 32);
    bool isSpace3 = (((int)(c3)) == 32);
    bool spaceAt0 = (hasPos0 && isSpace0);
    bool spaceAt1 = (hasPos1 && isSpace1);
    bool spaceAt2 = (hasPos2 && isSpace2);
    bool spaceAt3 = (hasPos3 && isSpace3);
    int wrapPosition = ((spaceAt0) ? ((pos0 + 1)) : (((spaceAt1) ? ((pos1 + 1)) : (((spaceAt2) ? ((pos2 + 1)) : (((spaceAt3) ? ((pos3 + 1)) : (widthBound))))))));
    int result = (((isValid && needsWrap)) ? (wrapPosition) : (-1));
    wrapPos[gid] = result;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_substring_bounds = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_substring_bounds(device const int* offsets [[buffer(0)]], device const uint* validity [[buffer(1)]], device const int* startIdx [[buffer(2)]], device const int* subLen [[buffer(3)]], device const uint* count [[buffer(4)]], device int* outStart [[buffer(5)]], device int* outLen [[buffer(6)]], device uint* outputValid [[buffer(7)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  int start = startIdx[0u];
  int maxLen = subLen[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int strStart = offsets[gid];
    uint nextIdx = (gid + 1u);
    int strEnd = offsets[nextIdx];
    int strLen = (strEnd - strStart);
    bool isNegative = (start < 0);
    int adjustedStart = ((isNegative) ? ((strLen + start)) : (start));
    int clampedStartLow = (((adjustedStart < 0)) ? (0) : (adjustedStart));
    int clampedStart = (((clampedStartLow > strLen)) ? (strLen) : (clampedStartLow));
    int remaining = (strLen - clampedStart);
    int actualLen = (((maxLen < remaining)) ? (maxLen) : (remaining));
    int actualLenNonNeg = (((actualLen < 0)) ? (0) : (actualLen));
    int newStart = (strStart + clampedStart);
    int resultStart = ((isValid) ? (newStart) : (0));
    int resultLen = ((isValid) ? (actualLenNonNeg) : (0));
    outStart[gid] = resultStart;
    outLen[gid] = resultLen;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_left_bounds = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_left_bounds(device const int* offsets [[buffer(0)]], device const uint* validity [[buffer(1)]], device const int* numChars [[buffer(2)]], device const uint* count [[buffer(3)]], device int* outStart [[buffer(4)]], device int* outLen [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  int nChars = numChars[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int strStart = offsets[gid];
    uint nextIdx = (gid + 1u);
    int strEnd = offsets[nextIdx];
    int strLen = (strEnd - strStart);
    int actualLen = (((nChars < strLen)) ? (nChars) : (strLen));
    int actualLenNonNeg = (((actualLen < 0)) ? (0) : (actualLen));
    int resultStart = ((isValid) ? (strStart) : (0));
    int resultLen = ((isValid) ? (actualLenNonNeg) : (0));
    outStart[gid] = resultStart;
    outLen[gid] = resultLen;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_right_bounds = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_right_bounds(device const int* offsets [[buffer(0)]], device const uint* validity [[buffer(1)]], device const int* numChars [[buffer(2)]], device const uint* count [[buffer(3)]], device int* outStart [[buffer(4)]], device int* outLen [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  int nChars = numChars[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int strStart = offsets[gid];
    uint nextIdx = (gid + 1u);
    int strEnd = offsets[nextIdx];
    int strLen = (strEnd - strStart);
    int actualLen = (((nChars < strLen)) ? (nChars) : (strLen));
    int actualLenNonNeg = (((actualLen < 0)) ? (0) : (actualLen));
    int newStart = (strEnd - actualLenNonNeg);
    int resultStart = ((isValid) ? (newStart) : (0));
    int resultLen = ((isValid) ? (actualLenNonNeg) : (0));
    outStart[gid] = resultStart;
    outLen[gid] = resultLen;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_regex_match = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_regex_match(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const int* patternOffsets [[buffer(3)]], device const char* patternChars [[buffer(4)]], device const uint* count [[buffer(5)]], device uint* _output [[buffer(6)]], device uint* outputValid [[buffer(7)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {

}

)kernel";
const char* k_metal_navatala_dataframe_str_regex_extract = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_regex_extract(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const int* patternOffsets [[buffer(3)]], device const char* patternChars [[buffer(4)]], device const uint* groupIndex [[buffer(5)]], device const uint* count [[buffer(6)]], device int* outputOffsets [[buffer(7)]], device char* outputChars [[buffer(8)]], device uint* outputValid [[buffer(9)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {

}

)kernel";
const char* k_metal_navatala_dataframe_str_regex_replace = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_regex_replace(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const int* patternOffsets [[buffer(3)]], device const char* patternChars [[buffer(4)]], device const int* replacementOffsets [[buffer(5)]], device const char* replacementChars [[buffer(6)]], device const uint* count [[buffer(7)]], device int* outputOffsets [[buffer(8)]], device char* outputChars [[buffer(9)]], device uint* outputValid [[buffer(10)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {

}

)kernel";
const char* k_metal_navatala_dataframe_str_find_char = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_find_char(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const char* targetChar [[buffer(3)]], device const uint* count [[buffer(4)]], device int* positions [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  char target = targetChar[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    bool hasChars = (startOffset < endOffset);
    uint firstCharIdx = ((uint)(startOffset));
    char firstChar = ((hasChars) ? (chars[firstCharIdx]) : (0));
    bool foundFirst = (firstChar == target);
    int pos = (((hasChars && foundFirst)) ? (0) : (-1));
    int outPos = ((isValid) ? (pos) : (-1));
    positions[gid] = outPos;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_count_char = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_count_char(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const char* targetChar [[buffer(3)]], device const uint* count [[buffer(4)]], device int* counts [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  char target = targetChar[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    int charCount = ((isValid) ? (0) : (0));
    counts[gid] = charCount;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_replace_char = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_replace_char(device const char* inChars [[buffer(0)]], device const uint* charCount [[buffer(1)]], device const char* oldChar [[buffer(2)]], device const char* newChar [[buffer(3)]], device char* outChars [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = charCount[0u];
  char old = oldChar[0u];
  char new = newChar[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    char c = inChars[gid];
    bool isMatch = (c == old);
    char outC = ((isMatch) ? (new) : (c));
    outChars[gid] = outC;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_contains_pattern2 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_contains_pattern2(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const char* pattern1 [[buffer(3)]], device const char* pattern2 [[buffer(4)]], device const uint* count [[buffer(5)]], device uint* found [[buffer(6)]], device uint* outputValid [[buffer(7)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  char p1 = pattern1[0u];
  char p2 = pattern2[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    bool hasEnough = (length >= 2);
    uint idx0 = ((uint)(startOffset));
    uint idx1 = (idx0 + 1u);
    char c0 = ((hasEnough) ? (chars[idx0]) : (0));
    char c1 = ((hasEnough) ? (chars[idx1]) : (0));
    bool matchFirst = ((c0 == p1) && (c1 == p2));
    uint result = (((isValid && (hasEnough && matchFirst))) ? (1u) : (0u));
    found[gid] = result;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_split_count_by_char = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_split_count_by_char(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const char* delimChar [[buffer(3)]], device const uint* count [[buffer(4)]], device int* splitCounts [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  char delim = delimChar[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    bool hasChars = (length > 0);
    uint idx0 = ((uint)(startOffset));
    uint idx1 = (idx0 + 1u);
    uint idx2 = (idx0 + 2u);
    uint idx3 = (idx0 + 3u);
    char c0 = (((length >= 1)) ? (chars[idx0]) : (0));
    char c1 = (((length >= 2)) ? (chars[idx1]) : (0));
    char c2 = (((length >= 3)) ? (chars[idx2]) : (0));
    char c3 = (((length >= 4)) ? (chars[idx3]) : (0));
    int match0 = (((c0 == delim)) ? (1) : (0));
    int match1 = (((c1 == delim)) ? (1) : (0));
    int match2 = (((c2 == delim)) ? (1) : (0));
    int match3 = (((c3 == delim)) ? (1) : (0));
    int delimCount = (match0 + (match1 + (match2 + match3)));
    int splitCount = (delimCount + 1);
    int outCount = ((isValid) ? (splitCount) : (0));
    splitCounts[gid] = outCount;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_split_by_char = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_split_by_char(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const char* delimChar [[buffer(3)]], device const uint* count [[buffer(4)]], device int* outputOffsets [[buffer(5)]], device uint* outputValid [[buffer(6)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  char delim = delimChar[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    uint idx0 = ((uint)(startOffset));
    uint idx1 = (idx0 + 1u);
    uint idx2 = (idx0 + 2u);
    uint idx3 = (idx0 + 3u);
    char c0 = (((length >= 1)) ? (chars[idx0]) : (0));
    char c1 = (((length >= 2)) ? (chars[idx1]) : (0));
    char c2 = (((length >= 3)) ? (chars[idx2]) : (0));
    char c3 = (((length >= 4)) ? (chars[idx3]) : (0));
    bool isDelim0 = (c0 == delim);
    bool isDelim1 = (c1 == delim);
    bool isDelim2 = (c2 == delim);
    bool isDelim3 = (c3 == delim);
    int seg0End = ((isDelim0) ? (startOffset) : (((isDelim1) ? ((startOffset + 1)) : (((isDelim2) ? ((startOffset + 2)) : (((isDelim3) ? ((startOffset + 3)) : (endOffset))))))));
    int outOffset = ((isValid) ? (seg0End) : (0));
    outputOffsets[gid] = outOffset;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_split_get_element = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_split_get_element(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const char* delimChar [[buffer(3)]], device const int* elementIdx [[buffer(4)]], device const uint* count [[buffer(5)]], device int* elementStart [[buffer(6)]], device int* elementEnd [[buffer(7)]], device uint* outputValid [[buffer(8)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  char delim = delimChar[0u];
  int elemIdx = elementIdx[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    bool wantsFirst = (elemIdx == 0);
    uint idx0 = ((uint)(startOffset));
    uint idx1 = (idx0 + 1u);
    uint idx2 = (idx0 + 2u);
    uint idx3 = (idx0 + 3u);
    char c0 = (((length >= 1)) ? (chars[idx0]) : (0));
    char c1 = (((length >= 2)) ? (chars[idx1]) : (0));
    char c2 = (((length >= 3)) ? (chars[idx2]) : (0));
    char c3 = (((length >= 4)) ? (chars[idx3]) : (0));
    bool isDelim0 = (c0 == delim);
    bool isDelim1 = (c1 == delim);
    bool isDelim2 = (c2 == delim);
    bool isDelim3 = (c3 == delim);
    int firstDelimPos = ((isDelim0) ? (0) : (((isDelim1) ? (1) : (((isDelim2) ? (2) : (((isDelim3) ? (3) : (-1))))))));
    bool hasDelim = (firstDelimPos != -1);
    int elem0Start = startOffset;
    int elem0End = ((hasDelim) ? ((startOffset + firstDelimPos)) : (endOffset));
    int elem1Start = ((hasDelim) ? ((elem0End + 1)) : (-1));
    int elem1End = ((hasDelim) ? (endOffset) : (-1));
    int elemStart = ((wantsFirst) ? (elem0Start) : (elem1Start));
    int elemEnd = ((wantsFirst) ? (elem0End) : (elem1End));
    int outStart = ((isValid) ? (elemStart) : (-1));
    int outEnd = ((isValid) ? (elemEnd) : (-1));
    elementStart[gid] = outStart;
    elementEnd[gid] = outEnd;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_split_by_whitespace = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_split_by_whitespace(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const uint* count [[buffer(3)]], device int* splitCounts [[buffer(4)]], device uint* outputValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    uint idx0 = ((uint)(startOffset));
    uint idx1 = (idx0 + 1u);
    uint idx2 = (idx0 + 2u);
    uint idx3 = (idx0 + 3u);
    char c0 = (((length >= 1)) ? (chars[idx0]) : (0));
    char c1 = (((length >= 2)) ? (chars[idx1]) : (0));
    char c2 = (((length >= 3)) ? (chars[idx2]) : (0));
    char c3 = (((length >= 4)) ? (chars[idx3]) : (0));
    int c0Int = ((int)(c0));
    int c1Int = ((int)(c1));
    int c2Int = ((int)(c2));
    int c3Int = ((int)(c3));
    bool isWs0 = (((c0Int == 32) || (c0Int == 9)) || ((c0Int == 10) || (c0Int == 13)));
    bool isWs1 = (((c1Int == 32) || (c1Int == 9)) || ((c1Int == 10) || (c1Int == 13)));
    bool isWs2 = (((c2Int == 32) || (c2Int == 9)) || ((c2Int == 10) || (c2Int == 13)));
    bool isWs3 = (((c3Int == 32) || (c3Int == 9)) || ((c3Int == 10) || (c3Int == 13)));
    int ws0 = ((isWs0) ? (1) : (0));
    int ws1 = ((isWs1) ? (1) : (0));
    int ws2 = ((isWs2) ? (1) : (0));
    int ws3 = ((isWs3) ? (1) : (0));
    int wsCount = (ws0 + (ws1 + (ws2 + ws3)));
    int splitCount = (wsCount + 1);
    bool hasChars = (length > 0);
    int finalCount = ((hasChars) ? (splitCount) : (1));
    int outCount = ((isValid) ? (finalCount) : (0));
    splitCounts[gid] = outCount;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_reverse_chars = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_reverse_chars(device const char* inChars [[buffer(0)]], device const int* inOffsets [[buffer(1)]], device const uint* charCount [[buffer(2)]], device const uint* stringCount [[buffer(3)]], device char* outChars [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint nChars = charCount[0u];
  bool inBounds = (gid < nChars);
  if (inBounds) {
    char c = inChars[gid];
    outChars[gid] = c;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_truncate_bounds = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_truncate_bounds(device const int* offsets [[buffer(0)]], device const uint* validity [[buffer(1)]], device const int* maxLen [[buffer(2)]], device const uint* count [[buffer(3)]], device int* newLengths [[buffer(4)]], device uint* outputValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  int maxLength = maxLen[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    int truncLen = (((length < maxLength)) ? (length) : (maxLength));
    int outLen = ((isValid) ? (truncLen) : (0));
    newLengths[gid] = outLen;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_pad_right_calc = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_pad_right_calc(device const int* offsets [[buffer(0)]], device const uint* validity [[buffer(1)]], device const int* targetLen [[buffer(2)]], device const uint* count [[buffer(3)]], device int* padLengths [[buffer(4)]], device uint* outputValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  int target = targetLen[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    int diff = (target - length);
    int padLen = (((diff > 0)) ? (diff) : (0));
    int outLen = ((isValid) ? (padLen) : (0));
    padLengths[gid] = outLen;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_repeat_bounds = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_str_repeat_bounds(device const int* offsets [[buffer(0)]], device const uint* validity [[buffer(1)]], device const int* repeatCount [[buffer(2)]], device const uint* count [[buffer(3)]], device int* newLengths [[buffer(4)]], device uint* outputValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  int repeat = repeatCount[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = validity[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    bool isValid = (validBit == 1u);
    int startOffset = offsets[gid];
    uint nextIdx = (gid + 1u);
    int endOffset = offsets[nextIdx];
    int length = (endOffset - startOffset);
    int newLen = (length * repeat);
    int outLen = ((isValid) ? (newLen) : (0));
    newLengths[gid] = outLen;
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_translate_char = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_translate_char(device const char* chars [[buffer(0)]], device const char* srcChar [[buffer(1)]], device const char* dstChar [[buffer(2)]], device const uint* count [[buffer(3)]], device char* _output [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  char src = srcChar[0u];
  char dst = dstChar[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    char c = chars[gid];
    bool isMatch = (c == src);
    char outChar = ((isMatch) ? (dst) : (c));
    _output[gid] = outChar;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_translate_table = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_translate_table(device const char* chars [[buffer(0)]], device const char* table [[buffer(1)]], device const uint* count [[buffer(2)]], device char* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    char c = chars[gid];
    int cInt = ((int)(c));
    int cUnsigned = (((cInt < 0)) ? ((cInt + 256)) : (cInt));
    uint tableIdx = ((uint)(cUnsigned));
    char translated = table[tableIdx];
    _output[gid] = translated;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_str_normalize = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_str_normalize(device const int* offsets [[buffer(0)]], device const char* chars [[buffer(1)]], device const uint* validity [[buffer(2)]], device const int* normForm [[buffer(3)]], device const uint* count [[buffer(4)]], device int* outOffsets [[buffer(5)]], device char* outChars [[buffer(6)]], device uint* outValidity [[buffer(7)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {

}

)kernel";
const char* k_metal_navatala_dataframe_negate_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_negate_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device float* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = _input[gid];
    float result = (-val);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_negate_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_negate_i32(device const int* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int val = _input[gid];
    int result = (-val);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_abs_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_abs_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device float* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = _input[gid];
    float result = abs(val);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_abs_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_abs_i32(device const int* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int val = _input[gid];
    bool isNeg = (val < 0);
    int negVal = (-val);
    int result = ((isNeg) ? (negVal) : (val));
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_ceil_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_ceil_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device float* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = _input[gid];
    float result = ceil(val);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_floor_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_floor_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device float* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = _input[gid];
    float result = floor(val);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_round_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_round_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device float* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = _input[gid];
    float result = round(val);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_trunc_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_trunc_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device float* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = _input[gid];
    bool isNonNeg = (val >= as_type<float>(0x00000000u));
    float floorVal = floor(val);
    float ceilVal = ceil(val);
    float result = ((isNonNeg) ? (floorVal) : (ceilVal));
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_negate_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_negate_i64(device const long* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device long* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    long val = _input[gid];
    long result = (-val);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_abs_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_abs_i64(device const long* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device long* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    long val = _input[gid];
    bool isNeg = (val < 0);
    long negVal = (-val);
    long result = ((isNeg) ? (negVal) : (val));
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_sqrt_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_sqrt_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device float* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = _input[gid];
    float result = sqrt(val);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_exp_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_exp_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device float* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = _input[gid];
    float result = exp(val);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_log_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_log_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device float* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = _input[gid];
    float result = log(val);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_sin_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_sin_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device float* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = _input[gid];
    float result = sin(val);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_cos_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_cos_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device float* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = _input[gid];
    float result = cos(val);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_tan_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_tan_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device float* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = _input[gid];
    float sinVal = sin(val);
    float cosVal = cos(val);
    float result = (sinVal / cosVal);
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_log10_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_log10_f32(device const float* _input [[buffer(0)]], device const uint* inputValid [[buffer(1)]], device const uint* count [[buffer(2)]], device float* _output [[buffer(3)]], device uint* outputValid [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = _input[gid];
    float logVal = log(val);
    float result = (logVal * as_type<float>(0x3ede5bd9u));
    _output[gid] = result;
    uint wordIdx = (gid / 32u);
    uint bitIdx = (gid % 32u);
    uint validWord = inputValid[wordIdx];
    uint validBit = ((validWord >> bitIdx) & 1u);
    uint outBit = (validBit << bitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&outputValid[wordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_compute_concat_offsets = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_compute_concat_offsets(device const uint* lengths [[buffer(0)]], device const uint* numColumns [[buffer(1)]], device uint* offsets [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = numColumns[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    bool isFirst = (gid == 0u);
    uint len0 = (((n >= 1u)) ? (lengths[0u]) : (0u));
    uint len1 = (((n >= 2u)) ? (lengths[1u]) : (0u));
    uint len2 = (((n >= 3u)) ? (lengths[2u]) : (0u));
    uint len3 = (((n >= 4u)) ? (lengths[3u]) : (0u));
    uint prefix0 = 0u;
    uint prefix1 = len0;
    uint prefix2 = (prefix1 + len1);
    uint prefix3 = (prefix2 + len2);
    bool isIdx0 = (gid == 0u);
    bool isIdx1 = (gid == 1u);
    bool isIdx2 = (gid == 2u);
    bool isIdx3 = (gid == 3u);
    uint result = ((isIdx0) ? (prefix0) : (((isIdx1) ? (prefix1) : (((isIdx2) ? (prefix2) : (((isIdx3) ? (prefix3) : (0u))))))));
    offsets[gid] = result;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gather_concat_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_gather_concat_i32(device const int* src [[buffer(0)]], device const uint* srcValid [[buffer(1)]], device const uint* srcLen [[buffer(2)]], device const uint* destOffset [[buffer(3)]], device int* dest [[buffer(4)]], device uint* destValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = srcLen[0u];
  uint offset = destOffset[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int val = src[gid];
    uint destIdx = (offset + gid);
    dest[destIdx] = val;
    uint srcWordIdx = (gid / 32u);
    uint srcBitIdx = (gid % 32u);
    uint srcValidWord = srcValid[srcWordIdx];
    uint srcValidBit = ((srcValidWord >> srcBitIdx) & 1u);
    uint destWordIdx = (destIdx / 32u);
    uint destBitIdx = (destIdx % 32u);
    uint outBit = (srcValidBit << destBitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&destValid[destWordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gather_concat_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_gather_concat_f32(device const float* src [[buffer(0)]], device const uint* srcValid [[buffer(1)]], device const uint* srcLen [[buffer(2)]], device const uint* destOffset [[buffer(3)]], device float* dest [[buffer(4)]], device uint* destValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = srcLen[0u];
  uint offset = destOffset[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float val = src[gid];
    uint destIdx = (offset + gid);
    dest[destIdx] = val;
    uint srcWordIdx = (gid / 32u);
    uint srcBitIdx = (gid % 32u);
    uint srcValidWord = srcValid[srcWordIdx];
    uint srcValidBit = ((srcValidWord >> srcBitIdx) & 1u);
    uint destWordIdx = (destIdx / 32u);
    uint destBitIdx = (destIdx % 32u);
    uint outBit = (srcValidBit << destBitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&destValid[destWordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gather_concat_i64 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_gather_concat_i64(device const long* src [[buffer(0)]], device const uint* srcValid [[buffer(1)]], device const uint* srcLen [[buffer(2)]], device const uint* destOffset [[buffer(3)]], device long* dest [[buffer(4)]], device uint* destValid [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = srcLen[0u];
  uint offset = destOffset[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    long val = src[gid];
    uint destIdx = (offset + gid);
    dest[destIdx] = val;
    uint srcWordIdx = (gid / 32u);
    uint srcBitIdx = (gid % 32u);
    uint srcValidWord = srcValid[srcWordIdx];
    uint srcValidBit = ((srcValidWord >> srcBitIdx) & 1u);
    uint destWordIdx = (destIdx / 32u);
    uint destBitIdx = (destIdx % 32u);
    uint outBit = (srcValidBit << destBitIdx);
    atomic_fetch_or_explicit((device atomic_uint*)(&destValid[destWordIdx]), outBit, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_hash_partition_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_hash_partition_i32(device const int* keys [[buffer(0)]], device const uint* numPartitions [[buffer(1)]], device const uint* count [[buffer(2)]], device uint* partitionIds [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint numParts = numPartitions[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int key = keys[gid];
    uint k = ((uint)(key));
    uint h1 = (k ^ (k >> 16u));
    uint h2 = (h1 * 2246822507u);
    uint h3 = (h2 ^ (h2 >> 13u));
    uint h4 = (h3 * 3266489909u);
    uint hash = (h4 ^ (h4 >> 16u));
    uint partId = (hash % numParts);
    partitionIds[gid] = partId;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_hash_partition_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_hash_partition_f32(device const float* keys [[buffer(0)]], device const uint* numPartitions [[buffer(1)]], device const uint* count [[buffer(2)]], device uint* partitionIds [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint numParts = numPartitions[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float key = keys[gid];
    float scaled = (key * as_type<float>(0x4f000000u));
    int k = ((int)(scaled));
    uint kU = ((uint)(k));
    uint h1 = (kU ^ (kU >> 16u));
    uint h2 = (h1 * 2246822507u);
    uint h3 = (h2 ^ (h2 >> 13u));
    uint h4 = (h3 * 3266489909u);
    uint hash = (h4 ^ (h4 >> 16u));
    uint partId = (hash % numParts);
    partitionIds[gid] = partId;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_round_robin_partition = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_round_robin_partition(device const uint* numPartitions [[buffer(0)]], device const uint* count [[buffer(1)]], device uint* partitionIds [[buffer(2)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint numParts = numPartitions[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint partId = (gid % numParts);
    partitionIds[gid] = partId;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_range_partition_bounds = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_range_partition_bounds(device const int* sortedData [[buffer(0)]], device const uint* dataCount [[buffer(1)]], device const uint* numPartitions [[buffer(2)]], device int* bounds [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = dataCount[0u];
  uint numParts = numPartitions[0u];
  uint numBounds = (numParts - 1u);
  bool inBounds = (gid < numBounds);
  if (inBounds) {
    uint boundIdx = (gid + 1u);
    uint sampleIdx = ((boundIdx * n) / numParts);
    uint maxIdx = (n - 1u);
    uint clampedIdx = (((sampleIdx < maxIdx)) ? (sampleIdx) : (maxIdx));
    int boundValue = sortedData[clampedIdx];
    bounds[gid] = boundValue;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_range_partition_assign = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_range_partition_assign(device const int* keys [[buffer(0)]], device const int* bounds [[buffer(1)]], device const uint* numPartitions [[buffer(2)]], device const uint* count [[buffer(3)]], device uint* partitionIds [[buffer(4)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint numParts = numPartitions[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int key = keys[gid];
    uint numBounds = (numParts - 1u);
    int b0 = (((numBounds >= 1u)) ? (bounds[0u]) : (2147483647));
    int b1 = (((numBounds >= 2u)) ? (bounds[1u]) : (2147483647));
    int b2 = (((numBounds >= 3u)) ? (bounds[2u]) : (2147483647));
    int b3 = (((numBounds >= 4u)) ? (bounds[3u]) : (2147483647));
    uint ge0 = (((key >= b0)) ? (1u) : (0u));
    uint ge1 = (((key >= b1)) ? (1u) : (0u));
    uint ge2 = (((key >= b2)) ? (1u) : (0u));
    uint ge3 = (((key >= b3)) ? (1u) : (0u));
    uint partId = (ge0 + (ge1 + (ge2 + ge3)));
    uint maxPartId = (numParts - 1u);
    uint clampedPartId = (((partId < maxPartId)) ? (partId) : (maxPartId));
    partitionIds[gid] = clampedPartId;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gather_partition_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_gather_partition_i32(device const int* _input [[buffer(0)]], device const uint* partitionIds [[buffer(1)]], device const uint* count [[buffer(2)]], device uint* partitionOffsets [[buffer(3)]], device int* _output [[buffer(4)]], device uint* outputIndices [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int value = _input[gid];
    uint partId = partitionIds[gid];
    atomic_fetch_add_explicit((device atomic_uint*)(&partitionOffsets[partId]), 1u, memory_order_relaxed);
    uint baseOffset = partitionOffsets[partId];
    uint outPos = (baseOffset - 1u);
    _output[outPos] = value;
    outputIndices[gid] = outPos;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_gather_partition_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_gather_partition_f32(device const float* _input [[buffer(0)]], device const uint* partitionIds [[buffer(1)]], device const uint* count [[buffer(2)]], device uint* partitionOffsets [[buffer(3)]], device float* _output [[buffer(4)]], device uint* outputIndices [[buffer(5)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    float value = _input[gid];
    uint partId = partitionIds[gid];
    atomic_fetch_add_explicit((device atomic_uint*)(&partitionOffsets[partId]), 1u, memory_order_relaxed);
    uint baseOffset = partitionOffsets[partId];
    uint outPos = (baseOffset - 1u);
    _output[outPos] = value;
    outputIndices[gid] = outPos;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_compute_partition_offsets = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_compute_partition_offsets(device const uint* partitionIds [[buffer(0)]], device const uint* numPartitions [[buffer(1)]], device const uint* count [[buffer(2)]], device uint* partitionCounts [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint partId = partitionIds[gid];
    atomic_fetch_add_explicit((device atomic_uint*)(&partitionCounts[partId]), 1u, memory_order_relaxed);
  }
}

)kernel";
const char* k_metal_navatala_dataframe_transpose_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_transpose_i32(device const int* _input [[buffer(0)]], device const uint* rows [[buffer(1)]], device const uint* cols [[buffer(2)]], device int* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint numRows = rows[0u];
  uint numCols = cols[0u];
  uint totalElements = (numRows * numCols);
  bool inBounds = (gid < totalElements);
  if (inBounds) {
    uint i = (gid / numCols);
    uint j = (gid % numCols);
    int val = _input[gid];
    uint outIdx = ((j * numRows) + i);
    _output[outIdx] = val;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_transpose_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_transpose_f32(device const float* _input [[buffer(0)]], device const uint* rows [[buffer(1)]], device const uint* cols [[buffer(2)]], device float* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint numRows = rows[0u];
  uint numCols = cols[0u];
  uint totalElements = (numRows * numCols);
  bool inBounds = (gid < totalElements);
  if (inBounds) {
    uint i = (gid / numCols);
    uint j = (gid % numCols);
    float val = _input[gid];
    uint outIdx = ((j * numRows) + i);
    _output[outIdx] = val;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_flatten_offsets = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_flatten_offsets(device const int* outerOffsets [[buffer(0)]], device const int* innerOffsets [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int outerIdx = outerOffsets[gid];
    bool isNegative = (outerIdx < 0);
    uint safeIdx = ((isNegative) ? (0u) : (((uint)(outerIdx))));
    int flatOffset = innerOffsets[safeIdx];
    int result = ((isNegative) ? (-1) : (flatOffset));
    _output[gid] = result;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_unflatten_copy = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_unflatten_copy(device const int* flatData [[buffer(0)]], device const int* offsets [[buffer(1)]], device const uint* count [[buffer(2)]], device int* _output [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    int srcIdx = offsets[gid];
    bool isValid = (srcIdx != -1);
    bool isNegative = (srcIdx < 0);
    uint safeIdx = ((isNegative) ? (0u) : (((uint)(srcIdx))));
    int val = flatData[safeIdx];
    int result = ((isValid) ? (val) : (0));
    _output[gid] = result;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_compute_split_bounds = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_compute_split_bounds(device const uint* totalRows [[buffer(0)]], device const uint* numSplits [[buffer(1)]], device int* splitStarts [[buffer(2)]], device int* splitEnds [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint nRows = totalRows[0u];
  uint nSplits = numSplits[0u];
  bool inBounds = (gid < nSplits);
  if (inBounds) {
    uint baseSize = (nRows / nSplits);
    uint remainder = (nRows % nSplits);
    bool hasExtra = (gid < remainder);
    uint extraRows = ((hasExtra) ? (1u) : (0u));
    uint splitSize = (baseSize + extraRows);
    uint prevSplitsWithExtra = (((gid < remainder)) ? (gid) : (remainder));
    uint prevSplitsWithoutExtra = (gid - prevSplitsWithExtra);
    uint sizePlusOne = (baseSize + 1u);
    uint startFromExtra = (prevSplitsWithExtra * sizePlusOne);
    uint startFromRegular = (prevSplitsWithoutExtra * baseSize);
    uint startIdx = (startFromExtra + startFromRegular);
    uint endIdx = (startIdx + splitSize);
    int startInt = ((int)(startIdx));
    int endInt = ((int)(endIdx));
    splitStarts[gid] = startInt;
    splitEnds[gid] = endInt;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_split_copy_i32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_split_copy_i32(device const int* src [[buffer(0)]], device const uint* srcOffset [[buffer(1)]], device const uint* count [[buffer(2)]], device int* dst [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint offset = srcOffset[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint srcIdx = (offset + gid);
    int value = src[srcIdx];
    dst[gid] = value;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_split_copy_f32 = R"kernel(
#include <metal_stdlib>
using namespace metal;

kernel void navatala_dataframe_split_copy_f32(device const float* src [[buffer(0)]], device const uint* srcOffset [[buffer(1)]], device const uint* count [[buffer(2)]], device float* dst [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint offset = srcOffset[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint srcIdx = (offset + gid);
    float value = src[srcIdx];
    dst[gid] = value;
  }
}

)kernel";
const char* k_metal_navatala_dataframe_split_copy_validity = R"kernel(
#include <metal_stdlib>
using namespace metal;

static inline int gpu_atomic_cas_int(device atomic_int* ptr, int expected, int desired) {
  int exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

static inline uint gpu_atomic_cas_uint(device atomic_uint* ptr, uint expected, uint desired) {
  uint exp = expected;
  while (!atomic_compare_exchange_weak_explicit(ptr, &exp, desired,
      memory_order_relaxed, memory_order_relaxed) && exp == expected) {}
  return exp;
}

kernel void navatala_dataframe_split_copy_validity(device const uint* srcValidity [[buffer(0)]], device const uint* srcOffset [[buffer(1)]], device const uint* count [[buffer(2)]], device uint* dstValidity [[buffer(3)]], uint3 __gid [[thread_position_in_grid]], uint3 __tid [[thread_position_in_threadgroup]], uint3 __tgid [[threadgroup_position_in_grid]], uint3 __tgsz [[threads_per_threadgroup]], uint3 __grid_size [[threads_per_grid]], uint __lane [[thread_index_in_simdgroup]], uint __simd_size [[threads_per_simdgroup]]) {
  uint gid = ((uint)(int(__gid.x)));
  uint n = count[0u];
  uint offset = srcOffset[0u];
  bool inBounds = (gid < n);
  if (inBounds) {
    uint srcRowIdx = (offset + gid);
    uint srcWordIdx = (srcRowIdx / 32u);
    uint srcBitIdx = (srcRowIdx % 32u);
    uint srcWord = srcValidity[srcWordIdx];
    uint srcBit = ((srcWord >> srcBitIdx) & 1u);
    bool isValid = (srcBit == 1u);
    uint dstWordIdx = (gid / 32u);
    uint dstBitIdx = (gid % 32u);
    uint dstBit = ((isValid) ? ((1u << dstBitIdx)) : (0u));
    atomic_fetch_or_explicit((device atomic_uint*)(&dstValidity[dstWordIdx]), dstBit, memory_order_relaxed);
  }
}

)kernel";

namespace NavatalaRegistry {

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_writeback_sum_f32[] = {
  { "keys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "vals", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "dst", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_writeback_sum_f32 = {
  1,
  "navatala_dataframe_writeback_sum_f32",
  "metal",
  "navatala_dataframe_writeback_sum_f32",
  "kernel:metal:navatala_dataframe_writeback_sum_f32",
  "abi-r1:metal:navatala_dataframe_writeback_sum_f32",
  "abi-r1:metal:navatala_dataframe_writeback_sum_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_writeback_sum_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_writeback_sum_i32[] = {
  { "keys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "vals", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "dst", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_writeback_sum_i32 = {
  1,
  "navatala_dataframe_writeback_sum_i32",
  "metal",
  "navatala_dataframe_writeback_sum_i32",
  "kernel:metal:navatala_dataframe_writeback_sum_i32",
  "abi-r1:metal:navatala_dataframe_writeback_sum_i32",
  "abi-r1:metal:navatala_dataframe_writeback_sum_i32",
  4,
  kAbiArgs_metal_navatala_dataframe_writeback_sum_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_writeback_sum_u32[] = {
  { "keys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "vals", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "dst", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_writeback_sum_u32 = {
  1,
  "navatala_dataframe_writeback_sum_u32",
  "metal",
  "navatala_dataframe_writeback_sum_u32",
  "kernel:metal:navatala_dataframe_writeback_sum_u32",
  "abi-r1:metal:navatala_dataframe_writeback_sum_u32",
  "abi-r1:metal:navatala_dataframe_writeback_sum_u32",
  4,
  kAbiArgs_metal_navatala_dataframe_writeback_sum_u32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_writeback_min_f32[] = {
  { "keys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "vals", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "dst", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_writeback_min_f32 = {
  1,
  "navatala_dataframe_writeback_min_f32",
  "metal",
  "navatala_dataframe_writeback_min_f32",
  "kernel:metal:navatala_dataframe_writeback_min_f32",
  "abi-r1:metal:navatala_dataframe_writeback_min_f32",
  "abi-r1:metal:navatala_dataframe_writeback_min_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_writeback_min_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_writeback_max_f32[] = {
  { "keys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "vals", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "dst", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_writeback_max_f32 = {
  1,
  "navatala_dataframe_writeback_max_f32",
  "metal",
  "navatala_dataframe_writeback_max_f32",
  "kernel:metal:navatala_dataframe_writeback_max_f32",
  "abi-r1:metal:navatala_dataframe_writeback_max_f32",
  "abi-r1:metal:navatala_dataframe_writeback_max_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_writeback_max_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_init_welford_state_f32[] = {
  { "count", 0, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mean", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_init_welford_state_f32 = {
  1,
  "navatala_dataframe_init_welford_state_f32",
  "metal",
  "navatala_dataframe_init_welford_state_f32",
  "kernel:metal:navatala_dataframe_init_welford_state_f32",
  "abi-r1:metal:navatala_dataframe_init_welford_state_f32",
  "abi-r1:metal:navatala_dataframe_init_welford_state_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_init_welford_state_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_welford_update_f32[] = {
  { "x", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mean", 2, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_welford_update_f32 = {
  1,
  "navatala_dataframe_welford_update_f32",
  "metal",
  "navatala_dataframe_welford_update_f32",
  "kernel:metal:navatala_dataframe_welford_update_f32",
  "abi-r1:metal:navatala_dataframe_welford_update_f32",
  "abi-r1:metal:navatala_dataframe_welford_update_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_welford_update_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_welford_merge_f32[] = {
  { "countA", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanA", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2A", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "countB", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanB", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2B", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mean", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2", 8, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_welford_merge_f32 = {
  1,
  "navatala_dataframe_welford_merge_f32",
  "metal",
  "navatala_dataframe_welford_merge_f32",
  "kernel:metal:navatala_dataframe_welford_merge_f32",
  "abi-r1:metal:navatala_dataframe_welford_merge_f32",
  "abi-r1:metal:navatala_dataframe_welford_merge_f32",
  9,
  kAbiArgs_metal_navatala_dataframe_welford_merge_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_variance_f32[] = {
  { "count", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "variance", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_variance_f32 = {
  1,
  "navatala_dataframe_extract_variance_f32",
  "metal",
  "navatala_dataframe_extract_variance_f32",
  "kernel:metal:navatala_dataframe_extract_variance_f32",
  "abi-r1:metal:navatala_dataframe_extract_variance_f32",
  "abi-r1:metal:navatala_dataframe_extract_variance_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_extract_variance_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_sample_variance_f32[] = {
  { "count", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "variance", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_sample_variance_f32 = {
  1,
  "navatala_dataframe_extract_sample_variance_f32",
  "metal",
  "navatala_dataframe_extract_sample_variance_f32",
  "kernel:metal:navatala_dataframe_extract_sample_variance_f32",
  "abi-r1:metal:navatala_dataframe_extract_sample_variance_f32",
  "abi-r1:metal:navatala_dataframe_extract_sample_variance_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_extract_sample_variance_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_stddev_f32[] = {
  { "count", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "stddev", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_stddev_f32 = {
  1,
  "navatala_dataframe_extract_stddev_f32",
  "metal",
  "navatala_dataframe_extract_stddev_f32",
  "kernel:metal:navatala_dataframe_extract_stddev_f32",
  "abi-r1:metal:navatala_dataframe_extract_stddev_f32",
  "abi-r1:metal:navatala_dataframe_extract_stddev_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_extract_stddev_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_init_covariance_state_f32[] = {
  { "count", 0, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanX", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanY", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "Cxy", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_init_covariance_state_f32 = {
  1,
  "navatala_dataframe_init_covariance_state_f32",
  "metal",
  "navatala_dataframe_init_covariance_state_f32",
  "kernel:metal:navatala_dataframe_init_covariance_state_f32",
  "abi-r1:metal:navatala_dataframe_init_covariance_state_f32",
  "abi-r1:metal:navatala_dataframe_init_covariance_state_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_init_covariance_state_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_covariance_update_f32[] = {
  { "x", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "y", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanX", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanY", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "Cxy", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_covariance_update_f32 = {
  1,
  "navatala_dataframe_covariance_update_f32",
  "metal",
  "navatala_dataframe_covariance_update_f32",
  "kernel:metal:navatala_dataframe_covariance_update_f32",
  "abi-r1:metal:navatala_dataframe_covariance_update_f32",
  "abi-r1:metal:navatala_dataframe_covariance_update_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_covariance_update_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_covariance_f32[] = {
  { "count", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "Cxy", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "covariance", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_covariance_f32 = {
  1,
  "navatala_dataframe_extract_covariance_f32",
  "metal",
  "navatala_dataframe_extract_covariance_f32",
  "kernel:metal:navatala_dataframe_extract_covariance_f32",
  "abi-r1:metal:navatala_dataframe_extract_covariance_f32",
  "abi-r1:metal:navatala_dataframe_extract_covariance_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_extract_covariance_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_correlation_f32[] = {
  { "Cxy", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2x", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2y", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "correlation", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_correlation_f32 = {
  1,
  "navatala_dataframe_extract_correlation_f32",
  "metal",
  "navatala_dataframe_extract_correlation_f32",
  "kernel:metal:navatala_dataframe_extract_correlation_f32",
  "abi-r1:metal:navatala_dataframe_extract_correlation_f32",
  "abi-r1:metal:navatala_dataframe_extract_correlation_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_extract_correlation_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_init_weighted_welford_f32[] = {
  { "weightSum", 0, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mean", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_init_weighted_welford_f32 = {
  1,
  "navatala_dataframe_init_weighted_welford_f32",
  "metal",
  "navatala_dataframe_init_weighted_welford_f32",
  "kernel:metal:navatala_dataframe_init_weighted_welford_f32",
  "abi-r1:metal:navatala_dataframe_init_weighted_welford_f32",
  "abi-r1:metal:navatala_dataframe_init_weighted_welford_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_init_weighted_welford_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_weighted_welford_update_f32[] = {
  { "x", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "w", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "weightSum", 2, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mean", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_weighted_welford_update_f32 = {
  1,
  "navatala_dataframe_weighted_welford_update_f32",
  "metal",
  "navatala_dataframe_weighted_welford_update_f32",
  "kernel:metal:navatala_dataframe_weighted_welford_update_f32",
  "abi-r1:metal:navatala_dataframe_weighted_welford_update_f32",
  "abi-r1:metal:navatala_dataframe_weighted_welford_update_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_weighted_welford_update_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_weighted_welford_merge_f32[] = {
  { "weightSumA", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanA", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2A", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "weightSumB", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanB", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2B", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "weightSum", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mean", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2", 8, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_weighted_welford_merge_f32 = {
  1,
  "navatala_dataframe_weighted_welford_merge_f32",
  "metal",
  "navatala_dataframe_weighted_welford_merge_f32",
  "kernel:metal:navatala_dataframe_weighted_welford_merge_f32",
  "abi-r1:metal:navatala_dataframe_weighted_welford_merge_f32",
  "abi-r1:metal:navatala_dataframe_weighted_welford_merge_f32",
  9,
  kAbiArgs_metal_navatala_dataframe_weighted_welford_merge_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_weighted_variance_f32[] = {
  { "weightSum", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "variance", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_weighted_variance_f32 = {
  1,
  "navatala_dataframe_extract_weighted_variance_f32",
  "metal",
  "navatala_dataframe_extract_weighted_variance_f32",
  "kernel:metal:navatala_dataframe_extract_weighted_variance_f32",
  "abi-r1:metal:navatala_dataframe_extract_weighted_variance_f32",
  "abi-r1:metal:navatala_dataframe_extract_weighted_variance_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_extract_weighted_variance_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_weighted_reliability_variance_f32[] = {
  { "weightSum", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "variance", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_weighted_reliability_variance_f32 = {
  1,
  "navatala_dataframe_extract_weighted_reliability_variance_f32",
  "metal",
  "navatala_dataframe_extract_weighted_reliability_variance_f32",
  "kernel:metal:navatala_dataframe_extract_weighted_reliability_variance_f32",
  "abi-r1:metal:navatala_dataframe_extract_weighted_reliability_variance_f32",
  "abi-r1:metal:navatala_dataframe_extract_weighted_reliability_variance_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_extract_weighted_reliability_variance_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_weighted_stddev_f32[] = {
  { "weightSum", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "stddev", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_weighted_stddev_f32 = {
  1,
  "navatala_dataframe_extract_weighted_stddev_f32",
  "metal",
  "navatala_dataframe_extract_weighted_stddev_f32",
  "kernel:metal:navatala_dataframe_extract_weighted_stddev_f32",
  "abi-r1:metal:navatala_dataframe_extract_weighted_stddev_f32",
  "abi-r1:metal:navatala_dataframe_extract_weighted_stddev_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_extract_weighted_stddev_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_init_weighted_covariance_f32[] = {
  { "weightSum", 0, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanX", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanY", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "Cxy", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_init_weighted_covariance_f32 = {
  1,
  "navatala_dataframe_init_weighted_covariance_f32",
  "metal",
  "navatala_dataframe_init_weighted_covariance_f32",
  "kernel:metal:navatala_dataframe_init_weighted_covariance_f32",
  "abi-r1:metal:navatala_dataframe_init_weighted_covariance_f32",
  "abi-r1:metal:navatala_dataframe_init_weighted_covariance_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_init_weighted_covariance_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_weighted_covariance_update_f32[] = {
  { "x", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "y", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "w", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "weightSum", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanX", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanY", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "Cxy", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_weighted_covariance_update_f32 = {
  1,
  "navatala_dataframe_weighted_covariance_update_f32",
  "metal",
  "navatala_dataframe_weighted_covariance_update_f32",
  "kernel:metal:navatala_dataframe_weighted_covariance_update_f32",
  "abi-r1:metal:navatala_dataframe_weighted_covariance_update_f32",
  "abi-r1:metal:navatala_dataframe_weighted_covariance_update_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_weighted_covariance_update_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_weighted_covariance_f32[] = {
  { "weightSum", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "Cxy", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "covariance", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_weighted_covariance_f32 = {
  1,
  "navatala_dataframe_extract_weighted_covariance_f32",
  "metal",
  "navatala_dataframe_extract_weighted_covariance_f32",
  "kernel:metal:navatala_dataframe_extract_weighted_covariance_f32",
  "abi-r1:metal:navatala_dataframe_extract_weighted_covariance_f32",
  "abi-r1:metal:navatala_dataframe_extract_weighted_covariance_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_extract_weighted_covariance_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_weighted_correlation_f32[] = {
  { "Cxy", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2x", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2y", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "correlation", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_weighted_correlation_f32 = {
  1,
  "navatala_dataframe_extract_weighted_correlation_f32",
  "metal",
  "navatala_dataframe_extract_weighted_correlation_f32",
  "kernel:metal:navatala_dataframe_extract_weighted_correlation_f32",
  "abi-r1:metal:navatala_dataframe_extract_weighted_correlation_f32",
  "abi-r1:metal:navatala_dataframe_extract_weighted_correlation_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_extract_weighted_correlation_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_init_weighted_mean_f32[] = {
  { "weightSum", 0, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mean", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_init_weighted_mean_f32 = {
  1,
  "navatala_dataframe_init_weighted_mean_f32",
  "metal",
  "navatala_dataframe_init_weighted_mean_f32",
  "kernel:metal:navatala_dataframe_init_weighted_mean_f32",
  "abi-r1:metal:navatala_dataframe_init_weighted_mean_f32",
  "abi-r1:metal:navatala_dataframe_init_weighted_mean_f32",
  2,
  kAbiArgs_metal_navatala_dataframe_init_weighted_mean_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_weighted_mean_update_f32[] = {
  { "x", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "w", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "weightSum", 2, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mean", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_weighted_mean_update_f32 = {
  1,
  "navatala_dataframe_weighted_mean_update_f32",
  "metal",
  "navatala_dataframe_weighted_mean_update_f32",
  "kernel:metal:navatala_dataframe_weighted_mean_update_f32",
  "abi-r1:metal:navatala_dataframe_weighted_mean_update_f32",
  "abi-r1:metal:navatala_dataframe_weighted_mean_update_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_weighted_mean_update_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_init_p2_state_f32[] = {
  { "p", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "q0", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "q1", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "q2", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "q3", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "q4", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "pos0", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "pos1", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "pos2", 8, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "pos3", 9, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "pos4", 10, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "targetP", 11, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 12, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_init_p2_state_f32 = {
  1,
  "navatala_dataframe_init_p2_state_f32",
  "metal",
  "navatala_dataframe_init_p2_state_f32",
  "kernel:metal:navatala_dataframe_init_p2_state_f32",
  "abi-r1:metal:navatala_dataframe_init_p2_state_f32",
  "abi-r1:metal:navatala_dataframe_init_p2_state_f32",
  13,
  kAbiArgs_metal_navatala_dataframe_init_p2_state_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_p2_update_f32[] = {
  { "x", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "q0", 1, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "q1", 2, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "q2", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "q3", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "q4", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "pos0", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "pos1", 7, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "pos2", 8, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "pos3", 9, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "pos4", 10, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "targetP", 11, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 12, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_p2_update_f32 = {
  1,
  "navatala_dataframe_p2_update_f32",
  "metal",
  "navatala_dataframe_p2_update_f32",
  "kernel:metal:navatala_dataframe_p2_update_f32",
  "abi-r1:metal:navatala_dataframe_p2_update_f32",
  "abi-r1:metal:navatala_dataframe_p2_update_f32",
  13,
  kAbiArgs_metal_navatala_dataframe_p2_update_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_p2_extract_f32[] = {
  { "q2", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "quantile", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_p2_extract_f32 = {
  1,
  "navatala_dataframe_p2_extract_f32",
  "metal",
  "navatala_dataframe_p2_extract_f32",
  "kernel:metal:navatala_dataframe_p2_extract_f32",
  "abi-r1:metal:navatala_dataframe_p2_extract_f32",
  "abi-r1:metal:navatala_dataframe_p2_extract_f32",
  2,
  kAbiArgs_metal_navatala_dataframe_p2_extract_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_init_reservoir256_f32[] = {
  { "initialSeed", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "reservoir", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "seed", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_init_reservoir256_f32 = {
  1,
  "navatala_dataframe_init_reservoir256_f32",
  "metal",
  "navatala_dataframe_init_reservoir256_f32",
  "kernel:metal:navatala_dataframe_init_reservoir256_f32",
  "abi-r1:metal:navatala_dataframe_init_reservoir256_f32",
  "abi-r1:metal:navatala_dataframe_init_reservoir256_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_init_reservoir256_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_reservoir_update256_f32[] = {
  { "x", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "reservoir", 1, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "seed", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_reservoir_update256_f32 = {
  1,
  "navatala_dataframe_reservoir_update256_f32",
  "metal",
  "navatala_dataframe_reservoir_update256_f32",
  "kernel:metal:navatala_dataframe_reservoir_update256_f32",
  "abi-r1:metal:navatala_dataframe_reservoir_update256_f32",
  "abi-r1:metal:navatala_dataframe_reservoir_update256_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_reservoir_update256_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_reservoir_min256_f32[] = {
  { "reservoir", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minVal", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_reservoir_min256_f32 = {
  1,
  "navatala_dataframe_reservoir_min256_f32",
  "metal",
  "navatala_dataframe_reservoir_min256_f32",
  "kernel:metal:navatala_dataframe_reservoir_min256_f32",
  "abi-r1:metal:navatala_dataframe_reservoir_min256_f32",
  "abi-r1:metal:navatala_dataframe_reservoir_min256_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_reservoir_min256_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_reservoir_max256_f32[] = {
  { "reservoir", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxVal", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_reservoir_max256_f32 = {
  1,
  "navatala_dataframe_reservoir_max256_f32",
  "metal",
  "navatala_dataframe_reservoir_max256_f32",
  "kernel:metal:navatala_dataframe_reservoir_max256_f32",
  "abi-r1:metal:navatala_dataframe_reservoir_max256_f32",
  "abi-r1:metal:navatala_dataframe_reservoir_max256_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_reservoir_max256_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_trimmed_mean_f32[] = {
  { "sortedData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "trimFraction", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "trimmedMean", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_trimmed_mean_f32 = {
  1,
  "navatala_dataframe_trimmed_mean_f32",
  "metal",
  "navatala_dataframe_trimmed_mean_f32",
  "kernel:metal:navatala_dataframe_trimmed_mean_f32",
  "abi-r1:metal:navatala_dataframe_trimmed_mean_f32",
  "abi-r1:metal:navatala_dataframe_trimmed_mean_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_trimmed_mean_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_iqm_f32[] = {
  { "sortedData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "iqm", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_iqm_f32 = {
  1,
  "navatala_dataframe_iqm_f32",
  "metal",
  "navatala_dataframe_iqm_f32",
  "kernel:metal:navatala_dataframe_iqm_f32",
  "abi-r1:metal:navatala_dataframe_iqm_f32",
  "abi-r1:metal:navatala_dataframe_iqm_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_iqm_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_winsorized_mean_f32[] = {
  { "sortedData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "trimFraction", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "winsorizedMean", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_winsorized_mean_f32 = {
  1,
  "navatala_dataframe_winsorized_mean_f32",
  "metal",
  "navatala_dataframe_winsorized_mean_f32",
  "kernel:metal:navatala_dataframe_winsorized_mean_f32",
  "abi-r1:metal:navatala_dataframe_winsorized_mean_f32",
  "abi-r1:metal:navatala_dataframe_winsorized_mean_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_winsorized_mean_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_mad_f32[] = {
  { "sortedData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "median", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mad", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_mad_f32 = {
  1,
  "navatala_dataframe_mad_f32",
  "metal",
  "navatala_dataframe_mad_f32",
  "kernel:metal:navatala_dataframe_mad_f32",
  "abi-r1:metal:navatala_dataframe_mad_f32",
  "abi-r1:metal:navatala_dataframe_mad_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_mad_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_normalized_mad_f32[] = {
  { "mad", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "normalizedMad", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_normalized_mad_f32 = {
  1,
  "navatala_dataframe_normalized_mad_f32",
  "metal",
  "navatala_dataframe_normalized_mad_f32",
  "kernel:metal:navatala_dataframe_normalized_mad_f32",
  "abi-r1:metal:navatala_dataframe_normalized_mad_f32",
  "abi-r1:metal:navatala_dataframe_normalized_mad_f32",
  2,
  kAbiArgs_metal_navatala_dataframe_normalized_mad_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_robust_z_score_f32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "median", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mad", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "zScores", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_robust_z_score_f32 = {
  1,
  "navatala_dataframe_robust_z_score_f32",
  "metal",
  "navatala_dataframe_robust_z_score_f32",
  "kernel:metal:navatala_dataframe_robust_z_score_f32",
  "abi-r1:metal:navatala_dataframe_robust_z_score_f32",
  "abi-r1:metal:navatala_dataframe_robust_z_score_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_robust_z_score_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_detect_outliers_f32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "median", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mad", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "threshold", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "isOutlier", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_detect_outliers_f32 = {
  1,
  "navatala_dataframe_detect_outliers_f32",
  "metal",
  "navatala_dataframe_detect_outliers_f32",
  "kernel:metal:navatala_dataframe_detect_outliers_f32",
  "abi-r1:metal:navatala_dataframe_detect_outliers_f32",
  "abi-r1:metal:navatala_dataframe_detect_outliers_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_detect_outliers_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_init_t_digest_f32[] = {
  { "compressionIn", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "centroidMeans", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "centroidWeights", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "centroidCount", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "totalWeight", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minVal", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxVal", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "compression", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_init_t_digest_f32 = {
  1,
  "navatala_dataframe_init_t_digest_f32",
  "metal",
  "navatala_dataframe_init_t_digest_f32",
  "kernel:metal:navatala_dataframe_init_t_digest_f32",
  "abi-r1:metal:navatala_dataframe_init_t_digest_f32",
  "abi-r1:metal:navatala_dataframe_init_t_digest_f32",
  8,
  kAbiArgs_metal_navatala_dataframe_init_t_digest_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_tdigest_add_f32[] = {
  { "value", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "weight", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "centroidMeans", 2, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "centroidWeights", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "centroidCount", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "totalWeight", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minVal", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxVal", 7, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_tdigest_add_f32 = {
  1,
  "navatala_dataframe_tdigest_add_f32",
  "metal",
  "navatala_dataframe_tdigest_add_f32",
  "kernel:metal:navatala_dataframe_tdigest_add_f32",
  "abi-r1:metal:navatala_dataframe_tdigest_add_f32",
  "abi-r1:metal:navatala_dataframe_tdigest_add_f32",
  8,
  kAbiArgs_metal_navatala_dataframe_tdigest_add_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_tdigest_merge_f32[] = {
  { "meansA", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "weightsA", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "countA", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "totalWeightA", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minA", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxA", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meansB", 6, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "weightsB", 7, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "countB", 8, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "totalWeightB", 9, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minB", 10, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxB", 11, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "compressionIn", 12, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meansOut", 13, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "weightsOut", 14, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "countOut", 15, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "totalWeightOut", 16, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minOut", 17, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxOut", 18, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "compressionOut", 19, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_tdigest_merge_f32 = {
  1,
  "navatala_dataframe_tdigest_merge_f32",
  "metal",
  "navatala_dataframe_tdigest_merge_f32",
  "kernel:metal:navatala_dataframe_tdigest_merge_f32",
  "abi-r1:metal:navatala_dataframe_tdigest_merge_f32",
  "abi-r1:metal:navatala_dataframe_tdigest_merge_f32",
  20,
  kAbiArgs_metal_navatala_dataframe_tdigest_merge_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_tdigest_quantile_f32[] = {
  { "p", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "centroidMeans", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "centroidWeights", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "centroidCount", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "totalWeight", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minVal", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxVal", 6, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "quantile", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_tdigest_quantile_f32 = {
  1,
  "navatala_dataframe_tdigest_quantile_f32",
  "metal",
  "navatala_dataframe_tdigest_quantile_f32",
  "kernel:metal:navatala_dataframe_tdigest_quantile_f32",
  "abi-r1:metal:navatala_dataframe_tdigest_quantile_f32",
  "abi-r1:metal:navatala_dataframe_tdigest_quantile_f32",
  8,
  kAbiArgs_metal_navatala_dataframe_tdigest_quantile_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_tdigest_cdf_f32[] = {
  { "x", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "centroidMeans", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "centroidWeights", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "centroidCount", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "totalWeight", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minVal", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxVal", 6, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "cdf", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_tdigest_cdf_f32 = {
  1,
  "navatala_dataframe_tdigest_cdf_f32",
  "metal",
  "navatala_dataframe_tdigest_cdf_f32",
  "kernel:metal:navatala_dataframe_tdigest_cdf_f32",
  "abi-r1:metal:navatala_dataframe_tdigest_cdf_f32",
  "abi-r1:metal:navatala_dataframe_tdigest_cdf_f32",
  8,
  kAbiArgs_metal_navatala_dataframe_tdigest_cdf_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_tdigest_mean_f32[] = {
  { "centroidMeans", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "centroidWeights", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1024, 1024, 256, nullptr, 0, 0 },
  { "centroidCount", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "totalWeight", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minVal", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxVal", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mean", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_tdigest_mean_f32 = {
  1,
  "navatala_dataframe_tdigest_mean_f32",
  "metal",
  "navatala_dataframe_tdigest_mean_f32",
  "kernel:metal:navatala_dataframe_tdigest_mean_f32",
  "abi-r1:metal:navatala_dataframe_tdigest_mean_f32",
  "abi-r1:metal:navatala_dataframe_tdigest_mean_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_tdigest_mean_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_tdigest_min_f32[] = {
  { "minVal", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "centroidCount", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minOut", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_tdigest_min_f32 = {
  1,
  "navatala_dataframe_tdigest_min_f32",
  "metal",
  "navatala_dataframe_tdigest_min_f32",
  "kernel:metal:navatala_dataframe_tdigest_min_f32",
  "abi-r1:metal:navatala_dataframe_tdigest_min_f32",
  "abi-r1:metal:navatala_dataframe_tdigest_min_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_tdigest_min_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_tdigest_max_f32[] = {
  { "maxVal", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "centroidCount", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxOut", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_tdigest_max_f32 = {
  1,
  "navatala_dataframe_tdigest_max_f32",
  "metal",
  "navatala_dataframe_tdigest_max_f32",
  "kernel:metal:navatala_dataframe_tdigest_max_f32",
  "abi-r1:metal:navatala_dataframe_tdigest_max_f32",
  "abi-r1:metal:navatala_dataframe_tdigest_max_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_tdigest_max_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_tdigest_reset_f32[] = {
  { "compression", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "centroidCount", 1, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "totalWeight", 2, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minVal", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxVal", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_tdigest_reset_f32 = {
  1,
  "navatala_dataframe_tdigest_reset_f32",
  "metal",
  "navatala_dataframe_tdigest_reset_f32",
  "kernel:metal:navatala_dataframe_tdigest_reset_f32",
  "abi-r1:metal:navatala_dataframe_tdigest_reset_f32",
  "abi-r1:metal:navatala_dataframe_tdigest_reset_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_tdigest_reset_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_inclusive_scan_sum_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_inclusive_scan_sum_f32 = {
  1,
  "navatala_dataframe_inclusive_scan_sum_f32",
  "metal",
  "navatala_dataframe_inclusive_scan_sum_f32",
  "kernel:metal:navatala_dataframe_inclusive_scan_sum_f32",
  "abi-r1:metal:navatala_dataframe_inclusive_scan_sum_f32",
  "abi-r1:metal:navatala_dataframe_inclusive_scan_sum_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_inclusive_scan_sum_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_inclusive_scan_sum_i32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_inclusive_scan_sum_i32 = {
  1,
  "navatala_dataframe_inclusive_scan_sum_i32",
  "metal",
  "navatala_dataframe_inclusive_scan_sum_i32",
  "kernel:metal:navatala_dataframe_inclusive_scan_sum_i32",
  "abi-r1:metal:navatala_dataframe_inclusive_scan_sum_i32",
  "abi-r1:metal:navatala_dataframe_inclusive_scan_sum_i32",
  5,
  kAbiArgs_metal_navatala_dataframe_inclusive_scan_sum_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_inclusive_scan_sum_i32_to_i64_skip_nulls[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_inclusive_scan_sum_i32_to_i64_skip_nulls = {
  1,
  "navatala_dataframe_inclusive_scan_sum_i32_to_i64_skip_nulls",
  "metal",
  "navatala_dataframe_inclusive_scan_sum_i32_to_i64_skip_nulls",
  "kernel:metal:navatala_dataframe_inclusive_scan_sum_i32_to_i64_skip_nulls",
  "abi-r1:metal:navatala_dataframe_inclusive_scan_sum_i32_to_i64_skip_nulls",
  "abi-r1:metal:navatala_dataframe_inclusive_scan_sum_i32_to_i64_skip_nulls",
  5,
  kAbiArgs_metal_navatala_dataframe_inclusive_scan_sum_i32_to_i64_skip_nulls
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_inclusive_scan_sum_i64[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_inclusive_scan_sum_i64 = {
  1,
  "navatala_dataframe_inclusive_scan_sum_i64",
  "metal",
  "navatala_dataframe_inclusive_scan_sum_i64",
  "kernel:metal:navatala_dataframe_inclusive_scan_sum_i64",
  "abi-r1:metal:navatala_dataframe_inclusive_scan_sum_i64",
  "abi-r1:metal:navatala_dataframe_inclusive_scan_sum_i64",
  5,
  kAbiArgs_metal_navatala_dataframe_inclusive_scan_sum_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_reduce_sum_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_reduce_sum_f32 = {
  1,
  "navatala_dataframe_reduce_sum_f32",
  "metal",
  "navatala_dataframe_reduce_sum_f32",
  "kernel:metal:navatala_dataframe_reduce_sum_f32",
  "abi-r1:metal:navatala_dataframe_reduce_sum_f32",
  "abi-r1:metal:navatala_dataframe_reduce_sum_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_reduce_sum_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_reduce_sum_i32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_reduce_sum_i32 = {
  1,
  "navatala_dataframe_reduce_sum_i32",
  "metal",
  "navatala_dataframe_reduce_sum_i32",
  "kernel:metal:navatala_dataframe_reduce_sum_i32",
  "abi-r1:metal:navatala_dataframe_reduce_sum_i32",
  "abi-r1:metal:navatala_dataframe_reduce_sum_i32",
  5,
  kAbiArgs_metal_navatala_dataframe_reduce_sum_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_reduce_sum_i32_to_i64[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 8, 8, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_reduce_sum_i32_to_i64 = {
  1,
  "navatala_dataframe_reduce_sum_i32_to_i64",
  "metal",
  "navatala_dataframe_reduce_sum_i32_to_i64",
  "kernel:metal:navatala_dataframe_reduce_sum_i32_to_i64",
  "abi-r1:metal:navatala_dataframe_reduce_sum_i32_to_i64",
  "abi-r1:metal:navatala_dataframe_reduce_sum_i32_to_i64",
  5,
  kAbiArgs_metal_navatala_dataframe_reduce_sum_i32_to_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_reduce_min_i32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_reduce_min_i32 = {
  1,
  "navatala_dataframe_reduce_min_i32",
  "metal",
  "navatala_dataframe_reduce_min_i32",
  "kernel:metal:navatala_dataframe_reduce_min_i32",
  "abi-r1:metal:navatala_dataframe_reduce_min_i32",
  "abi-r1:metal:navatala_dataframe_reduce_min_i32",
  5,
  kAbiArgs_metal_navatala_dataframe_reduce_min_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_reduce_max_i32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_reduce_max_i32 = {
  1,
  "navatala_dataframe_reduce_max_i32",
  "metal",
  "navatala_dataframe_reduce_max_i32",
  "kernel:metal:navatala_dataframe_reduce_max_i32",
  "abi-r1:metal:navatala_dataframe_reduce_max_i32",
  "abi-r1:metal:navatala_dataframe_reduce_max_i32",
  5,
  kAbiArgs_metal_navatala_dataframe_reduce_max_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_count_valid[] = {
  { "inputValid", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 2, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_count_valid = {
  1,
  "navatala_dataframe_count_valid",
  "metal",
  "navatala_dataframe_count_valid",
  "kernel:metal:navatala_dataframe_count_valid",
  "abi-r1:metal:navatala_dataframe_count_valid",
  "abi-r1:metal:navatala_dataframe_count_valid",
  3,
  kAbiArgs_metal_navatala_dataframe_count_valid
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gather_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "indices", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputSize", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputSize", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gather_f32 = {
  1,
  "navatala_dataframe_gather_f32",
  "metal",
  "navatala_dataframe_gather_f32",
  "kernel:metal:navatala_dataframe_gather_f32",
  "abi-r1:metal:navatala_dataframe_gather_f32",
  "abi-r1:metal:navatala_dataframe_gather_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_gather_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gather_i32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "indices", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputSize", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputSize", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gather_i32 = {
  1,
  "navatala_dataframe_gather_i32",
  "metal",
  "navatala_dataframe_gather_i32",
  "kernel:metal:navatala_dataframe_gather_i32",
  "abi-r1:metal:navatala_dataframe_gather_i32",
  "abi-r1:metal:navatala_dataframe_gather_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_gather_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gather_i64[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "indices", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputSize", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputSize", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gather_i64 = {
  1,
  "navatala_dataframe_gather_i64",
  "metal",
  "navatala_dataframe_gather_i64",
  "kernel:metal:navatala_dataframe_gather_i64",
  "abi-r1:metal:navatala_dataframe_gather_i64",
  "abi-r1:metal:navatala_dataframe_gather_i64",
  7,
  kAbiArgs_metal_navatala_dataframe_gather_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_scatter_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "indices", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputSize", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputSize", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_scatter_f32 = {
  1,
  "navatala_dataframe_scatter_f32",
  "metal",
  "navatala_dataframe_scatter_f32",
  "kernel:metal:navatala_dataframe_scatter_f32",
  "abi-r1:metal:navatala_dataframe_scatter_f32",
  "abi-r1:metal:navatala_dataframe_scatter_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_scatter_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_scatter_i32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "indices", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputSize", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputSize", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_scatter_i32 = {
  1,
  "navatala_dataframe_scatter_i32",
  "metal",
  "navatala_dataframe_scatter_i32",
  "kernel:metal:navatala_dataframe_scatter_i32",
  "abi-r1:metal:navatala_dataframe_scatter_i32",
  "abi-r1:metal:navatala_dataframe_scatter_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_scatter_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_compact_by_mask_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "mask", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "prefixSum", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_compact_by_mask_f32 = {
  1,
  "navatala_dataframe_compact_by_mask_f32",
  "metal",
  "navatala_dataframe_compact_by_mask_f32",
  "kernel:metal:navatala_dataframe_compact_by_mask_f32",
  "abi-r1:metal:navatala_dataframe_compact_by_mask_f32",
  "abi-r1:metal:navatala_dataframe_compact_by_mask_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_compact_by_mask_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_compact_by_mask_i32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "mask", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "prefixSum", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_compact_by_mask_i32 = {
  1,
  "navatala_dataframe_compact_by_mask_i32",
  "metal",
  "navatala_dataframe_compact_by_mask_i32",
  "kernel:metal:navatala_dataframe_compact_by_mask_i32",
  "abi-r1:metal:navatala_dataframe_compact_by_mask_i32",
  "abi-r1:metal:navatala_dataframe_compact_by_mask_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_compact_by_mask_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_compact_valid_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "prefixSum", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_compact_valid_f32 = {
  1,
  "navatala_dataframe_compact_valid_f32",
  "metal",
  "navatala_dataframe_compact_valid_f32",
  "kernel:metal:navatala_dataframe_compact_valid_f32",
  "abi-r1:metal:navatala_dataframe_compact_valid_f32",
  "abi-r1:metal:navatala_dataframe_compact_valid_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_compact_valid_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_fill_constant_f32[] = {
  { "value", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_fill_constant_f32 = {
  1,
  "navatala_dataframe_fill_constant_f32",
  "metal",
  "navatala_dataframe_fill_constant_f32",
  "kernel:metal:navatala_dataframe_fill_constant_f32",
  "abi-r1:metal:navatala_dataframe_fill_constant_f32",
  "abi-r1:metal:navatala_dataframe_fill_constant_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_fill_constant_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_fill_constant_i32[] = {
  { "value", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_fill_constant_i32 = {
  1,
  "navatala_dataframe_fill_constant_i32",
  "metal",
  "navatala_dataframe_fill_constant_i32",
  "kernel:metal:navatala_dataframe_fill_constant_i32",
  "abi-r1:metal:navatala_dataframe_fill_constant_i32",
  "abi-r1:metal:navatala_dataframe_fill_constant_i32",
  3,
  kAbiArgs_metal_navatala_dataframe_fill_constant_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_fill_constant_i64[] = {
  { "value", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 8, 8, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_fill_constant_i64 = {
  1,
  "navatala_dataframe_fill_constant_i64",
  "metal",
  "navatala_dataframe_fill_constant_i64",
  "kernel:metal:navatala_dataframe_fill_constant_i64",
  "abi-r1:metal:navatala_dataframe_fill_constant_i64",
  "abi-r1:metal:navatala_dataframe_fill_constant_i64",
  3,
  kAbiArgs_metal_navatala_dataframe_fill_constant_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_fill_range_i32[] = {
  { "start", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_fill_range_i32 = {
  1,
  "navatala_dataframe_fill_range_i32",
  "metal",
  "navatala_dataframe_fill_range_i32",
  "kernel:metal:navatala_dataframe_fill_range_i32",
  "abi-r1:metal:navatala_dataframe_fill_range_i32",
  "abi-r1:metal:navatala_dataframe_fill_range_i32",
  3,
  kAbiArgs_metal_navatala_dataframe_fill_range_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_fill_range_i64[] = {
  { "start", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 8, 8, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_fill_range_i64 = {
  1,
  "navatala_dataframe_fill_range_i64",
  "metal",
  "navatala_dataframe_fill_range_i64",
  "kernel:metal:navatala_dataframe_fill_range_i64",
  "abi-r1:metal:navatala_dataframe_fill_range_i64",
  "abi-r1:metal:navatala_dataframe_fill_range_i64",
  3,
  kAbiArgs_metal_navatala_dataframe_fill_range_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_fill_validity_all_valid[] = {
  { "numElements", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_fill_validity_all_valid = {
  1,
  "navatala_dataframe_fill_validity_all_valid",
  "metal",
  "navatala_dataframe_fill_validity_all_valid",
  "kernel:metal:navatala_dataframe_fill_validity_all_valid",
  "abi-r1:metal:navatala_dataframe_fill_validity_all_valid",
  "abi-r1:metal:navatala_dataframe_fill_validity_all_valid",
  2,
  kAbiArgs_metal_navatala_dataframe_fill_validity_all_valid
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_fill_validity_all_null[] = {
  { "numElements", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_fill_validity_all_null = {
  1,
  "navatala_dataframe_fill_validity_all_null",
  "metal",
  "navatala_dataframe_fill_validity_all_null",
  "kernel:metal:navatala_dataframe_fill_validity_all_null",
  "abi-r1:metal:navatala_dataframe_fill_validity_all_null",
  "abi-r1:metal:navatala_dataframe_fill_validity_all_null",
  2,
  kAbiArgs_metal_navatala_dataframe_fill_validity_all_null
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_fill_null_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "fillValue", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_fill_null_f32 = {
  1,
  "navatala_dataframe_fill_null_f32",
  "metal",
  "navatala_dataframe_fill_null_f32",
  "kernel:metal:navatala_dataframe_fill_null_f32",
  "abi-r1:metal:navatala_dataframe_fill_null_f32",
  "abi-r1:metal:navatala_dataframe_fill_null_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_fill_null_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_fill_null_i32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "fillValue", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_fill_null_i32 = {
  1,
  "navatala_dataframe_fill_null_i32",
  "metal",
  "navatala_dataframe_fill_null_i32",
  "kernel:metal:navatala_dataframe_fill_null_i32",
  "abi-r1:metal:navatala_dataframe_fill_null_i32",
  "abi-r1:metal:navatala_dataframe_fill_null_i32",
  6,
  kAbiArgs_metal_navatala_dataframe_fill_null_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_is_null[] = {
  { "inputValid", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_is_null = {
  1,
  "navatala_dataframe_is_null",
  "metal",
  "navatala_dataframe_is_null",
  "kernel:metal:navatala_dataframe_is_null",
  "abi-r1:metal:navatala_dataframe_is_null",
  "abi-r1:metal:navatala_dataframe_is_null",
  3,
  kAbiArgs_metal_navatala_dataframe_is_null
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_is_valid[] = {
  { "inputValid", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_is_valid = {
  1,
  "navatala_dataframe_is_valid",
  "metal",
  "navatala_dataframe_is_valid",
  "kernel:metal:navatala_dataframe_is_valid",
  "abi-r1:metal:navatala_dataframe_is_valid",
  "abi-r1:metal:navatala_dataframe_is_valid",
  3,
  kAbiArgs_metal_navatala_dataframe_is_valid
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_copy_if_valid_f32[] = {
  { "src", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "srcValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "dstValid", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "dst", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_copy_if_valid_f32 = {
  1,
  "navatala_dataframe_copy_if_valid_f32",
  "metal",
  "navatala_dataframe_copy_if_valid_f32",
  "kernel:metal:navatala_dataframe_copy_if_valid_f32",
  "abi-r1:metal:navatala_dataframe_copy_if_valid_f32",
  "abi-r1:metal:navatala_dataframe_copy_if_valid_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_copy_if_valid_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_coalesce_f32[] = {
  { "col1", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "col1Valid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "col2", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "col2Valid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_coalesce_f32 = {
  1,
  "navatala_dataframe_coalesce_f32",
  "metal",
  "navatala_dataframe_coalesce_f32",
  "kernel:metal:navatala_dataframe_coalesce_f32",
  "abi-r1:metal:navatala_dataframe_coalesce_f32",
  "abi-r1:metal:navatala_dataframe_coalesce_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_coalesce_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_arg_max_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "tiePolicy", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxValue", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxIndex", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_arg_max_f32 = {
  1,
  "navatala_dataframe_arg_max_f32",
  "metal",
  "navatala_dataframe_arg_max_f32",
  "kernel:metal:navatala_dataframe_arg_max_f32",
  "abi-r1:metal:navatala_dataframe_arg_max_f32",
  "abi-r1:metal:navatala_dataframe_arg_max_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_arg_max_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_arg_min_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "tiePolicy", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minValue", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minIndex", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_arg_min_f32 = {
  1,
  "navatala_dataframe_arg_min_f32",
  "metal",
  "navatala_dataframe_arg_min_f32",
  "kernel:metal:navatala_dataframe_arg_min_f32",
  "abi-r1:metal:navatala_dataframe_arg_min_f32",
  "abi-r1:metal:navatala_dataframe_arg_min_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_arg_min_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_scan_sum_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "scanMode", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_scan_sum_f32 = {
  1,
  "navatala_dataframe_scan_sum_f32",
  "metal",
  "navatala_dataframe_scan_sum_f32",
  "kernel:metal:navatala_dataframe_scan_sum_f32",
  "abi-r1:metal:navatala_dataframe_scan_sum_f32",
  "abi-r1:metal:navatala_dataframe_scan_sum_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_scan_sum_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_scan_sum_u32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "scanMode", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_scan_sum_u32 = {
  1,
  "navatala_dataframe_scan_sum_u32",
  "metal",
  "navatala_dataframe_scan_sum_u32",
  "kernel:metal:navatala_dataframe_scan_sum_u32",
  "abi-r1:metal:navatala_dataframe_scan_sum_u32",
  "abi-r1:metal:navatala_dataframe_scan_sum_u32",
  4,
  kAbiArgs_metal_navatala_dataframe_scan_sum_u32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_scan_exclusive_write_total_u32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 2, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_scan_exclusive_write_total_u32 = {
  1,
  "navatala_dataframe_scan_exclusive_write_total_u32",
  "metal",
  "navatala_dataframe_scan_exclusive_write_total_u32",
  "kernel:metal:navatala_dataframe_scan_exclusive_write_total_u32",
  "abi-r1:metal:navatala_dataframe_scan_exclusive_write_total_u32",
  "abi-r1:metal:navatala_dataframe_scan_exclusive_write_total_u32",
  3,
  kAbiArgs_metal_navatala_dataframe_scan_exclusive_write_total_u32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_scan_max_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "scanMode", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_scan_max_f32 = {
  1,
  "navatala_dataframe_scan_max_f32",
  "metal",
  "navatala_dataframe_scan_max_f32",
  "kernel:metal:navatala_dataframe_scan_max_f32",
  "abi-r1:metal:navatala_dataframe_scan_max_f32",
  "abi-r1:metal:navatala_dataframe_scan_max_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_scan_max_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_scan_min_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "scanMode", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_scan_min_f32 = {
  1,
  "navatala_dataframe_scan_min_f32",
  "metal",
  "navatala_dataframe_scan_min_f32",
  "kernel:metal:navatala_dataframe_scan_min_f32",
  "abi-r1:metal:navatala_dataframe_scan_min_f32",
  "abi-r1:metal:navatala_dataframe_scan_min_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_scan_min_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_histogram_fast_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "numBins", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minVal", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxVal", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "bins", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_histogram_fast_f32 = {
  1,
  "navatala_dataframe_histogram_fast_f32",
  "metal",
  "navatala_dataframe_histogram_fast_f32",
  "kernel:metal:navatala_dataframe_histogram_fast_f32",
  "abi-r1:metal:navatala_dataframe_histogram_fast_f32",
  "abi-r1:metal:navatala_dataframe_histogram_fast_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_histogram_fast_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_zero_bins[] = {
  { "numBins", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "bins", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_zero_bins = {
  1,
  "navatala_dataframe_zero_bins",
  "metal",
  "navatala_dataframe_zero_bins",
  "kernel:metal:navatala_dataframe_zero_bins",
  "abi-r1:metal:navatala_dataframe_zero_bins",
  "abi-r1:metal:navatala_dataframe_zero_bins",
  2,
  kAbiArgs_metal_navatala_dataframe_zero_bins
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_compute_bin_indices_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "numBins", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minVal", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxVal", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "binIndices", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_compute_bin_indices_f32 = {
  1,
  "navatala_dataframe_compute_bin_indices_f32",
  "metal",
  "navatala_dataframe_compute_bin_indices_f32",
  "kernel:metal:navatala_dataframe_compute_bin_indices_f32",
  "abi-r1:metal:navatala_dataframe_compute_bin_indices_f32",
  "abi-r1:metal:navatala_dataframe_compute_bin_indices_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_compute_bin_indices_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_sum_reduce_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_sum_reduce_f32 = {
  1,
  "navatala_dataframe_sum_reduce_f32",
  "metal",
  "navatala_dataframe_sum_reduce_f32",
  "kernel:metal:navatala_dataframe_sum_reduce_f32",
  "abi-r1:metal:navatala_dataframe_sum_reduce_f32",
  "abi-r1:metal:navatala_dataframe_sum_reduce_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_sum_reduce_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_mean_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mean", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_mean_f32 = {
  1,
  "navatala_dataframe_mean_f32",
  "metal",
  "navatala_dataframe_mean_f32",
  "kernel:metal:navatala_dataframe_mean_f32",
  "abi-r1:metal:navatala_dataframe_mean_f32",
  "abi-r1:metal:navatala_dataframe_mean_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_mean_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_variance_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mean", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "variance", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_variance_f32 = {
  1,
  "navatala_dataframe_variance_f32",
  "metal",
  "navatala_dataframe_variance_f32",
  "kernel:metal:navatala_dataframe_variance_f32",
  "abi-r1:metal:navatala_dataframe_variance_f32",
  "abi-r1:metal:navatala_dataframe_variance_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_variance_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_stddev_f32[] = {
  { "variance", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "stddev", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_stddev_f32 = {
  1,
  "navatala_dataframe_stddev_f32",
  "metal",
  "navatala_dataframe_stddev_f32",
  "kernel:metal:navatala_dataframe_stddev_f32",
  "abi-r1:metal:navatala_dataframe_stddev_f32",
  "abi-r1:metal:navatala_dataframe_stddev_f32",
  2,
  kAbiArgs_metal_navatala_dataframe_stddev_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_normalize_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mean", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "stddev", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_normalize_f32 = {
  1,
  "navatala_dataframe_normalize_f32",
  "metal",
  "navatala_dataframe_normalize_f32",
  "kernel:metal:navatala_dataframe_normalize_f32",
  "abi-r1:metal:navatala_dataframe_normalize_f32",
  "abi-r1:metal:navatala_dataframe_normalize_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_normalize_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_init_rng_state[] = {
  { "seed", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 8, 8, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "state", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_init_rng_state = {
  1,
  "navatala_dataframe_init_rng_state",
  "metal",
  "navatala_dataframe_init_rng_state",
  "kernel:metal:navatala_dataframe_init_rng_state",
  "abi-r1:metal:navatala_dataframe_init_rng_state",
  "abi-r1:metal:navatala_dataframe_init_rng_state",
  3,
  kAbiArgs_metal_navatala_dataframe_init_rng_state
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_uniform_lcg_f32[] = {
  { "state", 0, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_uniform_lcg_f32 = {
  1,
  "navatala_dataframe_uniform_lcg_f32",
  "metal",
  "navatala_dataframe_uniform_lcg_f32",
  "kernel:metal:navatala_dataframe_uniform_lcg_f32",
  "abi-r1:metal:navatala_dataframe_uniform_lcg_f32",
  "abi-r1:metal:navatala_dataframe_uniform_lcg_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_uniform_lcg_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_normal_box_muller_f32[] = {
  { "state", 0, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_normal_box_muller_f32 = {
  1,
  "navatala_dataframe_normal_box_muller_f32",
  "metal",
  "navatala_dataframe_normal_box_muller_f32",
  "kernel:metal:navatala_dataframe_normal_box_muller_f32",
  "abi-r1:metal:navatala_dataframe_normal_box_muller_f32",
  "abi-r1:metal:navatala_dataframe_normal_box_muller_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_normal_box_muller_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_init_indices[] = {
  { "count", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "indices", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_init_indices = {
  1,
  "navatala_dataframe_init_indices",
  "metal",
  "navatala_dataframe_init_indices",
  "kernel:metal:navatala_dataframe_init_indices",
  "abi-r1:metal:navatala_dataframe_init_indices",
  "abi-r1:metal:navatala_dataframe_init_indices",
  2,
  kAbiArgs_metal_navatala_dataframe_init_indices
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_covariance_f32[] = {
  { "inputX", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputY", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanX", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanY", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "covariance", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_covariance_f32 = {
  1,
  "navatala_dataframe_covariance_f32",
  "metal",
  "navatala_dataframe_covariance_f32",
  "kernel:metal:navatala_dataframe_covariance_f32",
  "abi-r1:metal:navatala_dataframe_covariance_f32",
  "abi-r1:metal:navatala_dataframe_covariance_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_covariance_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_center_data_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mean", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_center_data_f32 = {
  1,
  "navatala_dataframe_center_data_f32",
  "metal",
  "navatala_dataframe_center_data_f32",
  "kernel:metal:navatala_dataframe_center_data_f32",
  "abi-r1:metal:navatala_dataframe_center_data_f32",
  "abi-r1:metal:navatala_dataframe_center_data_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_center_data_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_center_matrix_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "nSamples", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "nFeatures", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "means", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_center_matrix_f32 = {
  1,
  "navatala_dataframe_center_matrix_f32",
  "metal",
  "navatala_dataframe_center_matrix_f32",
  "kernel:metal:navatala_dataframe_center_matrix_f32",
  "abi-r1:metal:navatala_dataframe_center_matrix_f32",
  "abi-r1:metal:navatala_dataframe_center_matrix_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_center_matrix_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_column_means_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "nSamples", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "nFeatures", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "means", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_column_means_f32 = {
  1,
  "navatala_dataframe_column_means_f32",
  "metal",
  "navatala_dataframe_column_means_f32",
  "kernel:metal:navatala_dataframe_column_means_f32",
  "abi-r1:metal:navatala_dataframe_column_means_f32",
  "abi-r1:metal:navatala_dataframe_column_means_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_column_means_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_correlation_f32[] = {
  { "covariance", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "stddevX", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "stddevY", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "correlation", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_correlation_f32 = {
  1,
  "navatala_dataframe_correlation_f32",
  "metal",
  "navatala_dataframe_correlation_f32",
  "kernel:metal:navatala_dataframe_correlation_f32",
  "abi-r1:metal:navatala_dataframe_correlation_f32",
  "abi-r1:metal:navatala_dataframe_correlation_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_correlation_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_cov_to_correlation_matrix_f32[] = {
  { "covMatrix", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "nFeatures", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "corMatrix", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_cov_to_correlation_matrix_f32 = {
  1,
  "navatala_dataframe_cov_to_correlation_matrix_f32",
  "metal",
  "navatala_dataframe_cov_to_correlation_matrix_f32",
  "kernel:metal:navatala_dataframe_cov_to_correlation_matrix_f32",
  "abi-r1:metal:navatala_dataframe_cov_to_correlation_matrix_f32",
  "abi-r1:metal:navatala_dataframe_cov_to_correlation_matrix_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_cov_to_correlation_matrix_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_quantile_f32[] = {
  { "sortedInput", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "quantileP", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_quantile_f32 = {
  1,
  "navatala_dataframe_quantile_f32",
  "metal",
  "navatala_dataframe_quantile_f32",
  "kernel:metal:navatala_dataframe_quantile_f32",
  "abi-r1:metal:navatala_dataframe_quantile_f32",
  "abi-r1:metal:navatala_dataframe_quantile_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_quantile_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_quantile_batch_f32[] = {
  { "sortedInput", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "quantilePs", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "numQuantiles", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "results", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_quantile_batch_f32 = {
  1,
  "navatala_dataframe_quantile_batch_f32",
  "metal",
  "navatala_dataframe_quantile_batch_f32",
  "kernel:metal:navatala_dataframe_quantile_batch_f32",
  "abi-r1:metal:navatala_dataframe_quantile_batch_f32",
  "abi-r1:metal:navatala_dataframe_quantile_batch_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_quantile_batch_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_median_f32[] = {
  { "sortedInput", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "median", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_median_f32 = {
  1,
  "navatala_dataframe_median_f32",
  "metal",
  "navatala_dataframe_median_f32",
  "kernel:metal:navatala_dataframe_median_f32",
  "abi-r1:metal:navatala_dataframe_median_f32",
  "abi-r1:metal:navatala_dataframe_median_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_median_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_iqr_f32[] = {
  { "sortedInput", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "iqr", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_iqr_f32 = {
  1,
  "navatala_dataframe_iqr_f32",
  "metal",
  "navatala_dataframe_iqr_f32",
  "kernel:metal:navatala_dataframe_iqr_f32",
  "abi-r1:metal:navatala_dataframe_iqr_f32",
  "abi-r1:metal:navatala_dataframe_iqr_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_iqr_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_init_e_w_m_c_f32[] = {
  { "alphaIn", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "alpha", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "nEff", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanX", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanY", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "Cxy", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "varX", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "varY", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_init_e_w_m_c_f32 = {
  1,
  "navatala_dataframe_init_e_w_m_c_f32",
  "metal",
  "navatala_dataframe_init_e_w_m_c_f32",
  "kernel:metal:navatala_dataframe_init_e_w_m_c_f32",
  "abi-r1:metal:navatala_dataframe_init_e_w_m_c_f32",
  "abi-r1:metal:navatala_dataframe_init_e_w_m_c_f32",
  8,
  kAbiArgs_metal_navatala_dataframe_init_e_w_m_c_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_init_e_w_m_c_from_halflife_f32[] = {
  { "halflife", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "alpha", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "nEff", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanX", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanY", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "Cxy", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "varX", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "varY", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_init_e_w_m_c_from_halflife_f32 = {
  1,
  "navatala_dataframe_init_e_w_m_c_from_halflife_f32",
  "metal",
  "navatala_dataframe_init_e_w_m_c_from_halflife_f32",
  "kernel:metal:navatala_dataframe_init_e_w_m_c_from_halflife_f32",
  "abi-r1:metal:navatala_dataframe_init_e_w_m_c_from_halflife_f32",
  "abi-r1:metal:navatala_dataframe_init_e_w_m_c_from_halflife_f32",
  8,
  kAbiArgs_metal_navatala_dataframe_init_e_w_m_c_from_halflife_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_ewmc_update_f32[] = {
  { "x", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "y", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "alpha", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "nEff", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanX", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanY", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "Cxy", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "varX", 7, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "varY", 8, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_ewmc_update_f32 = {
  1,
  "navatala_dataframe_ewmc_update_f32",
  "metal",
  "navatala_dataframe_ewmc_update_f32",
  "kernel:metal:navatala_dataframe_ewmc_update_f32",
  "abi-r1:metal:navatala_dataframe_ewmc_update_f32",
  "abi-r1:metal:navatala_dataframe_ewmc_update_f32",
  9,
  kAbiArgs_metal_navatala_dataframe_ewmc_update_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_ewmc_extract_covariance_f32[] = {
  { "Cxy", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "nEff", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_ewmc_extract_covariance_f32 = {
  1,
  "navatala_dataframe_ewmc_extract_covariance_f32",
  "metal",
  "navatala_dataframe_ewmc_extract_covariance_f32",
  "kernel:metal:navatala_dataframe_ewmc_extract_covariance_f32",
  "abi-r1:metal:navatala_dataframe_ewmc_extract_covariance_f32",
  "abi-r1:metal:navatala_dataframe_ewmc_extract_covariance_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_ewmc_extract_covariance_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_ewmc_extract_correlation_f32[] = {
  { "Cxy", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "varX", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "varY", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "nEff", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_ewmc_extract_correlation_f32 = {
  1,
  "navatala_dataframe_ewmc_extract_correlation_f32",
  "metal",
  "navatala_dataframe_ewmc_extract_correlation_f32",
  "kernel:metal:navatala_dataframe_ewmc_extract_correlation_f32",
  "abi-r1:metal:navatala_dataframe_ewmc_extract_correlation_f32",
  "abi-r1:metal:navatala_dataframe_ewmc_extract_correlation_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_ewmc_extract_correlation_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_init_m_c_d_f32[] = {
  { "thresholdIn", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "robustMeanX", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "robustMeanY", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "robustCov", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "determinant", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "threshold", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_init_m_c_d_f32 = {
  1,
  "navatala_dataframe_init_m_c_d_f32",
  "metal",
  "navatala_dataframe_init_m_c_d_f32",
  "kernel:metal:navatala_dataframe_init_m_c_d_f32",
  "abi-r1:metal:navatala_dataframe_init_m_c_d_f32",
  "abi-r1:metal:navatala_dataframe_init_m_c_d_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_init_m_c_d_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_mcd_compute_center_f32[] = {
  { "dataX", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "dataY", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "mask", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "robustMeanX", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "robustMeanY", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_mcd_compute_center_f32 = {
  1,
  "navatala_dataframe_mcd_compute_center_f32",
  "metal",
  "navatala_dataframe_mcd_compute_center_f32",
  "kernel:metal:navatala_dataframe_mcd_compute_center_f32",
  "abi-r1:metal:navatala_dataframe_mcd_compute_center_f32",
  "abi-r1:metal:navatala_dataframe_mcd_compute_center_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_mcd_compute_center_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_mcd_compute_covariance_f32[] = {
  { "dataX", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "dataY", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "mask", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "robustMeanX", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "robustMeanY", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "n", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "robustCov", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "determinant", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_mcd_compute_covariance_f32 = {
  1,
  "navatala_dataframe_mcd_compute_covariance_f32",
  "metal",
  "navatala_dataframe_mcd_compute_covariance_f32",
  "kernel:metal:navatala_dataframe_mcd_compute_covariance_f32",
  "abi-r1:metal:navatala_dataframe_mcd_compute_covariance_f32",
  "abi-r1:metal:navatala_dataframe_mcd_compute_covariance_f32",
  8,
  kAbiArgs_metal_navatala_dataframe_mcd_compute_covariance_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_mcd_mahalanobis_distance_f32[] = {
  { "dataX", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "dataY", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "robustMeanX", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "robustMeanY", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "robustCov", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "varX", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "varY", 6, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "n", 7, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "distances", 8, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_mcd_mahalanobis_distance_f32 = {
  1,
  "navatala_dataframe_mcd_mahalanobis_distance_f32",
  "metal",
  "navatala_dataframe_mcd_mahalanobis_distance_f32",
  "kernel:metal:navatala_dataframe_mcd_mahalanobis_distance_f32",
  "abi-r1:metal:navatala_dataframe_mcd_mahalanobis_distance_f32",
  "abi-r1:metal:navatala_dataframe_mcd_mahalanobis_distance_f32",
  9,
  kAbiArgs_metal_navatala_dataframe_mcd_mahalanobis_distance_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_mcd_detect_outliers_f32[] = {
  { "distances", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "threshold", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "n", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outlierFlags", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_mcd_detect_outliers_f32 = {
  1,
  "navatala_dataframe_mcd_detect_outliers_f32",
  "metal",
  "navatala_dataframe_mcd_detect_outliers_f32",
  "kernel:metal:navatala_dataframe_mcd_detect_outliers_f32",
  "abi-r1:metal:navatala_dataframe_mcd_detect_outliers_f32",
  "abi-r1:metal:navatala_dataframe_mcd_detect_outliers_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_mcd_detect_outliers_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_mcd_extract_mean_f32[] = {
  { "robustMeanX", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "robustMeanY", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanX", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "meanY", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_mcd_extract_mean_f32 = {
  1,
  "navatala_dataframe_mcd_extract_mean_f32",
  "metal",
  "navatala_dataframe_mcd_extract_mean_f32",
  "kernel:metal:navatala_dataframe_mcd_extract_mean_f32",
  "abi-r1:metal:navatala_dataframe_mcd_extract_mean_f32",
  "abi-r1:metal:navatala_dataframe_mcd_extract_mean_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_mcd_extract_mean_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_init_moments_state_f32[] = {
  { "n", 0, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M1", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M3", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M4", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_init_moments_state_f32 = {
  1,
  "navatala_dataframe_init_moments_state_f32",
  "metal",
  "navatala_dataframe_init_moments_state_f32",
  "kernel:metal:navatala_dataframe_init_moments_state_f32",
  "abi-r1:metal:navatala_dataframe_init_moments_state_f32",
  "abi-r1:metal:navatala_dataframe_init_moments_state_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_init_moments_state_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_moments_update_f32[] = {
  { "x", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "n", 1, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M1", 2, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M3", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M4", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_moments_update_f32 = {
  1,
  "navatala_dataframe_moments_update_f32",
  "metal",
  "navatala_dataframe_moments_update_f32",
  "kernel:metal:navatala_dataframe_moments_update_f32",
  "abi-r1:metal:navatala_dataframe_moments_update_f32",
  "abi-r1:metal:navatala_dataframe_moments_update_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_moments_update_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_moments_merge_f32[] = {
  { "nA", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M1A", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2A", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M3A", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M4A", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "nB", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M1B", 6, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2B", 7, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M3B", 8, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M4B", 9, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "nOut", 10, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M1Out", 11, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2Out", 12, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M3Out", 13, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M4Out", 14, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_moments_merge_f32 = {
  1,
  "navatala_dataframe_moments_merge_f32",
  "metal",
  "navatala_dataframe_moments_merge_f32",
  "kernel:metal:navatala_dataframe_moments_merge_f32",
  "abi-r1:metal:navatala_dataframe_moments_merge_f32",
  "abi-r1:metal:navatala_dataframe_moments_merge_f32",
  15,
  kAbiArgs_metal_navatala_dataframe_moments_merge_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_skewness_f32[] = {
  { "n", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M3", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "skewness", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_skewness_f32 = {
  1,
  "navatala_dataframe_extract_skewness_f32",
  "metal",
  "navatala_dataframe_extract_skewness_f32",
  "kernel:metal:navatala_dataframe_extract_skewness_f32",
  "abi-r1:metal:navatala_dataframe_extract_skewness_f32",
  "abi-r1:metal:navatala_dataframe_extract_skewness_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_extract_skewness_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_kurtosis_f32[] = {
  { "n", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M4", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "kurtosis", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_kurtosis_f32 = {
  1,
  "navatala_dataframe_extract_kurtosis_f32",
  "metal",
  "navatala_dataframe_extract_kurtosis_f32",
  "kernel:metal:navatala_dataframe_extract_kurtosis_f32",
  "abi-r1:metal:navatala_dataframe_extract_kurtosis_f32",
  "abi-r1:metal:navatala_dataframe_extract_kurtosis_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_extract_kurtosis_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_excess_kurtosis_f32[] = {
  { "n", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M2", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "M4", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "excessKurtosis", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_excess_kurtosis_f32 = {
  1,
  "navatala_dataframe_extract_excess_kurtosis_f32",
  "metal",
  "navatala_dataframe_extract_excess_kurtosis_f32",
  "kernel:metal:navatala_dataframe_extract_excess_kurtosis_f32",
  "abi-r1:metal:navatala_dataframe_extract_excess_kurtosis_f32",
  "abi-r1:metal:navatala_dataframe_extract_excess_kurtosis_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_extract_excess_kurtosis_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_contingency_matrix_f32[] = {
  { "trueLabels", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "predLabels", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "numTrueClasses", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "numPredClusters", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "contingency", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_contingency_matrix_f32 = {
  1,
  "navatala_dataframe_contingency_matrix_f32",
  "metal",
  "navatala_dataframe_contingency_matrix_f32",
  "kernel:metal:navatala_dataframe_contingency_matrix_f32",
  "abi-r1:metal:navatala_dataframe_contingency_matrix_f32",
  "abi-r1:metal:navatala_dataframe_contingency_matrix_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_contingency_matrix_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_silhouette_score_f32[] = {
  { "distMatrix", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "labels", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "clusterCounts", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "k", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "silhouette", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_silhouette_score_f32 = {
  1,
  "navatala_dataframe_silhouette_score_f32",
  "metal",
  "navatala_dataframe_silhouette_score_f32",
  "kernel:metal:navatala_dataframe_silhouette_score_f32",
  "abi-r1:metal:navatala_dataframe_silhouette_score_f32",
  "abi-r1:metal:navatala_dataframe_silhouette_score_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_silhouette_score_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_adjusted_rand_index_f32[] = {
  { "contingency", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rowSums", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "colSums", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "numTrue", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "numPred", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "ari", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_adjusted_rand_index_f32 = {
  1,
  "navatala_dataframe_adjusted_rand_index_f32",
  "metal",
  "navatala_dataframe_adjusted_rand_index_f32",
  "kernel:metal:navatala_dataframe_adjusted_rand_index_f32",
  "abi-r1:metal:navatala_dataframe_adjusted_rand_index_f32",
  "abi-r1:metal:navatala_dataframe_adjusted_rand_index_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_adjusted_rand_index_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_normalized_mutual_info_f32[] = {
  { "contingency", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rowSums", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "colSums", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "numTrue", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "numPred", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "nmi", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_normalized_mutual_info_f32 = {
  1,
  "navatala_dataframe_normalized_mutual_info_f32",
  "metal",
  "navatala_dataframe_normalized_mutual_info_f32",
  "kernel:metal:navatala_dataframe_normalized_mutual_info_f32",
  "abi-r1:metal:navatala_dataframe_normalized_mutual_info_f32",
  "abi-r1:metal:navatala_dataframe_normalized_mutual_info_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_normalized_mutual_info_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_homogeneity_score_f32[] = {
  { "contingency", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rowSums", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "colSums", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "numTrue", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "numPred", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "homogeneity", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_homogeneity_score_f32 = {
  1,
  "navatala_dataframe_homogeneity_score_f32",
  "metal",
  "navatala_dataframe_homogeneity_score_f32",
  "kernel:metal:navatala_dataframe_homogeneity_score_f32",
  "abi-r1:metal:navatala_dataframe_homogeneity_score_f32",
  "abi-r1:metal:navatala_dataframe_homogeneity_score_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_homogeneity_score_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_completeness_score_f32[] = {
  { "contingency", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rowSums", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "colSums", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "numTrue", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "numPred", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "completeness", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_completeness_score_f32 = {
  1,
  "navatala_dataframe_completeness_score_f32",
  "metal",
  "navatala_dataframe_completeness_score_f32",
  "kernel:metal:navatala_dataframe_completeness_score_f32",
  "abi-r1:metal:navatala_dataframe_completeness_score_f32",
  "abi-r1:metal:navatala_dataframe_completeness_score_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_completeness_score_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_v_measure_f32[] = {
  { "homogeneity", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "completeness", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "vMeasure", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_v_measure_f32 = {
  1,
  "navatala_dataframe_v_measure_f32",
  "metal",
  "navatala_dataframe_v_measure_f32",
  "kernel:metal:navatala_dataframe_v_measure_f32",
  "abi-r1:metal:navatala_dataframe_v_measure_f32",
  "abi-r1:metal:navatala_dataframe_v_measure_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_v_measure_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_r2_score_f32[] = {
  { "y_true", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "y_pred", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "y_mean", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "n", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_r2_score_f32 = {
  1,
  "navatala_dataframe_r2_score_f32",
  "metal",
  "navatala_dataframe_r2_score_f32",
  "kernel:metal:navatala_dataframe_r2_score_f32",
  "abi-r1:metal:navatala_dataframe_r2_score_f32",
  "abi-r1:metal:navatala_dataframe_r2_score_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_r2_score_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_mse_f32[] = {
  { "y_true", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "y_pred", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_mse_f32 = {
  1,
  "navatala_dataframe_mse_f32",
  "metal",
  "navatala_dataframe_mse_f32",
  "kernel:metal:navatala_dataframe_mse_f32",
  "abi-r1:metal:navatala_dataframe_mse_f32",
  "abi-r1:metal:navatala_dataframe_mse_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_mse_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_rmse_f32[] = {
  { "mse", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "rmse", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_rmse_f32 = {
  1,
  "navatala_dataframe_rmse_f32",
  "metal",
  "navatala_dataframe_rmse_f32",
  "kernel:metal:navatala_dataframe_rmse_f32",
  "abi-r1:metal:navatala_dataframe_rmse_f32",
  "abi-r1:metal:navatala_dataframe_rmse_f32",
  2,
  kAbiArgs_metal_navatala_dataframe_rmse_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_mae_f32[] = {
  { "y_true", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "y_pred", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_mae_f32 = {
  1,
  "navatala_dataframe_mae_f32",
  "metal",
  "navatala_dataframe_mae_f32",
  "kernel:metal:navatala_dataframe_mae_f32",
  "abi-r1:metal:navatala_dataframe_mae_f32",
  "abi-r1:metal:navatala_dataframe_mae_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_mae_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_mape_f32[] = {
  { "y_true", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "y_pred", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_mape_f32 = {
  1,
  "navatala_dataframe_mape_f32",
  "metal",
  "navatala_dataframe_mape_f32",
  "kernel:metal:navatala_dataframe_mape_f32",
  "abi-r1:metal:navatala_dataframe_mape_f32",
  "abi-r1:metal:navatala_dataframe_mape_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_mape_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_explained_variance_f32[] = {
  { "y_true", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "y_pred", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "mean_residuals", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mean_true", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "n", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_explained_variance_f32 = {
  1,
  "navatala_dataframe_explained_variance_f32",
  "metal",
  "navatala_dataframe_explained_variance_f32",
  "kernel:metal:navatala_dataframe_explained_variance_f32",
  "abi-r1:metal:navatala_dataframe_explained_variance_f32",
  "abi-r1:metal:navatala_dataframe_explained_variance_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_explained_variance_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_entropy_f32[] = {
  { "p", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_entropy_f32 = {
  1,
  "navatala_dataframe_entropy_f32",
  "metal",
  "navatala_dataframe_entropy_f32",
  "kernel:metal:navatala_dataframe_entropy_f32",
  "abi-r1:metal:navatala_dataframe_entropy_f32",
  "abi-r1:metal:navatala_dataframe_entropy_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_entropy_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_cross_entropy_f32[] = {
  { "p", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "q", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_cross_entropy_f32 = {
  1,
  "navatala_dataframe_cross_entropy_f32",
  "metal",
  "navatala_dataframe_cross_entropy_f32",
  "kernel:metal:navatala_dataframe_cross_entropy_f32",
  "abi-r1:metal:navatala_dataframe_cross_entropy_f32",
  "abi-r1:metal:navatala_dataframe_cross_entropy_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_cross_entropy_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_kl_divergence_f32[] = {
  { "p", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "q", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_kl_divergence_f32 = {
  1,
  "navatala_dataframe_kl_divergence_f32",
  "metal",
  "navatala_dataframe_kl_divergence_f32",
  "kernel:metal:navatala_dataframe_kl_divergence_f32",
  "abi-r1:metal:navatala_dataframe_kl_divergence_f32",
  "abi-r1:metal:navatala_dataframe_kl_divergence_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_kl_divergence_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_mutual_information_f32[] = {
  { "hX", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "hY", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "hXY", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_mutual_information_f32 = {
  1,
  "navatala_dataframe_mutual_information_f32",
  "metal",
  "navatala_dataframe_mutual_information_f32",
  "kernel:metal:navatala_dataframe_mutual_information_f32",
  "abi-r1:metal:navatala_dataframe_mutual_information_f32",
  "abi-r1:metal:navatala_dataframe_mutual_information_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_mutual_information_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_conditional_entropy_f32[] = {
  { "hX", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "hXY", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_conditional_entropy_f32 = {
  1,
  "navatala_dataframe_conditional_entropy_f32",
  "metal",
  "navatala_dataframe_conditional_entropy_f32",
  "kernel:metal:navatala_dataframe_conditional_entropy_f32",
  "abi-r1:metal:navatala_dataframe_conditional_entropy_f32",
  "abi-r1:metal:navatala_dataframe_conditional_entropy_f32",
  3,
  kAbiArgs_metal_navatala_dataframe_conditional_entropy_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_permute_f32[] = {
  { "data", 0, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "random", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "data", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_permute_f32 = {
  1,
  "navatala_dataframe_permute_f32",
  "metal",
  "navatala_dataframe_permute_f32",
  "kernel:metal:navatala_dataframe_permute_f32",
  "abi-r1:metal:navatala_dataframe_permute_f32",
  "abi-r1:metal:navatala_dataframe_permute_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_permute_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_shuffle_rows_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "random", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "m", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "n", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_shuffle_rows_f32 = {
  1,
  "navatala_dataframe_shuffle_rows_f32",
  "metal",
  "navatala_dataframe_shuffle_rows_f32",
  "kernel:metal:navatala_dataframe_shuffle_rows_f32",
  "abi-r1:metal:navatala_dataframe_shuffle_rows_f32",
  "abi-r1:metal:navatala_dataframe_shuffle_rows_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_shuffle_rows_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_sample_without_replacement_f32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "random", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "k", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "indices", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_sample_without_replacement_f32 = {
  1,
  "navatala_dataframe_sample_without_replacement_f32",
  "metal",
  "navatala_dataframe_sample_without_replacement_f32",
  "kernel:metal:navatala_dataframe_sample_without_replacement_f32",
  "abi-r1:metal:navatala_dataframe_sample_without_replacement_f32",
  "abi-r1:metal:navatala_dataframe_sample_without_replacement_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_sample_without_replacement_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_sample_with_replacement_f32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "random", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "k", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_sample_with_replacement_f32 = {
  1,
  "navatala_dataframe_sample_with_replacement_f32",
  "metal",
  "navatala_dataframe_sample_with_replacement_f32",
  "kernel:metal:navatala_dataframe_sample_with_replacement_f32",
  "abi-r1:metal:navatala_dataframe_sample_with_replacement_f32",
  "abi-r1:metal:navatala_dataframe_sample_with_replacement_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_sample_with_replacement_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_bootstrap_sample_f32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "random", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "n", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_bootstrap_sample_f32 = {
  1,
  "navatala_dataframe_bootstrap_sample_f32",
  "metal",
  "navatala_dataframe_bootstrap_sample_f32",
  "kernel:metal:navatala_dataframe_bootstrap_sample_f32",
  "abi-r1:metal:navatala_dataframe_bootstrap_sample_f32",
  "abi-r1:metal:navatala_dataframe_bootstrap_sample_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_bootstrap_sample_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_select_top_k_per_row_f32[] = {
  { "distances", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "q", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "n", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "k", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outDistances", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outIndices", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_select_top_k_per_row_f32 = {
  1,
  "navatala_dataframe_select_top_k_per_row_f32",
  "metal",
  "navatala_dataframe_select_top_k_per_row_f32",
  "kernel:metal:navatala_dataframe_select_top_k_per_row_f32",
  "abi-r1:metal:navatala_dataframe_select_top_k_per_row_f32",
  "abi-r1:metal:navatala_dataframe_select_top_k_per_row_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_select_top_k_per_row_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_select_top_k_f32[] = {
  { "values", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "k", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outValues", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outIndices", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_select_top_k_f32 = {
  1,
  "navatala_dataframe_select_top_k_f32",
  "metal",
  "navatala_dataframe_select_top_k_f32",
  "kernel:metal:navatala_dataframe_select_top_k_f32",
  "abi-r1:metal:navatala_dataframe_select_top_k_f32",
  "abi-r1:metal:navatala_dataframe_select_top_k_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_select_top_k_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_arg_top_k_per_row_f32[] = {
  { "values", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "q", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "n", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "k", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outIndices", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_arg_top_k_per_row_f32 = {
  1,
  "navatala_dataframe_arg_top_k_per_row_f32",
  "metal",
  "navatala_dataframe_arg_top_k_per_row_f32",
  "kernel:metal:navatala_dataframe_arg_top_k_per_row_f32",
  "abi-r1:metal:navatala_dataframe_arg_top_k_per_row_f32",
  "abi-r1:metal:navatala_dataframe_arg_top_k_per_row_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_arg_top_k_per_row_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_arg_top_k_f32[] = {
  { "values", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "k", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outIndices", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_arg_top_k_f32 = {
  1,
  "navatala_dataframe_arg_top_k_f32",
  "metal",
  "navatala_dataframe_arg_top_k_f32",
  "kernel:metal:navatala_dataframe_arg_top_k_f32",
  "abi-r1:metal:navatala_dataframe_arg_top_k_f32",
  "abi-r1:metal:navatala_dataframe_arg_top_k_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_arg_top_k_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_partial_sort_per_row_f32[] = {
  { "values", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "q", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "n", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "k", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outValues", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outIndices", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_partial_sort_per_row_f32 = {
  1,
  "navatala_dataframe_partial_sort_per_row_f32",
  "metal",
  "navatala_dataframe_partial_sort_per_row_f32",
  "kernel:metal:navatala_dataframe_partial_sort_per_row_f32",
  "abi-r1:metal:navatala_dataframe_partial_sort_per_row_f32",
  "abi-r1:metal:navatala_dataframe_partial_sort_per_row_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_partial_sort_per_row_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_group_by_sum_i32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "groupIds", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_group_by_sum_i32 = {
  1,
  "navatala_dataframe_group_by_sum_i32",
  "metal",
  "navatala_dataframe_group_by_sum_i32",
  "kernel:metal:navatala_dataframe_group_by_sum_i32",
  "abi-r1:metal:navatala_dataframe_group_by_sum_i32",
  "abi-r1:metal:navatala_dataframe_group_by_sum_i32",
  5,
  kAbiArgs_metal_navatala_dataframe_group_by_sum_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_group_by_sum_i64[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "groupIds", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_group_by_sum_i64 = {
  1,
  "navatala_dataframe_group_by_sum_i64",
  "metal",
  "navatala_dataframe_group_by_sum_i64",
  "kernel:metal:navatala_dataframe_group_by_sum_i64",
  "abi-r1:metal:navatala_dataframe_group_by_sum_i64",
  "abi-r1:metal:navatala_dataframe_group_by_sum_i64",
  5,
  kAbiArgs_metal_navatala_dataframe_group_by_sum_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_group_by_count[] = {
  { "validity", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "groupIds", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_group_by_count = {
  1,
  "navatala_dataframe_group_by_count",
  "metal",
  "navatala_dataframe_group_by_count",
  "kernel:metal:navatala_dataframe_group_by_count",
  "abi-r1:metal:navatala_dataframe_group_by_count",
  "abi-r1:metal:navatala_dataframe_group_by_count",
  4,
  kAbiArgs_metal_navatala_dataframe_group_by_count
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_group_by_min_i32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "groupIds", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_group_by_min_i32 = {
  1,
  "navatala_dataframe_group_by_min_i32",
  "metal",
  "navatala_dataframe_group_by_min_i32",
  "kernel:metal:navatala_dataframe_group_by_min_i32",
  "abi-r1:metal:navatala_dataframe_group_by_min_i32",
  "abi-r1:metal:navatala_dataframe_group_by_min_i32",
  5,
  kAbiArgs_metal_navatala_dataframe_group_by_min_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_group_by_max_i32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "groupIds", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_group_by_max_i32 = {
  1,
  "navatala_dataframe_group_by_max_i32",
  "metal",
  "navatala_dataframe_group_by_max_i32",
  "kernel:metal:navatala_dataframe_group_by_max_i32",
  "abi-r1:metal:navatala_dataframe_group_by_max_i32",
  "abi-r1:metal:navatala_dataframe_group_by_max_i32",
  5,
  kAbiArgs_metal_navatala_dataframe_group_by_max_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_group_by_sum_for_mean_f32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "groupIds", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "sumOutput", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "countOutput", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_group_by_sum_for_mean_f32 = {
  1,
  "navatala_dataframe_group_by_sum_for_mean_f32",
  "metal",
  "navatala_dataframe_group_by_sum_for_mean_f32",
  "kernel:metal:navatala_dataframe_group_by_sum_for_mean_f32",
  "abi-r1:metal:navatala_dataframe_group_by_sum_for_mean_f32",
  "abi-r1:metal:navatala_dataframe_group_by_sum_for_mean_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_group_by_sum_for_mean_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_group_by_nunique_helper_i32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "groupIds", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "hashOutput", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validOutput", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_group_by_nunique_helper_i32 = {
  1,
  "navatala_dataframe_group_by_nunique_helper_i32",
  "metal",
  "navatala_dataframe_group_by_nunique_helper_i32",
  "kernel:metal:navatala_dataframe_group_by_nunique_helper_i32",
  "abi-r1:metal:navatala_dataframe_group_by_nunique_helper_i32",
  "abi-r1:metal:navatala_dataframe_group_by_nunique_helper_i32",
  6,
  kAbiArgs_metal_navatala_dataframe_group_by_nunique_helper_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_group_by_nunique_helper_i64[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "groupIds", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "hashOutput", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validOutput", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_group_by_nunique_helper_i64 = {
  1,
  "navatala_dataframe_group_by_nunique_helper_i64",
  "metal",
  "navatala_dataframe_group_by_nunique_helper_i64",
  "kernel:metal:navatala_dataframe_group_by_nunique_helper_i64",
  "abi-r1:metal:navatala_dataframe_group_by_nunique_helper_i64",
  "abi-r1:metal:navatala_dataframe_group_by_nunique_helper_i64",
  6,
  kAbiArgs_metal_navatala_dataframe_group_by_nunique_helper_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_group_by_sorted_sum[] = {
  { "sortedKeys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "sortedData", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "groupSums", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "groupBounds", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_group_by_sorted_sum = {
  1,
  "navatala_dataframe_group_by_sorted_sum",
  "metal",
  "navatala_dataframe_group_by_sorted_sum",
  "kernel:metal:navatala_dataframe_group_by_sorted_sum",
  "abi-r1:metal:navatala_dataframe_group_by_sorted_sum",
  "abi-r1:metal:navatala_dataframe_group_by_sorted_sum",
  6,
  kAbiArgs_metal_navatala_dataframe_group_by_sorted_sum
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_group_by_sorted_count[] = {
  { "sortedKeys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "groupCounts", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "groupBounds", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_group_by_sorted_count = {
  1,
  "navatala_dataframe_group_by_sorted_count",
  "metal",
  "navatala_dataframe_group_by_sorted_count",
  "kernel:metal:navatala_dataframe_group_by_sorted_count",
  "abi-r1:metal:navatala_dataframe_group_by_sorted_count",
  "abi-r1:metal:navatala_dataframe_group_by_sorted_count",
  5,
  kAbiArgs_metal_navatala_dataframe_group_by_sorted_count
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_column_sum_i32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_column_sum_i32 = {
  1,
  "navatala_dataframe_column_sum_i32",
  "metal",
  "navatala_dataframe_column_sum_i32",
  "kernel:metal:navatala_dataframe_column_sum_i32",
  "abi-r1:metal:navatala_dataframe_column_sum_i32",
  "abi-r1:metal:navatala_dataframe_column_sum_i32",
  4,
  kAbiArgs_metal_navatala_dataframe_column_sum_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_column_sum_i64[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 8, 8, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_column_sum_i64 = {
  1,
  "navatala_dataframe_column_sum_i64",
  "metal",
  "navatala_dataframe_column_sum_i64",
  "kernel:metal:navatala_dataframe_column_sum_i64",
  "abi-r1:metal:navatala_dataframe_column_sum_i64",
  "abi-r1:metal:navatala_dataframe_column_sum_i64",
  4,
  kAbiArgs_metal_navatala_dataframe_column_sum_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_column_sum_f32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 8, 8, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_column_sum_f32 = {
  1,
  "navatala_dataframe_column_sum_f32",
  "metal",
  "navatala_dataframe_column_sum_f32",
  "kernel:metal:navatala_dataframe_column_sum_f32",
  "abi-r1:metal:navatala_dataframe_column_sum_f32",
  "abi-r1:metal:navatala_dataframe_column_sum_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_column_sum_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_column_min_i32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_column_min_i32 = {
  1,
  "navatala_dataframe_column_min_i32",
  "metal",
  "navatala_dataframe_column_min_i32",
  "kernel:metal:navatala_dataframe_column_min_i32",
  "abi-r1:metal:navatala_dataframe_column_min_i32",
  "abi-r1:metal:navatala_dataframe_column_min_i32",
  4,
  kAbiArgs_metal_navatala_dataframe_column_min_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_column_max_i32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_column_max_i32 = {
  1,
  "navatala_dataframe_column_max_i32",
  "metal",
  "navatala_dataframe_column_max_i32",
  "kernel:metal:navatala_dataframe_column_max_i32",
  "abi-r1:metal:navatala_dataframe_column_max_i32",
  "abi-r1:metal:navatala_dataframe_column_max_i32",
  4,
  kAbiArgs_metal_navatala_dataframe_column_max_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_column_count[] = {
  { "validity", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 2, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_column_count = {
  1,
  "navatala_dataframe_column_count",
  "metal",
  "navatala_dataframe_column_count",
  "kernel:metal:navatala_dataframe_column_count",
  "abi-r1:metal:navatala_dataframe_column_count",
  "abi-r1:metal:navatala_dataframe_column_count",
  3,
  kAbiArgs_metal_navatala_dataframe_column_count
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_column_mean_helper_f32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "sumResult", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 8, 8, 256, nullptr, 0, 0 },
  { "countResult", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_column_mean_helper_f32 = {
  1,
  "navatala_dataframe_column_mean_helper_f32",
  "metal",
  "navatala_dataframe_column_mean_helper_f32",
  "kernel:metal:navatala_dataframe_column_mean_helper_f32",
  "abi-r1:metal:navatala_dataframe_column_mean_helper_f32",
  "abi-r1:metal:navatala_dataframe_column_mean_helper_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_column_mean_helper_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_column_variance_helper_f32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "sumResult", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 8, 8, 256, nullptr, 0, 0 },
  { "sumSqResult", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 8, 8, 256, nullptr, 0, 0 },
  { "countResult", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_column_variance_helper_f32 = {
  1,
  "navatala_dataframe_column_variance_helper_f32",
  "metal",
  "navatala_dataframe_column_variance_helper_f32",
  "kernel:metal:navatala_dataframe_column_variance_helper_f32",
  "abi-r1:metal:navatala_dataframe_column_variance_helper_f32",
  "abi-r1:metal:navatala_dataframe_column_variance_helper_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_column_variance_helper_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_column_arg_min_i32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minVal", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minIdx", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_column_arg_min_i32 = {
  1,
  "navatala_dataframe_column_arg_min_i32",
  "metal",
  "navatala_dataframe_column_arg_min_i32",
  "kernel:metal:navatala_dataframe_column_arg_min_i32",
  "abi-r1:metal:navatala_dataframe_column_arg_min_i32",
  "abi-r1:metal:navatala_dataframe_column_arg_min_i32",
  5,
  kAbiArgs_metal_navatala_dataframe_column_arg_min_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_column_arg_max_i32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxVal", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxIdx", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_column_arg_max_i32 = {
  1,
  "navatala_dataframe_column_arg_max_i32",
  "metal",
  "navatala_dataframe_column_arg_max_i32",
  "kernel:metal:navatala_dataframe_column_arg_max_i32",
  "abi-r1:metal:navatala_dataframe_column_arg_max_i32",
  "abi-r1:metal:navatala_dataframe_column_arg_max_i32",
  5,
  kAbiArgs_metal_navatala_dataframe_column_arg_max_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_column_arg_min_f32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minValBits", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minIdx", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_column_arg_min_f32 = {
  1,
  "navatala_dataframe_column_arg_min_f32",
  "metal",
  "navatala_dataframe_column_arg_min_f32",
  "kernel:metal:navatala_dataframe_column_arg_min_f32",
  "abi-r1:metal:navatala_dataframe_column_arg_min_f32",
  "abi-r1:metal:navatala_dataframe_column_arg_min_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_column_arg_min_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_column_arg_max_f32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxValBits", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxIdx", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_column_arg_max_f32 = {
  1,
  "navatala_dataframe_column_arg_max_f32",
  "metal",
  "navatala_dataframe_column_arg_max_f32",
  "kernel:metal:navatala_dataframe_column_arg_max_f32",
  "abi-r1:metal:navatala_dataframe_column_arg_max_f32",
  "abi-r1:metal:navatala_dataframe_column_arg_max_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_column_arg_max_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_column_std_helper[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "mean", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "sumSqDiffResult", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 8, 8, 256, nullptr, 0, 0 },
  { "countResult", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_column_std_helper = {
  1,
  "navatala_dataframe_column_std_helper",
  "metal",
  "navatala_dataframe_column_std_helper",
  "kernel:metal:navatala_dataframe_column_std_helper",
  "abi-r1:metal:navatala_dataframe_column_std_helper",
  "abi-r1:metal:navatala_dataframe_column_std_helper",
  6,
  kAbiArgs_metal_navatala_dataframe_column_std_helper
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_column_product_i32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "sumLogResult", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 8, 8, 256, nullptr, 0, 0 },
  { "negCount", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "zeroFlag", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_column_product_i32 = {
  1,
  "navatala_dataframe_column_product_i32",
  "metal",
  "navatala_dataframe_column_product_i32",
  "kernel:metal:navatala_dataframe_column_product_i32",
  "abi-r1:metal:navatala_dataframe_column_product_i32",
  "abi-r1:metal:navatala_dataframe_column_product_i32",
  6,
  kAbiArgs_metal_navatala_dataframe_column_product_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_column_product_f32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "sumLogResult", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 8, 8, 256, nullptr, 0, 0 },
  { "negCount", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "zeroFlag", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_column_product_f32 = {
  1,
  "navatala_dataframe_column_product_f32",
  "metal",
  "navatala_dataframe_column_product_f32",
  "kernel:metal:navatala_dataframe_column_product_f32",
  "abi-r1:metal:navatala_dataframe_column_product_f32",
  "abi-r1:metal:navatala_dataframe_column_product_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_column_product_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_rolling_sum_f32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "windowSize", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "minPeriods", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_rolling_sum_f32 = {
  1,
  "navatala_dataframe_rolling_sum_f32",
  "metal",
  "navatala_dataframe_rolling_sum_f32",
  "kernel:metal:navatala_dataframe_rolling_sum_f32",
  "abi-r1:metal:navatala_dataframe_rolling_sum_f32",
  "abi-r1:metal:navatala_dataframe_rolling_sum_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_rolling_sum_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_rolling_count[] = {
  { "validity", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "windowSize", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_rolling_count = {
  1,
  "navatala_dataframe_rolling_count",
  "metal",
  "navatala_dataframe_rolling_count",
  "kernel:metal:navatala_dataframe_rolling_count",
  "abi-r1:metal:navatala_dataframe_rolling_count",
  "abi-r1:metal:navatala_dataframe_rolling_count",
  4,
  kAbiArgs_metal_navatala_dataframe_rolling_count
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_rolling_min_i32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "windowSize", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_rolling_min_i32 = {
  1,
  "navatala_dataframe_rolling_min_i32",
  "metal",
  "navatala_dataframe_rolling_min_i32",
  "kernel:metal:navatala_dataframe_rolling_min_i32",
  "abi-r1:metal:navatala_dataframe_rolling_min_i32",
  "abi-r1:metal:navatala_dataframe_rolling_min_i32",
  6,
  kAbiArgs_metal_navatala_dataframe_rolling_min_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_rolling_max_i32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "windowSize", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_rolling_max_i32 = {
  1,
  "navatala_dataframe_rolling_max_i32",
  "metal",
  "navatala_dataframe_rolling_max_i32",
  "kernel:metal:navatala_dataframe_rolling_max_i32",
  "abi-r1:metal:navatala_dataframe_rolling_max_i32",
  "abi-r1:metal:navatala_dataframe_rolling_max_i32",
  6,
  kAbiArgs_metal_navatala_dataframe_rolling_max_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_rolling_mean_helper_f32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "windowSize", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "sumOutput", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "countOutput", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_rolling_mean_helper_f32 = {
  1,
  "navatala_dataframe_rolling_mean_helper_f32",
  "metal",
  "navatala_dataframe_rolling_mean_helper_f32",
  "kernel:metal:navatala_dataframe_rolling_mean_helper_f32",
  "abi-r1:metal:navatala_dataframe_rolling_mean_helper_f32",
  "abi-r1:metal:navatala_dataframe_rolling_mean_helper_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_rolling_mean_helper_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_rolling_variance_helper_f32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "windowSize", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "sumOutput", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "sumSqOutput", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "countOutput", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 7, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_rolling_variance_helper_f32 = {
  1,
  "navatala_dataframe_rolling_variance_helper_f32",
  "metal",
  "navatala_dataframe_rolling_variance_helper_f32",
  "kernel:metal:navatala_dataframe_rolling_variance_helper_f32",
  "abi-r1:metal:navatala_dataframe_rolling_variance_helper_f32",
  "abi-r1:metal:navatala_dataframe_rolling_variance_helper_f32",
  8,
  kAbiArgs_metal_navatala_dataframe_rolling_variance_helper_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_expanding_sum_f32[] = {
  { "data", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_expanding_sum_f32 = {
  1,
  "navatala_dataframe_expanding_sum_f32",
  "metal",
  "navatala_dataframe_expanding_sum_f32",
  "kernel:metal:navatala_dataframe_expanding_sum_f32",
  "abi-r1:metal:navatala_dataframe_expanding_sum_f32",
  "abi-r1:metal:navatala_dataframe_expanding_sum_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_expanding_sum_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_expanding_count_valid[] = {
  { "validity", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_expanding_count_valid = {
  1,
  "navatala_dataframe_expanding_count_valid",
  "metal",
  "navatala_dataframe_expanding_count_valid",
  "kernel:metal:navatala_dataframe_expanding_count_valid",
  "abi-r1:metal:navatala_dataframe_expanding_count_valid",
  "abi-r1:metal:navatala_dataframe_expanding_count_valid",
  3,
  kAbiArgs_metal_navatala_dataframe_expanding_count_valid
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_compute_quantile_indices[] = {
  { "quantile", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "floorIdx", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "ceilIdx", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "fraction", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_compute_quantile_indices = {
  1,
  "navatala_dataframe_compute_quantile_indices",
  "metal",
  "navatala_dataframe_compute_quantile_indices",
  "kernel:metal:navatala_dataframe_compute_quantile_indices",
  "abi-r1:metal:navatala_dataframe_compute_quantile_indices",
  "abi-r1:metal:navatala_dataframe_compute_quantile_indices",
  5,
  kAbiArgs_metal_navatala_dataframe_compute_quantile_indices
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_select_quantile_f32[] = {
  { "sortedData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "floorIdx", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "ceilIdx", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "fraction", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_select_quantile_f32 = {
  1,
  "navatala_dataframe_select_quantile_f32",
  "metal",
  "navatala_dataframe_select_quantile_f32",
  "kernel:metal:navatala_dataframe_select_quantile_f32",
  "abi-r1:metal:navatala_dataframe_select_quantile_f32",
  "abi-r1:metal:navatala_dataframe_select_quantile_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_select_quantile_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_select_quantile_i32[] = {
  { "sortedData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "floorIdx", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_select_quantile_i32 = {
  1,
  "navatala_dataframe_select_quantile_i32",
  "metal",
  "navatala_dataframe_select_quantile_i32",
  "kernel:metal:navatala_dataframe_select_quantile_i32",
  "abi-r1:metal:navatala_dataframe_select_quantile_i32",
  "abi-r1:metal:navatala_dataframe_select_quantile_i32",
  3,
  kAbiArgs_metal_navatala_dataframe_select_quantile_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_batch_quantile_f32[] = {
  { "sortedData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "quantiles", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "dataCount", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "numQuantiles", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "results", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_batch_quantile_f32 = {
  1,
  "navatala_dataframe_batch_quantile_f32",
  "metal",
  "navatala_dataframe_batch_quantile_f32",
  "kernel:metal:navatala_dataframe_batch_quantile_f32",
  "abi-r1:metal:navatala_dataframe_batch_quantile_f32",
  "abi-r1:metal:navatala_dataframe_batch_quantile_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_batch_quantile_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_compute_median_indices[] = {
  { "count", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "floorIdx", 1, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "ceilIdx", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "isEven", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_compute_median_indices = {
  1,
  "navatala_dataframe_compute_median_indices",
  "metal",
  "navatala_dataframe_compute_median_indices",
  "kernel:metal:navatala_dataframe_compute_median_indices",
  "abi-r1:metal:navatala_dataframe_compute_median_indices",
  "abi-r1:metal:navatala_dataframe_compute_median_indices",
  4,
  kAbiArgs_metal_navatala_dataframe_compute_median_indices
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_select_median_f32[] = {
  { "sortedData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "floorIdx", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "ceilIdx", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "isEven", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_select_median_f32 = {
  1,
  "navatala_dataframe_select_median_f32",
  "metal",
  "navatala_dataframe_select_median_f32",
  "kernel:metal:navatala_dataframe_select_median_f32",
  "abi-r1:metal:navatala_dataframe_select_median_f32",
  "abi-r1:metal:navatala_dataframe_select_median_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_select_median_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_add_f32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_add_f32 = {
  1,
  "navatala_dataframe_add_f32",
  "metal",
  "navatala_dataframe_add_f32",
  "kernel:metal:navatala_dataframe_add_f32",
  "abi-r1:metal:navatala_dataframe_add_f32",
  "abi-r1:metal:navatala_dataframe_add_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_add_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_add_i32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_add_i32 = {
  1,
  "navatala_dataframe_add_i32",
  "metal",
  "navatala_dataframe_add_i32",
  "kernel:metal:navatala_dataframe_add_i32",
  "abi-r1:metal:navatala_dataframe_add_i32",
  "abi-r1:metal:navatala_dataframe_add_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_add_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_sub_f32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_sub_f32 = {
  1,
  "navatala_dataframe_sub_f32",
  "metal",
  "navatala_dataframe_sub_f32",
  "kernel:metal:navatala_dataframe_sub_f32",
  "abi-r1:metal:navatala_dataframe_sub_f32",
  "abi-r1:metal:navatala_dataframe_sub_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_sub_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_mul_f32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_mul_f32 = {
  1,
  "navatala_dataframe_mul_f32",
  "metal",
  "navatala_dataframe_mul_f32",
  "kernel:metal:navatala_dataframe_mul_f32",
  "abi-r1:metal:navatala_dataframe_mul_f32",
  "abi-r1:metal:navatala_dataframe_mul_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_mul_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_div_f32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_div_f32 = {
  1,
  "navatala_dataframe_div_f32",
  "metal",
  "navatala_dataframe_div_f32",
  "kernel:metal:navatala_dataframe_div_f32",
  "abi-r1:metal:navatala_dataframe_div_f32",
  "abi-r1:metal:navatala_dataframe_div_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_div_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_mod_i32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_mod_i32 = {
  1,
  "navatala_dataframe_mod_i32",
  "metal",
  "navatala_dataframe_mod_i32",
  "kernel:metal:navatala_dataframe_mod_i32",
  "abi-r1:metal:navatala_dataframe_mod_i32",
  "abi-r1:metal:navatala_dataframe_mod_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_mod_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_sub_i32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_sub_i32 = {
  1,
  "navatala_dataframe_sub_i32",
  "metal",
  "navatala_dataframe_sub_i32",
  "kernel:metal:navatala_dataframe_sub_i32",
  "abi-r1:metal:navatala_dataframe_sub_i32",
  "abi-r1:metal:navatala_dataframe_sub_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_sub_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_mul_i32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_mul_i32 = {
  1,
  "navatala_dataframe_mul_i32",
  "metal",
  "navatala_dataframe_mul_i32",
  "kernel:metal:navatala_dataframe_mul_i32",
  "abi-r1:metal:navatala_dataframe_mul_i32",
  "abi-r1:metal:navatala_dataframe_mul_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_mul_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_div_i32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_div_i32 = {
  1,
  "navatala_dataframe_div_i32",
  "metal",
  "navatala_dataframe_div_i32",
  "kernel:metal:navatala_dataframe_div_i32",
  "abi-r1:metal:navatala_dataframe_div_i32",
  "abi-r1:metal:navatala_dataframe_div_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_div_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_add_i64[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_add_i64 = {
  1,
  "navatala_dataframe_add_i64",
  "metal",
  "navatala_dataframe_add_i64",
  "kernel:metal:navatala_dataframe_add_i64",
  "abi-r1:metal:navatala_dataframe_add_i64",
  "abi-r1:metal:navatala_dataframe_add_i64",
  7,
  kAbiArgs_metal_navatala_dataframe_add_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_sub_i64[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_sub_i64 = {
  1,
  "navatala_dataframe_sub_i64",
  "metal",
  "navatala_dataframe_sub_i64",
  "kernel:metal:navatala_dataframe_sub_i64",
  "abi-r1:metal:navatala_dataframe_sub_i64",
  "abi-r1:metal:navatala_dataframe_sub_i64",
  7,
  kAbiArgs_metal_navatala_dataframe_sub_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_mul_i64[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_mul_i64 = {
  1,
  "navatala_dataframe_mul_i64",
  "metal",
  "navatala_dataframe_mul_i64",
  "kernel:metal:navatala_dataframe_mul_i64",
  "abi-r1:metal:navatala_dataframe_mul_i64",
  "abi-r1:metal:navatala_dataframe_mul_i64",
  7,
  kAbiArgs_metal_navatala_dataframe_mul_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_div_i64[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_div_i64 = {
  1,
  "navatala_dataframe_div_i64",
  "metal",
  "navatala_dataframe_div_i64",
  "kernel:metal:navatala_dataframe_div_i64",
  "abi-r1:metal:navatala_dataframe_div_i64",
  "abi-r1:metal:navatala_dataframe_div_i64",
  7,
  kAbiArgs_metal_navatala_dataframe_div_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_mod_i64[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_mod_i64 = {
  1,
  "navatala_dataframe_mod_i64",
  "metal",
  "navatala_dataframe_mod_i64",
  "kernel:metal:navatala_dataframe_mod_i64",
  "abi-r1:metal:navatala_dataframe_mod_i64",
  "abi-r1:metal:navatala_dataframe_mod_i64",
  7,
  kAbiArgs_metal_navatala_dataframe_mod_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_eq_f32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_eq_f32 = {
  1,
  "navatala_dataframe_eq_f32",
  "metal",
  "navatala_dataframe_eq_f32",
  "kernel:metal:navatala_dataframe_eq_f32",
  "abi-r1:metal:navatala_dataframe_eq_f32",
  "abi-r1:metal:navatala_dataframe_eq_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_eq_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_eq_i32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_eq_i32 = {
  1,
  "navatala_dataframe_eq_i32",
  "metal",
  "navatala_dataframe_eq_i32",
  "kernel:metal:navatala_dataframe_eq_i32",
  "abi-r1:metal:navatala_dataframe_eq_i32",
  "abi-r1:metal:navatala_dataframe_eq_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_eq_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_lt_f32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_lt_f32 = {
  1,
  "navatala_dataframe_lt_f32",
  "metal",
  "navatala_dataframe_lt_f32",
  "kernel:metal:navatala_dataframe_lt_f32",
  "abi-r1:metal:navatala_dataframe_lt_f32",
  "abi-r1:metal:navatala_dataframe_lt_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_lt_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gt_f32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gt_f32 = {
  1,
  "navatala_dataframe_gt_f32",
  "metal",
  "navatala_dataframe_gt_f32",
  "kernel:metal:navatala_dataframe_gt_f32",
  "abi-r1:metal:navatala_dataframe_gt_f32",
  "abi-r1:metal:navatala_dataframe_gt_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_gt_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_le_f32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_le_f32 = {
  1,
  "navatala_dataframe_le_f32",
  "metal",
  "navatala_dataframe_le_f32",
  "kernel:metal:navatala_dataframe_le_f32",
  "abi-r1:metal:navatala_dataframe_le_f32",
  "abi-r1:metal:navatala_dataframe_le_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_le_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_ge_f32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_ge_f32 = {
  1,
  "navatala_dataframe_ge_f32",
  "metal",
  "navatala_dataframe_ge_f32",
  "kernel:metal:navatala_dataframe_ge_f32",
  "abi-r1:metal:navatala_dataframe_ge_f32",
  "abi-r1:metal:navatala_dataframe_ge_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_ge_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_ne_f32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_ne_f32 = {
  1,
  "navatala_dataframe_ne_f32",
  "metal",
  "navatala_dataframe_ne_f32",
  "kernel:metal:navatala_dataframe_ne_f32",
  "abi-r1:metal:navatala_dataframe_ne_f32",
  "abi-r1:metal:navatala_dataframe_ne_f32",
  7,
  kAbiArgs_metal_navatala_dataframe_ne_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_ne_i32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_ne_i32 = {
  1,
  "navatala_dataframe_ne_i32",
  "metal",
  "navatala_dataframe_ne_i32",
  "kernel:metal:navatala_dataframe_ne_i32",
  "abi-r1:metal:navatala_dataframe_ne_i32",
  "abi-r1:metal:navatala_dataframe_ne_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_ne_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_lt_i32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_lt_i32 = {
  1,
  "navatala_dataframe_lt_i32",
  "metal",
  "navatala_dataframe_lt_i32",
  "kernel:metal:navatala_dataframe_lt_i32",
  "abi-r1:metal:navatala_dataframe_lt_i32",
  "abi-r1:metal:navatala_dataframe_lt_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_lt_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gt_i32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gt_i32 = {
  1,
  "navatala_dataframe_gt_i32",
  "metal",
  "navatala_dataframe_gt_i32",
  "kernel:metal:navatala_dataframe_gt_i32",
  "abi-r1:metal:navatala_dataframe_gt_i32",
  "abi-r1:metal:navatala_dataframe_gt_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_gt_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_le_i32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_le_i32 = {
  1,
  "navatala_dataframe_le_i32",
  "metal",
  "navatala_dataframe_le_i32",
  "kernel:metal:navatala_dataframe_le_i32",
  "abi-r1:metal:navatala_dataframe_le_i32",
  "abi-r1:metal:navatala_dataframe_le_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_le_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_ge_i32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_ge_i32 = {
  1,
  "navatala_dataframe_ge_i32",
  "metal",
  "navatala_dataframe_ge_i32",
  "kernel:metal:navatala_dataframe_ge_i32",
  "abi-r1:metal:navatala_dataframe_ge_i32",
  "abi-r1:metal:navatala_dataframe_ge_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_ge_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_bitwise_and_i32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_bitwise_and_i32 = {
  1,
  "navatala_dataframe_bitwise_and_i32",
  "metal",
  "navatala_dataframe_bitwise_and_i32",
  "kernel:metal:navatala_dataframe_bitwise_and_i32",
  "abi-r1:metal:navatala_dataframe_bitwise_and_i32",
  "abi-r1:metal:navatala_dataframe_bitwise_and_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_bitwise_and_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_bitwise_and_i64[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_bitwise_and_i64 = {
  1,
  "navatala_dataframe_bitwise_and_i64",
  "metal",
  "navatala_dataframe_bitwise_and_i64",
  "kernel:metal:navatala_dataframe_bitwise_and_i64",
  "abi-r1:metal:navatala_dataframe_bitwise_and_i64",
  "abi-r1:metal:navatala_dataframe_bitwise_and_i64",
  7,
  kAbiArgs_metal_navatala_dataframe_bitwise_and_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_bitwise_or_i32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_bitwise_or_i32 = {
  1,
  "navatala_dataframe_bitwise_or_i32",
  "metal",
  "navatala_dataframe_bitwise_or_i32",
  "kernel:metal:navatala_dataframe_bitwise_or_i32",
  "abi-r1:metal:navatala_dataframe_bitwise_or_i32",
  "abi-r1:metal:navatala_dataframe_bitwise_or_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_bitwise_or_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_bitwise_or_i64[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_bitwise_or_i64 = {
  1,
  "navatala_dataframe_bitwise_or_i64",
  "metal",
  "navatala_dataframe_bitwise_or_i64",
  "kernel:metal:navatala_dataframe_bitwise_or_i64",
  "abi-r1:metal:navatala_dataframe_bitwise_or_i64",
  "abi-r1:metal:navatala_dataframe_bitwise_or_i64",
  7,
  kAbiArgs_metal_navatala_dataframe_bitwise_or_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_bitwise_xor_i32[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_bitwise_xor_i32 = {
  1,
  "navatala_dataframe_bitwise_xor_i32",
  "metal",
  "navatala_dataframe_bitwise_xor_i32",
  "kernel:metal:navatala_dataframe_bitwise_xor_i32",
  "abi-r1:metal:navatala_dataframe_bitwise_xor_i32",
  "abi-r1:metal:navatala_dataframe_bitwise_xor_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_bitwise_xor_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_bitwise_xor_i64[] = {
  { "left", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "right", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValid", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_bitwise_xor_i64 = {
  1,
  "navatala_dataframe_bitwise_xor_i64",
  "metal",
  "navatala_dataframe_bitwise_xor_i64",
  "kernel:metal:navatala_dataframe_bitwise_xor_i64",
  "abi-r1:metal:navatala_dataframe_bitwise_xor_i64",
  "abi-r1:metal:navatala_dataframe_bitwise_xor_i64",
  7,
  kAbiArgs_metal_navatala_dataframe_bitwise_xor_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_bitwise_not_i32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_bitwise_not_i32 = {
  1,
  "navatala_dataframe_bitwise_not_i32",
  "metal",
  "navatala_dataframe_bitwise_not_i32",
  "kernel:metal:navatala_dataframe_bitwise_not_i32",
  "abi-r1:metal:navatala_dataframe_bitwise_not_i32",
  "abi-r1:metal:navatala_dataframe_bitwise_not_i32",
  5,
  kAbiArgs_metal_navatala_dataframe_bitwise_not_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_bitwise_not_i64[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_bitwise_not_i64 = {
  1,
  "navatala_dataframe_bitwise_not_i64",
  "metal",
  "navatala_dataframe_bitwise_not_i64",
  "kernel:metal:navatala_dataframe_bitwise_not_i64",
  "abi-r1:metal:navatala_dataframe_bitwise_not_i64",
  "abi-r1:metal:navatala_dataframe_bitwise_not_i64",
  5,
  kAbiArgs_metal_navatala_dataframe_bitwise_not_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_hour_from_seconds[] = {
  { "timestamps", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_hour_from_seconds = {
  1,
  "navatala_dataframe_extract_hour_from_seconds",
  "metal",
  "navatala_dataframe_extract_hour_from_seconds",
  "kernel:metal:navatala_dataframe_extract_hour_from_seconds",
  "abi-r1:metal:navatala_dataframe_extract_hour_from_seconds",
  "abi-r1:metal:navatala_dataframe_extract_hour_from_seconds",
  5,
  kAbiArgs_metal_navatala_dataframe_extract_hour_from_seconds
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_minute_from_seconds[] = {
  { "timestamps", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_minute_from_seconds = {
  1,
  "navatala_dataframe_extract_minute_from_seconds",
  "metal",
  "navatala_dataframe_extract_minute_from_seconds",
  "kernel:metal:navatala_dataframe_extract_minute_from_seconds",
  "abi-r1:metal:navatala_dataframe_extract_minute_from_seconds",
  "abi-r1:metal:navatala_dataframe_extract_minute_from_seconds",
  5,
  kAbiArgs_metal_navatala_dataframe_extract_minute_from_seconds
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_second_from_seconds[] = {
  { "timestamps", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_second_from_seconds = {
  1,
  "navatala_dataframe_extract_second_from_seconds",
  "metal",
  "navatala_dataframe_extract_second_from_seconds",
  "kernel:metal:navatala_dataframe_extract_second_from_seconds",
  "abi-r1:metal:navatala_dataframe_extract_second_from_seconds",
  "abi-r1:metal:navatala_dataframe_extract_second_from_seconds",
  5,
  kAbiArgs_metal_navatala_dataframe_extract_second_from_seconds
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_day_of_week_from_seconds[] = {
  { "timestamps", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_day_of_week_from_seconds = {
  1,
  "navatala_dataframe_extract_day_of_week_from_seconds",
  "metal",
  "navatala_dataframe_extract_day_of_week_from_seconds",
  "kernel:metal:navatala_dataframe_extract_day_of_week_from_seconds",
  "abi-r1:metal:navatala_dataframe_extract_day_of_week_from_seconds",
  "abi-r1:metal:navatala_dataframe_extract_day_of_week_from_seconds",
  5,
  kAbiArgs_metal_navatala_dataframe_extract_day_of_week_from_seconds
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_year_from_seconds[] = {
  { "timestamps", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_year_from_seconds = {
  1,
  "navatala_dataframe_extract_year_from_seconds",
  "metal",
  "navatala_dataframe_extract_year_from_seconds",
  "kernel:metal:navatala_dataframe_extract_year_from_seconds",
  "abi-r1:metal:navatala_dataframe_extract_year_from_seconds",
  "abi-r1:metal:navatala_dataframe_extract_year_from_seconds",
  5,
  kAbiArgs_metal_navatala_dataframe_extract_year_from_seconds
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_month_from_seconds[] = {
  { "timestamps", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_month_from_seconds = {
  1,
  "navatala_dataframe_extract_month_from_seconds",
  "metal",
  "navatala_dataframe_extract_month_from_seconds",
  "kernel:metal:navatala_dataframe_extract_month_from_seconds",
  "abi-r1:metal:navatala_dataframe_extract_month_from_seconds",
  "abi-r1:metal:navatala_dataframe_extract_month_from_seconds",
  5,
  kAbiArgs_metal_navatala_dataframe_extract_month_from_seconds
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_day_from_seconds[] = {
  { "timestamps", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_day_from_seconds = {
  1,
  "navatala_dataframe_extract_day_from_seconds",
  "metal",
  "navatala_dataframe_extract_day_from_seconds",
  "kernel:metal:navatala_dataframe_extract_day_from_seconds",
  "abi-r1:metal:navatala_dataframe_extract_day_from_seconds",
  "abi-r1:metal:navatala_dataframe_extract_day_from_seconds",
  5,
  kAbiArgs_metal_navatala_dataframe_extract_day_from_seconds
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_extract_day_of_year_from_seconds[] = {
  { "timestamps", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_extract_day_of_year_from_seconds = {
  1,
  "navatala_dataframe_extract_day_of_year_from_seconds",
  "metal",
  "navatala_dataframe_extract_day_of_year_from_seconds",
  "kernel:metal:navatala_dataframe_extract_day_of_year_from_seconds",
  "abi-r1:metal:navatala_dataframe_extract_day_of_year_from_seconds",
  "abi-r1:metal:navatala_dataframe_extract_day_of_year_from_seconds",
  5,
  kAbiArgs_metal_navatala_dataframe_extract_day_of_year_from_seconds
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_add_days_to_timestamp[] = {
  { "timestamps", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "days", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_add_days_to_timestamp = {
  1,
  "navatala_dataframe_add_days_to_timestamp",
  "metal",
  "navatala_dataframe_add_days_to_timestamp",
  "kernel:metal:navatala_dataframe_add_days_to_timestamp",
  "abi-r1:metal:navatala_dataframe_add_days_to_timestamp",
  "abi-r1:metal:navatala_dataframe_add_days_to_timestamp",
  6,
  kAbiArgs_metal_navatala_dataframe_add_days_to_timestamp
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_add_months_to_timestamp[] = {
  { "timestamps", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "months", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_add_months_to_timestamp = {
  1,
  "navatala_dataframe_add_months_to_timestamp",
  "metal",
  "navatala_dataframe_add_months_to_timestamp",
  "kernel:metal:navatala_dataframe_add_months_to_timestamp",
  "abi-r1:metal:navatala_dataframe_add_months_to_timestamp",
  "abi-r1:metal:navatala_dataframe_add_months_to_timestamp",
  6,
  kAbiArgs_metal_navatala_dataframe_add_months_to_timestamp
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_subtract_timestamps[] = {
  { "timestamps1", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "timestamps2", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity1", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity2", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_subtract_timestamps = {
  1,
  "navatala_dataframe_subtract_timestamps",
  "metal",
  "navatala_dataframe_subtract_timestamps",
  "kernel:metal:navatala_dataframe_subtract_timestamps",
  "abi-r1:metal:navatala_dataframe_subtract_timestamps",
  "abi-r1:metal:navatala_dataframe_subtract_timestamps",
  7,
  kAbiArgs_metal_navatala_dataframe_subtract_timestamps
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_timestamp_to_date_parts[] = {
  { "timestamps", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "years", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "months", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "daysOut", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_timestamp_to_date_parts = {
  1,
  "navatala_dataframe_timestamp_to_date_parts",
  "metal",
  "navatala_dataframe_timestamp_to_date_parts",
  "kernel:metal:navatala_dataframe_timestamp_to_date_parts",
  "abi-r1:metal:navatala_dataframe_timestamp_to_date_parts",
  "abi-r1:metal:navatala_dataframe_timestamp_to_date_parts",
  7,
  kAbiArgs_metal_navatala_dataframe_timestamp_to_date_parts
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_date_parts_to_timestamp[] = {
  { "yearsIn", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "monthsIn", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "daysIn", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_date_parts_to_timestamp = {
  1,
  "navatala_dataframe_date_parts_to_timestamp",
  "metal",
  "navatala_dataframe_date_parts_to_timestamp",
  "kernel:metal:navatala_dataframe_date_parts_to_timestamp",
  "abi-r1:metal:navatala_dataframe_date_parts_to_timestamp",
  "abi-r1:metal:navatala_dataframe_date_parts_to_timestamp",
  7,
  kAbiArgs_metal_navatala_dataframe_date_parts_to_timestamp
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_convert_timestamp_precision[] = {
  { "timestamps", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "scaleFactor", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 8, 8, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_convert_timestamp_precision = {
  1,
  "navatala_dataframe_convert_timestamp_precision",
  "metal",
  "navatala_dataframe_convert_timestamp_precision",
  "kernel:metal:navatala_dataframe_convert_timestamp_precision",
  "abi-r1:metal:navatala_dataframe_convert_timestamp_precision",
  "abi-r1:metal:navatala_dataframe_convert_timestamp_precision",
  6,
  kAbiArgs_metal_navatala_dataframe_convert_timestamp_precision
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_combine_hash_values[] = {
  { "hashes1", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "hashes2", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "combined", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_combine_hash_values = {
  1,
  "navatala_dataframe_combine_hash_values",
  "metal",
  "navatala_dataframe_combine_hash_values",
  "kernel:metal:navatala_dataframe_combine_hash_values",
  "abi-r1:metal:navatala_dataframe_combine_hash_values",
  "abi-r1:metal:navatala_dataframe_combine_hash_values",
  4,
  kAbiArgs_metal_navatala_dataframe_combine_hash_values
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_hash_column_row[] = {
  { "colHashes1", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "colHashes2", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "rowHashes", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_hash_column_row = {
  1,
  "navatala_dataframe_hash_column_row",
  "metal",
  "navatala_dataframe_hash_column_row",
  "kernel:metal:navatala_dataframe_hash_column_row",
  "abi-r1:metal:navatala_dataframe_hash_column_row",
  "abi-r1:metal:navatala_dataframe_hash_column_row",
  4,
  kAbiArgs_metal_navatala_dataframe_hash_column_row
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_md5_hash[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputLength", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_md5_hash = {
  1,
  "navatala_dataframe_md5_hash",
  "metal",
  "navatala_dataframe_md5_hash",
  "kernel:metal:navatala_dataframe_md5_hash",
  "abi-r1:metal:navatala_dataframe_md5_hash",
  "abi-r1:metal:navatala_dataframe_md5_hash",
  3,
  kAbiArgs_metal_navatala_dataframe_md5_hash
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_sha256_hash[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputLength", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_sha256_hash = {
  1,
  "navatala_dataframe_sha256_hash",
  "metal",
  "navatala_dataframe_sha256_hash",
  "kernel:metal:navatala_dataframe_sha256_hash",
  "abi-r1:metal:navatala_dataframe_sha256_hash",
  "abi-r1:metal:navatala_dataframe_sha256_hash",
  3,
  kAbiArgs_metal_navatala_dataframe_sha256_hash
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_identity_hash_i32[] = {
  { "keys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "hashes", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_identity_hash_i32 = {
  1,
  "navatala_dataframe_identity_hash_i32",
  "metal",
  "navatala_dataframe_identity_hash_i32",
  "kernel:metal:navatala_dataframe_identity_hash_i32",
  "abi-r1:metal:navatala_dataframe_identity_hash_i32",
  "abi-r1:metal:navatala_dataframe_identity_hash_i32",
  3,
  kAbiArgs_metal_navatala_dataframe_identity_hash_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_identity_hash_i64[] = {
  { "keys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "hashes", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_identity_hash_i64 = {
  1,
  "navatala_dataframe_identity_hash_i64",
  "metal",
  "navatala_dataframe_identity_hash_i64",
  "kernel:metal:navatala_dataframe_identity_hash_i64",
  "abi-r1:metal:navatala_dataframe_identity_hash_i64",
  "abi-r1:metal:navatala_dataframe_identity_hash_i64",
  3,
  kAbiArgs_metal_navatala_dataframe_identity_hash_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_murmur3_hash32_i32[] = {
  { "keys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "seed", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "hashes", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_murmur3_hash32_i32 = {
  1,
  "navatala_dataframe_murmur3_hash32_i32",
  "metal",
  "navatala_dataframe_murmur3_hash32_i32",
  "kernel:metal:navatala_dataframe_murmur3_hash32_i32",
  "abi-r1:metal:navatala_dataframe_murmur3_hash32_i32",
  "abi-r1:metal:navatala_dataframe_murmur3_hash32_i32",
  4,
  kAbiArgs_metal_navatala_dataframe_murmur3_hash32_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_murmur3_hash32_i64[] = {
  { "keys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "seed", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "hashes", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_murmur3_hash32_i64 = {
  1,
  "navatala_dataframe_murmur3_hash32_i64",
  "metal",
  "navatala_dataframe_murmur3_hash32_i64",
  "kernel:metal:navatala_dataframe_murmur3_hash32_i64",
  "abi-r1:metal:navatala_dataframe_murmur3_hash32_i64",
  "abi-r1:metal:navatala_dataframe_murmur3_hash32_i64",
  4,
  kAbiArgs_metal_navatala_dataframe_murmur3_hash32_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_cross_join_compute_output_size[] = {
  { "leftCount", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "rightCount", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputSize", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 8, 8, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_cross_join_compute_output_size = {
  1,
  "navatala_dataframe_cross_join_compute_output_size",
  "metal",
  "navatala_dataframe_cross_join_compute_output_size",
  "kernel:metal:navatala_dataframe_cross_join_compute_output_size",
  "abi-r1:metal:navatala_dataframe_cross_join_compute_output_size",
  "abi-r1:metal:navatala_dataframe_cross_join_compute_output_size",
  3,
  kAbiArgs_metal_navatala_dataframe_cross_join_compute_output_size
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_cross_join_gather_i32[] = {
  { "leftData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightData", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftCount", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "rightCount", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputCount", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 8, 8, 256, nullptr, 0, 0 },
  { "leftOutput", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightOutput", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_cross_join_gather_i32 = {
  1,
  "navatala_dataframe_cross_join_gather_i32",
  "metal",
  "navatala_dataframe_cross_join_gather_i32",
  "kernel:metal:navatala_dataframe_cross_join_gather_i32",
  "abi-r1:metal:navatala_dataframe_cross_join_gather_i32",
  "abi-r1:metal:navatala_dataframe_cross_join_gather_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_cross_join_gather_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_hash_keys_i32[] = {
  { "keys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "tableSize", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "hashes", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_hash_keys_i32 = {
  1,
  "navatala_dataframe_hash_keys_i32",
  "metal",
  "navatala_dataframe_hash_keys_i32",
  "kernel:metal:navatala_dataframe_hash_keys_i32",
  "abi-r1:metal:navatala_dataframe_hash_keys_i32",
  "abi-r1:metal:navatala_dataframe_hash_keys_i32",
  5,
  kAbiArgs_metal_navatala_dataframe_hash_keys_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_build_hash_table_i32[] = {
  { "keys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "hashes", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "tableSize", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "tableKeys", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "tableRowIds", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_build_hash_table_i32 = {
  1,
  "navatala_dataframe_build_hash_table_i32",
  "metal",
  "navatala_dataframe_build_hash_table_i32",
  "kernel:metal:navatala_dataframe_build_hash_table_i32",
  "abi-r1:metal:navatala_dataframe_build_hash_table_i32",
  "abi-r1:metal:navatala_dataframe_build_hash_table_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_build_hash_table_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_probe_hash_table_i32[] = {
  { "rightKeys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValidity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightHashes", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightCount", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "tableKeys", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "tableRowIds", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "tableSize", 6, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "leftMatches", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightMatches", 8, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_probe_hash_table_i32 = {
  1,
  "navatala_dataframe_probe_hash_table_i32",
  "metal",
  "navatala_dataframe_probe_hash_table_i32",
  "kernel:metal:navatala_dataframe_probe_hash_table_i32",
  "abi-r1:metal:navatala_dataframe_probe_hash_table_i32",
  "abi-r1:metal:navatala_dataframe_probe_hash_table_i32",
  9,
  kAbiArgs_metal_navatala_dataframe_probe_hash_table_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gather_join_results_i32[] = {
  { "leftData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftIndices", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputCount", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gather_join_results_i32 = {
  1,
  "navatala_dataframe_gather_join_results_i32",
  "metal",
  "navatala_dataframe_gather_join_results_i32",
  "kernel:metal:navatala_dataframe_gather_join_results_i32",
  "abi-r1:metal:navatala_dataframe_gather_join_results_i32",
  "abi-r1:metal:navatala_dataframe_gather_join_results_i32",
  4,
  kAbiArgs_metal_navatala_dataframe_gather_join_results_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gather_join_results_f32[] = {
  { "leftData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftIndices", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputCount", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gather_join_results_f32 = {
  1,
  "navatala_dataframe_gather_join_results_f32",
  "metal",
  "navatala_dataframe_gather_join_results_f32",
  "kernel:metal:navatala_dataframe_gather_join_results_f32",
  "abi-r1:metal:navatala_dataframe_gather_join_results_f32",
  "abi-r1:metal:navatala_dataframe_gather_join_results_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_gather_join_results_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_mark_left_matched[] = {
  { "leftMatches", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "probeCount", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "leftMatched", 2, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_mark_left_matched = {
  1,
  "navatala_dataframe_mark_left_matched",
  "metal",
  "navatala_dataframe_mark_left_matched",
  "kernel:metal:navatala_dataframe_mark_left_matched",
  "abi-r1:metal:navatala_dataframe_mark_left_matched",
  "abi-r1:metal:navatala_dataframe_mark_left_matched",
  3,
  kAbiArgs_metal_navatala_dataframe_mark_left_matched
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_filter_semi_join[] = {
  { "leftMatches", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "semiFlag", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_filter_semi_join = {
  1,
  "navatala_dataframe_filter_semi_join",
  "metal",
  "navatala_dataframe_filter_semi_join",
  "kernel:metal:navatala_dataframe_filter_semi_join",
  "abi-r1:metal:navatala_dataframe_filter_semi_join",
  "abi-r1:metal:navatala_dataframe_filter_semi_join",
  3,
  kAbiArgs_metal_navatala_dataframe_filter_semi_join
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_filter_anti_join[] = {
  { "leftMatches", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "antiFlag", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_filter_anti_join = {
  1,
  "navatala_dataframe_filter_anti_join",
  "metal",
  "navatala_dataframe_filter_anti_join",
  "kernel:metal:navatala_dataframe_filter_anti_join",
  "abi-r1:metal:navatala_dataframe_filter_anti_join",
  "abi-r1:metal:navatala_dataframe_filter_anti_join",
  3,
  kAbiArgs_metal_navatala_dataframe_filter_anti_join
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_count_unmatched_left[] = {
  { "leftMatched", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftRowCount", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "unmatchedCount", 2, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_count_unmatched_left = {
  1,
  "navatala_dataframe_count_unmatched_left",
  "metal",
  "navatala_dataframe_count_unmatched_left",
  "kernel:metal:navatala_dataframe_count_unmatched_left",
  "abi-r1:metal:navatala_dataframe_count_unmatched_left",
  "abi-r1:metal:navatala_dataframe_count_unmatched_left",
  3,
  kAbiArgs_metal_navatala_dataframe_count_unmatched_left
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gather_unmatched_left_indices[] = {
  { "leftMatched", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftRowCount", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputOffset", 2, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "unmatchedIndices", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gather_unmatched_left_indices = {
  1,
  "navatala_dataframe_gather_unmatched_left_indices",
  "metal",
  "navatala_dataframe_gather_unmatched_left_indices",
  "kernel:metal:navatala_dataframe_gather_unmatched_left_indices",
  "abi-r1:metal:navatala_dataframe_gather_unmatched_left_indices",
  "abi-r1:metal:navatala_dataframe_gather_unmatched_left_indices",
  4,
  kAbiArgs_metal_navatala_dataframe_gather_unmatched_left_indices
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gather_left_join_i32[] = {
  { "leftData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValidity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftIndices", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputCount", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValidity", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gather_left_join_i32 = {
  1,
  "navatala_dataframe_gather_left_join_i32",
  "metal",
  "navatala_dataframe_gather_left_join_i32",
  "kernel:metal:navatala_dataframe_gather_left_join_i32",
  "abi-r1:metal:navatala_dataframe_gather_left_join_i32",
  "abi-r1:metal:navatala_dataframe_gather_left_join_i32",
  6,
  kAbiArgs_metal_navatala_dataframe_gather_left_join_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gather_left_join_f32[] = {
  { "leftData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValidity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftIndices", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputCount", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValidity", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gather_left_join_f32 = {
  1,
  "navatala_dataframe_gather_left_join_f32",
  "metal",
  "navatala_dataframe_gather_left_join_f32",
  "kernel:metal:navatala_dataframe_gather_left_join_f32",
  "abi-r1:metal:navatala_dataframe_gather_left_join_f32",
  "abi-r1:metal:navatala_dataframe_gather_left_join_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_gather_left_join_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gather_right_join_i32[] = {
  { "rightData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValidity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightIndices", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputCount", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValidity", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gather_right_join_i32 = {
  1,
  "navatala_dataframe_gather_right_join_i32",
  "metal",
  "navatala_dataframe_gather_right_join_i32",
  "kernel:metal:navatala_dataframe_gather_right_join_i32",
  "abi-r1:metal:navatala_dataframe_gather_right_join_i32",
  "abi-r1:metal:navatala_dataframe_gather_right_join_i32",
  6,
  kAbiArgs_metal_navatala_dataframe_gather_right_join_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gather_right_join_f32[] = {
  { "rightData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValidity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightIndices", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputCount", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValidity", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gather_right_join_f32 = {
  1,
  "navatala_dataframe_gather_right_join_f32",
  "metal",
  "navatala_dataframe_gather_right_join_f32",
  "kernel:metal:navatala_dataframe_gather_right_join_f32",
  "abi-r1:metal:navatala_dataframe_gather_right_join_f32",
  "abi-r1:metal:navatala_dataframe_gather_right_join_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_gather_right_join_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_count_unmatched_right[] = {
  { "rightMatched", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightRowCount", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "unmatchedCount", 2, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_count_unmatched_right = {
  1,
  "navatala_dataframe_count_unmatched_right",
  "metal",
  "navatala_dataframe_count_unmatched_right",
  "kernel:metal:navatala_dataframe_count_unmatched_right",
  "abi-r1:metal:navatala_dataframe_count_unmatched_right",
  "abi-r1:metal:navatala_dataframe_count_unmatched_right",
  3,
  kAbiArgs_metal_navatala_dataframe_count_unmatched_right
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gather_unmatched_right_indices[] = {
  { "rightMatched", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightRowCount", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputOffset", 2, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "unmatchedIndices", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gather_unmatched_right_indices = {
  1,
  "navatala_dataframe_gather_unmatched_right_indices",
  "metal",
  "navatala_dataframe_gather_unmatched_right_indices",
  "kernel:metal:navatala_dataframe_gather_unmatched_right_indices",
  "abi-r1:metal:navatala_dataframe_gather_unmatched_right_indices",
  "abi-r1:metal:navatala_dataframe_gather_unmatched_right_indices",
  4,
  kAbiArgs_metal_navatala_dataframe_gather_unmatched_right_indices
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_nested_loop_join_probe[] = {
  { "leftKeys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightKeys", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftCount", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "rightCount", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "maxMatches", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "matchCount", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "matchLeft", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "matchRight", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_nested_loop_join_probe = {
  1,
  "navatala_dataframe_nested_loop_join_probe",
  "metal",
  "navatala_dataframe_nested_loop_join_probe",
  "kernel:metal:navatala_dataframe_nested_loop_join_probe",
  "abi-r1:metal:navatala_dataframe_nested_loop_join_probe",
  "abi-r1:metal:navatala_dataframe_nested_loop_join_probe",
  8,
  kAbiArgs_metal_navatala_dataframe_nested_loop_join_probe
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_nested_loop_join_gather_i32[] = {
  { "leftData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightData", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftIndices", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightIndices", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "leftOutput", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightOutput", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_nested_loop_join_gather_i32 = {
  1,
  "navatala_dataframe_nested_loop_join_gather_i32",
  "metal",
  "navatala_dataframe_nested_loop_join_gather_i32",
  "kernel:metal:navatala_dataframe_nested_loop_join_gather_i32",
  "abi-r1:metal:navatala_dataframe_nested_loop_join_gather_i32",
  "abi-r1:metal:navatala_dataframe_nested_loop_join_gather_i32",
  7,
  kAbiArgs_metal_navatala_dataframe_nested_loop_join_gather_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_sort_merge_join_probe[] = {
  { "leftKeys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftValidity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftCount", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "rightKeys", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightValidity", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightCount", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "leftMatches", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightMatches", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_sort_merge_join_probe = {
  1,
  "navatala_dataframe_sort_merge_join_probe",
  "metal",
  "navatala_dataframe_sort_merge_join_probe",
  "kernel:metal:navatala_dataframe_sort_merge_join_probe",
  "abi-r1:metal:navatala_dataframe_sort_merge_join_probe",
  "abi-r1:metal:navatala_dataframe_sort_merge_join_probe",
  8,
  kAbiArgs_metal_navatala_dataframe_sort_merge_join_probe
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_sort_merge_join_gather_i32[] = {
  { "leftData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightData", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "leftIndices", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightIndices", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputCount", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "leftOutput", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightOutput", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 7, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_sort_merge_join_gather_i32 = {
  1,
  "navatala_dataframe_sort_merge_join_gather_i32",
  "metal",
  "navatala_dataframe_sort_merge_join_gather_i32",
  "kernel:metal:navatala_dataframe_sort_merge_join_gather_i32",
  "abi-r1:metal:navatala_dataframe_sort_merge_join_gather_i32",
  "abi-r1:metal:navatala_dataframe_sort_merge_join_gather_i32",
  8,
  kAbiArgs_metal_navatala_dataframe_sort_merge_join_gather_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_merge_step_i32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "blockSize", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_merge_step_i32 = {
  1,
  "navatala_dataframe_merge_step_i32",
  "metal",
  "navatala_dataframe_merge_step_i32",
  "kernel:metal:navatala_dataframe_merge_step_i32",
  "abi-r1:metal:navatala_dataframe_merge_step_i32",
  "abi-r1:metal:navatala_dataframe_merge_step_i32",
  4,
  kAbiArgs_metal_navatala_dataframe_merge_step_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_merge_step_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "blockSize", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_merge_step_f32 = {
  1,
  "navatala_dataframe_merge_step_f32",
  "metal",
  "navatala_dataframe_merge_step_f32",
  "kernel:metal:navatala_dataframe_merge_step_f32",
  "abi-r1:metal:navatala_dataframe_merge_step_f32",
  "abi-r1:metal:navatala_dataframe_merge_step_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_merge_step_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_merge_step_i64[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "blockSize", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_merge_step_i64 = {
  1,
  "navatala_dataframe_merge_step_i64",
  "metal",
  "navatala_dataframe_merge_step_i64",
  "kernel:metal:navatala_dataframe_merge_step_i64",
  "abi-r1:metal:navatala_dataframe_merge_step_i64",
  "abi-r1:metal:navatala_dataframe_merge_step_i64",
  4,
  kAbiArgs_metal_navatala_dataframe_merge_step_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_radix_scatter_i32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "bitOffset", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "prefixSums", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_radix_scatter_i32 = {
  1,
  "navatala_dataframe_radix_scatter_i32",
  "metal",
  "navatala_dataframe_radix_scatter_i32",
  "kernel:metal:navatala_dataframe_radix_scatter_i32",
  "abi-r1:metal:navatala_dataframe_radix_scatter_i32",
  "abi-r1:metal:navatala_dataframe_radix_scatter_i32",
  5,
  kAbiArgs_metal_navatala_dataframe_radix_scatter_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_radix_scatter_i64[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "bitOffset", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "prefixSums", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_radix_scatter_i64 = {
  1,
  "navatala_dataframe_radix_scatter_i64",
  "metal",
  "navatala_dataframe_radix_scatter_i64",
  "kernel:metal:navatala_dataframe_radix_scatter_i64",
  "abi-r1:metal:navatala_dataframe_radix_scatter_i64",
  "abi-r1:metal:navatala_dataframe_radix_scatter_i64",
  5,
  kAbiArgs_metal_navatala_dataframe_radix_scatter_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_radix_scatter_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "bitOffset", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "prefixSums", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_radix_scatter_f32 = {
  1,
  "navatala_dataframe_radix_scatter_f32",
  "metal",
  "navatala_dataframe_radix_scatter_f32",
  "kernel:metal:navatala_dataframe_radix_scatter_f32",
  "abi-r1:metal:navatala_dataframe_radix_scatter_f32",
  "abi-r1:metal:navatala_dataframe_radix_scatter_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_radix_scatter_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_len[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_len = {
  1,
  "navatala_dataframe_str_len",
  "metal",
  "navatala_dataframe_str_len",
  "kernel:metal:navatala_dataframe_str_len",
  "abi-r1:metal:navatala_dataframe_str_len",
  "abi-r1:metal:navatala_dataframe_str_len",
  5,
  kAbiArgs_metal_navatala_dataframe_str_len
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_upper_chars[] = {
  { "chars", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_upper_chars = {
  1,
  "navatala_dataframe_str_upper_chars",
  "metal",
  "navatala_dataframe_str_upper_chars",
  "kernel:metal:navatala_dataframe_str_upper_chars",
  "abi-r1:metal:navatala_dataframe_str_upper_chars",
  "abi-r1:metal:navatala_dataframe_str_upper_chars",
  3,
  kAbiArgs_metal_navatala_dataframe_str_upper_chars
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_lower_chars[] = {
  { "chars", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_lower_chars = {
  1,
  "navatala_dataframe_str_lower_chars",
  "metal",
  "navatala_dataframe_str_lower_chars",
  "kernel:metal:navatala_dataframe_str_lower_chars",
  "abi-r1:metal:navatala_dataframe_str_lower_chars",
  "abi-r1:metal:navatala_dataframe_str_lower_chars",
  3,
  kAbiArgs_metal_navatala_dataframe_str_lower_chars
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_starts_with_char[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "targetChar", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1, 1, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_starts_with_char = {
  1,
  "navatala_dataframe_str_starts_with_char",
  "metal",
  "navatala_dataframe_str_starts_with_char",
  "kernel:metal:navatala_dataframe_str_starts_with_char",
  "abi-r1:metal:navatala_dataframe_str_starts_with_char",
  "abi-r1:metal:navatala_dataframe_str_starts_with_char",
  7,
  kAbiArgs_metal_navatala_dataframe_str_starts_with_char
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_capitalize_first_char[] = {
  { "chars", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "offsets", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputChars", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_capitalize_first_char = {
  1,
  "navatala_dataframe_str_capitalize_first_char",
  "metal",
  "navatala_dataframe_str_capitalize_first_char",
  "kernel:metal:navatala_dataframe_str_capitalize_first_char",
  "abi-r1:metal:navatala_dataframe_str_capitalize_first_char",
  "abi-r1:metal:navatala_dataframe_str_capitalize_first_char",
  5,
  kAbiArgs_metal_navatala_dataframe_str_capitalize_first_char
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_title_case_char[] = {
  { "chars", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "offsets", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "prevIsSpace", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "charCount", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputChars", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_title_case_char = {
  1,
  "navatala_dataframe_str_title_case_char",
  "metal",
  "navatala_dataframe_str_title_case_char",
  "kernel:metal:navatala_dataframe_str_title_case_char",
  "abi-r1:metal:navatala_dataframe_str_title_case_char",
  "abi-r1:metal:navatala_dataframe_str_title_case_char",
  5,
  kAbiArgs_metal_navatala_dataframe_str_title_case_char
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_swap_case_char[] = {
  { "chars", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "charCount", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputChars", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_swap_case_char = {
  1,
  "navatala_dataframe_str_swap_case_char",
  "metal",
  "navatala_dataframe_str_swap_case_char",
  "kernel:metal:navatala_dataframe_str_swap_case_char",
  "abi-r1:metal:navatala_dataframe_str_swap_case_char",
  "abi-r1:metal:navatala_dataframe_str_swap_case_char",
  3,
  kAbiArgs_metal_navatala_dataframe_str_swap_case_char
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_normalize_whitespace_char[] = {
  { "chars", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "prevIsSpace", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "charCount", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "shouldKeep", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_normalize_whitespace_char = {
  1,
  "navatala_dataframe_str_normalize_whitespace_char",
  "metal",
  "navatala_dataframe_str_normalize_whitespace_char",
  "kernel:metal:navatala_dataframe_str_normalize_whitespace_char",
  "abi-r1:metal:navatala_dataframe_str_normalize_whitespace_char",
  "abi-r1:metal:navatala_dataframe_str_normalize_whitespace_char",
  4,
  kAbiArgs_metal_navatala_dataframe_str_normalize_whitespace_char
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_is_digit[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_is_digit = {
  1,
  "navatala_dataframe_str_is_digit",
  "metal",
  "navatala_dataframe_str_is_digit",
  "kernel:metal:navatala_dataframe_str_is_digit",
  "abi-r1:metal:navatala_dataframe_str_is_digit",
  "abi-r1:metal:navatala_dataframe_str_is_digit",
  6,
  kAbiArgs_metal_navatala_dataframe_str_is_digit
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_is_alpha[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_is_alpha = {
  1,
  "navatala_dataframe_str_is_alpha",
  "metal",
  "navatala_dataframe_str_is_alpha",
  "kernel:metal:navatala_dataframe_str_is_alpha",
  "abi-r1:metal:navatala_dataframe_str_is_alpha",
  "abi-r1:metal:navatala_dataframe_str_is_alpha",
  6,
  kAbiArgs_metal_navatala_dataframe_str_is_alpha
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_is_alnum[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_is_alnum = {
  1,
  "navatala_dataframe_str_is_alnum",
  "metal",
  "navatala_dataframe_str_is_alnum",
  "kernel:metal:navatala_dataframe_str_is_alnum",
  "abi-r1:metal:navatala_dataframe_str_is_alnum",
  "abi-r1:metal:navatala_dataframe_str_is_alnum",
  6,
  kAbiArgs_metal_navatala_dataframe_str_is_alnum
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_is_space[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_is_space = {
  1,
  "navatala_dataframe_str_is_space",
  "metal",
  "navatala_dataframe_str_is_space",
  "kernel:metal:navatala_dataframe_str_is_space",
  "abi-r1:metal:navatala_dataframe_str_is_space",
  "abi-r1:metal:navatala_dataframe_str_is_space",
  6,
  kAbiArgs_metal_navatala_dataframe_str_is_space
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_is_upper[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_is_upper = {
  1,
  "navatala_dataframe_str_is_upper",
  "metal",
  "navatala_dataframe_str_is_upper",
  "kernel:metal:navatala_dataframe_str_is_upper",
  "abi-r1:metal:navatala_dataframe_str_is_upper",
  "abi-r1:metal:navatala_dataframe_str_is_upper",
  6,
  kAbiArgs_metal_navatala_dataframe_str_is_upper
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_is_lower[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_is_lower = {
  1,
  "navatala_dataframe_str_is_lower",
  "metal",
  "navatala_dataframe_str_is_lower",
  "kernel:metal:navatala_dataframe_str_is_lower",
  "abi-r1:metal:navatala_dataframe_str_is_lower",
  "abi-r1:metal:navatala_dataframe_str_is_lower",
  6,
  kAbiArgs_metal_navatala_dataframe_str_is_lower
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_concat_pair_offsets[] = {
  { "offsetsA", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "offsetsB", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validityA", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validityB", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputLengths", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_concat_pair_offsets = {
  1,
  "navatala_dataframe_str_concat_pair_offsets",
  "metal",
  "navatala_dataframe_str_concat_pair_offsets",
  "kernel:metal:navatala_dataframe_str_concat_pair_offsets",
  "abi-r1:metal:navatala_dataframe_str_concat_pair_offsets",
  "abi-r1:metal:navatala_dataframe_str_concat_pair_offsets",
  7,
  kAbiArgs_metal_navatala_dataframe_str_concat_pair_offsets
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_concat_copy_chars[] = {
  { "charsA", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "charsB", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "offsetsA", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "offsetsB", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputOffsets", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputChars", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_concat_copy_chars = {
  1,
  "navatala_dataframe_str_concat_copy_chars",
  "metal",
  "navatala_dataframe_str_concat_copy_chars",
  "kernel:metal:navatala_dataframe_str_concat_copy_chars",
  "abi-r1:metal:navatala_dataframe_str_concat_copy_chars",
  "abi-r1:metal:navatala_dataframe_str_concat_copy_chars",
  7,
  kAbiArgs_metal_navatala_dataframe_str_concat_copy_chars
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_join_with_sep_offsets[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "sepLen", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "totalLength", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validCount", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_join_with_sep_offsets = {
  1,
  "navatala_dataframe_str_join_with_sep_offsets",
  "metal",
  "navatala_dataframe_str_join_with_sep_offsets",
  "kernel:metal:navatala_dataframe_str_join_with_sep_offsets",
  "abi-r1:metal:navatala_dataframe_str_join_with_sep_offsets",
  "abi-r1:metal:navatala_dataframe_str_join_with_sep_offsets",
  6,
  kAbiArgs_metal_navatala_dataframe_str_join_with_sep_offsets
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_join_copy_with_sep[] = {
  { "chars", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "offsets", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "sepChar", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1, 1, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputPositions", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputChars", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_join_copy_with_sep = {
  1,
  "navatala_dataframe_str_join_copy_with_sep",
  "metal",
  "navatala_dataframe_str_join_copy_with_sep",
  "kernel:metal:navatala_dataframe_str_join_copy_with_sep",
  "abi-r1:metal:navatala_dataframe_str_join_copy_with_sep",
  "abi-r1:metal:navatala_dataframe_str_join_copy_with_sep",
  7,
  kAbiArgs_metal_navatala_dataframe_str_join_copy_with_sep
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_compare[] = {
  { "offsetsA", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "offsetsB", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "charsA", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "charsB", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validityA", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validityB", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 6, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 8, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_compare = {
  1,
  "navatala_dataframe_str_compare",
  "metal",
  "navatala_dataframe_str_compare",
  "kernel:metal:navatala_dataframe_str_compare",
  "abi-r1:metal:navatala_dataframe_str_compare",
  "abi-r1:metal:navatala_dataframe_str_compare",
  9,
  kAbiArgs_metal_navatala_dataframe_str_compare
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_equals[] = {
  { "offsetsA", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "offsetsB", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "charsA", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "charsB", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validityA", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validityB", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 6, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 8, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_equals = {
  1,
  "navatala_dataframe_str_equals",
  "metal",
  "navatala_dataframe_str_equals",
  "kernel:metal:navatala_dataframe_str_equals",
  "abi-r1:metal:navatala_dataframe_str_equals",
  "abi-r1:metal:navatala_dataframe_str_equals",
  9,
  kAbiArgs_metal_navatala_dataframe_str_equals
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_less_than[] = {
  { "offsetsA", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "offsetsB", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "charsA", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "charsB", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validityA", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validityB", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 6, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "result", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 8, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_less_than = {
  1,
  "navatala_dataframe_str_less_than",
  "metal",
  "navatala_dataframe_str_less_than",
  "kernel:metal:navatala_dataframe_str_less_than",
  "abi-r1:metal:navatala_dataframe_str_less_than",
  "abi-r1:metal:navatala_dataframe_str_less_than",
  9,
  kAbiArgs_metal_navatala_dataframe_str_less_than
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_strip_leading_count[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "leadingSpaces", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_strip_leading_count = {
  1,
  "navatala_dataframe_str_strip_leading_count",
  "metal",
  "navatala_dataframe_str_strip_leading_count",
  "kernel:metal:navatala_dataframe_str_strip_leading_count",
  "abi-r1:metal:navatala_dataframe_str_strip_leading_count",
  "abi-r1:metal:navatala_dataframe_str_strip_leading_count",
  5,
  kAbiArgs_metal_navatala_dataframe_str_strip_leading_count
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_strip_trailing_count[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "trailingSpaces", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_strip_trailing_count = {
  1,
  "navatala_dataframe_str_strip_trailing_count",
  "metal",
  "navatala_dataframe_str_strip_trailing_count",
  "kernel:metal:navatala_dataframe_str_strip_trailing_count",
  "abi-r1:metal:navatala_dataframe_str_strip_trailing_count",
  "abi-r1:metal:navatala_dataframe_str_strip_trailing_count",
  5,
  kAbiArgs_metal_navatala_dataframe_str_strip_trailing_count
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_strip_both_count[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "leadingCounts", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "trailingCounts", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_strip_both_count = {
  1,
  "navatala_dataframe_str_strip_both_count",
  "metal",
  "navatala_dataframe_str_strip_both_count",
  "kernel:metal:navatala_dataframe_str_strip_both_count",
  "abi-r1:metal:navatala_dataframe_str_strip_both_count",
  "abi-r1:metal:navatala_dataframe_str_strip_both_count",
  6,
  kAbiArgs_metal_navatala_dataframe_str_strip_both_count
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_slice_bounds[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "sliceStart", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "sliceLen", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "newStartOffsets", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "newLengths", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 7, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_slice_bounds = {
  1,
  "navatala_dataframe_str_slice_bounds",
  "metal",
  "navatala_dataframe_str_slice_bounds",
  "kernel:metal:navatala_dataframe_str_slice_bounds",
  "abi-r1:metal:navatala_dataframe_str_slice_bounds",
  "abi-r1:metal:navatala_dataframe_str_slice_bounds",
  8,
  kAbiArgs_metal_navatala_dataframe_str_slice_bounds
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_contains_char[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "targetChar", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1, 1, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_contains_char = {
  1,
  "navatala_dataframe_str_contains_char",
  "metal",
  "navatala_dataframe_str_contains_char",
  "kernel:metal:navatala_dataframe_str_contains_char",
  "abi-r1:metal:navatala_dataframe_str_contains_char",
  "abi-r1:metal:navatala_dataframe_str_contains_char",
  7,
  kAbiArgs_metal_navatala_dataframe_str_contains_char
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_ends_with_char[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "targetChar", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1, 1, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_ends_with_char = {
  1,
  "navatala_dataframe_str_ends_with_char",
  "metal",
  "navatala_dataframe_str_ends_with_char",
  "kernel:metal:navatala_dataframe_str_ends_with_char",
  "abi-r1:metal:navatala_dataframe_str_ends_with_char",
  "abi-r1:metal:navatala_dataframe_str_ends_with_char",
  7,
  kAbiArgs_metal_navatala_dataframe_str_ends_with_char
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_pad_left_calc[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "targetWidth", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "paddingNeeded", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_pad_left_calc = {
  1,
  "navatala_dataframe_str_pad_left_calc",
  "metal",
  "navatala_dataframe_str_pad_left_calc",
  "kernel:metal:navatala_dataframe_str_pad_left_calc",
  "abi-r1:metal:navatala_dataframe_str_pad_left_calc",
  "abi-r1:metal:navatala_dataframe_str_pad_left_calc",
  5,
  kAbiArgs_metal_navatala_dataframe_str_pad_left_calc
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_pad_center[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "targetWidth", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "leftPad", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rightPad", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_pad_center = {
  1,
  "navatala_dataframe_str_pad_center",
  "metal",
  "navatala_dataframe_str_pad_center",
  "kernel:metal:navatala_dataframe_str_pad_center",
  "abi-r1:metal:navatala_dataframe_str_pad_center",
  "abi-r1:metal:navatala_dataframe_str_pad_center",
  6,
  kAbiArgs_metal_navatala_dataframe_str_pad_center
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_zfill[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "targetWidth", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "zeroCount", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "hasSign", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_zfill = {
  1,
  "navatala_dataframe_str_zfill",
  "metal",
  "navatala_dataframe_str_zfill",
  "kernel:metal:navatala_dataframe_str_zfill",
  "abi-r1:metal:navatala_dataframe_str_zfill",
  "abi-r1:metal:navatala_dataframe_str_zfill",
  7,
  kAbiArgs_metal_navatala_dataframe_str_zfill
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_wrap_bounds[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "maxWidth", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "wrapPos", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_wrap_bounds = {
  1,
  "navatala_dataframe_str_wrap_bounds",
  "metal",
  "navatala_dataframe_str_wrap_bounds",
  "kernel:metal:navatala_dataframe_str_wrap_bounds",
  "abi-r1:metal:navatala_dataframe_str_wrap_bounds",
  "abi-r1:metal:navatala_dataframe_str_wrap_bounds",
  6,
  kAbiArgs_metal_navatala_dataframe_str_wrap_bounds
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_substring_bounds[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "startIdx", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "subLen", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outStart", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outLen", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 7, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_substring_bounds = {
  1,
  "navatala_dataframe_str_substring_bounds",
  "metal",
  "navatala_dataframe_str_substring_bounds",
  "kernel:metal:navatala_dataframe_str_substring_bounds",
  "abi-r1:metal:navatala_dataframe_str_substring_bounds",
  "abi-r1:metal:navatala_dataframe_str_substring_bounds",
  8,
  kAbiArgs_metal_navatala_dataframe_str_substring_bounds
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_left_bounds[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "numChars", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outStart", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outLen", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_left_bounds = {
  1,
  "navatala_dataframe_str_left_bounds",
  "metal",
  "navatala_dataframe_str_left_bounds",
  "kernel:metal:navatala_dataframe_str_left_bounds",
  "abi-r1:metal:navatala_dataframe_str_left_bounds",
  "abi-r1:metal:navatala_dataframe_str_left_bounds",
  6,
  kAbiArgs_metal_navatala_dataframe_str_left_bounds
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_right_bounds[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "numChars", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outStart", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outLen", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_right_bounds = {
  1,
  "navatala_dataframe_str_right_bounds",
  "metal",
  "navatala_dataframe_str_right_bounds",
  "kernel:metal:navatala_dataframe_str_right_bounds",
  "abi-r1:metal:navatala_dataframe_str_right_bounds",
  "abi-r1:metal:navatala_dataframe_str_right_bounds",
  6,
  kAbiArgs_metal_navatala_dataframe_str_right_bounds
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_regex_match[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "patternOffsets", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "patternChars", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_regex_match = {
  1,
  "navatala_dataframe_str_regex_match",
  "metal",
  "navatala_dataframe_str_regex_match",
  "kernel:metal:navatala_dataframe_str_regex_match",
  "abi-r1:metal:navatala_dataframe_str_regex_match",
  "abi-r1:metal:navatala_dataframe_str_regex_match",
  8,
  kAbiArgs_metal_navatala_dataframe_str_regex_match
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_regex_extract[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "patternOffsets", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "patternChars", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "groupIndex", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 6, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputOffsets", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputChars", 8, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 9, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_regex_extract = {
  1,
  "navatala_dataframe_str_regex_extract",
  "metal",
  "navatala_dataframe_str_regex_extract",
  "kernel:metal:navatala_dataframe_str_regex_extract",
  "abi-r1:metal:navatala_dataframe_str_regex_extract",
  "abi-r1:metal:navatala_dataframe_str_regex_extract",
  10,
  kAbiArgs_metal_navatala_dataframe_str_regex_extract
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_regex_replace[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "patternOffsets", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "patternChars", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "replacementOffsets", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "replacementChars", 6, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 7, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputOffsets", 8, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputChars", 9, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 10, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_regex_replace = {
  1,
  "navatala_dataframe_str_regex_replace",
  "metal",
  "navatala_dataframe_str_regex_replace",
  "kernel:metal:navatala_dataframe_str_regex_replace",
  "abi-r1:metal:navatala_dataframe_str_regex_replace",
  "abi-r1:metal:navatala_dataframe_str_regex_replace",
  11,
  kAbiArgs_metal_navatala_dataframe_str_regex_replace
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_find_char[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "targetChar", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1, 1, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "positions", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_find_char = {
  1,
  "navatala_dataframe_str_find_char",
  "metal",
  "navatala_dataframe_str_find_char",
  "kernel:metal:navatala_dataframe_str_find_char",
  "abi-r1:metal:navatala_dataframe_str_find_char",
  "abi-r1:metal:navatala_dataframe_str_find_char",
  7,
  kAbiArgs_metal_navatala_dataframe_str_find_char
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_count_char[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "targetChar", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1, 1, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "counts", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_count_char = {
  1,
  "navatala_dataframe_str_count_char",
  "metal",
  "navatala_dataframe_str_count_char",
  "kernel:metal:navatala_dataframe_str_count_char",
  "abi-r1:metal:navatala_dataframe_str_count_char",
  "abi-r1:metal:navatala_dataframe_str_count_char",
  7,
  kAbiArgs_metal_navatala_dataframe_str_count_char
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_replace_char[] = {
  { "inChars", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "charCount", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "oldChar", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1, 1, 256, nullptr, 0, 0 },
  { "newChar", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1, 1, 256, nullptr, 0, 0 },
  { "outChars", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_replace_char = {
  1,
  "navatala_dataframe_str_replace_char",
  "metal",
  "navatala_dataframe_str_replace_char",
  "kernel:metal:navatala_dataframe_str_replace_char",
  "abi-r1:metal:navatala_dataframe_str_replace_char",
  "abi-r1:metal:navatala_dataframe_str_replace_char",
  5,
  kAbiArgs_metal_navatala_dataframe_str_replace_char
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_contains_pattern2[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "pattern1", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1, 1, 256, nullptr, 0, 0 },
  { "pattern2", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1, 1, 256, nullptr, 0, 0 },
  { "count", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "found", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 7, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_contains_pattern2 = {
  1,
  "navatala_dataframe_str_contains_pattern2",
  "metal",
  "navatala_dataframe_str_contains_pattern2",
  "kernel:metal:navatala_dataframe_str_contains_pattern2",
  "abi-r1:metal:navatala_dataframe_str_contains_pattern2",
  "abi-r1:metal:navatala_dataframe_str_contains_pattern2",
  8,
  kAbiArgs_metal_navatala_dataframe_str_contains_pattern2
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_split_count_by_char[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "delimChar", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1, 1, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "splitCounts", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_split_count_by_char = {
  1,
  "navatala_dataframe_str_split_count_by_char",
  "metal",
  "navatala_dataframe_str_split_count_by_char",
  "kernel:metal:navatala_dataframe_str_split_count_by_char",
  "abi-r1:metal:navatala_dataframe_str_split_count_by_char",
  "abi-r1:metal:navatala_dataframe_str_split_count_by_char",
  7,
  kAbiArgs_metal_navatala_dataframe_str_split_count_by_char
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_split_by_char[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "delimChar", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1, 1, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outputOffsets", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 6, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_split_by_char = {
  1,
  "navatala_dataframe_str_split_by_char",
  "metal",
  "navatala_dataframe_str_split_by_char",
  "kernel:metal:navatala_dataframe_str_split_by_char",
  "abi-r1:metal:navatala_dataframe_str_split_by_char",
  "abi-r1:metal:navatala_dataframe_str_split_by_char",
  7,
  kAbiArgs_metal_navatala_dataframe_str_split_by_char
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_split_get_element[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "delimChar", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1, 1, 256, nullptr, 0, 0 },
  { "elementIdx", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 5, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "elementStart", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "elementEnd", 7, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 8, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_split_get_element = {
  1,
  "navatala_dataframe_str_split_get_element",
  "metal",
  "navatala_dataframe_str_split_get_element",
  "kernel:metal:navatala_dataframe_str_split_get_element",
  "abi-r1:metal:navatala_dataframe_str_split_get_element",
  "abi-r1:metal:navatala_dataframe_str_split_get_element",
  9,
  kAbiArgs_metal_navatala_dataframe_str_split_get_element
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_split_by_whitespace[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "splitCounts", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_split_by_whitespace = {
  1,
  "navatala_dataframe_str_split_by_whitespace",
  "metal",
  "navatala_dataframe_str_split_by_whitespace",
  "kernel:metal:navatala_dataframe_str_split_by_whitespace",
  "abi-r1:metal:navatala_dataframe_str_split_by_whitespace",
  "abi-r1:metal:navatala_dataframe_str_split_by_whitespace",
  6,
  kAbiArgs_metal_navatala_dataframe_str_split_by_whitespace
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_reverse_chars[] = {
  { "inChars", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inOffsets", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "charCount", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "stringCount", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outChars", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_reverse_chars = {
  1,
  "navatala_dataframe_str_reverse_chars",
  "metal",
  "navatala_dataframe_str_reverse_chars",
  "kernel:metal:navatala_dataframe_str_reverse_chars",
  "abi-r1:metal:navatala_dataframe_str_reverse_chars",
  "abi-r1:metal:navatala_dataframe_str_reverse_chars",
  5,
  kAbiArgs_metal_navatala_dataframe_str_reverse_chars
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_truncate_bounds[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "maxLen", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "newLengths", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_truncate_bounds = {
  1,
  "navatala_dataframe_str_truncate_bounds",
  "metal",
  "navatala_dataframe_str_truncate_bounds",
  "kernel:metal:navatala_dataframe_str_truncate_bounds",
  "abi-r1:metal:navatala_dataframe_str_truncate_bounds",
  "abi-r1:metal:navatala_dataframe_str_truncate_bounds",
  6,
  kAbiArgs_metal_navatala_dataframe_str_truncate_bounds
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_pad_right_calc[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "targetLen", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "padLengths", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_pad_right_calc = {
  1,
  "navatala_dataframe_str_pad_right_calc",
  "metal",
  "navatala_dataframe_str_pad_right_calc",
  "kernel:metal:navatala_dataframe_str_pad_right_calc",
  "abi-r1:metal:navatala_dataframe_str_pad_right_calc",
  "abi-r1:metal:navatala_dataframe_str_pad_right_calc",
  6,
  kAbiArgs_metal_navatala_dataframe_str_pad_right_calc
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_repeat_bounds[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "repeatCount", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "newLengths", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_repeat_bounds = {
  1,
  "navatala_dataframe_str_repeat_bounds",
  "metal",
  "navatala_dataframe_str_repeat_bounds",
  "kernel:metal:navatala_dataframe_str_repeat_bounds",
  "abi-r1:metal:navatala_dataframe_str_repeat_bounds",
  "abi-r1:metal:navatala_dataframe_str_repeat_bounds",
  6,
  kAbiArgs_metal_navatala_dataframe_str_repeat_bounds
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_translate_char[] = {
  { "chars", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "srcChar", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1, 1, 256, nullptr, 0, 0 },
  { "dstChar", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 1, 1, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_translate_char = {
  1,
  "navatala_dataframe_str_translate_char",
  "metal",
  "navatala_dataframe_str_translate_char",
  "kernel:metal:navatala_dataframe_str_translate_char",
  "abi-r1:metal:navatala_dataframe_str_translate_char",
  "abi-r1:metal:navatala_dataframe_str_translate_char",
  5,
  kAbiArgs_metal_navatala_dataframe_str_translate_char
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_translate_table[] = {
  { "chars", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "table", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 256, 256, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_translate_table = {
  1,
  "navatala_dataframe_str_translate_table",
  "metal",
  "navatala_dataframe_str_translate_table",
  "kernel:metal:navatala_dataframe_str_translate_table",
  "abi-r1:metal:navatala_dataframe_str_translate_table",
  "abi-r1:metal:navatala_dataframe_str_translate_table",
  4,
  kAbiArgs_metal_navatala_dataframe_str_translate_table
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_str_normalize[] = {
  { "offsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "chars", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "validity", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "normForm", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 4, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "outOffsets", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outChars", 6, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outValidity", 7, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_str_normalize = {
  1,
  "navatala_dataframe_str_normalize",
  "metal",
  "navatala_dataframe_str_normalize",
  "kernel:metal:navatala_dataframe_str_normalize",
  "abi-r1:metal:navatala_dataframe_str_normalize",
  "abi-r1:metal:navatala_dataframe_str_normalize",
  8,
  kAbiArgs_metal_navatala_dataframe_str_normalize
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_negate_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_negate_f32 = {
  1,
  "navatala_dataframe_negate_f32",
  "metal",
  "navatala_dataframe_negate_f32",
  "kernel:metal:navatala_dataframe_negate_f32",
  "abi-r1:metal:navatala_dataframe_negate_f32",
  "abi-r1:metal:navatala_dataframe_negate_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_negate_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_negate_i32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_negate_i32 = {
  1,
  "navatala_dataframe_negate_i32",
  "metal",
  "navatala_dataframe_negate_i32",
  "kernel:metal:navatala_dataframe_negate_i32",
  "abi-r1:metal:navatala_dataframe_negate_i32",
  "abi-r1:metal:navatala_dataframe_negate_i32",
  5,
  kAbiArgs_metal_navatala_dataframe_negate_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_abs_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_abs_f32 = {
  1,
  "navatala_dataframe_abs_f32",
  "metal",
  "navatala_dataframe_abs_f32",
  "kernel:metal:navatala_dataframe_abs_f32",
  "abi-r1:metal:navatala_dataframe_abs_f32",
  "abi-r1:metal:navatala_dataframe_abs_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_abs_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_abs_i32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_abs_i32 = {
  1,
  "navatala_dataframe_abs_i32",
  "metal",
  "navatala_dataframe_abs_i32",
  "kernel:metal:navatala_dataframe_abs_i32",
  "abi-r1:metal:navatala_dataframe_abs_i32",
  "abi-r1:metal:navatala_dataframe_abs_i32",
  5,
  kAbiArgs_metal_navatala_dataframe_abs_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_ceil_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_ceil_f32 = {
  1,
  "navatala_dataframe_ceil_f32",
  "metal",
  "navatala_dataframe_ceil_f32",
  "kernel:metal:navatala_dataframe_ceil_f32",
  "abi-r1:metal:navatala_dataframe_ceil_f32",
  "abi-r1:metal:navatala_dataframe_ceil_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_ceil_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_floor_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_floor_f32 = {
  1,
  "navatala_dataframe_floor_f32",
  "metal",
  "navatala_dataframe_floor_f32",
  "kernel:metal:navatala_dataframe_floor_f32",
  "abi-r1:metal:navatala_dataframe_floor_f32",
  "abi-r1:metal:navatala_dataframe_floor_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_floor_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_round_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_round_f32 = {
  1,
  "navatala_dataframe_round_f32",
  "metal",
  "navatala_dataframe_round_f32",
  "kernel:metal:navatala_dataframe_round_f32",
  "abi-r1:metal:navatala_dataframe_round_f32",
  "abi-r1:metal:navatala_dataframe_round_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_round_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_trunc_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_trunc_f32 = {
  1,
  "navatala_dataframe_trunc_f32",
  "metal",
  "navatala_dataframe_trunc_f32",
  "kernel:metal:navatala_dataframe_trunc_f32",
  "abi-r1:metal:navatala_dataframe_trunc_f32",
  "abi-r1:metal:navatala_dataframe_trunc_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_trunc_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_negate_i64[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_negate_i64 = {
  1,
  "navatala_dataframe_negate_i64",
  "metal",
  "navatala_dataframe_negate_i64",
  "kernel:metal:navatala_dataframe_negate_i64",
  "abi-r1:metal:navatala_dataframe_negate_i64",
  "abi-r1:metal:navatala_dataframe_negate_i64",
  5,
  kAbiArgs_metal_navatala_dataframe_negate_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_abs_i64[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_abs_i64 = {
  1,
  "navatala_dataframe_abs_i64",
  "metal",
  "navatala_dataframe_abs_i64",
  "kernel:metal:navatala_dataframe_abs_i64",
  "abi-r1:metal:navatala_dataframe_abs_i64",
  "abi-r1:metal:navatala_dataframe_abs_i64",
  5,
  kAbiArgs_metal_navatala_dataframe_abs_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_sqrt_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_sqrt_f32 = {
  1,
  "navatala_dataframe_sqrt_f32",
  "metal",
  "navatala_dataframe_sqrt_f32",
  "kernel:metal:navatala_dataframe_sqrt_f32",
  "abi-r1:metal:navatala_dataframe_sqrt_f32",
  "abi-r1:metal:navatala_dataframe_sqrt_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_sqrt_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_exp_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_exp_f32 = {
  1,
  "navatala_dataframe_exp_f32",
  "metal",
  "navatala_dataframe_exp_f32",
  "kernel:metal:navatala_dataframe_exp_f32",
  "abi-r1:metal:navatala_dataframe_exp_f32",
  "abi-r1:metal:navatala_dataframe_exp_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_exp_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_log_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_log_f32 = {
  1,
  "navatala_dataframe_log_f32",
  "metal",
  "navatala_dataframe_log_f32",
  "kernel:metal:navatala_dataframe_log_f32",
  "abi-r1:metal:navatala_dataframe_log_f32",
  "abi-r1:metal:navatala_dataframe_log_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_log_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_sin_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_sin_f32 = {
  1,
  "navatala_dataframe_sin_f32",
  "metal",
  "navatala_dataframe_sin_f32",
  "kernel:metal:navatala_dataframe_sin_f32",
  "abi-r1:metal:navatala_dataframe_sin_f32",
  "abi-r1:metal:navatala_dataframe_sin_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_sin_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_cos_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_cos_f32 = {
  1,
  "navatala_dataframe_cos_f32",
  "metal",
  "navatala_dataframe_cos_f32",
  "kernel:metal:navatala_dataframe_cos_f32",
  "abi-r1:metal:navatala_dataframe_cos_f32",
  "abi-r1:metal:navatala_dataframe_cos_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_cos_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_tan_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_tan_f32 = {
  1,
  "navatala_dataframe_tan_f32",
  "metal",
  "navatala_dataframe_tan_f32",
  "kernel:metal:navatala_dataframe_tan_f32",
  "abi-r1:metal:navatala_dataframe_tan_f32",
  "abi-r1:metal:navatala_dataframe_tan_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_tan_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_log10_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "inputValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputValid", 4, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_log10_f32 = {
  1,
  "navatala_dataframe_log10_f32",
  "metal",
  "navatala_dataframe_log10_f32",
  "kernel:metal:navatala_dataframe_log10_f32",
  "abi-r1:metal:navatala_dataframe_log10_f32",
  "abi-r1:metal:navatala_dataframe_log10_f32",
  5,
  kAbiArgs_metal_navatala_dataframe_log10_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_compute_concat_offsets[] = {
  { "lengths", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "numColumns", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "offsets", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_compute_concat_offsets = {
  1,
  "navatala_dataframe_compute_concat_offsets",
  "metal",
  "navatala_dataframe_compute_concat_offsets",
  "kernel:metal:navatala_dataframe_compute_concat_offsets",
  "abi-r1:metal:navatala_dataframe_compute_concat_offsets",
  "abi-r1:metal:navatala_dataframe_compute_concat_offsets",
  3,
  kAbiArgs_metal_navatala_dataframe_compute_concat_offsets
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gather_concat_i32[] = {
  { "src", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "srcValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "srcLen", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "destOffset", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "dest", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "destValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gather_concat_i32 = {
  1,
  "navatala_dataframe_gather_concat_i32",
  "metal",
  "navatala_dataframe_gather_concat_i32",
  "kernel:metal:navatala_dataframe_gather_concat_i32",
  "abi-r1:metal:navatala_dataframe_gather_concat_i32",
  "abi-r1:metal:navatala_dataframe_gather_concat_i32",
  6,
  kAbiArgs_metal_navatala_dataframe_gather_concat_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gather_concat_f32[] = {
  { "src", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "srcValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "srcLen", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "destOffset", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "dest", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "destValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gather_concat_f32 = {
  1,
  "navatala_dataframe_gather_concat_f32",
  "metal",
  "navatala_dataframe_gather_concat_f32",
  "kernel:metal:navatala_dataframe_gather_concat_f32",
  "abi-r1:metal:navatala_dataframe_gather_concat_f32",
  "abi-r1:metal:navatala_dataframe_gather_concat_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_gather_concat_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gather_concat_i64[] = {
  { "src", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "srcValid", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "srcLen", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "destOffset", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "dest", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "destValid", 5, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gather_concat_i64 = {
  1,
  "navatala_dataframe_gather_concat_i64",
  "metal",
  "navatala_dataframe_gather_concat_i64",
  "kernel:metal:navatala_dataframe_gather_concat_i64",
  "abi-r1:metal:navatala_dataframe_gather_concat_i64",
  "abi-r1:metal:navatala_dataframe_gather_concat_i64",
  6,
  kAbiArgs_metal_navatala_dataframe_gather_concat_i64
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_hash_partition_i32[] = {
  { "keys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "numPartitions", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "partitionIds", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_hash_partition_i32 = {
  1,
  "navatala_dataframe_hash_partition_i32",
  "metal",
  "navatala_dataframe_hash_partition_i32",
  "kernel:metal:navatala_dataframe_hash_partition_i32",
  "abi-r1:metal:navatala_dataframe_hash_partition_i32",
  "abi-r1:metal:navatala_dataframe_hash_partition_i32",
  4,
  kAbiArgs_metal_navatala_dataframe_hash_partition_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_hash_partition_f32[] = {
  { "keys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "numPartitions", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "partitionIds", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_hash_partition_f32 = {
  1,
  "navatala_dataframe_hash_partition_f32",
  "metal",
  "navatala_dataframe_hash_partition_f32",
  "kernel:metal:navatala_dataframe_hash_partition_f32",
  "abi-r1:metal:navatala_dataframe_hash_partition_f32",
  "abi-r1:metal:navatala_dataframe_hash_partition_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_hash_partition_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_round_robin_partition[] = {
  { "numPartitions", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "partitionIds", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_round_robin_partition = {
  1,
  "navatala_dataframe_round_robin_partition",
  "metal",
  "navatala_dataframe_round_robin_partition",
  "kernel:metal:navatala_dataframe_round_robin_partition",
  "abi-r1:metal:navatala_dataframe_round_robin_partition",
  "abi-r1:metal:navatala_dataframe_round_robin_partition",
  3,
  kAbiArgs_metal_navatala_dataframe_round_robin_partition
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_range_partition_bounds[] = {
  { "sortedData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "dataCount", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "numPartitions", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "bounds", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_range_partition_bounds = {
  1,
  "navatala_dataframe_range_partition_bounds",
  "metal",
  "navatala_dataframe_range_partition_bounds",
  "kernel:metal:navatala_dataframe_range_partition_bounds",
  "abi-r1:metal:navatala_dataframe_range_partition_bounds",
  "abi-r1:metal:navatala_dataframe_range_partition_bounds",
  4,
  kAbiArgs_metal_navatala_dataframe_range_partition_bounds
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_range_partition_assign[] = {
  { "keys", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "bounds", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "numPartitions", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 3, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "partitionIds", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_range_partition_assign = {
  1,
  "navatala_dataframe_range_partition_assign",
  "metal",
  "navatala_dataframe_range_partition_assign",
  "kernel:metal:navatala_dataframe_range_partition_assign",
  "abi-r1:metal:navatala_dataframe_range_partition_assign",
  "abi-r1:metal:navatala_dataframe_range_partition_assign",
  5,
  kAbiArgs_metal_navatala_dataframe_range_partition_assign
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gather_partition_i32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "partitionIds", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "partitionOffsets", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputIndices", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gather_partition_i32 = {
  1,
  "navatala_dataframe_gather_partition_i32",
  "metal",
  "navatala_dataframe_gather_partition_i32",
  "kernel:metal:navatala_dataframe_gather_partition_i32",
  "abi-r1:metal:navatala_dataframe_gather_partition_i32",
  "abi-r1:metal:navatala_dataframe_gather_partition_i32",
  6,
  kAbiArgs_metal_navatala_dataframe_gather_partition_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_gather_partition_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "partitionIds", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "partitionOffsets", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "output", 4, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "outputIndices", 5, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_gather_partition_f32 = {
  1,
  "navatala_dataframe_gather_partition_f32",
  "metal",
  "navatala_dataframe_gather_partition_f32",
  "kernel:metal:navatala_dataframe_gather_partition_f32",
  "abi-r1:metal:navatala_dataframe_gather_partition_f32",
  "abi-r1:metal:navatala_dataframe_gather_partition_f32",
  6,
  kAbiArgs_metal_navatala_dataframe_gather_partition_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_compute_partition_offsets[] = {
  { "partitionIds", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "numPartitions", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "partitionCounts", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_compute_partition_offsets = {
  1,
  "navatala_dataframe_compute_partition_offsets",
  "metal",
  "navatala_dataframe_compute_partition_offsets",
  "kernel:metal:navatala_dataframe_compute_partition_offsets",
  "abi-r1:metal:navatala_dataframe_compute_partition_offsets",
  "abi-r1:metal:navatala_dataframe_compute_partition_offsets",
  4,
  kAbiArgs_metal_navatala_dataframe_compute_partition_offsets
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_transpose_i32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rows", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "cols", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_transpose_i32 = {
  1,
  "navatala_dataframe_transpose_i32",
  "metal",
  "navatala_dataframe_transpose_i32",
  "kernel:metal:navatala_dataframe_transpose_i32",
  "abi-r1:metal:navatala_dataframe_transpose_i32",
  "abi-r1:metal:navatala_dataframe_transpose_i32",
  4,
  kAbiArgs_metal_navatala_dataframe_transpose_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_transpose_f32[] = {
  { "input", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "rows", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "cols", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_transpose_f32 = {
  1,
  "navatala_dataframe_transpose_f32",
  "metal",
  "navatala_dataframe_transpose_f32",
  "kernel:metal:navatala_dataframe_transpose_f32",
  "abi-r1:metal:navatala_dataframe_transpose_f32",
  "abi-r1:metal:navatala_dataframe_transpose_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_transpose_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_flatten_offsets[] = {
  { "outerOffsets", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "innerOffsets", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_flatten_offsets = {
  1,
  "navatala_dataframe_flatten_offsets",
  "metal",
  "navatala_dataframe_flatten_offsets",
  "kernel:metal:navatala_dataframe_flatten_offsets",
  "abi-r1:metal:navatala_dataframe_flatten_offsets",
  "abi-r1:metal:navatala_dataframe_flatten_offsets",
  4,
  kAbiArgs_metal_navatala_dataframe_flatten_offsets
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_unflatten_copy[] = {
  { "flatData", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "offsets", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "output", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_unflatten_copy = {
  1,
  "navatala_dataframe_unflatten_copy",
  "metal",
  "navatala_dataframe_unflatten_copy",
  "kernel:metal:navatala_dataframe_unflatten_copy",
  "abi-r1:metal:navatala_dataframe_unflatten_copy",
  "abi-r1:metal:navatala_dataframe_unflatten_copy",
  4,
  kAbiArgs_metal_navatala_dataframe_unflatten_copy
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_compute_split_bounds[] = {
  { "totalRows", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "numSplits", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "splitStarts", 2, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "splitEnds", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_compute_split_bounds = {
  1,
  "navatala_dataframe_compute_split_bounds",
  "metal",
  "navatala_dataframe_compute_split_bounds",
  "kernel:metal:navatala_dataframe_compute_split_bounds",
  "abi-r1:metal:navatala_dataframe_compute_split_bounds",
  "abi-r1:metal:navatala_dataframe_compute_split_bounds",
  4,
  kAbiArgs_metal_navatala_dataframe_compute_split_bounds
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_split_copy_i32[] = {
  { "src", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "srcOffset", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "dst", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_split_copy_i32 = {
  1,
  "navatala_dataframe_split_copy_i32",
  "metal",
  "navatala_dataframe_split_copy_i32",
  "kernel:metal:navatala_dataframe_split_copy_i32",
  "abi-r1:metal:navatala_dataframe_split_copy_i32",
  "abi-r1:metal:navatala_dataframe_split_copy_i32",
  4,
  kAbiArgs_metal_navatala_dataframe_split_copy_i32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_split_copy_f32[] = {
  { "src", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "srcOffset", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "dst", 3, KernelArgumentRole::Output, KernelAccessMode::WriteOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_split_copy_f32 = {
  1,
  "navatala_dataframe_split_copy_f32",
  "metal",
  "navatala_dataframe_split_copy_f32",
  "kernel:metal:navatala_dataframe_split_copy_f32",
  "abi-r1:metal:navatala_dataframe_split_copy_f32",
  "abi-r1:metal:navatala_dataframe_split_copy_f32",
  4,
  kAbiArgs_metal_navatala_dataframe_split_copy_f32
};

const KernelArgumentInfo kAbiArgs_metal_navatala_dataframe_split_copy_validity[] = {
  { "srcValidity", 0, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 },
  { "srcOffset", 1, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "count", 2, KernelArgumentRole::Input, KernelAccessMode::ReadOnly, GpuRuntime::MemoryKind::Device, true, 4, 4, 256, nullptr, 0, 0 },
  { "dstValidity", 3, KernelArgumentRole::InputOutput, KernelAccessMode::ReadWrite, GpuRuntime::MemoryKind::Device, true, 0, 0, 256, nullptr, 0, 0 }
};
const KernelAbiManifestInfo kAbiManifest_metal_navatala_dataframe_split_copy_validity = {
  1,
  "navatala_dataframe_split_copy_validity",
  "metal",
  "navatala_dataframe_split_copy_validity",
  "kernel:metal:navatala_dataframe_split_copy_validity",
  "abi-r1:metal:navatala_dataframe_split_copy_validity",
  "abi-r1:metal:navatala_dataframe_split_copy_validity",
  4,
  kAbiArgs_metal_navatala_dataframe_split_copy_validity
};

bool tryGetKernelAbiManifest_metal_dataframe(const std::string& backend, const std::string& kernelName, const KernelAbiManifestInfo*& out) {
  if (backend == "metal" && kernelName == "navatala_dataframe_writeback_sum_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_writeback_sum_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_writeback_sum_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_writeback_sum_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_writeback_sum_u32") {
    out = &kAbiManifest_metal_navatala_dataframe_writeback_sum_u32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_writeback_min_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_writeback_min_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_writeback_max_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_writeback_max_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_welford_state_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_init_welford_state_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_welford_update_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_welford_update_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_welford_merge_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_welford_merge_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_variance_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_variance_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_sample_variance_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_sample_variance_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_stddev_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_stddev_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_covariance_state_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_init_covariance_state_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_covariance_update_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_covariance_update_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_covariance_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_covariance_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_correlation_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_correlation_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_weighted_welford_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_init_weighted_welford_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_weighted_welford_update_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_weighted_welford_update_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_weighted_welford_merge_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_weighted_welford_merge_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_weighted_variance_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_weighted_variance_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_weighted_reliability_variance_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_weighted_reliability_variance_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_weighted_stddev_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_weighted_stddev_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_weighted_covariance_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_init_weighted_covariance_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_weighted_covariance_update_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_weighted_covariance_update_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_weighted_covariance_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_weighted_covariance_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_weighted_correlation_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_weighted_correlation_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_weighted_mean_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_init_weighted_mean_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_weighted_mean_update_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_weighted_mean_update_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_p2_state_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_init_p2_state_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_p2_update_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_p2_update_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_p2_extract_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_p2_extract_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_reservoir256_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_init_reservoir256_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_reservoir_update256_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_reservoir_update256_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_reservoir_min256_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_reservoir_min256_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_reservoir_max256_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_reservoir_max256_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_trimmed_mean_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_trimmed_mean_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_iqm_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_iqm_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_winsorized_mean_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_winsorized_mean_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mad_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_mad_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_normalized_mad_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_normalized_mad_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_robust_z_score_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_robust_z_score_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_detect_outliers_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_detect_outliers_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_t_digest_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_init_t_digest_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tdigest_add_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_tdigest_add_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tdigest_merge_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_tdigest_merge_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tdigest_quantile_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_tdigest_quantile_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tdigest_cdf_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_tdigest_cdf_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tdigest_mean_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_tdigest_mean_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tdigest_min_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_tdigest_min_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tdigest_max_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_tdigest_max_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tdigest_reset_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_tdigest_reset_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_inclusive_scan_sum_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_inclusive_scan_sum_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_inclusive_scan_sum_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_inclusive_scan_sum_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_inclusive_scan_sum_i32_to_i64_skip_nulls") {
    out = &kAbiManifest_metal_navatala_dataframe_inclusive_scan_sum_i32_to_i64_skip_nulls;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_inclusive_scan_sum_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_inclusive_scan_sum_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_reduce_sum_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_reduce_sum_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_reduce_sum_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_reduce_sum_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_reduce_sum_i32_to_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_reduce_sum_i32_to_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_reduce_min_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_reduce_min_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_reduce_max_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_reduce_max_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_count_valid") {
    out = &kAbiManifest_metal_navatala_dataframe_count_valid;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_gather_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_gather_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_gather_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_scatter_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_scatter_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_scatter_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_scatter_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compact_by_mask_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_compact_by_mask_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compact_by_mask_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_compact_by_mask_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compact_valid_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_compact_valid_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_constant_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_fill_constant_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_constant_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_fill_constant_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_constant_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_fill_constant_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_range_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_fill_range_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_range_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_fill_range_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_validity_all_valid") {
    out = &kAbiManifest_metal_navatala_dataframe_fill_validity_all_valid;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_validity_all_null") {
    out = &kAbiManifest_metal_navatala_dataframe_fill_validity_all_null;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_null_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_fill_null_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_null_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_fill_null_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_is_null") {
    out = &kAbiManifest_metal_navatala_dataframe_is_null;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_is_valid") {
    out = &kAbiManifest_metal_navatala_dataframe_is_valid;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_copy_if_valid_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_copy_if_valid_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_coalesce_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_coalesce_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_arg_max_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_arg_max_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_arg_min_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_arg_min_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_scan_sum_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_scan_sum_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_scan_sum_u32") {
    out = &kAbiManifest_metal_navatala_dataframe_scan_sum_u32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_scan_exclusive_write_total_u32") {
    out = &kAbiManifest_metal_navatala_dataframe_scan_exclusive_write_total_u32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_scan_max_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_scan_max_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_scan_min_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_scan_min_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_histogram_fast_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_histogram_fast_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_zero_bins") {
    out = &kAbiManifest_metal_navatala_dataframe_zero_bins;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compute_bin_indices_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_compute_bin_indices_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sum_reduce_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_sum_reduce_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mean_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_mean_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_variance_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_variance_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_stddev_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_stddev_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_normalize_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_normalize_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_rng_state") {
    out = &kAbiManifest_metal_navatala_dataframe_init_rng_state;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_uniform_lcg_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_uniform_lcg_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_normal_box_muller_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_normal_box_muller_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_indices") {
    out = &kAbiManifest_metal_navatala_dataframe_init_indices;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_covariance_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_covariance_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_center_data_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_center_data_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_center_matrix_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_center_matrix_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_means_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_column_means_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_correlation_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_correlation_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_cov_to_correlation_matrix_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_cov_to_correlation_matrix_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_quantile_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_quantile_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_quantile_batch_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_quantile_batch_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_median_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_median_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_iqr_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_iqr_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_e_w_m_c_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_init_e_w_m_c_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_e_w_m_c_from_halflife_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_init_e_w_m_c_from_halflife_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_ewmc_update_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_ewmc_update_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_ewmc_extract_covariance_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_ewmc_extract_covariance_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_ewmc_extract_correlation_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_ewmc_extract_correlation_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_m_c_d_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_init_m_c_d_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mcd_compute_center_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_mcd_compute_center_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mcd_compute_covariance_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_mcd_compute_covariance_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mcd_mahalanobis_distance_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_mcd_mahalanobis_distance_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mcd_detect_outliers_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_mcd_detect_outliers_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mcd_extract_mean_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_mcd_extract_mean_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_moments_state_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_init_moments_state_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_moments_update_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_moments_update_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_moments_merge_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_moments_merge_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_skewness_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_skewness_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_kurtosis_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_kurtosis_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_excess_kurtosis_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_excess_kurtosis_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_contingency_matrix_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_contingency_matrix_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_silhouette_score_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_silhouette_score_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_adjusted_rand_index_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_adjusted_rand_index_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_normalized_mutual_info_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_normalized_mutual_info_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_homogeneity_score_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_homogeneity_score_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_completeness_score_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_completeness_score_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_v_measure_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_v_measure_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_r2_score_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_r2_score_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mse_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_mse_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_rmse_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_rmse_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mae_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_mae_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mape_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_mape_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_explained_variance_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_explained_variance_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_entropy_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_entropy_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_cross_entropy_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_cross_entropy_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_kl_divergence_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_kl_divergence_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mutual_information_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_mutual_information_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_conditional_entropy_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_conditional_entropy_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_permute_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_permute_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_shuffle_rows_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_shuffle_rows_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sample_without_replacement_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_sample_without_replacement_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sample_with_replacement_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_sample_with_replacement_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bootstrap_sample_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_bootstrap_sample_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_select_top_k_per_row_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_select_top_k_per_row_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_select_top_k_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_select_top_k_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_arg_top_k_per_row_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_arg_top_k_per_row_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_arg_top_k_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_arg_top_k_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_partial_sort_per_row_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_partial_sort_per_row_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_sum_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_group_by_sum_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_sum_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_group_by_sum_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_count") {
    out = &kAbiManifest_metal_navatala_dataframe_group_by_count;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_min_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_group_by_min_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_max_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_group_by_max_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_sum_for_mean_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_group_by_sum_for_mean_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_nunique_helper_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_group_by_nunique_helper_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_nunique_helper_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_group_by_nunique_helper_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_sorted_sum") {
    out = &kAbiManifest_metal_navatala_dataframe_group_by_sorted_sum;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_sorted_count") {
    out = &kAbiManifest_metal_navatala_dataframe_group_by_sorted_count;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_sum_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_column_sum_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_sum_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_column_sum_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_sum_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_column_sum_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_min_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_column_min_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_max_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_column_max_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_count") {
    out = &kAbiManifest_metal_navatala_dataframe_column_count;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_mean_helper_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_column_mean_helper_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_variance_helper_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_column_variance_helper_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_arg_min_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_column_arg_min_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_arg_max_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_column_arg_max_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_arg_min_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_column_arg_min_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_arg_max_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_column_arg_max_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_std_helper") {
    out = &kAbiManifest_metal_navatala_dataframe_column_std_helper;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_product_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_column_product_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_product_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_column_product_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_rolling_sum_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_rolling_sum_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_rolling_count") {
    out = &kAbiManifest_metal_navatala_dataframe_rolling_count;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_rolling_min_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_rolling_min_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_rolling_max_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_rolling_max_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_rolling_mean_helper_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_rolling_mean_helper_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_rolling_variance_helper_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_rolling_variance_helper_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_expanding_sum_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_expanding_sum_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_expanding_count_valid") {
    out = &kAbiManifest_metal_navatala_dataframe_expanding_count_valid;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compute_quantile_indices") {
    out = &kAbiManifest_metal_navatala_dataframe_compute_quantile_indices;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_select_quantile_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_select_quantile_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_select_quantile_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_select_quantile_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_batch_quantile_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_batch_quantile_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compute_median_indices") {
    out = &kAbiManifest_metal_navatala_dataframe_compute_median_indices;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_select_median_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_select_median_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_add_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_add_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_add_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_add_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sub_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_sub_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mul_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_mul_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_div_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_div_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mod_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_mod_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sub_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_sub_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mul_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_mul_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_div_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_div_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_add_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_add_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sub_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_sub_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mul_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_mul_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_div_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_div_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mod_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_mod_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_eq_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_eq_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_eq_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_eq_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_lt_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_lt_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gt_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_gt_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_le_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_le_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_ge_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_ge_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_ne_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_ne_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_ne_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_ne_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_lt_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_lt_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gt_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_gt_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_le_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_le_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_ge_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_ge_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bitwise_and_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_bitwise_and_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bitwise_and_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_bitwise_and_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bitwise_or_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_bitwise_or_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bitwise_or_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_bitwise_or_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bitwise_xor_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_bitwise_xor_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bitwise_xor_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_bitwise_xor_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bitwise_not_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_bitwise_not_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bitwise_not_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_bitwise_not_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_hour_from_seconds") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_hour_from_seconds;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_minute_from_seconds") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_minute_from_seconds;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_second_from_seconds") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_second_from_seconds;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_day_of_week_from_seconds") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_day_of_week_from_seconds;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_year_from_seconds") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_year_from_seconds;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_month_from_seconds") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_month_from_seconds;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_day_from_seconds") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_day_from_seconds;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_day_of_year_from_seconds") {
    out = &kAbiManifest_metal_navatala_dataframe_extract_day_of_year_from_seconds;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_add_days_to_timestamp") {
    out = &kAbiManifest_metal_navatala_dataframe_add_days_to_timestamp;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_add_months_to_timestamp") {
    out = &kAbiManifest_metal_navatala_dataframe_add_months_to_timestamp;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_subtract_timestamps") {
    out = &kAbiManifest_metal_navatala_dataframe_subtract_timestamps;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_timestamp_to_date_parts") {
    out = &kAbiManifest_metal_navatala_dataframe_timestamp_to_date_parts;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_date_parts_to_timestamp") {
    out = &kAbiManifest_metal_navatala_dataframe_date_parts_to_timestamp;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_convert_timestamp_precision") {
    out = &kAbiManifest_metal_navatala_dataframe_convert_timestamp_precision;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_combine_hash_values") {
    out = &kAbiManifest_metal_navatala_dataframe_combine_hash_values;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_hash_column_row") {
    out = &kAbiManifest_metal_navatala_dataframe_hash_column_row;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_md5_hash") {
    out = &kAbiManifest_metal_navatala_dataframe_md5_hash;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sha256_hash") {
    out = &kAbiManifest_metal_navatala_dataframe_sha256_hash;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_identity_hash_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_identity_hash_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_identity_hash_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_identity_hash_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_murmur3_hash32_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_murmur3_hash32_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_murmur3_hash32_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_murmur3_hash32_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_cross_join_compute_output_size") {
    out = &kAbiManifest_metal_navatala_dataframe_cross_join_compute_output_size;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_cross_join_gather_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_cross_join_gather_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_hash_keys_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_hash_keys_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_build_hash_table_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_build_hash_table_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_probe_hash_table_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_probe_hash_table_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_join_results_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_gather_join_results_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_join_results_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_gather_join_results_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mark_left_matched") {
    out = &kAbiManifest_metal_navatala_dataframe_mark_left_matched;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_filter_semi_join") {
    out = &kAbiManifest_metal_navatala_dataframe_filter_semi_join;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_filter_anti_join") {
    out = &kAbiManifest_metal_navatala_dataframe_filter_anti_join;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_count_unmatched_left") {
    out = &kAbiManifest_metal_navatala_dataframe_count_unmatched_left;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_unmatched_left_indices") {
    out = &kAbiManifest_metal_navatala_dataframe_gather_unmatched_left_indices;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_left_join_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_gather_left_join_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_left_join_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_gather_left_join_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_right_join_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_gather_right_join_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_right_join_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_gather_right_join_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_count_unmatched_right") {
    out = &kAbiManifest_metal_navatala_dataframe_count_unmatched_right;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_unmatched_right_indices") {
    out = &kAbiManifest_metal_navatala_dataframe_gather_unmatched_right_indices;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_nested_loop_join_probe") {
    out = &kAbiManifest_metal_navatala_dataframe_nested_loop_join_probe;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_nested_loop_join_gather_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_nested_loop_join_gather_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sort_merge_join_probe") {
    out = &kAbiManifest_metal_navatala_dataframe_sort_merge_join_probe;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sort_merge_join_gather_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_sort_merge_join_gather_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_merge_step_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_merge_step_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_merge_step_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_merge_step_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_merge_step_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_merge_step_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_radix_scatter_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_radix_scatter_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_radix_scatter_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_radix_scatter_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_radix_scatter_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_radix_scatter_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_len") {
    out = &kAbiManifest_metal_navatala_dataframe_str_len;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_upper_chars") {
    out = &kAbiManifest_metal_navatala_dataframe_str_upper_chars;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_lower_chars") {
    out = &kAbiManifest_metal_navatala_dataframe_str_lower_chars;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_starts_with_char") {
    out = &kAbiManifest_metal_navatala_dataframe_str_starts_with_char;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_capitalize_first_char") {
    out = &kAbiManifest_metal_navatala_dataframe_str_capitalize_first_char;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_title_case_char") {
    out = &kAbiManifest_metal_navatala_dataframe_str_title_case_char;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_swap_case_char") {
    out = &kAbiManifest_metal_navatala_dataframe_str_swap_case_char;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_normalize_whitespace_char") {
    out = &kAbiManifest_metal_navatala_dataframe_str_normalize_whitespace_char;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_is_digit") {
    out = &kAbiManifest_metal_navatala_dataframe_str_is_digit;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_is_alpha") {
    out = &kAbiManifest_metal_navatala_dataframe_str_is_alpha;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_is_alnum") {
    out = &kAbiManifest_metal_navatala_dataframe_str_is_alnum;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_is_space") {
    out = &kAbiManifest_metal_navatala_dataframe_str_is_space;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_is_upper") {
    out = &kAbiManifest_metal_navatala_dataframe_str_is_upper;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_is_lower") {
    out = &kAbiManifest_metal_navatala_dataframe_str_is_lower;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_concat_pair_offsets") {
    out = &kAbiManifest_metal_navatala_dataframe_str_concat_pair_offsets;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_concat_copy_chars") {
    out = &kAbiManifest_metal_navatala_dataframe_str_concat_copy_chars;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_join_with_sep_offsets") {
    out = &kAbiManifest_metal_navatala_dataframe_str_join_with_sep_offsets;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_join_copy_with_sep") {
    out = &kAbiManifest_metal_navatala_dataframe_str_join_copy_with_sep;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_compare") {
    out = &kAbiManifest_metal_navatala_dataframe_str_compare;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_equals") {
    out = &kAbiManifest_metal_navatala_dataframe_str_equals;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_less_than") {
    out = &kAbiManifest_metal_navatala_dataframe_str_less_than;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_strip_leading_count") {
    out = &kAbiManifest_metal_navatala_dataframe_str_strip_leading_count;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_strip_trailing_count") {
    out = &kAbiManifest_metal_navatala_dataframe_str_strip_trailing_count;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_strip_both_count") {
    out = &kAbiManifest_metal_navatala_dataframe_str_strip_both_count;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_slice_bounds") {
    out = &kAbiManifest_metal_navatala_dataframe_str_slice_bounds;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_contains_char") {
    out = &kAbiManifest_metal_navatala_dataframe_str_contains_char;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_ends_with_char") {
    out = &kAbiManifest_metal_navatala_dataframe_str_ends_with_char;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_pad_left_calc") {
    out = &kAbiManifest_metal_navatala_dataframe_str_pad_left_calc;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_pad_center") {
    out = &kAbiManifest_metal_navatala_dataframe_str_pad_center;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_zfill") {
    out = &kAbiManifest_metal_navatala_dataframe_str_zfill;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_wrap_bounds") {
    out = &kAbiManifest_metal_navatala_dataframe_str_wrap_bounds;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_substring_bounds") {
    out = &kAbiManifest_metal_navatala_dataframe_str_substring_bounds;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_left_bounds") {
    out = &kAbiManifest_metal_navatala_dataframe_str_left_bounds;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_right_bounds") {
    out = &kAbiManifest_metal_navatala_dataframe_str_right_bounds;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_regex_match") {
    out = &kAbiManifest_metal_navatala_dataframe_str_regex_match;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_regex_extract") {
    out = &kAbiManifest_metal_navatala_dataframe_str_regex_extract;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_regex_replace") {
    out = &kAbiManifest_metal_navatala_dataframe_str_regex_replace;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_find_char") {
    out = &kAbiManifest_metal_navatala_dataframe_str_find_char;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_count_char") {
    out = &kAbiManifest_metal_navatala_dataframe_str_count_char;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_replace_char") {
    out = &kAbiManifest_metal_navatala_dataframe_str_replace_char;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_contains_pattern2") {
    out = &kAbiManifest_metal_navatala_dataframe_str_contains_pattern2;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_split_count_by_char") {
    out = &kAbiManifest_metal_navatala_dataframe_str_split_count_by_char;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_split_by_char") {
    out = &kAbiManifest_metal_navatala_dataframe_str_split_by_char;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_split_get_element") {
    out = &kAbiManifest_metal_navatala_dataframe_str_split_get_element;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_split_by_whitespace") {
    out = &kAbiManifest_metal_navatala_dataframe_str_split_by_whitespace;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_reverse_chars") {
    out = &kAbiManifest_metal_navatala_dataframe_str_reverse_chars;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_truncate_bounds") {
    out = &kAbiManifest_metal_navatala_dataframe_str_truncate_bounds;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_pad_right_calc") {
    out = &kAbiManifest_metal_navatala_dataframe_str_pad_right_calc;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_repeat_bounds") {
    out = &kAbiManifest_metal_navatala_dataframe_str_repeat_bounds;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_translate_char") {
    out = &kAbiManifest_metal_navatala_dataframe_str_translate_char;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_translate_table") {
    out = &kAbiManifest_metal_navatala_dataframe_str_translate_table;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_normalize") {
    out = &kAbiManifest_metal_navatala_dataframe_str_normalize;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_negate_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_negate_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_negate_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_negate_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_abs_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_abs_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_abs_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_abs_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_ceil_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_ceil_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_floor_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_floor_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_round_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_round_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_trunc_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_trunc_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_negate_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_negate_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_abs_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_abs_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sqrt_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_sqrt_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_exp_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_exp_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_log_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_log_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sin_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_sin_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_cos_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_cos_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tan_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_tan_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_log10_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_log10_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compute_concat_offsets") {
    out = &kAbiManifest_metal_navatala_dataframe_compute_concat_offsets;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_concat_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_gather_concat_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_concat_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_gather_concat_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_concat_i64") {
    out = &kAbiManifest_metal_navatala_dataframe_gather_concat_i64;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_hash_partition_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_hash_partition_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_hash_partition_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_hash_partition_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_round_robin_partition") {
    out = &kAbiManifest_metal_navatala_dataframe_round_robin_partition;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_range_partition_bounds") {
    out = &kAbiManifest_metal_navatala_dataframe_range_partition_bounds;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_range_partition_assign") {
    out = &kAbiManifest_metal_navatala_dataframe_range_partition_assign;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_partition_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_gather_partition_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_partition_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_gather_partition_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compute_partition_offsets") {
    out = &kAbiManifest_metal_navatala_dataframe_compute_partition_offsets;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_transpose_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_transpose_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_transpose_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_transpose_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_flatten_offsets") {
    out = &kAbiManifest_metal_navatala_dataframe_flatten_offsets;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_unflatten_copy") {
    out = &kAbiManifest_metal_navatala_dataframe_unflatten_copy;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compute_split_bounds") {
    out = &kAbiManifest_metal_navatala_dataframe_compute_split_bounds;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_split_copy_i32") {
    out = &kAbiManifest_metal_navatala_dataframe_split_copy_i32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_split_copy_f32") {
    out = &kAbiManifest_metal_navatala_dataframe_split_copy_f32;
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_split_copy_validity") {
    out = &kAbiManifest_metal_navatala_dataframe_split_copy_validity;
    return true;
  }
  out = nullptr;
  return false;
}

bool tryGetKernelSource_metal_dataframe(const std::string& backend, const std::string& kernelName, GpuRuntime::ProgramSource& out) {
  if (backend == "metal" && kernelName == "navatala_dataframe_writeback_sum_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_writeback_sum_f32";
    std::string_view sv(k_metal_navatala_dataframe_writeback_sum_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_writeback_sum_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_writeback_sum_i32";
    std::string_view sv(k_metal_navatala_dataframe_writeback_sum_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_writeback_sum_u32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_writeback_sum_u32";
    std::string_view sv(k_metal_navatala_dataframe_writeback_sum_u32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_writeback_min_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_writeback_min_f32";
    std::string_view sv(k_metal_navatala_dataframe_writeback_min_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_writeback_max_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_writeback_max_f32";
    std::string_view sv(k_metal_navatala_dataframe_writeback_max_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_welford_state_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_init_welford_state_f32";
    std::string_view sv(k_metal_navatala_dataframe_init_welford_state_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_welford_update_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_welford_update_f32";
    std::string_view sv(k_metal_navatala_dataframe_welford_update_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_welford_merge_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_welford_merge_f32";
    std::string_view sv(k_metal_navatala_dataframe_welford_merge_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_variance_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_variance_f32";
    std::string_view sv(k_metal_navatala_dataframe_extract_variance_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_sample_variance_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_sample_variance_f32";
    std::string_view sv(k_metal_navatala_dataframe_extract_sample_variance_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_stddev_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_stddev_f32";
    std::string_view sv(k_metal_navatala_dataframe_extract_stddev_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_covariance_state_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_init_covariance_state_f32";
    std::string_view sv(k_metal_navatala_dataframe_init_covariance_state_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_covariance_update_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_covariance_update_f32";
    std::string_view sv(k_metal_navatala_dataframe_covariance_update_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_covariance_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_covariance_f32";
    std::string_view sv(k_metal_navatala_dataframe_extract_covariance_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_correlation_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_correlation_f32";
    std::string_view sv(k_metal_navatala_dataframe_extract_correlation_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_weighted_welford_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_init_weighted_welford_f32";
    std::string_view sv(k_metal_navatala_dataframe_init_weighted_welford_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_weighted_welford_update_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_weighted_welford_update_f32";
    std::string_view sv(k_metal_navatala_dataframe_weighted_welford_update_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_weighted_welford_merge_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_weighted_welford_merge_f32";
    std::string_view sv(k_metal_navatala_dataframe_weighted_welford_merge_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_weighted_variance_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_weighted_variance_f32";
    std::string_view sv(k_metal_navatala_dataframe_extract_weighted_variance_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_weighted_reliability_variance_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_weighted_reliability_variance_f32";
    std::string_view sv(k_metal_navatala_dataframe_extract_weighted_reliability_variance_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_weighted_stddev_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_weighted_stddev_f32";
    std::string_view sv(k_metal_navatala_dataframe_extract_weighted_stddev_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_weighted_covariance_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_init_weighted_covariance_f32";
    std::string_view sv(k_metal_navatala_dataframe_init_weighted_covariance_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_weighted_covariance_update_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_weighted_covariance_update_f32";
    std::string_view sv(k_metal_navatala_dataframe_weighted_covariance_update_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_weighted_covariance_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_weighted_covariance_f32";
    std::string_view sv(k_metal_navatala_dataframe_extract_weighted_covariance_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_weighted_correlation_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_weighted_correlation_f32";
    std::string_view sv(k_metal_navatala_dataframe_extract_weighted_correlation_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_weighted_mean_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_init_weighted_mean_f32";
    std::string_view sv(k_metal_navatala_dataframe_init_weighted_mean_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_weighted_mean_update_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_weighted_mean_update_f32";
    std::string_view sv(k_metal_navatala_dataframe_weighted_mean_update_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_p2_state_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_init_p2_state_f32";
    std::string_view sv(k_metal_navatala_dataframe_init_p2_state_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_p2_update_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_p2_update_f32";
    std::string_view sv(k_metal_navatala_dataframe_p2_update_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_p2_extract_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_p2_extract_f32";
    std::string_view sv(k_metal_navatala_dataframe_p2_extract_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_reservoir256_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_init_reservoir256_f32";
    std::string_view sv(k_metal_navatala_dataframe_init_reservoir256_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_reservoir_update256_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_reservoir_update256_f32";
    std::string_view sv(k_metal_navatala_dataframe_reservoir_update256_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_reservoir_min256_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_reservoir_min256_f32";
    std::string_view sv(k_metal_navatala_dataframe_reservoir_min256_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_reservoir_max256_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_reservoir_max256_f32";
    std::string_view sv(k_metal_navatala_dataframe_reservoir_max256_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_trimmed_mean_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_trimmed_mean_f32";
    std::string_view sv(k_metal_navatala_dataframe_trimmed_mean_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_iqm_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_iqm_f32";
    std::string_view sv(k_metal_navatala_dataframe_iqm_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_winsorized_mean_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_winsorized_mean_f32";
    std::string_view sv(k_metal_navatala_dataframe_winsorized_mean_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mad_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_mad_f32";
    std::string_view sv(k_metal_navatala_dataframe_mad_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_normalized_mad_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_normalized_mad_f32";
    std::string_view sv(k_metal_navatala_dataframe_normalized_mad_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_robust_z_score_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_robust_z_score_f32";
    std::string_view sv(k_metal_navatala_dataframe_robust_z_score_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_detect_outliers_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_detect_outliers_f32";
    std::string_view sv(k_metal_navatala_dataframe_detect_outliers_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_t_digest_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_init_t_digest_f32";
    std::string_view sv(k_metal_navatala_dataframe_init_t_digest_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tdigest_add_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_tdigest_add_f32";
    std::string_view sv(k_metal_navatala_dataframe_tdigest_add_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tdigest_merge_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_tdigest_merge_f32";
    std::string_view sv(k_metal_navatala_dataframe_tdigest_merge_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tdigest_quantile_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_tdigest_quantile_f32";
    std::string_view sv(k_metal_navatala_dataframe_tdigest_quantile_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tdigest_cdf_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_tdigest_cdf_f32";
    std::string_view sv(k_metal_navatala_dataframe_tdigest_cdf_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tdigest_mean_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_tdigest_mean_f32";
    std::string_view sv(k_metal_navatala_dataframe_tdigest_mean_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tdigest_min_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_tdigest_min_f32";
    std::string_view sv(k_metal_navatala_dataframe_tdigest_min_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tdigest_max_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_tdigest_max_f32";
    std::string_view sv(k_metal_navatala_dataframe_tdigest_max_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tdigest_reset_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_tdigest_reset_f32";
    std::string_view sv(k_metal_navatala_dataframe_tdigest_reset_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_inclusive_scan_sum_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_inclusive_scan_sum_f32";
    std::string_view sv(k_metal_navatala_dataframe_inclusive_scan_sum_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_inclusive_scan_sum_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_inclusive_scan_sum_i32";
    std::string_view sv(k_metal_navatala_dataframe_inclusive_scan_sum_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_inclusive_scan_sum_i32_to_i64_skip_nulls") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_inclusive_scan_sum_i32_to_i64_skip_nulls";
    std::string_view sv(k_metal_navatala_dataframe_inclusive_scan_sum_i32_to_i64_skip_nulls);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_inclusive_scan_sum_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_inclusive_scan_sum_i64";
    std::string_view sv(k_metal_navatala_dataframe_inclusive_scan_sum_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_reduce_sum_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_reduce_sum_f32";
    std::string_view sv(k_metal_navatala_dataframe_reduce_sum_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_reduce_sum_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_reduce_sum_i32";
    std::string_view sv(k_metal_navatala_dataframe_reduce_sum_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_reduce_sum_i32_to_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_reduce_sum_i32_to_i64";
    std::string_view sv(k_metal_navatala_dataframe_reduce_sum_i32_to_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_reduce_min_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_reduce_min_i32";
    std::string_view sv(k_metal_navatala_dataframe_reduce_min_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_reduce_max_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_reduce_max_i32";
    std::string_view sv(k_metal_navatala_dataframe_reduce_max_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_count_valid") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_count_valid";
    std::string_view sv(k_metal_navatala_dataframe_count_valid);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gather_f32";
    std::string_view sv(k_metal_navatala_dataframe_gather_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gather_i32";
    std::string_view sv(k_metal_navatala_dataframe_gather_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gather_i64";
    std::string_view sv(k_metal_navatala_dataframe_gather_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_scatter_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_scatter_f32";
    std::string_view sv(k_metal_navatala_dataframe_scatter_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_scatter_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_scatter_i32";
    std::string_view sv(k_metal_navatala_dataframe_scatter_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compact_by_mask_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_compact_by_mask_f32";
    std::string_view sv(k_metal_navatala_dataframe_compact_by_mask_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compact_by_mask_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_compact_by_mask_i32";
    std::string_view sv(k_metal_navatala_dataframe_compact_by_mask_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compact_valid_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_compact_valid_f32";
    std::string_view sv(k_metal_navatala_dataframe_compact_valid_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_constant_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_fill_constant_f32";
    std::string_view sv(k_metal_navatala_dataframe_fill_constant_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_constant_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_fill_constant_i32";
    std::string_view sv(k_metal_navatala_dataframe_fill_constant_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_constant_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_fill_constant_i64";
    std::string_view sv(k_metal_navatala_dataframe_fill_constant_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_range_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_fill_range_i32";
    std::string_view sv(k_metal_navatala_dataframe_fill_range_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_range_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_fill_range_i64";
    std::string_view sv(k_metal_navatala_dataframe_fill_range_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_validity_all_valid") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_fill_validity_all_valid";
    std::string_view sv(k_metal_navatala_dataframe_fill_validity_all_valid);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_validity_all_null") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_fill_validity_all_null";
    std::string_view sv(k_metal_navatala_dataframe_fill_validity_all_null);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_null_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_fill_null_f32";
    std::string_view sv(k_metal_navatala_dataframe_fill_null_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_fill_null_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_fill_null_i32";
    std::string_view sv(k_metal_navatala_dataframe_fill_null_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_is_null") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_is_null";
    std::string_view sv(k_metal_navatala_dataframe_is_null);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_is_valid") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_is_valid";
    std::string_view sv(k_metal_navatala_dataframe_is_valid);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_copy_if_valid_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_copy_if_valid_f32";
    std::string_view sv(k_metal_navatala_dataframe_copy_if_valid_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_coalesce_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_coalesce_f32";
    std::string_view sv(k_metal_navatala_dataframe_coalesce_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_arg_max_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_arg_max_f32";
    std::string_view sv(k_metal_navatala_dataframe_arg_max_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_arg_min_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_arg_min_f32";
    std::string_view sv(k_metal_navatala_dataframe_arg_min_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_scan_sum_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_scan_sum_f32";
    std::string_view sv(k_metal_navatala_dataframe_scan_sum_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_scan_sum_u32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_scan_sum_u32";
    std::string_view sv(k_metal_navatala_dataframe_scan_sum_u32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_scan_exclusive_write_total_u32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_scan_exclusive_write_total_u32";
    std::string_view sv(k_metal_navatala_dataframe_scan_exclusive_write_total_u32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_scan_max_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_scan_max_f32";
    std::string_view sv(k_metal_navatala_dataframe_scan_max_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_scan_min_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_scan_min_f32";
    std::string_view sv(k_metal_navatala_dataframe_scan_min_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_histogram_fast_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_histogram_fast_f32";
    std::string_view sv(k_metal_navatala_dataframe_histogram_fast_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_zero_bins") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_zero_bins";
    std::string_view sv(k_metal_navatala_dataframe_zero_bins);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compute_bin_indices_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_compute_bin_indices_f32";
    std::string_view sv(k_metal_navatala_dataframe_compute_bin_indices_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sum_reduce_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_sum_reduce_f32";
    std::string_view sv(k_metal_navatala_dataframe_sum_reduce_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mean_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_mean_f32";
    std::string_view sv(k_metal_navatala_dataframe_mean_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_variance_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_variance_f32";
    std::string_view sv(k_metal_navatala_dataframe_variance_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_stddev_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_stddev_f32";
    std::string_view sv(k_metal_navatala_dataframe_stddev_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_normalize_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_normalize_f32";
    std::string_view sv(k_metal_navatala_dataframe_normalize_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_rng_state") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_init_rng_state";
    std::string_view sv(k_metal_navatala_dataframe_init_rng_state);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_uniform_lcg_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_uniform_lcg_f32";
    std::string_view sv(k_metal_navatala_dataframe_uniform_lcg_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_normal_box_muller_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_normal_box_muller_f32";
    std::string_view sv(k_metal_navatala_dataframe_normal_box_muller_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_indices") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_init_indices";
    std::string_view sv(k_metal_navatala_dataframe_init_indices);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_covariance_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_covariance_f32";
    std::string_view sv(k_metal_navatala_dataframe_covariance_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_center_data_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_center_data_f32";
    std::string_view sv(k_metal_navatala_dataframe_center_data_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_center_matrix_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_center_matrix_f32";
    std::string_view sv(k_metal_navatala_dataframe_center_matrix_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_means_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_column_means_f32";
    std::string_view sv(k_metal_navatala_dataframe_column_means_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_correlation_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_correlation_f32";
    std::string_view sv(k_metal_navatala_dataframe_correlation_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_cov_to_correlation_matrix_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_cov_to_correlation_matrix_f32";
    std::string_view sv(k_metal_navatala_dataframe_cov_to_correlation_matrix_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_quantile_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_quantile_f32";
    std::string_view sv(k_metal_navatala_dataframe_quantile_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_quantile_batch_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_quantile_batch_f32";
    std::string_view sv(k_metal_navatala_dataframe_quantile_batch_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_median_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_median_f32";
    std::string_view sv(k_metal_navatala_dataframe_median_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_iqr_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_iqr_f32";
    std::string_view sv(k_metal_navatala_dataframe_iqr_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_e_w_m_c_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_init_e_w_m_c_f32";
    std::string_view sv(k_metal_navatala_dataframe_init_e_w_m_c_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_e_w_m_c_from_halflife_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_init_e_w_m_c_from_halflife_f32";
    std::string_view sv(k_metal_navatala_dataframe_init_e_w_m_c_from_halflife_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_ewmc_update_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_ewmc_update_f32";
    std::string_view sv(k_metal_navatala_dataframe_ewmc_update_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_ewmc_extract_covariance_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_ewmc_extract_covariance_f32";
    std::string_view sv(k_metal_navatala_dataframe_ewmc_extract_covariance_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_ewmc_extract_correlation_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_ewmc_extract_correlation_f32";
    std::string_view sv(k_metal_navatala_dataframe_ewmc_extract_correlation_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_m_c_d_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_init_m_c_d_f32";
    std::string_view sv(k_metal_navatala_dataframe_init_m_c_d_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mcd_compute_center_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_mcd_compute_center_f32";
    std::string_view sv(k_metal_navatala_dataframe_mcd_compute_center_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mcd_compute_covariance_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_mcd_compute_covariance_f32";
    std::string_view sv(k_metal_navatala_dataframe_mcd_compute_covariance_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mcd_mahalanobis_distance_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_mcd_mahalanobis_distance_f32";
    std::string_view sv(k_metal_navatala_dataframe_mcd_mahalanobis_distance_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mcd_detect_outliers_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_mcd_detect_outliers_f32";
    std::string_view sv(k_metal_navatala_dataframe_mcd_detect_outliers_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mcd_extract_mean_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_mcd_extract_mean_f32";
    std::string_view sv(k_metal_navatala_dataframe_mcd_extract_mean_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_init_moments_state_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_init_moments_state_f32";
    std::string_view sv(k_metal_navatala_dataframe_init_moments_state_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_moments_update_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_moments_update_f32";
    std::string_view sv(k_metal_navatala_dataframe_moments_update_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_moments_merge_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_moments_merge_f32";
    std::string_view sv(k_metal_navatala_dataframe_moments_merge_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_skewness_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_skewness_f32";
    std::string_view sv(k_metal_navatala_dataframe_extract_skewness_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_kurtosis_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_kurtosis_f32";
    std::string_view sv(k_metal_navatala_dataframe_extract_kurtosis_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_excess_kurtosis_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_excess_kurtosis_f32";
    std::string_view sv(k_metal_navatala_dataframe_extract_excess_kurtosis_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_contingency_matrix_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_contingency_matrix_f32";
    std::string_view sv(k_metal_navatala_dataframe_contingency_matrix_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_silhouette_score_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_silhouette_score_f32";
    std::string_view sv(k_metal_navatala_dataframe_silhouette_score_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_adjusted_rand_index_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_adjusted_rand_index_f32";
    std::string_view sv(k_metal_navatala_dataframe_adjusted_rand_index_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_normalized_mutual_info_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_normalized_mutual_info_f32";
    std::string_view sv(k_metal_navatala_dataframe_normalized_mutual_info_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_homogeneity_score_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_homogeneity_score_f32";
    std::string_view sv(k_metal_navatala_dataframe_homogeneity_score_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_completeness_score_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_completeness_score_f32";
    std::string_view sv(k_metal_navatala_dataframe_completeness_score_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_v_measure_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_v_measure_f32";
    std::string_view sv(k_metal_navatala_dataframe_v_measure_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_r2_score_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_r2_score_f32";
    std::string_view sv(k_metal_navatala_dataframe_r2_score_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mse_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_mse_f32";
    std::string_view sv(k_metal_navatala_dataframe_mse_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_rmse_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_rmse_f32";
    std::string_view sv(k_metal_navatala_dataframe_rmse_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mae_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_mae_f32";
    std::string_view sv(k_metal_navatala_dataframe_mae_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mape_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_mape_f32";
    std::string_view sv(k_metal_navatala_dataframe_mape_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_explained_variance_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_explained_variance_f32";
    std::string_view sv(k_metal_navatala_dataframe_explained_variance_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_entropy_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_entropy_f32";
    std::string_view sv(k_metal_navatala_dataframe_entropy_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_cross_entropy_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_cross_entropy_f32";
    std::string_view sv(k_metal_navatala_dataframe_cross_entropy_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_kl_divergence_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_kl_divergence_f32";
    std::string_view sv(k_metal_navatala_dataframe_kl_divergence_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mutual_information_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_mutual_information_f32";
    std::string_view sv(k_metal_navatala_dataframe_mutual_information_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_conditional_entropy_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_conditional_entropy_f32";
    std::string_view sv(k_metal_navatala_dataframe_conditional_entropy_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_permute_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_permute_f32";
    std::string_view sv(k_metal_navatala_dataframe_permute_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_shuffle_rows_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_shuffle_rows_f32";
    std::string_view sv(k_metal_navatala_dataframe_shuffle_rows_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sample_without_replacement_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_sample_without_replacement_f32";
    std::string_view sv(k_metal_navatala_dataframe_sample_without_replacement_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sample_with_replacement_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_sample_with_replacement_f32";
    std::string_view sv(k_metal_navatala_dataframe_sample_with_replacement_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bootstrap_sample_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_bootstrap_sample_f32";
    std::string_view sv(k_metal_navatala_dataframe_bootstrap_sample_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_select_top_k_per_row_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_select_top_k_per_row_f32";
    std::string_view sv(k_metal_navatala_dataframe_select_top_k_per_row_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_select_top_k_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_select_top_k_f32";
    std::string_view sv(k_metal_navatala_dataframe_select_top_k_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_arg_top_k_per_row_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_arg_top_k_per_row_f32";
    std::string_view sv(k_metal_navatala_dataframe_arg_top_k_per_row_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_arg_top_k_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_arg_top_k_f32";
    std::string_view sv(k_metal_navatala_dataframe_arg_top_k_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_partial_sort_per_row_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_partial_sort_per_row_f32";
    std::string_view sv(k_metal_navatala_dataframe_partial_sort_per_row_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_sum_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_group_by_sum_i32";
    std::string_view sv(k_metal_navatala_dataframe_group_by_sum_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_sum_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_group_by_sum_i64";
    std::string_view sv(k_metal_navatala_dataframe_group_by_sum_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_count") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_group_by_count";
    std::string_view sv(k_metal_navatala_dataframe_group_by_count);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_min_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_group_by_min_i32";
    std::string_view sv(k_metal_navatala_dataframe_group_by_min_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_max_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_group_by_max_i32";
    std::string_view sv(k_metal_navatala_dataframe_group_by_max_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_sum_for_mean_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_group_by_sum_for_mean_f32";
    std::string_view sv(k_metal_navatala_dataframe_group_by_sum_for_mean_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_nunique_helper_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_group_by_nunique_helper_i32";
    std::string_view sv(k_metal_navatala_dataframe_group_by_nunique_helper_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_nunique_helper_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_group_by_nunique_helper_i64";
    std::string_view sv(k_metal_navatala_dataframe_group_by_nunique_helper_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_sorted_sum") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_group_by_sorted_sum";
    std::string_view sv(k_metal_navatala_dataframe_group_by_sorted_sum);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_group_by_sorted_count") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_group_by_sorted_count";
    std::string_view sv(k_metal_navatala_dataframe_group_by_sorted_count);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_sum_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_column_sum_i32";
    std::string_view sv(k_metal_navatala_dataframe_column_sum_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_sum_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_column_sum_i64";
    std::string_view sv(k_metal_navatala_dataframe_column_sum_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_sum_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_column_sum_f32";
    std::string_view sv(k_metal_navatala_dataframe_column_sum_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_min_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_column_min_i32";
    std::string_view sv(k_metal_navatala_dataframe_column_min_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_max_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_column_max_i32";
    std::string_view sv(k_metal_navatala_dataframe_column_max_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_count") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_column_count";
    std::string_view sv(k_metal_navatala_dataframe_column_count);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_mean_helper_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_column_mean_helper_f32";
    std::string_view sv(k_metal_navatala_dataframe_column_mean_helper_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_variance_helper_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_column_variance_helper_f32";
    std::string_view sv(k_metal_navatala_dataframe_column_variance_helper_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_arg_min_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_column_arg_min_i32";
    std::string_view sv(k_metal_navatala_dataframe_column_arg_min_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_arg_max_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_column_arg_max_i32";
    std::string_view sv(k_metal_navatala_dataframe_column_arg_max_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_arg_min_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_column_arg_min_f32";
    std::string_view sv(k_metal_navatala_dataframe_column_arg_min_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_arg_max_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_column_arg_max_f32";
    std::string_view sv(k_metal_navatala_dataframe_column_arg_max_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_std_helper") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_column_std_helper";
    std::string_view sv(k_metal_navatala_dataframe_column_std_helper);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_product_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_column_product_i32";
    std::string_view sv(k_metal_navatala_dataframe_column_product_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_column_product_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_column_product_f32";
    std::string_view sv(k_metal_navatala_dataframe_column_product_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_rolling_sum_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_rolling_sum_f32";
    std::string_view sv(k_metal_navatala_dataframe_rolling_sum_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_rolling_count") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_rolling_count";
    std::string_view sv(k_metal_navatala_dataframe_rolling_count);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_rolling_min_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_rolling_min_i32";
    std::string_view sv(k_metal_navatala_dataframe_rolling_min_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_rolling_max_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_rolling_max_i32";
    std::string_view sv(k_metal_navatala_dataframe_rolling_max_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_rolling_mean_helper_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_rolling_mean_helper_f32";
    std::string_view sv(k_metal_navatala_dataframe_rolling_mean_helper_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_rolling_variance_helper_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_rolling_variance_helper_f32";
    std::string_view sv(k_metal_navatala_dataframe_rolling_variance_helper_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_expanding_sum_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_expanding_sum_f32";
    std::string_view sv(k_metal_navatala_dataframe_expanding_sum_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_expanding_count_valid") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_expanding_count_valid";
    std::string_view sv(k_metal_navatala_dataframe_expanding_count_valid);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compute_quantile_indices") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_compute_quantile_indices";
    std::string_view sv(k_metal_navatala_dataframe_compute_quantile_indices);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_select_quantile_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_select_quantile_f32";
    std::string_view sv(k_metal_navatala_dataframe_select_quantile_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_select_quantile_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_select_quantile_i32";
    std::string_view sv(k_metal_navatala_dataframe_select_quantile_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_batch_quantile_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_batch_quantile_f32";
    std::string_view sv(k_metal_navatala_dataframe_batch_quantile_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compute_median_indices") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_compute_median_indices";
    std::string_view sv(k_metal_navatala_dataframe_compute_median_indices);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_select_median_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_select_median_f32";
    std::string_view sv(k_metal_navatala_dataframe_select_median_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_add_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_add_f32";
    std::string_view sv(k_metal_navatala_dataframe_add_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_add_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_add_i32";
    std::string_view sv(k_metal_navatala_dataframe_add_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sub_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_sub_f32";
    std::string_view sv(k_metal_navatala_dataframe_sub_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mul_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_mul_f32";
    std::string_view sv(k_metal_navatala_dataframe_mul_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_div_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_div_f32";
    std::string_view sv(k_metal_navatala_dataframe_div_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mod_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_mod_i32";
    std::string_view sv(k_metal_navatala_dataframe_mod_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sub_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_sub_i32";
    std::string_view sv(k_metal_navatala_dataframe_sub_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mul_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_mul_i32";
    std::string_view sv(k_metal_navatala_dataframe_mul_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_div_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_div_i32";
    std::string_view sv(k_metal_navatala_dataframe_div_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_add_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_add_i64";
    std::string_view sv(k_metal_navatala_dataframe_add_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sub_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_sub_i64";
    std::string_view sv(k_metal_navatala_dataframe_sub_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mul_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_mul_i64";
    std::string_view sv(k_metal_navatala_dataframe_mul_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_div_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_div_i64";
    std::string_view sv(k_metal_navatala_dataframe_div_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mod_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_mod_i64";
    std::string_view sv(k_metal_navatala_dataframe_mod_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_eq_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_eq_f32";
    std::string_view sv(k_metal_navatala_dataframe_eq_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_eq_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_eq_i32";
    std::string_view sv(k_metal_navatala_dataframe_eq_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_lt_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_lt_f32";
    std::string_view sv(k_metal_navatala_dataframe_lt_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gt_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gt_f32";
    std::string_view sv(k_metal_navatala_dataframe_gt_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_le_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_le_f32";
    std::string_view sv(k_metal_navatala_dataframe_le_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_ge_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_ge_f32";
    std::string_view sv(k_metal_navatala_dataframe_ge_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_ne_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_ne_f32";
    std::string_view sv(k_metal_navatala_dataframe_ne_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_ne_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_ne_i32";
    std::string_view sv(k_metal_navatala_dataframe_ne_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_lt_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_lt_i32";
    std::string_view sv(k_metal_navatala_dataframe_lt_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gt_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gt_i32";
    std::string_view sv(k_metal_navatala_dataframe_gt_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_le_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_le_i32";
    std::string_view sv(k_metal_navatala_dataframe_le_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_ge_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_ge_i32";
    std::string_view sv(k_metal_navatala_dataframe_ge_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bitwise_and_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_bitwise_and_i32";
    std::string_view sv(k_metal_navatala_dataframe_bitwise_and_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bitwise_and_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_bitwise_and_i64";
    std::string_view sv(k_metal_navatala_dataframe_bitwise_and_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bitwise_or_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_bitwise_or_i32";
    std::string_view sv(k_metal_navatala_dataframe_bitwise_or_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bitwise_or_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_bitwise_or_i64";
    std::string_view sv(k_metal_navatala_dataframe_bitwise_or_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bitwise_xor_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_bitwise_xor_i32";
    std::string_view sv(k_metal_navatala_dataframe_bitwise_xor_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bitwise_xor_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_bitwise_xor_i64";
    std::string_view sv(k_metal_navatala_dataframe_bitwise_xor_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bitwise_not_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_bitwise_not_i32";
    std::string_view sv(k_metal_navatala_dataframe_bitwise_not_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_bitwise_not_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_bitwise_not_i64";
    std::string_view sv(k_metal_navatala_dataframe_bitwise_not_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_hour_from_seconds") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_hour_from_seconds";
    std::string_view sv(k_metal_navatala_dataframe_extract_hour_from_seconds);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_minute_from_seconds") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_minute_from_seconds";
    std::string_view sv(k_metal_navatala_dataframe_extract_minute_from_seconds);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_second_from_seconds") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_second_from_seconds";
    std::string_view sv(k_metal_navatala_dataframe_extract_second_from_seconds);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_day_of_week_from_seconds") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_day_of_week_from_seconds";
    std::string_view sv(k_metal_navatala_dataframe_extract_day_of_week_from_seconds);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_year_from_seconds") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_year_from_seconds";
    std::string_view sv(k_metal_navatala_dataframe_extract_year_from_seconds);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_month_from_seconds") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_month_from_seconds";
    std::string_view sv(k_metal_navatala_dataframe_extract_month_from_seconds);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_day_from_seconds") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_day_from_seconds";
    std::string_view sv(k_metal_navatala_dataframe_extract_day_from_seconds);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_extract_day_of_year_from_seconds") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_extract_day_of_year_from_seconds";
    std::string_view sv(k_metal_navatala_dataframe_extract_day_of_year_from_seconds);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_add_days_to_timestamp") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_add_days_to_timestamp";
    std::string_view sv(k_metal_navatala_dataframe_add_days_to_timestamp);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_add_months_to_timestamp") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_add_months_to_timestamp";
    std::string_view sv(k_metal_navatala_dataframe_add_months_to_timestamp);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_subtract_timestamps") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_subtract_timestamps";
    std::string_view sv(k_metal_navatala_dataframe_subtract_timestamps);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_timestamp_to_date_parts") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_timestamp_to_date_parts";
    std::string_view sv(k_metal_navatala_dataframe_timestamp_to_date_parts);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_date_parts_to_timestamp") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_date_parts_to_timestamp";
    std::string_view sv(k_metal_navatala_dataframe_date_parts_to_timestamp);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_convert_timestamp_precision") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_convert_timestamp_precision";
    std::string_view sv(k_metal_navatala_dataframe_convert_timestamp_precision);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_combine_hash_values") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_combine_hash_values";
    std::string_view sv(k_metal_navatala_dataframe_combine_hash_values);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_hash_column_row") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_hash_column_row";
    std::string_view sv(k_metal_navatala_dataframe_hash_column_row);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_md5_hash") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_md5_hash";
    std::string_view sv(k_metal_navatala_dataframe_md5_hash);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sha256_hash") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_sha256_hash";
    std::string_view sv(k_metal_navatala_dataframe_sha256_hash);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_identity_hash_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_identity_hash_i32";
    std::string_view sv(k_metal_navatala_dataframe_identity_hash_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_identity_hash_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_identity_hash_i64";
    std::string_view sv(k_metal_navatala_dataframe_identity_hash_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_murmur3_hash32_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_murmur3_hash32_i32";
    std::string_view sv(k_metal_navatala_dataframe_murmur3_hash32_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_murmur3_hash32_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_murmur3_hash32_i64";
    std::string_view sv(k_metal_navatala_dataframe_murmur3_hash32_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_cross_join_compute_output_size") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_cross_join_compute_output_size";
    std::string_view sv(k_metal_navatala_dataframe_cross_join_compute_output_size);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_cross_join_gather_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_cross_join_gather_i32";
    std::string_view sv(k_metal_navatala_dataframe_cross_join_gather_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_hash_keys_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_hash_keys_i32";
    std::string_view sv(k_metal_navatala_dataframe_hash_keys_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_build_hash_table_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_build_hash_table_i32";
    std::string_view sv(k_metal_navatala_dataframe_build_hash_table_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_probe_hash_table_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_probe_hash_table_i32";
    std::string_view sv(k_metal_navatala_dataframe_probe_hash_table_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_join_results_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gather_join_results_i32";
    std::string_view sv(k_metal_navatala_dataframe_gather_join_results_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_join_results_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gather_join_results_f32";
    std::string_view sv(k_metal_navatala_dataframe_gather_join_results_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_mark_left_matched") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_mark_left_matched";
    std::string_view sv(k_metal_navatala_dataframe_mark_left_matched);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_filter_semi_join") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_filter_semi_join";
    std::string_view sv(k_metal_navatala_dataframe_filter_semi_join);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_filter_anti_join") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_filter_anti_join";
    std::string_view sv(k_metal_navatala_dataframe_filter_anti_join);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_count_unmatched_left") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_count_unmatched_left";
    std::string_view sv(k_metal_navatala_dataframe_count_unmatched_left);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_unmatched_left_indices") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gather_unmatched_left_indices";
    std::string_view sv(k_metal_navatala_dataframe_gather_unmatched_left_indices);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_left_join_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gather_left_join_i32";
    std::string_view sv(k_metal_navatala_dataframe_gather_left_join_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_left_join_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gather_left_join_f32";
    std::string_view sv(k_metal_navatala_dataframe_gather_left_join_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_right_join_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gather_right_join_i32";
    std::string_view sv(k_metal_navatala_dataframe_gather_right_join_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_right_join_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gather_right_join_f32";
    std::string_view sv(k_metal_navatala_dataframe_gather_right_join_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_count_unmatched_right") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_count_unmatched_right";
    std::string_view sv(k_metal_navatala_dataframe_count_unmatched_right);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_unmatched_right_indices") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gather_unmatched_right_indices";
    std::string_view sv(k_metal_navatala_dataframe_gather_unmatched_right_indices);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_nested_loop_join_probe") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_nested_loop_join_probe";
    std::string_view sv(k_metal_navatala_dataframe_nested_loop_join_probe);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_nested_loop_join_gather_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_nested_loop_join_gather_i32";
    std::string_view sv(k_metal_navatala_dataframe_nested_loop_join_gather_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sort_merge_join_probe") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_sort_merge_join_probe";
    std::string_view sv(k_metal_navatala_dataframe_sort_merge_join_probe);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sort_merge_join_gather_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_sort_merge_join_gather_i32";
    std::string_view sv(k_metal_navatala_dataframe_sort_merge_join_gather_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_merge_step_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_merge_step_i32";
    std::string_view sv(k_metal_navatala_dataframe_merge_step_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_merge_step_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_merge_step_f32";
    std::string_view sv(k_metal_navatala_dataframe_merge_step_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_merge_step_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_merge_step_i64";
    std::string_view sv(k_metal_navatala_dataframe_merge_step_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_radix_scatter_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_radix_scatter_i32";
    std::string_view sv(k_metal_navatala_dataframe_radix_scatter_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_radix_scatter_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_radix_scatter_i64";
    std::string_view sv(k_metal_navatala_dataframe_radix_scatter_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_radix_scatter_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_radix_scatter_f32";
    std::string_view sv(k_metal_navatala_dataframe_radix_scatter_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_len") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_len";
    std::string_view sv(k_metal_navatala_dataframe_str_len);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_upper_chars") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_upper_chars";
    std::string_view sv(k_metal_navatala_dataframe_str_upper_chars);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_lower_chars") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_lower_chars";
    std::string_view sv(k_metal_navatala_dataframe_str_lower_chars);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_starts_with_char") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_starts_with_char";
    std::string_view sv(k_metal_navatala_dataframe_str_starts_with_char);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_capitalize_first_char") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_capitalize_first_char";
    std::string_view sv(k_metal_navatala_dataframe_str_capitalize_first_char);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_title_case_char") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_title_case_char";
    std::string_view sv(k_metal_navatala_dataframe_str_title_case_char);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_swap_case_char") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_swap_case_char";
    std::string_view sv(k_metal_navatala_dataframe_str_swap_case_char);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_normalize_whitespace_char") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_normalize_whitespace_char";
    std::string_view sv(k_metal_navatala_dataframe_str_normalize_whitespace_char);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_is_digit") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_is_digit";
    std::string_view sv(k_metal_navatala_dataframe_str_is_digit);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_is_alpha") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_is_alpha";
    std::string_view sv(k_metal_navatala_dataframe_str_is_alpha);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_is_alnum") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_is_alnum";
    std::string_view sv(k_metal_navatala_dataframe_str_is_alnum);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_is_space") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_is_space";
    std::string_view sv(k_metal_navatala_dataframe_str_is_space);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_is_upper") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_is_upper";
    std::string_view sv(k_metal_navatala_dataframe_str_is_upper);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_is_lower") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_is_lower";
    std::string_view sv(k_metal_navatala_dataframe_str_is_lower);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_concat_pair_offsets") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_concat_pair_offsets";
    std::string_view sv(k_metal_navatala_dataframe_str_concat_pair_offsets);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_concat_copy_chars") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_concat_copy_chars";
    std::string_view sv(k_metal_navatala_dataframe_str_concat_copy_chars);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_join_with_sep_offsets") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_join_with_sep_offsets";
    std::string_view sv(k_metal_navatala_dataframe_str_join_with_sep_offsets);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_join_copy_with_sep") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_join_copy_with_sep";
    std::string_view sv(k_metal_navatala_dataframe_str_join_copy_with_sep);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_compare") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_compare";
    std::string_view sv(k_metal_navatala_dataframe_str_compare);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_equals") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_equals";
    std::string_view sv(k_metal_navatala_dataframe_str_equals);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_less_than") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_less_than";
    std::string_view sv(k_metal_navatala_dataframe_str_less_than);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_strip_leading_count") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_strip_leading_count";
    std::string_view sv(k_metal_navatala_dataframe_str_strip_leading_count);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_strip_trailing_count") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_strip_trailing_count";
    std::string_view sv(k_metal_navatala_dataframe_str_strip_trailing_count);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_strip_both_count") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_strip_both_count";
    std::string_view sv(k_metal_navatala_dataframe_str_strip_both_count);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_slice_bounds") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_slice_bounds";
    std::string_view sv(k_metal_navatala_dataframe_str_slice_bounds);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_contains_char") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_contains_char";
    std::string_view sv(k_metal_navatala_dataframe_str_contains_char);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_ends_with_char") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_ends_with_char";
    std::string_view sv(k_metal_navatala_dataframe_str_ends_with_char);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_pad_left_calc") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_pad_left_calc";
    std::string_view sv(k_metal_navatala_dataframe_str_pad_left_calc);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_pad_center") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_pad_center";
    std::string_view sv(k_metal_navatala_dataframe_str_pad_center);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_zfill") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_zfill";
    std::string_view sv(k_metal_navatala_dataframe_str_zfill);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_wrap_bounds") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_wrap_bounds";
    std::string_view sv(k_metal_navatala_dataframe_str_wrap_bounds);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_substring_bounds") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_substring_bounds";
    std::string_view sv(k_metal_navatala_dataframe_str_substring_bounds);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_left_bounds") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_left_bounds";
    std::string_view sv(k_metal_navatala_dataframe_str_left_bounds);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_right_bounds") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_right_bounds";
    std::string_view sv(k_metal_navatala_dataframe_str_right_bounds);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_regex_match") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_regex_match";
    std::string_view sv(k_metal_navatala_dataframe_str_regex_match);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_regex_extract") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_regex_extract";
    std::string_view sv(k_metal_navatala_dataframe_str_regex_extract);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_regex_replace") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_regex_replace";
    std::string_view sv(k_metal_navatala_dataframe_str_regex_replace);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_find_char") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_find_char";
    std::string_view sv(k_metal_navatala_dataframe_str_find_char);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_count_char") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_count_char";
    std::string_view sv(k_metal_navatala_dataframe_str_count_char);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_replace_char") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_replace_char";
    std::string_view sv(k_metal_navatala_dataframe_str_replace_char);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_contains_pattern2") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_contains_pattern2";
    std::string_view sv(k_metal_navatala_dataframe_str_contains_pattern2);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_split_count_by_char") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_split_count_by_char";
    std::string_view sv(k_metal_navatala_dataframe_str_split_count_by_char);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_split_by_char") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_split_by_char";
    std::string_view sv(k_metal_navatala_dataframe_str_split_by_char);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_split_get_element") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_split_get_element";
    std::string_view sv(k_metal_navatala_dataframe_str_split_get_element);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_split_by_whitespace") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_split_by_whitespace";
    std::string_view sv(k_metal_navatala_dataframe_str_split_by_whitespace);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_reverse_chars") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_reverse_chars";
    std::string_view sv(k_metal_navatala_dataframe_str_reverse_chars);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_truncate_bounds") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_truncate_bounds";
    std::string_view sv(k_metal_navatala_dataframe_str_truncate_bounds);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_pad_right_calc") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_pad_right_calc";
    std::string_view sv(k_metal_navatala_dataframe_str_pad_right_calc);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_repeat_bounds") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_repeat_bounds";
    std::string_view sv(k_metal_navatala_dataframe_str_repeat_bounds);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_translate_char") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_translate_char";
    std::string_view sv(k_metal_navatala_dataframe_str_translate_char);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_translate_table") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_translate_table";
    std::string_view sv(k_metal_navatala_dataframe_str_translate_table);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_str_normalize") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_str_normalize";
    std::string_view sv(k_metal_navatala_dataframe_str_normalize);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_negate_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_negate_f32";
    std::string_view sv(k_metal_navatala_dataframe_negate_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_negate_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_negate_i32";
    std::string_view sv(k_metal_navatala_dataframe_negate_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_abs_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_abs_f32";
    std::string_view sv(k_metal_navatala_dataframe_abs_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_abs_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_abs_i32";
    std::string_view sv(k_metal_navatala_dataframe_abs_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_ceil_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_ceil_f32";
    std::string_view sv(k_metal_navatala_dataframe_ceil_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_floor_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_floor_f32";
    std::string_view sv(k_metal_navatala_dataframe_floor_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_round_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_round_f32";
    std::string_view sv(k_metal_navatala_dataframe_round_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_trunc_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_trunc_f32";
    std::string_view sv(k_metal_navatala_dataframe_trunc_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_negate_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_negate_i64";
    std::string_view sv(k_metal_navatala_dataframe_negate_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_abs_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_abs_i64";
    std::string_view sv(k_metal_navatala_dataframe_abs_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sqrt_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_sqrt_f32";
    std::string_view sv(k_metal_navatala_dataframe_sqrt_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_exp_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_exp_f32";
    std::string_view sv(k_metal_navatala_dataframe_exp_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_log_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_log_f32";
    std::string_view sv(k_metal_navatala_dataframe_log_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_sin_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_sin_f32";
    std::string_view sv(k_metal_navatala_dataframe_sin_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_cos_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_cos_f32";
    std::string_view sv(k_metal_navatala_dataframe_cos_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_tan_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_tan_f32";
    std::string_view sv(k_metal_navatala_dataframe_tan_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_log10_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_log10_f32";
    std::string_view sv(k_metal_navatala_dataframe_log10_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compute_concat_offsets") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_compute_concat_offsets";
    std::string_view sv(k_metal_navatala_dataframe_compute_concat_offsets);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_concat_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gather_concat_i32";
    std::string_view sv(k_metal_navatala_dataframe_gather_concat_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_concat_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gather_concat_f32";
    std::string_view sv(k_metal_navatala_dataframe_gather_concat_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_concat_i64") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gather_concat_i64";
    std::string_view sv(k_metal_navatala_dataframe_gather_concat_i64);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_hash_partition_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_hash_partition_i32";
    std::string_view sv(k_metal_navatala_dataframe_hash_partition_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_hash_partition_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_hash_partition_f32";
    std::string_view sv(k_metal_navatala_dataframe_hash_partition_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_round_robin_partition") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_round_robin_partition";
    std::string_view sv(k_metal_navatala_dataframe_round_robin_partition);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_range_partition_bounds") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_range_partition_bounds";
    std::string_view sv(k_metal_navatala_dataframe_range_partition_bounds);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_range_partition_assign") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_range_partition_assign";
    std::string_view sv(k_metal_navatala_dataframe_range_partition_assign);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_partition_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gather_partition_i32";
    std::string_view sv(k_metal_navatala_dataframe_gather_partition_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_gather_partition_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_gather_partition_f32";
    std::string_view sv(k_metal_navatala_dataframe_gather_partition_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compute_partition_offsets") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_compute_partition_offsets";
    std::string_view sv(k_metal_navatala_dataframe_compute_partition_offsets);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_transpose_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_transpose_i32";
    std::string_view sv(k_metal_navatala_dataframe_transpose_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_transpose_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_transpose_f32";
    std::string_view sv(k_metal_navatala_dataframe_transpose_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_flatten_offsets") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_flatten_offsets";
    std::string_view sv(k_metal_navatala_dataframe_flatten_offsets);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_unflatten_copy") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_unflatten_copy";
    std::string_view sv(k_metal_navatala_dataframe_unflatten_copy);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_compute_split_bounds") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_compute_split_bounds";
    std::string_view sv(k_metal_navatala_dataframe_compute_split_bounds);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_split_copy_i32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_split_copy_i32";
    std::string_view sv(k_metal_navatala_dataframe_split_copy_i32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_split_copy_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_split_copy_f32";
    std::string_view sv(k_metal_navatala_dataframe_split_copy_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_split_copy_validity") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_split_copy_validity";
    std::string_view sv(k_metal_navatala_dataframe_split_copy_validity);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_uniform_lcg_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_uniform_lcg_f32";
    std::string_view sv(k_metal_navatala_dataframe_uniform_lcg_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_normal_box_muller_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_normal_box_muller_f32";
    std::string_view sv(k_metal_navatala_dataframe_normal_box_muller_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_uniform_lcg_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_uniform_lcg_f32";
    std::string_view sv(k_metal_navatala_dataframe_uniform_lcg_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  if (backend == "metal" && kernelName == "navatala_dataframe_normal_box_muller_f32") {
    out.kind = GpuRuntime::ProgramSource::Kind::Msl;
    out.entryPoint = "navatala_dataframe_normal_box_muller_f32";
    std::string_view sv(k_metal_navatala_dataframe_normal_box_muller_f32);
    out.bytes.assign(sv.begin(), sv.end());
    return true;
  }
  return false;
}

} // namespace NavatalaRegistry

