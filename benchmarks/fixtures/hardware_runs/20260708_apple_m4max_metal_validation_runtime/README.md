# Apple M4 Max Metal Validation Runtime Fixture

This is the Mac-side Metal runtime validation fixture captured from a regenerated public tree.

The fixture uses `--runtime-only` because this Apple Silicon build intentionally enables only Metal; full release manifest conformance for CUDA/HIP/Vulkan/OpenCL is handled on non-Metal release hosts.

Source provenance recorded in `metal_validation.json`:

- `navatala:3eee3d6`
- `runtime:9cc277c`
